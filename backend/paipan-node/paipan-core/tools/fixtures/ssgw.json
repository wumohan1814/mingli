{
  "capability": "ssgw",
  "note": "对拍夹具。defaultKeyFields 只放**确定性排盘事实**：签号（抽取结果）、抽取机构字段（method/poolSize/selectedIndex）与随机源档案（meta.random.mode / 首个均匀值逐位对照）。签题、签诗、释义、故事属白话文案，按母节 §3.6 铁则自行撰写或公网重采（见 rules/ssgw.js 的数据来源登记），允许与旧实现不同，故列信息性。ganzhi/timestamp 不进关键字段：旧实现两者都取自**调用时刻**（实测传 customDate/now/date/moment 一律被忽略），无法确定性对拍；本内核不读时钟，仅在显式注入 options.moment 时给 ganzhi（属已登记的有意分歧）。",
  "defaultKeyFields": [
    "number",
    "draw.method",
    "draw.poolSize",
    "draw.selectedIndex",
    "draw.selectedNumber",
    "meta.random.mode",
    "meta.random.samples[0]"
  ],
  "notes": [
    "随机源口径（对拍实测反推）：uniform = mulberry32(FNV-1a32(String(seed)))() 的首值；selectedIndex = floor(uniform × 92)；selectedNumber = selectedIndex + 1。",
    "种子覆盖：0/1/2/3/7/42/123/999/12345/20260101 等整数 + 负数 + 小数 + 大整数 + 文本种子（含空串与中文），共 25 条 seed 夹具。",
    "replay 覆盖：给出 [0,1) 均匀值数组（旧实现语义是**均匀值样本**，不是池下标），含 0 / 极小值 / 0.9999999 / 多元素（只取首值）等 6 条。",
    "fixtures 里不放「缺随机源」用例：旧实现会静默回落 Math.random（非确定性），本内核抛 PaipanInputError，两侧行为不可对拍——该类分歧写在能力文件头注释与本报告里，不进夹具。",
    "meta.random.samples[0] 作为关键字段，是为了把「同 seed 同结果」钉在**逐位浮点一致**上（旧实现也在 meta.random.samples 里记录了消费掉的均匀值）。"
  ],
  "fixtures": [
    { "name": "seed-00", "input": { "options": { "seed": 0 } } },
    { "name": "seed-01", "input": { "options": { "seed": 1 } } },
    { "name": "seed-02", "input": { "options": { "seed": 2 } } },
    { "name": "seed-03", "input": { "options": { "seed": 3 } } },
    { "name": "seed-04", "input": { "options": { "seed": 4 } } },
    { "name": "seed-05-经典42", "input": { "options": { "seed": 42 } } },
    { "name": "seed-06-经典123", "input": { "options": { "seed": 123 } } },
    { "name": "seed-07", "input": { "options": { "seed": 7 } } },
    { "name": "seed-08", "input": { "options": { "seed": 8 } } },
    { "name": "seed-09", "input": { "options": { "seed": 9 } } },
    { "name": "seed-10", "input": { "options": { "seed": 10 } } },
    { "name": "seed-99", "input": { "options": { "seed": 99 } } },
    { "name": "seed-999", "input": { "options": { "seed": 999 } } },
    { "name": "seed-12345", "input": { "options": { "seed": 12345 } } },
    { "name": "seed-20260101", "input": { "options": { "seed": 20260101 } } },
    { "name": "seed-2147483647-int32max", "input": { "options": { "seed": 2147483647 } } },
    { "name": "seed-4294967296-2pow32", "input": { "options": { "seed": 4294967296 } } },
    { "name": "seed-1e15", "input": { "options": { "seed": 1000000000000000 } } },
    { "name": "seed-负一", "input": { "options": { "seed": -1 } } },
    { "name": "seed-负七", "input": { "options": { "seed": -7 } } },
    { "name": "seed-负零点五", "input": { "options": { "seed": -0.5 } } },
    { "name": "seed-小数1.5", "input": { "options": { "seed": 1.5 } } },
    { "name": "seed-小数3.14159", "input": { "options": { "seed": 3.14159 } } },
    { "name": "seed-文本数字123", "input": { "options": { "seed": "123" } } },
    { "name": "seed-文本abc", "input": { "options": { "seed": "abc" } } },
    { "name": "seed-文本空串", "input": { "options": { "seed": "" } } },
    { "name": "seed-文本中文灵签", "input": { "options": { "seed": "灵签" } } },
    { "name": "seed-文本空格", "input": { "options": { "seed": " " } } },
    { "name": "replay-零", "input": { "options": { "replay": [0] } } },
    { "name": "replay-半点五", "input": { "options": { "replay": [0.5] } } },
    { "name": "replay-零点一", "input": { "options": { "replay": [0.1] } } },
    { "name": "replay-接近一", "input": { "options": { "replay": [0.9999999] } } },
    { "name": "replay-极小值", "input": { "options": { "replay": [0.0000001] } } },
    { "name": "replay-多元素只取首值", "input": { "options": { "replay": [0.25, 0.9, 0.1] } } }
  ]
}
