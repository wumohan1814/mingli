# 素材需求文档（第七轮 · REQ-084 桌面宠物式三化身动画）

> 定位：REQ-084（2026-09-09 需求方口径定稿）返工为**桌面宠物式三化身**所需的美术素材——①三只 Q 版化身的**待机循环动作帧**（自主动作，替换现"整体浮沉/晃动"简单动画）；②周边**互动元素**小图（纯氛围、不可点击）。
> 关联：`../需求表.md`（REQ-084 / REQ-116 / REQ-076）｜ 既有静态形象 `frontend/public/art/module-mascot/{guoxue,xishi,mbti}-mascot.webp`（沿用形象，**将被动作帧替换呈现**，旧文件保留供回滚）｜ 现有实现 commit 4334c33（简单 CSS 晃动版，将被本批替换）。
> 日期：2026-09-09。
> 状态：🟡 **样片已交付**（国学 10 帧 sprite + 小鸟 FX），待用户确认方向后批量生成西式 + MBTI + 剩余 4 枚 FX。

---

## 0. 结论速览

| 项 | 说明 |
|---|---|
| 素材 2 类 | ① 三只化身待机循环动作帧（sprite sheet ×3）② 周边互动元素小图（约 5~7 枚） |
| 呈现形态 | **桌面宠物式**：三只化身常驻落地页左侧三导航按钮（国学预测 / 西式占卜 / MBTI）下方；人物**自主动作**（待机循环动效）；点击任意一只直接进入「太初先生」会话；周边互动元素环绕/落肩/飞走，纯氛围不可点击 |
| 风格 | 沿用现有三只 Q 版形象（美术素材表-第三轮 §0.4 已交付版），对齐全站「高贵/奢华/内涵」基调；新动作帧须与既有形象**同一角色、同一画风**（可在其基础上微调表情/姿态） |
| 动画形态 | **序列帧 sprite sheet + CSS steps() 播放**（免构建前端最稳、体积可控；美术只画帧、不碰工具链） |
| 开发先用 | 占位（现有静态形象 + 现有 CSS 动画），素材回传后替换即生效 |
| 红线 | 图内不画文字；首尾帧可无缝循环；互动元素保持小尺寸低干扰、不遮挡导航按钮与主操作 |

---

## 1. 素材规格

### M1 · 三只化身待机循环动作帧（sprite sheet ×3）

| 项 | 规格 |
|---|---|
| 内容 | 每只化身一段**自主动作待机循环**（建议动作构成，美术按 Q 版风格自由发挥）：<br>· **国学**（打坐）：呼吸起伏 + 眨眼 + 微摆头/捻珠等小动作；<br>· **西式**（观星）：仰望 + 眨眼 + 轻摆手等小动作；<br>· **MBTI**（摇椅）：摇椅前后轻摇 + 眨眼 + 手部小动作。 |
| 帧规约 | **10 帧/循环**（8~12 帧皆可，偶数便于网格排布）；单帧 **256×256**（可选 @2x 512×512 超采样，美术自行决定采样但交付按 256 规格）；**第 1 帧与末帧须动作回环、可无缝循环播放** |
| 网格 | 建议 **2×5**（两行五行）横向排布（开发按实际排布适配，交付时注明行列即可） |
| 循环节奏 | 3.2~3.6s/循环（对齐现有节奏） |
| 格式/体积 | WebP（优先）或 PNG 透明；**≤400KB/张** |
| 命名/目录 | `frontend/public/art/taichu-pet/mascot-guoxue.webp` / `mascot-xishi.webp` / `mascot-mbti.webp`（新目录，**不覆盖** `module-mascot/` 旧静态图） |
| 约束 | 动作幅度适中（待机感、不夸张）；单帧角色居中、四周留白 ≥8%；不画文字 |

### M2 · 周边互动元素（纯氛围 · 不可点击 · 静态小图）

> 路径动效（环绕 / 落肩 / 飞走）由开发 CSS keyframes 实现，**美术只产静态小图、无需做帧动画**。

| 元素 | 归属 | 数量 | 画面建议 |
|---|---|---|---|
| 小鸟 | 国学 | 1 | Q 版小鸟（可飞落肩头） |
| 蝴蝶 | 国学 | 1 | Q 版蝴蝶（环绕飞） |
| 星星 | 西式 / MBTI | 2~3 | 闪光小星星（观星氛围） |
| 星座连线 / 神秘符号 | 西式 | 1 | 简洁星座连线或星月符号（与塔罗神秘学视觉一致） |

| 项 | 规格 |
|---|---|
| 尺寸 | 每枚 64~128px 方形透明底（按元素实际占比，最小边 ≥64px） |
| 格式/体积 | PNG/WebP 透明；**≤50KB/枚** |
| 命名/目录 | `frontend/public/art/taichu-pet/fx-bird.webp` / `fx-butterfly.webp` / `fx-star.webp` / `fx-constellation.webp`（其余按语义命名） |
| 约束 | 装饰性、低饱和不抢主体；不遮挡导航按钮与主操作；不画文字 |

---

## 2. 交付与接入说明

- 交付进 `docs/exchange/`（待接入交付物中心），开发接入后归档 `docs/_archive/`。
- 接入点（开发侧，素材回传后执行）：
  1. 化身：落地页三按钮（`.landing-mascot`）内 `<img>` 换 sprite sheet + CSS `steps()` 逐帧循环（替换 4334c33 的整体浮沉/晃动 keyframes）；
  2. 互动元素：按钮容器内叠加绝对定位元素 + CSS 路径动画（环绕/落肩/飞走）；
  3. 交互不变：点击进「太初先生」会话、跟随功能设置「太初先生」开关（agent_enabled）、`prefers-reduced-motion` 停循环降级（均已实现，直接沿用）。
- 开发先用占位（现有静态形象 + 现有 CSS 动画），素材回传后替换即生效；旧 `module-mascot/` 文件保留供回滚。

## 3. 待确认单（已按默认锁死 · 2026-09-09）

| # | 项目 | 锁死值 |
|---|---|---|
| 1 | 动画实现形态 | **序列帧 sprite sheet + CSS steps()**（免构建最稳，备选 Lottie JSON 复杂度高已弃） |
| 2 | 帧数 / 单帧尺寸 | **10 帧 × 256×256**（2×5 网格，生成时 @2x 512×512 超采样保证清晰，最终交付 512×1280） |
| 3 | 互动元素清单 | **小鸟×1 + 蝴蝶×1 + 星星×2 + 星座连线×1**（共 5 枚，按默认） |
| 4 | 动作内容 | **眨眼 + 呼吸/轻摇 + 小幅动作**（美术按 Q 版风格自由发挥，不要求动作逐帧对齐文字描述） |

> 生产策略：先出**国学化身**完整 10 帧样片 + 1 枚 FX（小鸟）→ 合成可循环预览验证角色一致性 → 确认后批量生产西式 + MBTI + 剩余 4 枚 FX。
> 节奏红线触发说明：动画为新资产形态，跨帧角色一致性是核心风险，先样片验证再批量以节省 credits（30+ 帧盲目生产若角色漂移则 ~150 credits 浪费）。

## 4. 备注

- 本单为 REQ-084 动画+素材的**唯一需求来源**；需求方确认待确认单后即可交美术生产，生产回执与接入状态回填 REQ-084 备注。
- 动画方案（帧规约/循环节奏/接入点）即本单 §1~2，已含"开发门禁"（REQ-061 同款：先交方案+素材表）要求的方案部分。
