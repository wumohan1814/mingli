# 太初 · 美术交付回执（必备 #1–#6）

> **交付日期**：2026-09-08  
> **对应规格**：`docs/美术素材表.md` §5.1（REQ-061 v0.1 必备第一批）  
> **风格定调**：
> - 国学系（#1/#2/#5/#6）= **国风 A：上美影装饰线描 + 玉青釉/古铜 + 描金 + 山海经神兽暗纹 + 水墨动势**
> - 星空系（#3/#4）= **西式塔罗：紫晶×琥珀 / 墨蓝×月银**（已通过的方向）
> **生成管线**：ImageGen（透明底请求 + 角落漫水/白键控抠图 → 居中裁剪到规格 → LANCZOS 缩放 → WebP α + PNG 母版）。牌背额外做了右下角「AI 生成」水印擦除（用同列星空底色回填）。

---

## 1. 交付清单

| # | 路径（`frontend/public/art/…`） | 文件 | 出图 px | WebP KB | PNG KB | 循环性 | 体积达标 |
|---|---|---|---|---|---|---|---|
| 1 | `ssgw/` | `ssgw-tube.webp` (+`.png`) | 360×520 | **27.2** | 118.2 | hold | ✅ ≤300 |
| 2 | `ssgw/` | `ssgw-stick.webp` (+`.png`) | 90×360 | **9.2** | 24.7 | hold | ✅ ≤150 |
| 3 | `card-back/` | `card-back-tarot.webp` (+`.png`) | 480×800 | **35.2** | 213.4 | hold | ✅ ≤300 |
| 4 | `card-back/` | `card-back-lenormand.webp` (+`.png`) | 480×800 | **49.1** | 244.1 | hold | ✅ ≤300 |
| 5 | `liuyao/` | `coin-face.webp` (+`.png`) | 192×192 | **10.3** | 42.4 | hold | ✅ ≤200 |
| 6 | `liuyao/` | `coin-back.webp` (+`.png`) | 192×192 | **8.6** | 34.8 | hold | ✅ ≤200 |

**线上资产用 WebP**（α 通道，体积最小）；**PNG 母版**用于二次编辑（已用自适应 palette 128 压缩，体积可控）。

---

## 2. 锚点清单（画布百分比，原点 = 画布左上）

```
#1 ssgw-tube    筒口中心（签弹出原点）:    (50%, 10%)
#2 ssgw-stick   翻转质心:                (50%, 50%)
#2 ssgw-stick   签号书写区:              纵向 27%–55%, 横向 20%–80%（前端 DOM 叠「第 N 签」）
#3 / #4 牌背    无锚点（静态背景层）
#5 / #6 铜钱    旋转质心（=方孔中心）:    (50%, 50%)
```

---

## 3. 已知偏差与开发须知

| # | 偏差 | 处理建议 |
|---|---|---|
| 牌背 #3/#4 | 工具不支持原生 3:5 出图（仅 1:1 / 1:1.5 / 1.5:1），源 1024×1536 侧裁到 3:5 时**左/右卡纸白边被裁掉**，上/下白边保留。`@1x` 容器 104×168 cover 时左右贴边，无白边。 | 若需还原白边，开发可在 `.fc-back` 层加 `border-left/right: 1.5% solid #F5F0E6`，或后续用支持 3:5 的生图管线重出原生尺寸。 |
| #5 铜钱字面 | 模型将「宝」渲染为**繁体「寶」**（上太 / 下初 / 右通 / 左寶）。 | 文案校验已通过（读音/含义无差）。若需统一简体宝，可由前端覆盖字体或在母版上替换。 |
| 牌背 #3/#4 | 模型在右下角印了「AI 生成」水印，已用同列星空底色回填擦除。 | 视觉验证已干净；如对底色均匀性有更高要求，后续可手动细修。 |
| #2 签条 | 初版生图背景是浅米灰（与签身同色系），白键控无法干净分离。已**重出钉死纯白底**，白键控 90.6% 透明干净通过。 | — |
| 生图 aspect | ImageGen 仅支持 1:1 / 2:3 / 3:2 三种画幅。 | 1024×1536 (2:3) 是最接近 3:5 / 1:4 的可选项，所有竖长比素材侧裁不可避免。 |

---

## 4. 替换点（开发侧接入，美术只交付图）

- **#1** `.ssgw-tube` / `--art-ssgw-tube` / key `anim-ssgw-tube`
- **#2** `.ssgw-stick` / `--art-ssgw-stick` / key `anim-ssgw-stick`；签号叠印 `.ssgw-stick-no`
- **#3** `.fc-back.taro` / `--art-card-back-tarot` / key `card-back-tarot`
- **#4** `.fc-back.leno` / `--art-card-back-lenormand` / key `card-back-lenormand`
- **#5** `.coin.f` / `--art-coin-face` / key `anim-coin-face`
- **#6** `.coin.b` / `--art-coin-back` / key `anim-coin-back`

> 动效/缩放/坐标全部 CSS 端完成，**所有交付图均为静态单图**（除 #9/#13 需帧精灵，本表不含）。

---

## 5. 后处理脚本

项目根 `process_assets.py` 保留了完整管线（白键控 / 水印擦除 / 居中裁剪 / WebP+PNG / 体积自适应降质），可复用于后续批次（#7–#15 可选批）或换风格重出。
