# 太初 · 测试组交接文档（新 Session 启动说明）

> 定位：**测试组 Session 的唯一工作依据**。本 Session 负责整个项目的测试工作：后端自动化测试的维护与扩充、前端真机走查、Bug 登记与验证、回归测试。
> 关联：`docs/需求表.md`（REQ 需求）｜ `docs/Bug管理表.md`（BUG 缺陷）｜ `docs/架构设计.md` + `docs/架构设计-横向扩展.md`（架构）｜ `docs/流程图.mmd`（页面流）。

---

## 1. 测试组职责

1. **自动化测试**：维护 `backend/tests/` 现有 12 个测试文件，为新功能补测试。
2. **真机走查**：在线上 `https://taichu.xyz` 逐模块手测，验证 UI/交互/后端联动。
3. **Bug 管理**：发现缺陷 → 登记 `docs/Bug管理表.md`（BUG-xxx）→ 追踪修复 → 验证关闭。
4. **回归**：每次功能改动后跑全量 pytest + 前端语法自检，确认不回归。
5. **验收**：新需求（REQ）上线后，按需求验收标准逐条验证，更新需求表状态。

---

## 2. 项目技术栈速览（测试需要知道的）

| 层 | 技术 | 测试关注点 |
|---|---|---|
| 后端 | Python 3.13 + FastAPI + SQLite（三库） | pytest 单测/集成；SQLite 文件在 `backend/data/` |
| 排盘 | lunar-python + Node 22 常驻服务 `paipan-node/server.mjs`（:9317） | 确定性计算契约；本地 Node 版本影响 node_endpoint 测试 |
| LLM | DeepSeek（chat/completions） | 测试用 mock（不真调 LLM） |
| 前端 | 免构建 CDN React（`frontend/public/index.html` 已预编译，**无 JSX 运行时**） | 真机手测 + `node new Function` 语法自检 |
| 部署 | Docker + Caddy，线上 `https://taichu.xyz` | 部署后真机回归 |

**本地启动**：
```bash
cd C:\Users\wumoh\Documents\Vibecoding\taichu
start.bat                    # 或 cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 前端 http://localhost:8000 ｜ API /docs ｜ 后台 /admin.html
```

---

## 3. 现有自动化测试（backend/tests/，12 文件 ~4854 行）

| 文件 | 覆盖 | 规模 |
|---|---|---|
| `unit/test_paipan.py` | 排盘引擎契约 / 降级 / 确定性 / slice / 打分 | 225 行 |
| `unit/test_orchestration.py` | 断前尘/预测编排（并发、逐法扣费、续跑） | 309 行 |
| `unit/test_method_anchor.py` | 方法提示词锚点 | 149 行 |
| `unit/test_credits.py` | 积分（扣费/充值/余额） | 214 行 |
| `unit/test_zodiac.py` | 生肖流年（Node 端点 + Python 端点 + taiSui） | 420 行 |
| `unit/test_divination.py` | 起卦（四法 + 断卦 interpret） | 744 行 |
| `unit/test_tarot.py` | 塔罗（抽牌 + interpret + 雷诺曼 lenormand） | 841 行 |
| `unit/test_astrology.py` | 星座（chart + interpret + 法达） | 842 行 |
| `unit/test_mbti.py` | MBTI（判型 + 题库 + 端点） | 543 行 |
| `integration/test_e2e_flow.py` | 端到端主流程 | 287 行 |
| `integration/test_e2e_isolation.py` | 多用户隔离 + 降级 | 129 行 |
| `conftest.py` | 公共 fixtures（临时库隔离等） | 151 行 |

**跑测试**：
```bash
cd backend
python -m pytest tests/ -q                 # 全量
python -m pytest tests/unit/test_xxx.py -q  # 单文件
python -m pytest tests/unit/test_xxx.py::test_yyy -q  # 单用例
```

**前端语法自检**（每次改 index.html/admin.html 后必跑）：
```bash
node -e "const s=require('fs').readFileSync('frontend/public/index.html','utf8'); const m=s.match(/<script>([\s\S]*)<\/script>/); new Function(m[1]); console.log('SYNTAX_OK')"
```

---

## 4. 已知问题（接手时须知，勿重复上报）

### 4.1 `test_paipan.py::test_fixture_snapshot_anchor` 失败（既有，无害）
- **现象**：chart.json 快照与当前排盘引擎输出在 `<root>.western.summary.patterns[0]` 不一致（西方盘 pattern 文案英文/中文差异）。
- **性质**：夹具漂移，非引擎 bug（排盘结果本身正确）。
- **建议**：确认当前输出正确后，重新生成 `backend/tests/fixtures/chart.json`（用 `paipan(**STANDARD_KW)` 输出覆盖快照），或标注为「已知夹具漂移、暂跳过」。

### 4.2 `*_node_endpoint_*` 本机偶发失败（环境性）
- **现象**：本机 Python httpx/urllib 请求 node **v24** 的 `server.mjs` 时 `RemoteProtocolError`/`WinError 10054`（连接重置）；node fetch / curl / 最小 node server + Python 均正常。
- **性质**：本机 node v24 + Windows 的特定交互问题；**生产镜像 node 是 v22.14.0，无此问题**。
- **处理**：本机跑失败时，可用 node v22 复验，或标记为环境性跳过；不要把这类失败当 Bug 上报。

### 4.3 测试纪律
- LLM 一律 mock（`chat` 打桩），**不真调 DeepSeek**（省 token + 稳定）。
- Node 端点测试真实拉起 `server.mjs`（随机端口），node 不可用则 skip。
- 数据库用临时库隔离（conftest 的 fixtures），不污染本地 `backend/data/*.db`。

---

## 5. 测试范围（真机走查清单）

### 5.1 国学主线（九法合一）
- [ ] 注册（验证码 + 手机号 + 确认密码）
- [ ] 登录 / 退出 / token 过期自动续期
- [ ] 建档 → 排盘 → 断前尘（3 并发进度）→ 校准问卷 → 预测（8 标题：综合趋势/性格/事业/财运/感情/健康/家庭/大势）→ 追问修正 → 档案
- [ ] 档案列表：查看解读 / 命盘 / 重命名（模态框）/ 删除 / 新建
- [ ] 命盘页：八字/紫微/占星/七政/奇门/五运六气 各 tab 盘面
- [ ] 浏览器返回键应用内回退（history/popstate）

### 5.2 横向扩展（Phase A–D）
- [ ] 首页落地页：左侧三按钮（仅落地页显示，点击展开文字竖→横）
- [ ] 三大落地宣传页 → 进入对应 HUB
- [ ] 生肖流年（零 LLM，报告正确渲染）
- [ ] 临时起卦：六爻/梅花/小六壬/观音灵签 + 断卦（付费 interpret + 缓存命中零扣费）
- [ ] 塔罗：抽牌 + 牌阵 + 综合解读（付费）+ 卡面美术
- [ ] 雷诺曼：抽牌 + 关键词组合 + 深度解读（付费）
- [ ] 星座：选已有档案 → 生成星盘（复用国学档案）+ 深度解读（付费）
- [ ] MBTI：60 题答题 + 判型 + 16 型文案（复用国学档案，回写 case.mbti_type）
- [ ] Banner 右上角菜单：未登录「登录/注册」、已登录汉堡菜单（国学/西式/MBTI/档案管理/退出登录）

### 5.3 积分 / 支付 / 后台
- [ ] 积分扣费：断前尘/预测逐法扣、interpret 即时扣（ref 格式 `job:{id}:{key}` / `divination:{id}` / `tarot:{id}` / `astrology:{id}`）
- [ ] 余额不足弹窗（5002）
- [ ] 后台：用户列表显示余额、赠积分、埋点报表（中文映射）
- [ ] 金数据支付（商户认证后用户自行验证）

### 5.4 部署 / 运维
- [ ] `/api/health` 200、排盘 :9317 监听
- [ ] 4 张新表（divinations/tarot_readings/astrology_readings/mbti_results）+ cases.mbti_type 迁移

### 5.5 当前待验收清单（2026-09-08 需求管理员整理，🟠 均待测试组验收）

> 来源：`docs/需求表.md`（🟠 40 条）与 `docs/Bug管理表.md`（🟠 4 条）。每条备注含实现 commit，验收可 `git log/show <commit>` 定位改动。**验收通过才可置 🟢（仅测试组可设）**。P1 优先。

**A. P1 高优先（建议先验收）**
- [ ] **REQ-080** 单页+真实子路径路由（P1，全站 URL 映射上生产：17 路径映射 + 归一化 + 刷新/直连可用）
- [ ] **REQ-078** 数据层与埋点扩充（P1，B 表/FTS5/C 埋点：asset_upload/pair_analysis/zodiac_match/manual_input）
- [ ] **REQ-085** 积分废除→余额绑定（P1，余额口径全站核对：余额 ¥ 展示、token 计费、后台余额充值/调整）
- [ ] **REQ-092** 新用户建档强制门（P1，0 档案强制、全部功能拦截、未完成下次仍进建档）——**与 BUG-015（建档无法保存）联动排查**
- [ ] **REQ-093** 九法合一配对/八字配对改版（P1，配对解析高亮按钮 + 八字配对）
- [ ] **REQ-063** 余额详情与消费明细可读化（P1，中文标签 + 档案名 + ¥ 单位）
- [ ] **REQ-065** 档案管理列表 CRM 化（P1，单行条目 + 四键 + 手机号/邮箱）
- [ ] **REQ-074** 深度断卦 prompt 增强（P1，liuyao 分层体系 + yaosDetail 富字段）
- [ ] **REQ-070** 分享帮填（P2，一次性链接 + 免登录代建 + 访客隔离）——关联 BUG-012
- [ ] **REQ-044** 临时起卦 v2（P1，单列列表 + 弹窗问题可选）——结果独立页按 REQ-104 后续演进
- [ ] **REQ-072** 三大模块配对解析（P1，pair 端点 + UI）——关联 BUG-021/023、REQ-110
- [ ] **REQ-067 / REQ-068** 档案解耦 + 九法选档案 UI（P1）

**B. 国学模块（P2/P3）**
- [ ] **REQ-037** 全站 UI 文案体系（P2，UI_COPY.buttons 收尾）
- [ ] **REQ-057** 星座恋爱关系板块（P2，匹配度结构 + 文案占位）
- [ ] **REQ-058** 九法合一等待页增强（P2）
- [ ] **REQ-062** 落地页宣传大色块 70% 透明（P2）
- [ ] **REQ-064** 首页 3 键 + 档案管理入口迁移（P2）
- [ ] **REQ-073** 六爻静态文案补齐（P2，S01-S07 全落地；呈现形态按 REQ-107 折叠菜单演进）
- [ ] **REQ-088** 九法合一说明调整（P3，红字提示——文案以 REQ-101 定稿为准）
- [ ] **REQ-094** 八字/九法起名（P2，入口布局以 REQ-100 为准）
- [ ] **REQ-100** 九法合一入口层级调整（P1 待开发）
- [ ] **REQ-101** 九法合一文案重写（P2 待需求方定稿）

**C. 西式 / 星座 / MBTI**
- [ ] **REQ-060** 全局页脚 Sticky（P2）
- [ ] **REQ-061** 占卜动效与翻牌（P2，占位动效 + 素材 6 槽）
- [ ] **REQ-066** 功能设置 7 开关（P2，各开关行为适配）——关联 BUG-013/017
- [ ] **REQ-069** 手动输入模式（P1，六法录入；小六壬后端落库遗留）
- [ ] **REQ-071** Banner 模块下拉导航（P2）
- [ ] **REQ-075** 六爻焦点详解（P2，逐项 LLM + 缓存）
- [ ] **REQ-076** 太初先生 Agent（P2，会话 + 计费 + 记忆）——关联 BUG-014
- [ ] **REQ-077** AI 长期记忆（P1，FTS5 + BM25）
- [ ] **REQ-079** 塔罗/雷诺曼问题改可选（P2）
- [ ] **REQ-081** 观音灵签独立结果页（P2）
- [ ] **REQ-082** 配对/关系入口布局（P2，星座三按钮 + 配对入口；MBTI 按钮位置按 REQ-109 修订）
- [ ] **REQ-083** 星座三关系按钮（P2，样式以 REQ-102 为准）
- [ ] **REQ-086** 余额消耗角标（P2）
- [ ] **REQ-087** 余额入菜单（P2）
- [ ] **REQ-089** 档案详情 v2（P2，修改信息以 REQ-114 为准）
- [ ] **REQ-090** 档案页底部按钮 v2（P2）
- [ ] **REQ-091** 建档下拉即点即填（P3）
- [ ] **REQ-095** 生肖三关系按键（P2，样式以 REQ-102 为准）
- [ ] **REQ-098** PWA（P2，manifest + SW + 缓存失效）

**D. 待验收 BUG（4 条 🟠）**
- [ ] **BUG-011** MBTI 分享越权（S0，7105869 会话隔离）
- [ ] **BUG-012** 分享帮填入口缺失（S2，e1916b9/e7dfcd5）
- [ ] **BUG-013** 功能设置开关不可点（S2，7105869/02382b6/6219573）
- [ ] **BUG-014** 太初先生展示开发说明（S2，789bee6）

> 已知缺口（验收时注意，非待验收项）：REQ-059 后台素材管理 UI 缺失（🔵，后端就绪）；BUG-015 建档无法保存（S1，待开发）；REQ-069 小六壬后端落库遗留；REQ-084 动画功能未接入（🟡）。

---

## 6. 工作流程

1. **接手**：读本文件 + `docs/需求表.md` + `docs/Bug管理表.md`，了解现状与待办。
2. **跑基线**：`cd backend && python -m pytest tests/ -q`，记录结果（当前基线：98 passed / 1 既有夹具失败）。
3. **发现 Bug**：登记 `docs/Bug管理表.md`（BUG-xxx，含严重度 S0–S3、复现步骤、期望）。
4. **验证修复**：修复后跑相关测试 + 真机复验，更新 Bug 状态 🟡→🔵→🟢。
5. **验收需求**：新 REQ 上线后逐条验收，更新 `docs/需求表.md` 状态。
6. **收尾**：把本 Session 的测试结论、新 Bug、状态变更同步回需求表/Bug 表。

---

## 7. 与需求表 / Bug 表的联动

- **需求（REQ）**：功能上线前，测试组按需求验收标准验证；通过后 REQ 状态 🟡→🟢。
- **Bug（BUG）**：测试发现的缺陷登记 Bug 表；Bug 修复涉及新功能时在需求表补 REQ；Bug 关闭时注明复现/修复 commit。
- 两份表都在 `docs/` 下，均有使用说明（字段、状态机、严重度/优先级）。

---

## 8. 测试工具与命令速查

| 用途 | 命令 |
|---|---|
| 全量后端测试 | `cd backend && python -m pytest tests/ -q` |
| 单文件测试 | `python -m pytest tests/unit/test_zodiac.py -q` |
| 单用例测试 | `python -m pytest tests/unit/test_zodiac.py::test_xxx -q` |
| 一键回归（pytest + 前端语法 + node 语法） | `python backend/scripts/run_regression.py`（`--strict` 已知漂移计入 / 可透传 `-k`） |
| 环境一键体检 | `python backend/scripts/env_doctor.py` |
| 前端语法自检 | `node -e "…new Function(script)…console.log('SYNTAX_OK')"` |
| Node 服务语法 | `cd backend/paipan-node && node --check server.mjs` |
| 本地起服务 | `start.bat`（或 uvicorn） |
| 线上访问 | `https://taichu.xyz`（C 端）/ `https://taichu.xyz/admin.html`（后台） |
| 服务器排查 | SSH `root@47.237.91.97`（密钥 `~/.ssh/taichu_sg_ed25519`），`cd /opt/taichu/app && docker compose ps/logs` |

> ⚠️ 沙箱提示：本机 DSH 沙箱下跑 pytest 需 **danger-full-access**（conftest 的 mkdtemp
> 0o700 临时库目录被 workspace-write 拒绝），详见 `docs/测试知识库.md` §1.2。
> 另注意后台测试（test_admin_prompts.py）会把 PROMPT_BASE monkeypatch 到临时目录，
> 不会污染真实 `backend/prompts/`。

---

> 本文档随项目演进持续更新；测试组 Session 的每轮工作，收尾前把结论同步回需求表/Bug 表。
