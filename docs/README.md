# docs/ — 太初 文档库（参考层）

> **定位变化（2026-09-10）**：项目治理已迁至 `00_根/` + `40_节/` + `99_状态/` + `50_痕/`（每轮入口：`00_根/入口.md`）。本目录降级为**参考 / 历史文档库**：规范细节、设计基线、法规、研究、归档。
> 结构约定：
> - **顶层**只放仍生效的参考文档；已过时的一律进**项目根** `_archive/`（2026-09-11 起在项目根，不在本目录下；含 `破竹迁移/` 子目录）。
> - **单一事实源**：实时状态看 `00_根/入口.md` 与 `40_节/`；决策速查看 `99_状态/决策.md`；已知问题看 `99_状态/已知问题.md`。

## 顶层参考文档

| 文档 | 内容 |
|---|---|
| `架构设计.md` | 设计基线：设计意图 / 范式 / 契约语义（鉴权 / 数据模型 / method-result v2）/ 成本口径 |
| `Code-Wiki.md` | 代码全景 Wiki：模块职责 / 数据模型 / 依赖 / 运行方式（实现细节以代码为准） |
| `登录指南.md` | 系统登录 / 账号 / 访问入口 / 后台地址（产品 / 运营用） |
| `API-Key安全与LLM接入说明.md` | DeepSeek 接入与 key 安全约定 |

## 子目录

| 子目录 | 内容 |
|---|---|
| `standards/` | 实例层细节规范：01 命名 / 02 技术栈 / 03 接口与数据字典 / 04 前端设计令牌 / 05 前端组件库规范 / 06 环境与部署 / 07 版本迭代记录 |
| `retrospective/` | 复盘与可复用资产包（含 `2026-09-10-破竹框架重构复盘.md`） |
| `research/` | 行业研究（竞品 / 商业化 / 形态 / 渠道） |
| `legal/` | 用户协议 / 隐私政策 / 交付说明与风险清单 |
| `runbooks/` | 运维手册（占位） |
| `exchange/` | **旧交付物中心**（历史；待处理项已登记 `40_节/待办/节109`，不再新增） |

> **已迁出本目录（2026-09-11）**：
> - 归档区：原 `docs/_archive/` → **项目根 `_archive/`**（破竹协议附录规定归档区在项目根）。内里含 `破竹迁移/`（太初守则、协作协议、测试知识库、overview 等）、`前端-vite-ts-参考实现/`、`美术-参考样本草稿/`。
> - ADR 原文：原 `docs/adr/` → **`_archive/adr/`**（0000–0009 + FE-001；历史档案，不再作为决策流程入口；决策速查见 `99_状态/决策.md`）。
> - **历史台账**：原 `docs/需求表.md` / `docs/Bug管理表.md` + 生成快照（`台账状态一览.md` / `台账快照-*.csv`）→ **`_archive/台账冻结/`**（2026-09-11 归档；实时状态见 `00_根/入口.md` + `40_节/`）。生成器 `tools/gen_ledger_snapshot.py` 同步归档至 `tools/_archive/`。

## 已归档（2026-09-10 破竹重构）

| 原文件 | 去向 |
|---|---|
| `AGENTS.md`（太初三角色守则全文） | `_archive/破竹迁移/AGENTS-太初守则-归档-2026-09-10.md`（字节级一致） |
| `docs/开发测试协作协议.md`（三角色流程已废弃） | `_archive/破竹迁移/开发测试协作协议-已废弃-三角色流程.md` |
| `docs/测试知识库.md`（内容已迁入 `99_状态/已知问题.md`） | `_archive/破竹迁移/测试知识库-已废弃-测试组角色.md` |
| `overview.md`（REQ-084 任务总览） | `_archive/破竹迁移/overview-REQ084-任务总览-2026-09-09.md` |

## 写作约定

1. 规范变更须同步 `standards/07-版本迭代记录.md`，并在 PR 描述引用（注意：版本记录是 **07**，05 是前端组件库规范）。
2. 决策先写 `99_状态/决策.md`（速查表，只追记"为什么"）；ADR 全文可存 `adr/` 但不再作为决策流程入口。
3. 顶层文档的移动 / 新增 / 删除须同步更新本 README 索引。
