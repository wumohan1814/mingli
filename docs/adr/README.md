# ADR · 架构决策记录

> 先写 ADR 再动手。模板见 `0000-决策记录模板.md`；文件名 `NNNN-<短标题>.md`。
> R1–R16 由技术框架方案承载；本目录登记**已正式成文的决策**与**剩余待决项**。

## 已决 ADR（2026-09-05 关闭 P0 与全部 P1）

| 编号 | 决策 | 关闭的问题 |
|---|---|---|
| [0001](0001-排盘层自研与复用边界.md) | 排盘层**自研为主**：底层用开源库（`lunar-python` / `iztro` / `mingyu-core`），胶水 / 切片 / 打分 / 提示词全部自研；mingli 仅作参考 | 原 R4 / C1「原样复用 Skill 契约」在执行中丢步骤；与产品化需求（多用户、异步、计量）不匹配；**排盘层技术路线** |
| [0002](0002-校验子会话仅断前尘阶段执行.md) | 校验**仅断前尘阶段**执行，抽为独立 `validation` 模块，用便宜模型单独计量 | **原 P0**：校验被整体省略 → 成本闸门失真约 2 倍、`score.py` 命题质量（30% 权重）恒取 0.5、阶段 5 降权无数据源 |
| [0003](0003-bazi-hunyin-caiyun两阶段纳入.md) | `bazi-hunyin-caiyun`（婚姻 / 财运 / 事业）**两阶段纳入**，成为常驻第 9 法 | 主打路径「事业运势」是否调用该专题未定义 |
| [0004](0004-路由决策归属routing模块.md) | 新增 `routing` 模块，路由表**配置化、零 LLM**，决策落库留存 | 阶段 4 路由无归属模块 |
| [0005](0005-长时编排采用jobId与代码轮询.md) | `duan-qian-chen` / `predict` 改异步：`jobId` + **前端代码轮询（零 LLM）**，新增 `jobs` 模块与表 | 断前尘可达数分钟，但 REST 全为同步接口，无异步端点 |
| [0006](0006-归档以数据库为唯一事实源.md) | DB 为唯一事实源，**不引入 `archive.py` / Obsidian**，导出留 Phase2 | 归档形态未决策 |
| [0007](0007-维持强制注册R7现状.md) | MVP **维持强制注册**；获客可测（有投放、日活可测）时复评 | 注册墙 vs 转化的张力未评估 |
| [0008](0008-JWT算法采用HS256.md) | JWT 用 **HS256**，密钥走环境变量；模块拆多实例（Phase2）时升级 RS256 | 算法与密钥轮换未定 |

### 关键连带影响（务必记住）

1. **成本口径已变**：首跑 LLM 调用从 ~13 次 → **~23 次**（9 法分析 + 9 法校验 + 预测 ~5 次）。
   校验用便宜模型折算后约 15.5 次分析当量。**原阈值 ¥0.3–0.5/case 作废**，
   建议上修 **¥0.5–1.0/case**，以 self-run 10× 实测为准。
2. **方法模块 8 → 9 法**：`bazi-hunyin-caiyun` 常驻，断前尘串行时长上升约 12%。
3. **契约归属自主**：`chart.json` / `method-result v2` 由太初定义，兼容 mingli 关键字段
   属**选择**而非义务（mingli 同为本人复刻产物，无第三方包袱）。
4. **失去上游自动惠及**：改为人工评估同步（ADR-0001 代价）；排盘回归基线与案例库锚点由
   Phase2 提前到 MVP 必做。

## 剩余待决（P2 · 实现期，不阻塞编码）

| 编号 | 决策点 | 建议 | 触发时点 |
|---|---|---|---|
| ADR-09 | 限流 / 失败锁定阈值、captcha 选型 | 先按 60s / ≥5 次失败锁定 15min 上线，实测后调；captcha 仅在异常频次触发（降注册摩擦） | 网关开发时 |
| ADR-10 | 禁区判定机制 | 双层：关键词表拦截 + LLM 二次判定 | 合规护栏开发时 |
| ADR-11 | `domain` 枚举冻结 | 冻结为 事业 / 财运 / 感情 / 健康 / 六亲 / 学业 / 性格 | 断前尘问卷合成开发时 |
| ADR-12 | SQLite → PostgreSQL 切换时点 | 多实例水平扩展前必须切换 | Phase2 拆分前 |
| ADR-13 | ORM / 迁移工具选型 | SQLAlchemy 2 + Alembic | 数据底座开发时 |
| ADR-14 | 前端三件套（路由 / 状态 / 请求层） | React Router + Zustand + TanStack Query | 前端初始化时 |
| ADR-15 | 校验模型选型与误报阈值 | 便宜模型；需在 fixtures 建校验回归锚点防误报污染权重 | `validation` 模块开发时 |
| ADR-16 | 主问领域识别方式 | **建档表单让用户直接选主场景**，省掉一次分类 LLM 调用 | 建档页开发时 |

## 复评触发条件

| 决策 | 何时必须复评 |
|---|---|
| ADR-0007 强制注册 | 获客开始投放、日活进入可测区间时 |
| ADR-0008 HS256 | 模块拆多进程 / 多实例，或引入第三方验签方时 |
| ADR-0002 仅断前尘校验 | self-run 中观察到预测阶段误判率偏高时 |

## 由技术框架方案承载的决策（不另立 ADR）

R1 完整断前尘进 MVP ｜ R2 / R6 / R8 模块化单体 + 微服务形状边界 ｜ R3 批量卡片问卷 + 多轮修正 ｜
R5 双层合规护栏 + MVP 不接支付 ｜ R7 用户名 / 密码 + JWT ｜ R9 成本闸门前置 ｜
R10 限流 + 锁定 + captcha ｜ R11 主模块统一合成两阶段 ｜ R12 手动配置切档 ｜
R13–R16 见技术框架方案 v2.4 §0。

> 注：原 R4「原样复用 Skill 契约」已由 **ADR-0001 修订**为「自主契约 + 兼容 mingli 关键字段」。
