# 太初 · Bug 管理表（Bug Tracker）

> 本表是项目 Bug 的**单一事实源**：所有 Bug（真机走查 / 用户反馈 / 测试 / 代码审查 / 后台 error_reports 等渠道发现）一律在此登记、分配 ID、追踪状态；修复后更新状态与 commit。
> 需求管理见 [需求表.md](需求表.md)（REQ 编号）；Bug 修复涉及新增功能 / 工程改造时按 §4 与需求表联动。
> 每次会话收尾前，将本 session 涉及的新 Bug / 状态变更同步回本表。

## 1. 使用说明

1. **登记**：新 Bug 在 §5 表格新增一行，分配 `BUG-<三位序号>`（全局递增 = 现有最大序号 + 1，不复用、不因关闭而重排）。
2. **状态流转**（颜色体系与需求表一致）：

   | 状态 | 含义 |
   |---|---|
   | 🟡 待办 | 已登记，待复现确认 / 待排期修复 |
   | 🔵 处理中 | 正在修复 |
   | 🟠 待验证 | 修复已提交，等测试验证 |
   | 🟢 已修复 | 修复已验证通过（本地 / 线上回归通过） |
   | ⚪ 已关闭 | 不复现 / 重复 / 明确不修 / 已废弃，须在「备注」注明原因 |

   状态流转：🟡 → 🔵 → 🟠（修复提交待验证）→ 🟢（测试验证通过）。🟢 只能由测试组设置。已关闭 Bug 复发 → 以新 BUG 编号重新登记 🟡，备注关联旧条目。
3. **严重度**：

   | 级别 | 定义 | 处理时限 |
   |---|---|---|
   | S0 致命 | 数据丢失/错乱、安全漏洞、全站或支付不可用、资金相关错误 | 立即 |
   | S1 严重 | 主流程（建档/排盘/断前尘/预测/付费解读）不可用或产出错误 | 当日 |
   | S2 一般 | 功能异常但有绕行，或仅影响单模块/单场景 | 排期 |
   | S3 轻微 | 文案、样式、体验、非关键边界问题 | 择机 |

4. **粒度**：一条 Bug 对应一个可复现的缺陷，不混多件事；同一根因的多个表象可合并为一条并列出全部入口。
5. **查重合并**：登记前先与 §5 全部条目（**含 ⚪ 已关闭**）比对——重复 / 同一根因者**不新增行**：新入口、新复现或补充信息并入已有条目（备注留痕，必要时重新打开状态）；与需求表（REQ）已知待办 / 缺陷重叠时归 Bug 并备注关联对应 REQ，避免同事项在需求表与 Bug 表双行漂移。
6. **可执行性保证**：登记 Bug 的描述须达到「开发可直接开始修复」的粒度——复现步骤含环境与前置条件、现象含报错码 / 文案、期望明确、严重度可判；任一项缺失或表述含糊（无法据此定位 / 复现）时，**先向上报方确认补齐后再登记**，不得凭推断补全。

## 2. 登记字段与填写要求

| 字段 | 必填 | 要求 |
|---|---|---|
| 标题 | ✅ | 一句话现象描述，含模块前缀（例：「星座页：出生表单重复提交会创建两个 case」） |
| 模块 | ✅ | 前端 C 端 / 后端 API / 排盘 / LLM / 积分支付 / admin 后台 / 部署运维 / 文档 |
| 严重度 | ✅ | S0–S3，见 §1.3 |
| 现象 | ✅ | 实际发生什么（含报错文案 / 状态码 / 截图路径） |
| 复现步骤 | ✅ | 最小复现路径：环境（线上 https://mingli.example.com 或本地 :8000）、设备与浏览器、前置条件、逐步操作 |
| 期望 | ✅ | 应该是什么行为 |
| 发现 | ✅ | 日期 + 渠道（真机走查 / 用户反馈 / 测试 / 审查 / 后台 error_reports） |
| 修复 | 修复后 | commit 哈希 + 涉及文件 / 版本 |
| 备注 | 条件 | 根因、关联 REQ/BUG、关闭原因、临时规避方案、待复现标记 |

> **登记模板（用户投喂 Bug 时按此给全即可）**
> 标题｜模块｜严重度｜现象（实际发生了什么）｜复现步骤（环境 + 操作）｜期望｜发现渠道

## 3. 管理流程

1. **登记**：任何渠道发现的缺陷先入表（🟡）。信息不全或不可复现时，备注标「待复现 / 待补信息」并写明最可能怀疑点。
2. **排期与处理**：按严重度定优先级；修复须遵循仓库规范——前端改动后跑 `node frontend/scripts/precompile.js` 校验预编译（守护 `<script type="text/babel">` 红线）；改接口 / 表结构先读 `docs/standards/03-接口与数据字典.md`；改动走 commit。完成后填「修复」列并置 🔵。
3. **验证与关闭**：回归验证通过 → 🟢；不通过退回 🔵。确定不复现 / 重复 / 不修 → ⚪ 并注明原因。
4. **发布留痕**：每次发布将本次已 🟢 的 Bug 汇总进 `docs/standards/05-版本迭代记录.md` 的 `### Fixed`。

## 4. Bug ↔ 需求联动

- Bug 修复若同时需要**新增用户可感知功能或明确工程改造** → 先在 [需求表.md](需求表.md) 登记 REQ，Bug 行备注「关联 REQ-xxx」。
- 新需求 / 重构上线后引发的回归缺陷 → 在本表登记 BUG，对应需求行备注「回归见 BUG-xxx」。
- 线上 S0/S1 事故复盘产出的改进项 → 记入需求表或本表，双向留痕。

## 5. Bug 登记表（按 BUG 编号升序）

| ID | 标题（模块·现象） | 严重度 | 状态 | 发现（日期·渠道） | 修复（commit/版本） | 备注（根因 / 关联 / 关闭原因） |
|---|---|---|---|---|---|---|
| BUG-001 | 前端 C 端 · 落地页小屏布局：左侧 3 按钮展开/凸起遮挡右侧落地宣传信息 | S2 | 🔵 | 2026-09-07 · 用户走查 | 1dd0b8f | 现象：手机小屏上按钮突出时盖住右侧内容；期望：按钮上移贴近 Banner、右侧内容下移错开不遮挡；关联 REQ-027（验收不通过，REQ-027 已退回 🔵）。（2026-09-07 真机复验退回 🔵：小屏下三按钮仍遮挡右侧落地信息，未按需求将落地信息下移/右移、整体仍居中布局——UI 需重构） |
| BUG-002 | 前端 C 端 · 入口登录判定缺失：未登录点「开始」（九法合一/生肖等）直接加载失败 | S1 | 🟢 | 2026-09-07 · 用户走查 | 70ae041 | 2026-09-07 真机复验通过：未登录点各入口均引导至登录页（主缺陷已修）。修复中发现衍生体验问题「登录失败报『登录信息已过期』而非『用户名或密码错误』」→ 已另登记 REQ-049；关联 REQ-001 |
| BUG-003 | 后端/前端 · 生肖流年结果底部字段空白（贵人/五行关系/风险关系/生肖地支本气生年干五行/行动建议） | S2 | 🔵 | 2026-09-07 · 用户走查 | d454f88/1dd0b8f/d69708f | 期望：确定性代码补齐上述字段（零 LLM，对齐 REQ-021）；图标重设计另列 REQ-041。（2026-09-07 真机复验退回 🔵：底部贵人/行动建议已出现，但中部「五行关系/中性关系/生肖地支本气克年干五行」等仍空白——需补中段字段渲染） |
| BUG-004 | 后端 · 六爻起卦结果仅显示单个爻，非完整卦象 | S1 | 🔵 | 2026-09-07 · 用户走查 | 70ae041 | 期望：完整显示六爻位（初爻→上爻）+ 本卦卦名，有动爻时显示变卦与动爻标注（数据不足则补数据结构）；关联 REQ-022/044。（2026-09-07 真机复验退回 🔵：①六爻位文字已显示 ✅ ②右侧柱状图阴阳爻绘制错误——阴爻未显示中间断开、仅全长阳爻 ③少阳/少阴等未居右、无法拼成完整卦象图 ④结果文案仅卦名、缺卦的通用解释 ⑤起卦前无问题文本框——关联已有 REQ-044；⑥塔罗/雷诺曼 UI 改版未完成——关联 REQ-042/043） |
| BUG-005 | 前端 C 端 · MBTI 手动输入类型无 16 型详细文案 | S2 | 🟢 | 2026-09-07 · 用户走查 | a379cc7/1dd0b8f | 2026-09-07 真机复验通过：手动选型后可见对应 16 型评语（主缺陷已修）。复验引出的延伸问题已归 REQ-039 细化：①档案内 MBTI 仅显示类型、缺倾向表单与评语 ②答题选型的倾向表单未入档案 ③已选型档案再入 MBTI 应直达结果+「重新测试」按钮而非直接进入重测 |
| BUG-006 | 前端 C 端 · 星座/MBTI「选择已有档案」入口 UI 不明显：档案项像注释不可点 + 「选择档案」按钮过小（远小于「生成星盘」主按钮） | S2 | 🟡 | 2026-09-07 · 用户走查 | | 现象：星座页档案项与「选择档案」按钮视觉层级弱，用户不确定是否需先选档案；MBTI 同问题。期望：档案项改为明显可选样式（卡片/列表带选中态），「选择档案」与主操作同尺寸层级、步骤引导清晰（2026-09-07 并入：选择按钮过小表象）；关联 REQ-028/039 |
| BUG-007 | 前端 C 端 · 星盘格局英文未翻译：星座结果页「元素模式格局」与档案命盘页「占星盘的格局」下方仍有大量英文 | S2 | 🟠 | 2026-09-07 · 用户走查 | b61f4e4 | 期望：两处英文术语/描述全部中文，与既有占星格局中文翻译一致（参照 commit e723d37 映射方案补全遗漏，无英文残留）；同一数据源两处表象已合并，疑似 chart.western.summary 英文文案缺映射，修复后请同步测试夹具 chart.json 基线（测试组交接文档 §4.1） |
| BUG-008 | 前端 C 端 · 档案按钮文案「命名」应为「重命名」 | S3 | 🟢 | 2026-09-07 · 用户走查 | 1dd0b8f | 2026-09-07 真机复验通过：按钮文案已为「重命名」。关联 REQ-037（全站文案轮次将统一复核按钮措辞） |
| BUG-009 | 前端 C 端 · MBTI 分享链接打开空白（REQ-047 回归） | S2 | 🟡 | 2026-09-07 · 真机走查 | | 现象：点击分享按钮拿到的链接，在新窗口打开为空白页。复现步骤：本地 :8000 登录 → MBTI 结果页分享 → 复制 url → 新窗口打开。期望：免登录直达答题页。本地排查（测试组）：GET /mbti/share/{token} 返回 index.html（SPA fallback 正常）、GET /api/mbti/share/{token} 无效 token 返回 code1002——API 与路由层均正常，疑前端 mbti-share 页渲染或 token 传递问题，备注「待复现：需真机抓浏览器 console 报错与完整 URL」；关联 REQ-047（分享入口时机另见 REQ-047 退回备注） |

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-07 | 建立 Bug 管理表：字段规范（§2）、状态机与严重度 S0–S3（§1）、管理流程（§3）、Bug↔REQ 联动约定（§4）；暂无登记条目 |
| 2026-09-07 | 状态机升级为 🟡→🔵→🟠 待验证→🟢（🟢 仅测试组可设）；§1 新增「查重合并」规则 |
| 2026-09-07 | §1 新增「可执行性保证」：Bug 描述须达可直修粒度，缺复现/期望/严重度先向上报方确认再登记 |
| 2026-09-07 | 首批登记 BUG-001~006（2026-09-07 用户走查/反馈，均 🟡）：落地页遮挡 / 入口登录判定 / 生肖空白 / 六爻卦象 / MBTI 手动文案 / 选档案 UI；均附现象-期望与关联 REQ |
| 2026-09-07 | 登记 BUG-007（星盘格局英文未翻译：星座结果页 + 档案命盘页同源合并）；BUG-006 并入「选择档案按钮过小」表象（星座/MBTI 同源） |
| 2026-09-07 | 登记 BUG-008（档案按钮「命名」→「重命名」，S3 文案）；关联 REQ-037 文案轮次 |
| 2026-09-07 | 首批真机复验（测试组）：BUG-002/005/008 → 🟢；BUG-001/003/004 退回 🔵（附复验细化现象）；BUG-007 开发已修复（🟠 待复验）；登记 BUG-009（MBTI 分享链接空白，待复现） |
