# 05 · 版本迭代记录

> 格式参考 Keep a Changelog。条目按倒序追加，每条含「变更 / 影响 / 迁移要求」。

## [未发布]（2026-09-14 · 项目代号 taichu→mingli · 节147）

> **运行逻辑零改动**，但**触及部署契约与数据形状**：env 前缀、DB 文件名、DB 列名、浏览器本地存储键、docker 名与宿主路径、CSS 令牌契约。**线上迁移未执行**（步骤见 `ops/deploy/README.md`）。

### Changed
- **JS 标识**：`TC_COPY`/`TC_SETTINGS`/`TC_PAIRS`/`TC_TERM_CARDS` → `ML_*`；`TCTerm{Card,Chip,Panel,Row}` → `MLTerm*`；10 个 `tc*`（`tcSet`/`tcSavedRef`/`tcLoadedRef`/`tcSeqRef`/`tcSave`/`tcShouldPlayAnim`/`tcShouldShowCards`/`tcAskNextDefault`/`tcCardMap`/`tcFindCard`）与 8 个 `*Tc*`（`applyTcSettings`/`loadTcSettings`/`normalizeTcSettings`/`patchTcSettings`/`installTcAliases`/`shallowEqTc`/`setTcSet`/`setTcSave`）→ `ml*` / `*Ml*`；`shareTaichuUi`→`shareMingliUi`；`applyTaichuTarotSpreads`→`applyMingliTarotSpreads`（共 417 处 / 37 文件）
- **CSS 令牌**：`--taichu-*` → `--mingli-*`（`tokens.css` 定义与自引用；`standards/04`、`05` 同步）。⚠️ **`--tc-*` 令牌族（273 个）本期未动**，理由与待裁决见 `99_状态/决策.md`
- **浏览器本地存储**：`taichu_token`/`_refresh_token`/`_session_id`/`_skin`/`_onboarding_form`/`_admin_token`/`_admin_role` → `mingli_*`；DOM id `taichu-toast`/`taichu-admin-toast` → `mingli-*`。**代价：现存用户一次强制登出 + 皮肤偏好重置**（内测期不做兼容迁移）
- **Service Worker**：缓存前缀 `taichu-img-` → `mingli-img-`，账本 `mingli-img-meta-v1`、key `/__mingli_sw_meta__`；**activate 保留对旧 `taichu-img*` 前缀的一次性回收**（老设备旧缓存必须清得掉）
- **环境变量前缀**：`TAICHU_` → `MINGLI_`（`config.py` 的 `env_prefix` + 2 处 `validation_alias` + 全部注释；`.env.example`、`.env`、`backend/.env`、`docker-compose.yml`、`standards/06`、`ops/env`、`standards/03` §7 同步）
- **数据库**：`data/taichu_{analytics,feedback,ops}.db` → `mingli_*.db`；`user_settings.share_taichu_ui` → `share_mingli_ui`（REST 字段名同源同改）
- **部署标识**：docker 网络/卷/镜像名 `taichu` → `mingli`；宿主路径 `/opt/taichu/` → `/opt/mingli/`；`pyproject.toml` 包名 `taichu-backend` → `mingli-backend`；`package.json` 名同步；`start.bat`/`start.sh` 标题与提示
- **`chart.json` 契约取值**：`meta.source` 由 `taichu-paipan` → `mingli-paipan`（**字段结构未变**，仅取值）
- **命名约定**：`standards/01` §8 环境变量前缀、方法 key 与 `mingli-reference` `SKILL.md` 的对齐说明

### Added
- `data/migrations/analytics/0002_rename_share_mingli_ui.sql`（+ 同名 rollback 脚本）：`user_settings` 列改名，含幂等自检说明
- `ops/deploy/README.md` 新增「**节147 · 代号 taichu→mingli 的线上迁移步骤**」：唯一清单 —— 第 0 步备份 → 仓库改名 → 部署目录 → `.env` 逐键改名 → DB 文件名与列迁移 → 起服务验证 → 「例外两项」可选改名 → 回退

### Renamed
- `docs/archify/taichu-{architecture,dataflow,sequence-duanqianchen}.json` → `mingli-*.json`
- `backend/paipan-node/taichu-tarot-spreads.mjs` → `mingli-tarot-spreads.mjs`
- `backend/reference/mingli/` → `backend/reference/mingli-reference/`（**消歧**：新项目代号也叫 `mingli`；其私有仓库地址 `github.com/wumohan1814/mingli` 是外部真实仓库名，不改）

### Added（2026-09-14 追加，用户第四条指令）
- **站点域名改为可配置项，真实域名/IP/私钥名一律不进仓库**：`Caddyfile` 主站地址改 `{$MINGLI_SITE_DOMAIN}`；原 `bb3a` OpenClaw 接入点抽为**可选附加站点**（`ops/deploy/caddy-extra.example` → 本地 `.gitignore` 覆盖的 `caddy-extra.caddy`）；`.env.example` 只留占位；`docker-compose.yml` 的 caddy 服务透传 env 并挂载 extra 文件
- `.gitignore` 新增 `ops/deploy/caddy-extra.caddy`（本地附加站点，含真实域名与内网地址）与 `docs/未公开/`（经营/策略类文档；`docs/命理-纯单机占卜工具上架可行性评估.md` 已移入）
- **品牌口径更正**：`太初` → **`命理`**（节145 第一轮按需求原话做的「命理太初」被用户 09-14 更正）。再改 379 处 / 98 文件 + 4 个文件名（`命理用户协议.md` / `命理隐私政策.md` / `节128-命理自研内核.md` / `命理-纯单机…评估.md`）

- **合规文书运营主体抽成配置**（2026-09-14 用户拍板）：`frontend/public/legal/*.html` 与 `docs/legal/*.md` 的真实姓名/手机号/邮箱换成 `{{OPERATOR_NAME}}` / `{{OPERATOR_CONTACT}}` / `{{OPERATOR_EMAIL}}` 占位符；真实值走 `MINGLI_OPERATOR_*`，由后端**服务端渲染注入**（新增 `app/legal.py`；`main.py::SPAStaticFiles` 只对含占位符的 `/legal/*.html` 做替换）。缺配置时协议页 **500（fail closed）**，启动打 warning。新增回归 `backend/tests/unit/test_legal_docs.py`
- ~~`LICENSE` 版权人~~：仍为真名，**未动**（不在用户本次授权范围；待其确认是否换组织名/笔名，或等发布换 Prosperity 许可时一并定）

### 例外（**有意不改**，均在 `40_节/完成/节147-*.md` 逐条具名）
- ✅ **~~线上域名~~ 已改为可配置项**（2026-09-14 用户更正）：值在未入库的 `.env`，仓库里只有占位 —— 开源不会暴露域名
- 服务器 SSH 私钥文件名（仓库里按 `<你的私钥文件>` 占位，真实名只在你的 `~/.ssh/config`）
- 设计工具产物 / 归档 / vendor / 节139 三个历史基线快照 / 节文件「用户原话」栏 / 节145·147 自身的改名对照文档
- 归档文件真实名 `taichu-h5-prototype.html`；节146 已 DROP 的充值码序列号格式串 `TC-XXXXXX`
- **`--tc-*` CSS 令牌族（273 个）**：经核查是主题色语义族、非项目代号 → 不动（理由见 `99_状态/决策.md`）

### 迁移要求
- **必须按窗口整体迁移**（`.env` 键名与 DB 文件名同窗口完成，否则服务起不来）：见 `ops/deploy/README.md`
- ⚠️ **GitHub 仓库改名必须早于服务器下次 `git pull`**（部署脚本已指向新地址）
- 回退：`git reset` + `.env` 键名改回 + DB 文件名/列名手工还原 + `0002_rollback_rename_share_mingli_ui.sql`

---

## [未发布]（2026-09-14 · 品牌更名「命理」与首页动画删除 · 节145）

> 中文品牌面 + 角色名 + 素材删除；**运行逻辑零改动**（只改显示文案、注释与静态素材）。拼音代号 `taichu`→`mingli` 归节147。

### Changed
- **品牌字样**：「太初」→「命理」——覆盖 `index.html` / `admin.html` / `manifest.webmanifest` / `sw.js` / `ui-copy.js` / 两份 legal HTML / 后端可见串（`app/api/agent.py`、`app/credits/labels.py`、`app/admin/router.py`）/ `main.py` 的 FastAPI title / `backend/prompts/` / 现行 docs 与 `00_根/` 五件 + 历史节文件
- **角色名**：「太初先生」→「王先生」——界面 / 开场白 / 输入占位 / 错误提示 / 后台提示词分类 / 流水标签 / `backend/prompts/agent/{master,greeting}.md`；开场白第 3 条「贫道太初…」按王先生口吻改写为「我是王先生…」
- `00_根/复用.md` §四「图标 / 美术素材」：`art/` 子目录清单删去 `module-mascot/`、`mingli-pet/`；引用三分法③的示例删去「mascot 配置」
- `00_根/导航.md` §7.4：`/art/` 根绝对引用 26 → **23 处**（Q 版动画相关 4 处随删除消失）
- `00_根/全貌.md` 第①层首页：删「三只 Q 版化身动画」描述，改注明进「王先生」会话走顶部 Banner「对坐谈心」

### Removed
- **首页三模块 Q 版循环动画整块**：`views-home.js` 的 3 处 `mascot` 配置 + 渲染块、`css/components.css` **92 行**（`.landing-mascot*` / `.anim-mascot` / `.mascot-sprite` / `.lm-fx` / 4 组 `@keyframes`）、`js/components.js` 的 `spriteKfName` / `ensureSpriteKeyframes`（**23 行**）、`ui-copy.js` 的 `landing['mascot-{guoxue,xishi,mbti}-alt']` **3 键**
- 素材：`frontend/public/art/mingli-pet/`（8 webp）、`frontend/public/art/module-mascot/`（3 webp）——**共 11 个文件、合计约 810 KB，不保留备份**（用户拍板「以后也不会需要恢复」）
- 生成脚本：`frontend/scripts/gen-mascot-placeholder.mjs`、`frontend/scripts/_gen-mascot-placeholder.cjs`
- `LandingPage` 的 `agentEnabled` 入参及其在 `index.html` 的传值（删除后无引用方）

### 迁移要求
- **无代码 / 数据迁移**。⚠️ 两点运维提示：① 已装 PWA 用户的桌面应用名随 `manifest.webmanifest` 更新（需一次 SW 更新生效）；② 老设备浏览器缓存里的 `/art/mingli-pet/*` 已无引用方，且 `/art/` 不在 SW 缓存白名单内，预计自然失效——**未真机确认**

---

## [未发布]（2026-09-12 · 文案/美术素材归口规范同步 · 节105 阶段5 + 节136 联动）

> 纯文档修订，运行层零改动。起因：用户追问「以后新 UI / 文案素材是否都在同一处改」，核查发现归口事实已在节105/135/136 落地，但 Agent 必读规范层有缺口与漂移。

### Added
- `00_根/复用.md` §四「图标 / 美术素材」行：确立美术素材**唯一物理归口** `frontend/public/art/<用途子目录>/` 与引用三分法（Icon/现有 sprite｜CSS `--art-*` 令牌｜JS 顶部路径常量 + 根绝对路径）；与「新增面向用户的文字」行共同构成文案/素材四归口（ui-copy.js / data/content/ / tokens.css / art/）
- `01-命名与变量约定.md` §12：补物理归口指针（唯一权威指向复用.md §四，防双写），明确破竹合规自检第 5/6 项以四归口为检查对象
- `04-前端设计令牌.md` §6：注明该表只管 CSS 位图令牌，JS `<img>`/sprite 类素材归口指向复用.md §四

### Changed
- `00_根/复用.md` §一设计值表 / §四样式行 / 皮肤注记：对齐节136 A/B 后现状（`--mingli-sp-*` 8px 基准、`--mingli-shadow-*` 三层、散色门禁 0 存量只许减、皮肤表已在 tokens.css）
- `04-前端设计令牌.md` §8：美术资源统计 18 → **9**（与 tokens.css 实测核对）；全表加漂移注记（实测 421 个 = mingli 75 + tc 273 + art 9 + 旧族 64，实时数以 tokens.css 为准）

### 迁移要求
- 无（规范文档修订，无代码/数据变更）

---

## [未发布]（2026-09-12 · 三主题视觉系统升级设计定稿）

> 对应设计稿：`太初三主题设计方向.design/`（8 页面 + 11 资产）；本次仅更新规范与交付设计资产，未改运行层代码。

### Added
- **三主题统一视觉系统**：国学预测 / 西式占卜 / 心理测试三域共用「午夜蓝 + 香槟金 + 象牙白」骨架，各自保留皮肤签名（`guoxue` 玉青/朱砂、`xishi` 紫晶/琥珀金、`psych` 雾蓝/暖杏）
- **设计令牌扩展**：`--mingli-ink-*` 10 阶、`--mingli-gold-*` 5 阶、语义别名、`--mingli-sp-*` 间距、`--mingli-fs-*` 字号、`--mingli-radius-xl`、三层阴影 `--mingli-shadow-*`、磨砂玻璃 `--mingli-glass-*`、动效 `--mingli-dur*` / `--mingli-ease*`
- **质感组件规范**：磨砂玻璃卡片、按钮按压反馈、卡片悬停抬升、聚焦光环、页面载入层级浮现、减少动效偏好
- **插画资产规范**：氛围图（16:9）、空状态插画（1:1）、装饰纹样（1:1）三类，按主题使用

### Changed
- `04-前端设计令牌.md`：生效日期更新为 2026-09-12，状态 v1.2；旧 `--ink-*` / `--gold-*` / `--skin-*` 等继续兼容，新代码优先 `--mingli-*`
- `05-前端组件库规范.md`：生效日期更新为 2026-09-12，状态 v1.2；Btn/Card/Empty 组件同步升级质感要求

### 迁移要求
- 运行层零改动；下一节实施时，将 `太初三主题设计方向.design/assets/` 复制到 `frontend/public/art/` 或接入素材管理后台 slot
- 旧 `[data-skin]` 表与三主题签名存在不一致，实施阶段需统一为 `[data-theme="guoxue|xishi|psych"]` 或做兼容映射

---

## [未发布]（2026-09-09 · REQ-099~117 批次，待测试组验收）

> 需求明细见 `docs/需求表.md`（REQ-099~117，本轮 dev 已完成、🟠 待测试组验收）；
> 缺陷见 `docs/Bug管理表.md`（BUG-015/016/017/021/022/023 随本批修复）。

### Added
- **起卦独立结果页 + 交互**：六爻/梅花/小六壬/观音抽签四法独立结果页 + 「回到选项」（REQ-104，修复 BUG-016）；起卦默认不弹输入法/问题框、占问问题改内联折叠（REQ-105）
- **六爻折叠 + 小六壬图像**：免费解读区折叠菜单化（REQ-107：卦理词条/六神释义/术语短释/爻辞/本卦命中折叠，「本卦大意」直显）；小六壬结果图像 UI 重做（REQ-106，美术已交付并接入）
- **三关系文案接入 + 按键改版**：生肖/星座三关系按键圆角矩形 + 图标（REQ-102）；生肖 396 对 + 星座 396 对文案接入（REQ-095/083/057 文案轮次，pairs.js T1–T4）；生肖「风险关系/五行关系」名词解释弹窗化（REQ-103）
- **星座页**：三卡横向堆叠轮播 + 星座图标（REQ-115）；底部操作键均匀分布（REQ-117）
- **配对解析 UI**：星座大按键改版（REQ-108）；MBTI 对齐 + 按键下移（REQ-109）；关系类型按钮图标化 + 2-2-1 排布（REQ-110，修复 BUG-023）
- **九法合一入口 + 文案**：配对/八字配对/起名同排 3 键上移一级（REQ-100）；九法合一文案重写（REQ-101，提案 B 定稿）
- **心理测试更名**：MBTI →「心理测试」Hub（REQ-112，/mbti；人格测试 /mbti/test）；档案 MBTI 板块类表格 + 仅最新 + 详情（REQ-112③）
- **默认档案体系（REQ-113）**：`cases.default` 字段 + 幂等迁移 + 首份即默认 + PATCH set_default + 档案管理打勾切换；王先生默认选中默认档案、切换非默认弹确认（REQ-113④）
- 档案「修改信息」改内联编辑（REQ-114，修复 BUG-022）；banner「对坐谈心」文字入口（REQ-097，含 REQ-111 归档合并）；建档页滚动落点修正（REQ-099）
- **三模块 Q 版循环动画**（REQ-084，美术素材已交付并接入）+ 跟随「王先生」开关（REQ-116）
- **名词白话卡 + 文案 key 规范**（REQ-128 阶段 0）：6 套名词白话卡（大六壬 42 / 金口诀 42 / 奇门时家 34 / 黄历择日 41 / 太乙神数 17 / 皇极经世 12）接入 6 个占卜/工具结果页——结果内渲染名词可点标签（点击展开白话）+ 全量「名词释义」兜底折叠面板（交互对齐 REQ-073/107）；数据文件 `frontend/public/data/term-cards.js`（文案组第四轮交付物，后续文案轮次只替换该文件）；全站文案 key 规范入 `docs/standards/01-命名与变量约定.md` §12（含「新功能验收含文案 key 合规检查」）

### Fixed
- BUG-015 档案页缺 caseId 防御 + 成功加载清错误态；BUG-016 三法卦象落点（随 REQ-104）；BUG-017（六爻折叠 / banner 对坐谈心批次）；BUG-021/023（配对解析 UI 批次）；BUG-022 修改信息弹窗位置（随 REQ-114）

### 迁移要求
- `cases.default` 列（BOOLEAN NOT NULL DEFAULT FALSE）幂等迁移（`ensure_schema`，REQ-113 后端 commit a99e855）

### 历史遗留（原 [未发布] 骨架条目，已发布、保留备查）
- 工程目录骨架：`docs/`、`product/`、`frontend/`、`backend/`、`data/`、`ops/`
- 5 份工程规范（命名约定 / 技术栈清单 / 接口与数据字典 / 环境与部署 / 本文件）与 ADR 模板

> **版本线说明（2026-09-09 校订）**：本记录无 0.1.0 条目——0.0.1（2026-09-05 文档期）之后直接进入 0.2.0（2026-09-06 MVP 全链闭环），0.1.0 从未单独发布（git 无 tag / 无对应提交），属历史编号跳号，不补虚构条目。

## [0.0.1 · 文档] 2026-09-05

### Added
- 《命理产品规划》：客群分野、合规与支付策略、MVP 范围、架构即成本原则
- 《命理技术框架方案》v2.3：模块化单体 MVP / 微服务就绪、R1–R12 决策、9 类模块、双库设计
- 《命理技术框架方案_审查》：C1–C26 问题闭环台账
- 《行业研究》：竞品格局、商业化结构、形态与实现方法、渠道，回答规划 Q1 / Q2

### Changed
- 架构物理形态由"微服务"修订为"模块化单体 MVP，Phase2 拆物理服务"（R8）
- 成本闸门前置到开发期，改为手动配置切档（R9 / R12）
- 断前尘问卷合成职责统一归主模块（R11）

---

## [0.2.0 · MVP 全链闭环 + 产品化扩展] 2026-09-06

> 对应 git 首个大提交「命理 H5 MVP 完整项目（Phase 1-7 全链闭环）」+ 当日上线后追加。需求/缺陷明细见 `docs/需求表.md` / `docs/Bug管理表.md`。

### Added
- Phase 1–7：工程骨架 + 数据底座 → 排盘引擎 → LLM 接入 → 异步编排 → API 层 → 前端全流程 → 集成测试与文档（REQ-001~006）
- P1 产品化扩展：`mingli_ops` 库 + 统一错误码 + 埋点三通道 + 后台最小版（REQ-012）
- P2 积分系统：credit 3 表 + 计费接入 + 注册赠送（REQ-010）
- 上线后追加：网站性能优化（系统字体 / Caddy gzip·zstd / JSX 预编译）、金数据支付接入（REQ-011，商户认证待用户验证）、注册图形验证码 + 确认密码（REQ-001）、免费积分 100→220、充值 UI 暂隐藏、docs 归档清理、admin 埋点中文映射

### Changed
- 计费口径：1 积分 = 1000 tokens（REQ-010）
- 前端免构建 CDN React；JSX 由 `frontend/scripts/precompile.js` 预编译，浏览器端无 Babel 实时转译

### Fixed
- 无（Bug 表自 2026-09-07 起建立，此前缺陷未留痕）

### 迁移要求
- 首次启动自动执行 `database.ensure_schema()`（幂等 ALTER + create_all）

---

## [0.3.0 · 横向扩展 Phase A–D] 2026-09-07

> 125 commits（git 批量导入日）。需求明细见 `docs/需求表.md`（REQ-020~098 主体）；缺陷见 `docs/Bug管理表.md`（BUG-001~010 已 🟢 收口）。

### Added
- 三大入口 + nav-rail + 模块主题背景（REQ-020）
- 生肖流年（确定性零 LLM，REQ-021）｜ 临时起卦：六爻/梅花/小六壬/观音灵签（REQ-022/044/055）｜ 塔罗 + 雷诺曼（REQ-023/024/042/043）｜ 星座本命盘（REQ-025/045）｜ MBTI 60 题 + 16 型文案（REQ-026/056）
- 导航交互重构：落地宣传页 + Banner 菜单（REQ-027）｜ 统一档案体系（REQ-028/039/040/046/052/053/054）
- 占卜动效与翻牌（REQ-061）｜ 功能设置 7 开关（REQ-066）｜ 档案与九法解耦（REQ-067/068）｜ 手动输入模式（REQ-069）｜ 分享帮填（REQ-070）｜ 配对解析（REQ-072）｜ 六爻文案补齐（REQ-073/074/075）｜ 王先生 Agent（REQ-076）｜ AI 长期记忆（REQ-077）｜ PWA（REQ-098）
- 单页 + 真实子路径路由（REQ-080）｜ 素材管理后端 + 后台（REQ-059 后端部分）

### Fixed
- **10 条 Bug 收口（BUG-001~010 → 🟢）**：落地页小屏遮挡 / 入口登录判定缺失 / 生肖空白字段 / 六爻卦象不完整 / MBTI 手动文案 / 选档案 UI / 星盘英文未翻译 / 按钮命名 / 分享链接白屏 / 档案本命盘跳转

### 迁移要求
- 4 张新表（divinations/tarot_readings/astrology_readings/mbti_results）+ cases.mbti_type 列

---

## [0.4.0 · 计费体系切换 + 档案/配对/记忆功能群] 2026-09-08

> 81 commits + 需求管理员台账建设。需求明细见 `docs/需求表.md`（REQ-085~116 主体）；缺陷见 `docs/Bug管理表.md`（BUG-011~024）。

### Added
- **计费体系切换：积分废除 → 余额与 Token 绑定**（REQ-085：1 元=10,000 tokens、注册送 22 元、后台余额充值/调整；REQ-063/086/087 余额明细/角标/入菜单）
- 档案详情 v2（REQ-089/090/091/114）、MBTI 更名心理测试（REQ-112）、默认档案体系（REQ-113）
- 配对解析 UI（REQ-108/109/110）、九法合一入口层级调整（REQ-100）、生肖/星座三关系按键（REQ-095/102）、星座配对（REQ-057/083）
- 起卦独立结果页 / 默认不弹输入法（REQ-104/105，待开发）、六爻折叠菜单（REQ-107，待开发）、星座三卡堆叠（REQ-115，待开发）、Q 版动画（REQ-084 素材已交付，功能待开发）
- 需求/Bug 台账建设：需求表 104 条 / Bug 表 24 条，审计补记 + 测试组验收清单（§5.5）

> 校订（2026-09-09）：上文所标「待开发」项（REQ-104/105/107/115/084）已于 2026-09-09 全部实现，详见上方 [未发布] 段。

### Fixed
- 台账审计补记（非代码）：20 REQ 实现 commit 回填、4 Bug 修复列、口径变更回填
- 待验收：BUG-011（S0 分享越权）/012/013/014（已修待测试组验收）

### Changed
- 全站「积分」文案 →「余额」口径（REQ-085）
- 后台「赠积分」→「余额充值/余额调整」（REQ-085③，BUG-019）

### 迁移要求
- case 模型新增 phone/email 字段（REQ-065）；余额口径存量账户按 1 元=10 积分折算（REQ-085 内部策略）

---

## 模板

```
## [<版本>] <日期>

### Added
- <新增能力 / 目录 / 规范>

### Changed
- <变更行为，标注是否破坏性>

### Fixed
- <修复项>

### 迁移要求
- <数据迁移 / 配置变更 / 手动操作步骤；无则写"无">
```
