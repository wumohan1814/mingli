# 05 · 前端组件库规范

> 生效日期：2026-09-10
> 适用范围：`frontend/public/index.html` 中所有通用组件与页面组件
> 状态：✅ 已定

## 1. 组件分类与命名约定

### 1.1 组件分层

| 层级 | 说明 | 命名规则 | 示例 |
|---|---|---|---|
| **原子组件** | 不可再分的基础 UI 元素 | 大驼峰，名词 | `Icon`、`Btn` |
| **通用组件** | 可跨模块复用的复合组件 | 大驼峰，名词 | `Card`、`Modal`、`Skeleton`、`Tag` |
| **业务组件** | 特定业务域内的复用组件 | 大驼峰，带域前缀 | `CreditBalance`、`FourPillars`、`ZodiacMedallion` |
| **页面组件** | 路由级页面 | 大驼峰，以 Page 结尾 | `LandingPage`、`CasesPage`、`PredictPage` |
| **弹窗组件** | 模态弹窗 | 大驼峰，以 Modal 结尾 | `RenameModal`、`CreditInsufficientModal` |

### 1.2 组件 Props 约定

```
// 推荐：解构 + 默认值
function Btn({ variant = 'primary', size = 'md', disabled = false, children, onClick }) {
  // ...
}

// 禁止：props.xxx 散点式读取
function Btn(props) {
  const variant = props.variant || 'primary';
  // ...
}
```

---

## 2. 原子组件

### 2.1 Icon — 内联 SVG 图标

**位置**：`index.html` 约 2629 行

| Prop | 类型 | 默认值 | 说明 |
|---|---|---|---|
| name | string | — | 图标名称（见下方图标清单） |
| size | number/string | 18 | 图标尺寸（px） |
| color | string | currentColor | 颜色，默认继承父元素 |
| className | string | '' | 附加样式类 |

**设计原则**：
- 全部使用内联 SVG，不依赖图标字体
- 颜色通过 `currentColor` 继承，随皮肤自动适配
- 尺寸用 `em` 或 `px`，与文本对齐

### 2.2 Btn — 按钮（待统一）

**当前状态**：按钮样式分散在各页面，尚未抽出统一 Btn 组件。
**计划统一**：

| 变体 (variant) | 使用场景 | 对应 CSS 类（现状） |
|---|---|---|
| `primary` | 主操作（提交、下一步） | `.btn-primary` 或各页面内联样式 |
| `secondary` | 次要操作（取消、返回） | `.btn-secondary` |
| `ghost` | 幽灵按钮（文本+边框） | — |
| `danger` | 危险操作（删除） | — |
| `skin` | 皮肤色按钮（随主题变化） | 各模块页自定义 |

| 尺寸 (size) | 说明 |
|---|---|
| `sm` | 小按钮（标签、辅助操作） |
| `md` | 默认尺寸（表单、弹窗） |
| `lg` | 大按钮（页脚主操作） |
| `block` | 块级按钮（全宽） |

---

## 3. 通用组件

### 3.1 ModalBase — 通用弹窗基座

**组件定义**：`frontend/public/index.html` 约 2830 行
**状态**：✅ 已实现（2026-09-10 Phase 1.5）

#### Props API

| Prop | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | string \| ReactNode | — | 弹窗标题 |
| `children` | ReactNode | — | 弹窗内容 |
| `footer` | ReactNode | — | 底部操作栏（不传则不显示） |
| `onClose` | function | — | 关闭回调（遮罩点击 / ESC / 关闭按钮均触发） |
| `closable` | boolean | `false` | 是否显示右上角 × 关闭按钮 |
| `maskClosable` | boolean | `true` | 点击遮罩层是否关闭 |
| `className` | string | `''` | 附加到 `.modal` 的自定义类名（如 `ask-modal`、`pair-modal`） |
| `width` | number \| string | `340px` | 弹窗最大宽度 |
| `align` | `'center' \| 'left'` | `'center'` | 内容对齐方式 |
| `scrollable` | boolean | `false` | 内容过高时是否可滚动（长内容建议设为 true） |
| `bodyClassName` | string | `''` | 内容区附加样式类 |

#### 内置能力

| 能力 | 说明 |
|---|---|
| ✅ ESC 键关闭 | 打开时监听 Escape 键，自动触发 onClose |
| ✅ 背景滚动锁定 | 打开时 `body.style.overflow = 'hidden'`，关闭后恢复 |
| ✅ 点击遮罩关闭 | 默认开启，可通过 `maskClosable={false}` 禁用 |
| ✅ 事件冒泡阻止 | 点击弹窗内部不触发遮罩关闭 |
| ✅ 无障碍 | 关闭按钮有 `aria-label` |

#### 用法示例

```jsx
// 简单确认弹窗
<ModalBase
  title="操作确认"
  onClose={handleClose}
  footer={<>
    <button className="btn btn-outline" onClick={handleClose}>取消</button>
    <button className="btn btn-primary" onClick={handleOk}>确定</button>
  </>}
>
  确定要执行此操作吗？
</ModalBase>

// 长内容表单弹窗
<ModalBase
  title="编辑信息"
  onClose={handleClose}
  align="left"
  scrollable={true}
  width={420}
  className="ask-modal"
  footer={/* 底部按钮 */}
>
  {/* 表单内容 */}
</ModalBase>
```

### 3.2 业务 Modal 列表与迁移状态

现有 10 个独立业务弹窗 + 1 个内联弹窗，**全部已迁移至 ModalBase**（2026-09-10 Phase 1.5）。

| 组件名 | 位置 | 迁移到 ModalBase | 备注 |
|---|---|---|---|
| `CreditInsufficientModal` | 约 3345 行 | ✅ | 确认弹窗，最简基座 |
| `RenameModal` | 约 4141 行 | ✅ | 输入弹窗，default 基座 |
| `CaseContactModal` | 约 4185 行 | ✅ | 表单类，`align="left"` + `scrollable` |
| `ShareFillModal` | 约 4271 行 | ✅ | 三态切换（成功/加载/失败），footer 动态 |
| `PairModal` | 约 3382 行 | ✅ | 最复杂：多步骤表单 + pair-modal 自定义头部 |
| `NamerModal` | 约 8848 行 | ✅ | pair-modal 风格 + 档案选择器 |
| `ZodiacRelModal` | 约 9598 行 | ✅ | pair-modal 自定义头部（图标 + ×关闭） |
| `ZodiacGuideModal` | 约 9767 行 | ✅ | pair-modal 自定义头部 + scrollable |
| `SunLoveModal` | 约 18309 行 | ✅ | pair-modal 风格 |
| `SunRelationModal` | 约 18490 行 | ✅ | pair-modal 风格 |
| `switchModal`（AgentPage 内联） | 约 21077 行 | ✅ | 内联弹窗，`maskClosable: !switchBusy` |

**迁移统计**：
- 迁移前 `modal-mask` 出现 **11 处**（10 独立组件 + 1 内联）
- 迁移后 `modal-mask` 出现 **1 处**（仅 ModalBase 自身实现）
- ModalBase 累计被引用 **26 处**（定义 + 11 调用 + 内部属性）

**ModalBase 获得的额外能力（迁移后自动解锁）**：
- ✅ ESC 键关闭（之前 11 个弹窗均未实现）
- ✅ 背景滚动锁定（之前 11 个弹窗均未实现）
- ✅ 统一遮罩点击关闭逻辑
- ✅ 可配置 `maskClosable` / `scrollable` / `width` / `align`

### 3.3 旧 Modal 结构对照（迁移参考）

迁移前的标准结构：
```jsx
<div className="modal-mask" onClick={onClose}>
  <div className="modal" onClick={e => e.stopPropagation()}>
    <div className="modal-title">{title}</div>
    <div className="modal-body">{content}</div>
    {/* 按钮 */}
  </div>
</div>
```

迁移后的等价结构：
```jsx
<ModalBase title={title} onClose={onClose} footer={buttons}>
  {content}
</ModalBase>
```

**注意事项**：
- 对齐方式：旧 Modal 默认居中对齐（`text-align: center`），与 `ModalBase` 默认值一致；左对齐的（如 `ask-modal`）需传 `align="left"`
- 自定义类名：旧实现的特殊样式类（如 `ask-modal`、`pair-modal`）通过 `className` prop 传入
- 特殊宽度：默认 340px，宽弹窗传 `width={420}` 或 `width="560px"`

### 3.2 Card — 卡片容器

**使用场景**：信息分组、模块入口、列表项

**结构约定**：
```
.card
  .card-header  // 卡片头部（可选）
  .card-body    // 卡片内容
  .card-footer  // 卡片底部（可选）
```

**变体**：
- 默认：白底 + 边框 + 圆角
- 悬浮态：hover 时提升阴影
- 皮肤色：`--skin-accent-soft` 背景

### 3.3 Skeleton — 骨架屏

**使用场景**：数据加载中占位

**类型**：
- `text`：文本行占位（1~N 行）
- `circle`：圆形头像占位
- `rect`：矩形图片/卡片占位
- `page`：整页骨架（组合型）

**动效**：shimmer 扫光动画（1.5s 循环）

### 3.4 Tag / Badge — 标签与角标

**标签 Tag**：分类、状态标记
- 尺寸：sm / md
- 变体：filled（实心）/ outlined（描边）/ soft（淡色）
- 颜色：随皮肤 accent 色

**角标 Badge**：数字红点、状态角标
- 位置：右上（默认）
- 支持：dot（纯点）/ count（数字）/ text（文字）

### 3.5 Toast — 轻提示

**全局函数**：`toast(msg, duration = 2600)`

**特性**：
- 单例模式（全局唯一 toast 元素）
- 顶部居中显示
- 自动消失（默认 2.6s）
- 多次调用重置计时器

---

## 4. 业务组件（精选）

### 4.1 CreditBalance / BannerBalance / MenuBalance

**定位**：余额展示组件的三种形态

| 组件 | 使用位置 | 形态 |
|---|---|---|
| `CreditBalance` | 余额页/档案页 | 完整余额卡片 |
| `BannerBalance` | 顶部 Banner 右侧 | 胶囊形精简展示 |
| `MenuBalance` | 下拉菜单顶部 | 菜单项式展示 |

### 4.2 FourPillars / BaziPlate / ZiweiPlate

**定位**：命盘展示组件（国学主线核心）

**命名规范**：以方法名 + Plate/Wheel 命名
- `BaziPlate`：八字命盘
- `ZiweiPlate`：紫微命盘
- `ZiweiWheel`：紫微星盘（圆形）
- `QimenPlate`：奇门遁甲盘
- `QizhengPlate`：七政四余盘
- `WuyunPlate`：五运六气盘

### 4.3 ChartView — 图表容器

**定位**：九法合一总览的图表切换容器

**Props**：
- `chart`：当前选中的图表类型
- `data`：命盘数据
- `onChartChange`：切换回调

---

## 5. 新增组件开发规范

### 5.1 Checklist

- [ ] Props 解构 + 默认值
- [ ] 样式使用 CSS 变量（皮肤色必须走 `--skin-*`）
- [ ] 动效走 `--dur` / `--ease` 变量
- [ ] 可访问性：按钮有 `aria-label`、图片有 `alt`
- [ ] 命名遵循本规范层级
- [ ] 在本文件中登记组件

### 5.2 组件放置位置

在 `index.html` 中，组件按以下顺序排列：

1. 工具函数（util helpers）
2. 原子组件（Icon, Btn 等）
3. 通用组件（Card, Modal, Skeleton 等）
4. 业务组件（按模块分组）
5. 页面组件（Page 结尾）
6. App 根组件
7. ReactDOM.render

---

## 6. 待统一组件列表（技术债）

以下组件当前分散在各页面，需要逐步抽离为通用组件：

| 组件 | 当前状态 | 优先级 | 预计影响面 |
|---|---|---|---|
| Btn 按钮 | 分散实现 | P1 | 全量按钮 |
| Card 卡片 | 部分统一 | P1 | 卡片容器 |
| Modal 弹窗基座 | 6+ 独立实现 | P1 | 所有弹窗 |
| Form Input 输入框 | 各页面自定义 | P2 | 表单页 |
| Empty 空状态 | 部分页面有 | P2 | 列表/档案空态 |
| Loading 加载态 | 各页面自处理 | P2 | 加载中展示 |
| Pagination 分页 | 未出现 | P3 | 长列表 |

> 注：考虑到单文件架构的约束，组件统一优先做**命名规范+样式类统一**，而非强制抽取为独立函数组件。
> 待迁移到工程化架构后再做完整的组件库抽离。
