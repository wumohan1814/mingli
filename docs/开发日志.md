# 太初 · 开发日志

## 2026-09-05 · Phase 1-2 工程骨架 + 数据底座

### 决策记录
- **UI组件库**：选择 antd-mobile 5.38（已安装），但MVP前端使用CDN React免构建方案
- **免构建前端**：由于沙箱阻止Vite/esbuild子进程spawn，采用CDN React + Babel Standalone方案
- **密码哈希**：放弃passlib（bcrypt兼容性问题），改用hashlib pbkdf2_hmac
- **前端服务**：FastAPI StaticFiles直接服务前端，单端口8000运行
- **皮肤系统**：MVP仅做国学皮肤

### 已完成
- [x] 项目目录结构初始化
- [x] 后端：FastAPI + SQLAlchemy + SQLite 双库
- [x] 后端：ORM模型（11张表）
- [x] 后端：JWT认证（HS256 + pbkdf2）
- [x] 后端：7个API端点全部通过测试
- [x] 后端：事件分发器
- [x] 后端：合规护栏
- [x] 后端：用户档案模块
- [x] 后端：质疑采集模块
- [x] 后端：9个方法模块桩（mock LLM）
- [x] 后端：路由决策模块
- [x] 后端：校验模块
- [x] 后端：合成模块
- [x] 后端：9个方法提示词文件
- [x] 前端：CDN React免构建SPA
- [x] 前端：8个页面（落地/登录/建档/等待/校准/解读/修正/档案）
- [x] 前端：API客户端 + JWT拦截
- [x] 前端：设计令牌系统
- [x] 根目录：启动脚本 + README + .gitignore
- [x] 依赖安装：Python（fastapi/sqlalchemy/pydantic等）
- [x] 依赖安装：Node（react/antd-mobile/vite等，供后续构建使用）

### 运行方式
```bash
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 访问 http://localhost:8000
```

### 待完成
- [ ] Phase 3: 排盘引擎集成（lunar-python/iztro/mingyu-core）
- [ ] Phase 4: 方法模块接入真实LLM
- [ ] Phase 6: 前端完善（CDN版增强/antd-mobile组件化）
- [ ] Phase 7: 端到端测试 + 排盘回归基线

### API端点（全部测试通过）
| 方法 | 路径 | 状态 |
|---|---|---|
| POST | /api/auth/register | ✅ |
| POST | /api/auth/login | ✅ |
| POST | /api/auth/refresh | ✅ |
| POST | /api/cases | ✅ |
| POST | /api/cases/{id}/paipan | ✅ |
| POST | /api/cases/{id}/duan-qian-chen | ✅ |
| POST | /api/cases/{id}/calibration | ✅ |
| POST | /api/cases/{id}/predict | ✅ |
| POST | /api/cases/{id}/revise | ✅ |
| GET  | /api/cases/{id}/archive | ✅ |
| GET  | /api/jobs/{jobId} | ✅ |

---

## 2026-09-05 · Phase 3 排盘 + Phase 4 真实 LLM 接入

### 已完成
- [x] Phase 3：`backend/app/paipan/` 排盘引擎（`engine.py` 胶水 + `shensha/slicer/scorer` 自研模块）
- [x] Phase 3：`paipan` 端点接入真实排盘（chart 落 `charts` 表 + `degradedMethods`）
- [x] Phase 3：排盘回归基线（`tests/fixtures/` chart + 9 片快照 + pytest）
- [x] Phase 4：LLM 客户端 `backend/app/llm/client.py`（DeepSeek OpenAI 兼容 + usage 计量）
- [x] Phase 4：9 个方法模块真实化（`app/methods/base.py` + importlib `ANALYZERS` 注册表）
- [x] Phase 4：校验真实化（`app/validation/validator.py` 契约对齐 `scorer.quality_score()`）
- [x] Phase 4：异步编排 `app/jobs/orchestrator.py`（断前尘 9 法串行 / 预测路由并行 + 合成 + 免责声明）
- [x] Phase 4：`Job.result_json` + `ensure_schema()` 迁移；端点 202 + `create_task`；`jobs` result 读库
- [x] Phase 4：API Key 方案（`backend/.env` 明文 + `API-Key安全与LLM接入说明.md` 升级路径）

### 决策记录
- LLM 统一走 DeepSeek 官方 API，模型 `deepseek-v4-flash`（`config.py` 配置化，可换 provider/档位）
- 校验模型与主模型分离配置（`TAICHU_LLM_VALIDATION_MODEL`），MVP 同档、留切便宜模型
- Key 明文存 `backend/.env`（`.gitignore` 已忽略），代码零硬编码；升级路径见安全说明文档
- 方法目录名带连字符 → `app/methods/__init__.py` 用 importlib 按路径加载（非标准 import）
- 编排走直接调用（单进程内等价于事件分发器），事件分发器保留 Phase2 升级用

### 待完成
- [ ] self-run 10× 成本实测（owner=用户，延后至全项目完成后）
- [ ] prompt 长度调优（单法 completion≈14k tokens 偏多）
- [ ] Phase 6：前端完善（CDN版增强/antd-mobile组件化）
- [ ] Phase 7：端到端测试 + 案例库回归锚点（防 prompt 漂移）

---

## 2026-09-05 · Phase 5 API 层真实化

### 已完成
- [x] 9 个 method-prompts/*.md 自研重写（参考 mingli 规则、对齐太初 slice + method-result v2；真调冒烟产出 4 条可证伪命题）
- [x] 合规护栏 `check_output` 在 `run_predict` 合成后、免责前接线（`_apply_compliance`）
- [x] `calibration` 端点真实化（断前尘结果聚合 → `score_fit` → 落 `Calibration` 表）
- [x] `revise` 端点真实化（历史对话 + chart 摘要 → 真实 LLM → 落 `Conversation`；`prompts/shared/revise.md`）
- [x] `archive` 端点真实化（`build_archive` 聚合 chart/校准/对话/方法结果）
- [x] 新增 `POST /api/cases/{id}/query/{method_key}` 单法直问（缓存优先零 LLM）
- [x] 统一错误处理（`_err` / `_get_owned_case` helper）

### 决策记录
- Phase 5 = API 层（REST 端点 + 异步 job 轮询 + 请求验证/错误处理），5.2 轮询端点在 Phase 4 已落地
- method-prompts 参考 mingli `references/method-prompts/*.md` 自研重写（ADR-0001：不复制、对齐太初 slice 字段）
- self-run 10× 成本实测由用户明确延后至全项目开发完成后

---

## 2026-09-06 · Phase 6 前端消费真实结果

### 已完成
- [x] 前端消费真实 job result：统一轮询 `GET /api/jobs/{jobId}` → 渲染真实结果，去 mock
- [x] `CalibrationPage` 契合度：读 `result.propositions` 展示校准契合度
- [x] `PredictPage` 读 `result.report` 渲染预测合成结果
- [x] `ArchivePage` 完整档案：`build_archive` 聚合 chart/校准/对话/方法结果
- [x] CalibrationPage / ArchivePage 两页收尾，前后端契约以 job result / method-result v2 为准

### 决策记录
- 前端继续 CDN React 免构建方案，页面直接消费真实 API 结果（去 mock），不再依赖假数据兜底
- 页面只做渲染与过渡态展示；契合度、档案聚合等业务计算一律由后端端点产出

---

## 2026-09-06 · Phase 7 端到端集成测试 + 文档收尾

### 已完成
- [x] 端到端集成测试：happy path（`tests/integration/test_e2e_flow.py::test_full_main_flow`）+ 隔离/降级（`test_multi_user_isolation` / `test_degraded_no_longitude`），pytest 全量 **17 passed**
- [x] README 重写（Phase 1–7 全貌 + 启动/测试指引）
- [x] `docs/人工注意点清单.md`：人工实测/技术债/部署/安全/防漂移清单（self-run 成本实测、浏览器实测、revise 补 check_output、prompt 长度、案例库回归锚点等）
- [x] 修复 `backend/app/api/cases.py` datetime import bug

### 决策记录
- Phase 7 用集成测试锁定主链（注册 → 建档 → 排盘 → 断前尘 → 校准 → 预测 → revise → archive）；隔离/降级用例覆盖多用户数据隔离与缺经纬度 4 法降级不破坏主链
- 收尾不再新开开发项；剩余需人工验证/决策项统一收敛到 `docs/人工注意点清单.md`（owner=用户，全项目完成后执行）
