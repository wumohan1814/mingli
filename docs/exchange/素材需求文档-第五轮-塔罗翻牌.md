# 素材需求文档（第五轮 · REQ-129 塔罗翻牌呈现重构配套美术）

> 定位：REQ-129 塔罗翻牌呈现重构需要的**美术素材**（牌桌背景 + 卡背新图）。开发先用占位，素材回传后接入（复用后台素材热更 asset_slots）。
> 关联：`docs/需求表.md`（REQ-129）｜ 既有塔罗卡面 `frontend/public/tarot/rider-waite/`（78 张，不动）｜ 现卡背 `frontend/public/art/card-back/card-back-tarot.webp`（将被替换）。
> 日期：2026-09-09。状态：待美术生产。

---

## 0. 结论速览

| 项 | 说明 |
|---|---|
| 素材 2 类 | ① 牌桌背景（神秘底纹，可热更换新槽）② 卡背神秘风新图（替换现卡背） |
| 风格 | 对齐全站「高贵/奢华/内涵」基调 + 塔罗既有神秘学视觉（现有 rider-waite 卡面风格） |
| 开发先用 | 占位（CSS 渐变桌布 + 现卡背），素材回传后接入 |
| 红线 | 图内不画文字；卡背不画可辨识图案内容（保持"背面不可读"） |

---

## 1. 素材规格

### M1 · 牌桌背景（神秘底纹）

| 项 | 规格 |
|---|---|
| 画面 | 塔罗牌桌氛围底纹：深色神秘底（墨蓝紫/暗金） + 细腻纹理（星图/符文/月相/星座连线等抽象元素） + 中央留空（不干扰牌阵摆位与读牌大图） |
| 格式 | JPG/WebP 无透明；全幅 cover |
| 尺寸 | 竖屏 1080×1920（或 1280×2400，9:19），≤800KB |
| 数量 | 1 张基础款（可选多款待定） |
| 命名/目录 | `frontend/public/art/tarot-bg/tarot-table.webp`（替换现有 `art/tarot-bg/` 下样片级图片） |
| 热更 | 接入后台素材热更槽（REQ-059）：key 建议 `card-table` 或复用模块级槽（开发定，与 asset_slots kind 对齐） |
| 约束 | 中央区域（占画布约 60% 中区）保持低对比度纯净，保证牌阵/大图可读；不画文字 |

### M2 · 塔罗卡背新图（替换现卡背）

| 项 | 规格 |
|---|---|
| 画面 | 神秘风卡背：深色底 + 中央主符号（如六芒星/月相/眼睛/符文组合，风格与卡面一致），四周对称边框；**不得含可辨识的牌面内容**（保持背面不可读） |
| 格式 | PNG/WebP α（透明或满幅圆角矩形，与现卡背一致） |
| 尺寸 | 卡面比例竖卡（约 2:3.5），@2x 超采样，如 300×525 或 512×896；≤300KB |
| 数量 | 1 张 |
| 命名/目录 | `frontend/public/art/card-back/card-back-tarot-v2.webp`（保留旧图待切换，或直接替换——开发定） |
| 约束 | 中心主符号在小尺寸（翻牌缩略）下仍可辨识；不画文字 |

---

## 2. 交付与接入说明

- 交付进 `docs/exchange/`（待接入交付物中心），开发接入后归档 `docs/_archive/`。
- 接入点：牌桌背景 → 塔罗页牌桌容器 `background-image`（可走 REQ-059 素材热更槽）；卡背 → 翻牌/牌阵展示的卡背图引用。
- 开发先用占位（CSS 渐变桌布 + 现卡背），素材回传后替换即生效。

## 3. 待确认单（带默认建议）

1. 牌桌背景**数量**：默认「1 张基础款」，可选多款轮换（后续再说）。
2. 卡背风格**主符号**：默认「六芒星 + 月相组合」（与现有卡面神秘学风格一致）；若你指定其它符号（如眼睛/符文/太初 logo），告知即可。
3. 牌桌背景是否**本轮就要**：默认「要」（REQ-129 呈现重构配套）；若先做阵型/交互、背景用占位也行（开发可先推进，背景后补）。
