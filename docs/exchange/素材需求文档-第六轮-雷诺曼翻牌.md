# 素材需求文档（第六轮 · REQ-043 雷诺曼翻牌呈现重构配套美术）

> 定位：REQ-043 雷诺曼改版（与 REQ-042 塔罗同构，🔵返工）需要的**美术素材**（牌桌背景 + 卡背新图）。开发先用占位，素材回传后接入（复用后台素材热更 asset_slots）。
> 关联：`../需求表.md`（REQ-043，与 REQ-042/061/069 同源）｜ 既有雷诺曼卡面 `frontend/public/lenormand/webp/`（36 张，不动）｜ 现卡背 `frontend/public/art/card-back/card-back-lenormand.webp`（将被替换）。
> 日期：2026-09-09。
> 状态：✅ **已交付**（详见同目录 `美术交付回执-2026-09-09-lenormand-r6.md`）。

---

## 0. 结论速览

| 项 | 说明 |
|---|---|
| 素材 2 类 | ① 牌桌背景（墨蓝·航海占卜风，可热更换新槽）② 卡背新图（沿用月银罗盘 + 6 张雷诺曼小符号环，替换现卡背） |
| 风格 | 对齐现卡背的「墨蓝 + 月银 + 罗盘」家族（`card-back-lenormand`），区别于塔罗的「紫晶 + 琥珀金」家族 |
| 开发先用 | 占位（CSS 渐变桌布 + 现卡背），素材回传后接入 |
| 红线 | 图内不画文字；卡背不画可辨识的 36 张牌面内容（保持"背面不可读"） |

---

## 1. 素材规格

### M1 · 雷诺曼牌桌背景（航海占卜风底纹）

| 项 | 规格 |
|---|---|
| 画面 | 雷诺曼牌桌氛围底纹：深色墨蓝底（`#0E1A35` 系） + 细腻纹理（航海占卜元素：隐约罗盘/钟面/锚/船/钥匙/星图等抽象线描） + 中央留空（不干扰 3-9 张牌阵摆位与读牌大图） |
| 格式 | JPG/WebP 无透明；全幅 cover |
| 尺寸 | 竖屏 1080×1920（或 1280×2400，9:19），≤800KB |
| 数量 | 1 张基础款（可选多款待定） |
| 命名/目录 | `frontend/public/art/lenormand-bg/lenormand-table.webp`（新建目录 `art/lenormand-bg/`） |
| 热更 | 接入后台素材热更槽（REQ-059）：key 建议 `card-table-lenormand` 或复用模块级槽（开发定，与 asset_slots kind 对齐） |
| 约束 | 中央区域（占画布约 60% 中区）保持低对比度纯净，保证牌阵/大图可读；不画文字 |

### M2 · 雷诺曼卡背新图（替换现卡背）

| 项 | 规格 |
|---|---|
| 画面 | 沿用现卡背「墨蓝 + 月银罗盘」家族，**主符号保持月银罗盘**（与现卡背一致），**新增 6 张小符号环**——围绕中央罗盘依次为：船（Ship）/ 钥匙（Key）/ 心（Heart）/ 锚（Anchor）/ 百合（Lily）/ 十字（Cross），作为对 36 张牌面的低调致敬。深色底 + 四周对称米白描银双边框；**不得含可辨识的牌面内容**（保持背面不可读） |
| 格式 | PNG/WebP α（透明或满幅圆角矩形，与现卡背一致） |
| 尺寸 | 卡面比例竖卡（约 2:3.5），@2x 超采样 512×896；≤300KB |
| 数量 | 1 张 |
| 命名/目录 | `frontend/public/art/card-back/card-back-lenormand-v2.webp`（保留旧图 `card-back-lenormand.webp` 待切换，或直接替换——开发定） |
| 约束 | 中心罗盘在小尺寸（翻牌缩略）下仍可辨识；不画文字 |

---

## 2. 交付与接入说明

- 交付进 `docs/exchange/`（A⇄A 对话通道），开发接入后归档 `docs/_archive/`。
- 接入点：牌桌背景 → 雷诺曼页牌桌容器 `background-image`（可走 REQ-059 素材热更槽）；卡背 → 翻牌/牌阵展示的卡背图引用，对应 CSS 变量 `--art-card-back-lenormand`（`index.html:68`）。
- 开发先用占位（CSS 渐变桌布 + 现卡背），素材回传后替换即生效。

## 3. 待确认单（已锁单 → 详见交付回执）

1. 牌桌背景**数量**：锁 **「1 张基础款」**（spec 默认，与塔罗同构）。
2. 卡背风格**主符号**：锁 **「月银罗盘 + 6 张雷诺曼小符号环」**（spec 默认：船/钥匙/心/锚/百合/十字）。
3. 牌桌背景是否**本轮交付**：锁 **「是」**（与 REQ-043 同期上线）。

## 4. 交付后接入

- 详见同目录 `美术交付回执-2026-09-09-lenormand-r6.md`（资产清单 + 接入点 + P0/P1 风险）
- 旧卡背 `card-back-lenormand.webp` 归档 `docs/_archive/r6-lenormand-old-card-back/`（保留回滚能力）
