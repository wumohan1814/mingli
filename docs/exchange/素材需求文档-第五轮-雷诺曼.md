# 素材需求文档（第五轮 · REQ-130 雷诺曼牌阵 + 摆位配套美术）

> 定位：REQ-130 雷诺曼牌阵补充 + 摆位呈现 v2 需要的**美术素材**（牌桌背景 + 卡背），规格同塔罗批（REQ-129）。
> 关联：`../需求表.md`（REQ-130 / REQ-129）｜ 塔罗批规格与回执见 `素材需求文档-第五轮-塔罗翻牌.md` / `美术交付回执-2026-09-09-tarot-r5.md` ｜ 既有雷诺曼卡面 `frontend/public/lenormand/`（36 张，不动）｜ 现卡背 `frontend/public/art/card-back/card-back-lenormand.webp`（将被替换）。
> 日期：2026-09-09。状态：待美术生产。

---

## 0. 结论速览

| 项 | 说明 |
|---|---|
| 素材 2 类 | ① 雷诺曼牌桌背景（神秘底纹）② 雷诺曼卡背新图（替换现卡背） |
| 独立插槽 | **塔罗与雷诺曼各自独立**（各 1 组背景 + 卡背，互不共用），后台各可热更调整 |
| 规格 | **同塔罗批**（牌桌 ≤800KB / 卡背 ≤300KB / 图内不画文字 / 卡背不可读） |
| 风格 | 对齐全站基调 + 雷诺曼既有视觉；卡背主符号可沿用塔罗同款神秘风或雷诺曼专属（见待确认单） |
| 开发先用 | 占位（CSS 渐变桌布 + 现卡背），素材回传后接入 |

## 1. 槽位设计（塔罗 / 雷诺曼独立，后台素材热更 asset_slots）

| 槽位 key | 含义 | 归属 |
|---|---|---|
| `card-table-tarot` | 塔罗牌桌背景 | REQ-129（塔罗，素材已交付 tarot-table.webp，接入此槽） |
| `card-back-tarot` | 塔罗卡背 | REQ-129（塔罗，已交付 card-back-tarot-v2.webp） |
| `card-table-lenormand` | **雷诺曼牌桌背景** | 本单（REQ-130） |
| `card-back-lenormand` | **雷诺曼卡背** | 本单（REQ-130） |

> 4 个槽互相独立、后台各自可上传替换/删除回退（对齐 REQ-059 素材热更）；编码时（REQ-129/130）接入后端白名单 + 后台素材管理面板 + 前端读取。

## 2. 素材规格（同塔罗批）

### M1 · 雷诺曼牌桌背景

| 项 | 规格 |
|---|---|
| 画面 | 雷诺曼牌桌氛围底纹：深色神秘底 + 细腻纹理（可与塔罗牌桌同基调，或略偏雷诺曼（凯尔特/古典）风味——开发建议**同神秘基调**以便两模块视觉统一，细部可区分）；中央留空不干扰牌阵 |
| 格式 | JPG/WebP 无透明；全幅 cover |
| 尺寸 | 竖屏 1080×1920（或 1280×2400，9:19），≤800KB |
| 数量 | 1 张基础款 |
| 命名/目录 | `frontend/public/art/lenormand-bg/lenormand-table.webp`（新建目录；接入槽 `card-table-lenormand`） |
| 约束 | 中央区域低对比纯净；不画文字 |

### M2 · 雷诺曼卡背新图（替换现卡背）

| 项 | 规格 |
|---|---|
| 画面 | 神秘风卡背：深色底 + 中央主符号 + 对称边框；**不得含可辨识的牌面内容** |
| 格式 | PNG/WebP α |
| 尺寸 | 卡面比例（雷诺曼竖卡，约 2:3.5），@2x，如 300×525 或 512×896；≤300KB |
| 数量 | 1 张 |
| 命名/目录 | `frontend/public/art/card-back/card-back-lenormand-v2.webp`（保留旧图可回滚；接入槽 `card-back-lenormand`） |
| 约束 | 中心主符号小尺寸可辨；不画文字 |

## 3. 待确认单（带默认建议）

1. 雷诺曼卡背**主符号**：默认「与塔罗同款神秘符号风格（六芒星/月相类），或雷诺曼专属（如凯尔特结/钥匙/锚组合）」——建议**同塔罗神秘风**（视觉统一、美术省力），若想区分再告知。
2. 雷诺曼牌桌背景**基调**：默认「同塔罗神秘基调、细部区分」。
3. **数量**：默认各 1 张基础款。

## 4. 交付与接入说明

- 交付进 `docs/exchange/`（待接入交付物中心），接入后归档 `docs/_archive/`。
- 接入点：雷诺曼页牌桌容器 `background-image`（读 `card-table-lenormand` 槽）+ 翻牌/牌阵卡背引用（读 `card-back-lenormand` 槽）。
- 开发先用占位，素材回传后替换即生效（走后台素材热更亦可）。
