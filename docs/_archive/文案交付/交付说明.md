# 太初 · 文案交付说明（文字需求-文案改版）

> 产出日期：2026-09-07
> 关联：`docs/文字需求-文案改版.md`（REQ-037/042/043/045/026、BUG-003/007）
> 执行顺序（依需求方要求）：**① 优先联网检索可复用公共文案 → 多源改写以规避版权 → ② 线上不足处原创自撰**。

## 一、交付清单与入库位置

| 文件 | 对应需求 | 规模 | 入库目标位置 | 说明 |
|---|---|---|---|---|
| `A1_zodiac_trends.json` | A1 / REQ-045 / C1 | 72 篇（太阳/月亮/上升 ×12 ×男女） | 后端 data 或前端常量；星座结果页三格读取 | 当前生产先占位，正文以此替换「文案待补充」 |
| `A2_tarot_meanings.json` | A2 / REQ-042 / C2 | 78 张（正逆位关键词+含义） | `paipan-node/.../divination/tarot-data.js` 的 `tarotCards`，为每张牌增补 `uprightKeywords/upright/reversedKeywords/reversed` | 抽牌结果页展示固定牌义 |
| `A3_lenormand_meanings.json` | A3 / REQ-043 / C2 | 36 张（主题+细节+组合语义） | `mingyu-core/dist/divination/algorithms/lenormand.js` 的 `LENORMAND_CARDS`，为每张牌增补 `theme/detail` | 单张/3张/9宫格抽出牌细节文案 |
| `A4_mbti_copy.json` | A4 / REQ-026 | 16 型（alias/优势/盲点/职场/关系/成长） | 直接替换 `backend/app/mbti/data/types.json` 的 `types` 字段 | 在现有文案基础上多源优化措辞 |
| `B1_ui_copy.json` | B1 / REQ-037 | 全站 UI 文案（落地页/按钮/提示/空态/报错/合规） | 前端文案集中常量模块；落地页与公共文案同步 | 调性高贵/含蓄，全程『趋势参考、不作建议』 |
| `B2_zodiac_annual.json` | B2 / BUG-003 | 12 生肖（贵人/本气/风险关系/行动建议）+ 年干关系模板 | 后端生肖流年模块 `zodiac/index.js` 年度报告输出 | 确定性代码补齐，零 LLM |
| `B3_astrology_translations.json` | B3 / BUG-007 | 8 种格局 + 行星 + 相位 中文映射 | 后端星盘模块共享映射表（星座结果页 + 档案命盘页两处同源） | 补全 commit e723d37 遗漏项 |

## 二、版权与合规处理

- **A 类（在线找）**：塔罗/雷诺曼/星座/MBTI 文案均来自多源公开资料检索（ok-tarot、bemore、neww.tw、lenormand.tw、hourlightkey、tarotassociation、wikiwand、oks60、golla、birddict、mbtionline 系、psychometricresearch、mbtiquiz、personalities16test 等），**多源比对后改写，未逐字复制任一单一商业源**。
- **B 类（生成）**：全站 UI 文案、生肖流年空白字段、星盘格局翻译为原创撰写。
- **合规红线（REQ-006）**：全部文案统一采用「倾向 / 宜 / 可关注」等软性措辞，杜绝「一定 / 注定 / 必然」等宿命式断言；全站显著保留免责声明（见 `B1_ui_copy.json` 的 `compliance`）。

## 三、接入建议

1. **A2/A3**：在 `tarot-data.js` / `lenormand.js` 现有卡片数组上**增量增补字段**，不改动既有 `name/type/suit/number/keywords`，保持向后兼容。
2. **A4**：`types.json` 字段名与原结构一致，可直接整体替换 `types` 对象。
3. **A1**：建议抽为 `zodiacTrends` 常量（按 `sun/moon/rising → 星座 → male/female` 索引），三格组件按档案的日月升分别读取。
4. **B3**：将 `patterns/planets/aspects` 三表抽为单一共享文件（如 `astrologyLabels.ts`），两页面统一 import，避免日后双处维护漂移。
5. **B2**：`五行关系/风险关系` 由代码按「流年天干五行 × 生肖本气五行」实时判定（沿用现有 `有利关系/风险关系/中性关系` 三档），`贵人/本气/行动建议` 用本文件确定性文案填充。

## 四、遗留与待确认

- A1 的 72 篇为「整体趋势」统一口径；若后续要细分「感情/事业/健康」多段，可在现有段落内再拆，不改变文件结构。
- C1/C2 占位策略仍保留：未接入前生产显示「文案待补充」，接入上述文件即替换占位。
- 上述文件为「文案正文交付物」，尚未写入生产代码；是否直接落地入参（尤其 A2/A3 改 `mingyu-core` vendor 文件、A4 改 `types.json`）建议由开发按上述接入建议执行，需求方验收。
