# 太初 · 需求表（Requirement Tracker）

> 本表是项目需求的**单一事实源**：新需求在此登记、分配 ID、追踪状态；开发按表推进，完成后更新状态。
> 每次会话涉及的新需求 / 状态变更，收尾前同步回本表。

## 使用说明

1. **登记**：新需求在对应模块新增一行，分配 `REQ-<三位序号>`（全局递增）。
2. **查重合并**：登记前先与全表比对——与已有条目重复 / 高度相似者**不新增行**，并入已有条目（保留原 ID，合并补充描述，备注注明「并入于 <日期>：<新增点>」）；部分重叠者拆并入最贴近条目或在备注交叉引用，避免同一需求多行、状态分叉。
3. **状态**：🟡 待办 · 🔵 进行中 · 🟠 待验收（开发完成等测试）· 🟢 已验收 · ⚪ 已归档（明确不做）。
4. **优先级**：P0 阻塞 / P1 高 / P2 中 / P3 低。
5. **粒度**：一条需求对应一个可验收的用户可感知功能或明确的工程改造，不混多件事。
6. 状态流转：🟡 → 🔵 → 🟠 → 🟢（或 ⚪）。🟢 只能由测试组验收通过后设置；开发完成置 🟠 并在备注写「待验收」。
7. **可执行性保证**：本表发出的需求必须做到「开发可直接照做」——含明确动作主体与范围 + 可验收口径（期望行为 / 通过标准）；描述含糊（如「优化体验」「尽快」）、缺验收口径或多义时，**先向需求方确认补齐后再登记**，不得凭推测落表。

---

## A. 国学主线（Phase 1–7，已上线）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-001 | 用户系统：手机号注册/登录、JWT、图形验证码、设备指纹、IP 限流、失败锁定 | P0 | 🟢 | 注册强制（ADR-0007） |
| REQ-002 | 排盘引擎：9 法确定性排盘（八字×4/紫微/西占/七政/奇门/五运六气） | P0 | 🟢 | 自研为主，Node 常驻服务 + subprocess 降级 |
| REQ-003 | LLM 方法模块：9 法分析 + 校验（仅断前尘） | P0 | 🟢 | DeepSeek，method-result v2 契约 |
| REQ-004 | 异步编排：断前尘/预测 jobId + 前端轮询，并行 3 并发 + 全局信号量 | P0 | 🟢 | 逐法扣费 + 断点续跑 |
| REQ-005 | 前端：建档→等待→校准→预测→追问→档案 全流程 | P0 | 🟢 | 免构建预编译 JS |
| REQ-006 | 合规护栏：双层过滤 + 免责声明 + 全 prompt 合规边界 | P0 | 🟢 | 8 标题固定产出 |

## B. 积分与支付（P1/P2）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-010 | 积分系统：账户/流水/扣费（1 积分=1000 tokens）/充值码/注册赠送 220 | P0 | 🟢 | 逐法扣费 + interpret 即时扣费 |
| REQ-011 | 金数据支付：表单 + API 轮询 + 幂等入账 | P1 | 🟢 | 商户认证待用户自行验证 |
| REQ-012 | 后台：埋点报表、用户管理、积分查看、赠积分 | P1 | 🟢 | `EVENT_NAME_ZH` 中文映射 |
| REQ-051 | 积分余额显示改版：①档案相关界面不再显示积分数字，改为全局显示在 Banner 右上角菜单左侧（仅登录）；②显示形式由「积分」改为「余额 ¥」= 积分÷汇率（1 元=10 积分，动态读 system_configs），**保留积分底层计费逻辑不变**，仅改前端展示 | P2 | 🟢 | 2026-09-07 用户确认口径（余额 ¥、动态汇率、仅登录）；关联 REQ-010/012。（2026-09-07 二轮复验通过 → 🟢；02a6c7c 已落地）。入口内详情/流水的积分记载统一问题见 REQ-063。**2026-09-07 被 REQ-085/087 演进取代**：积分概念废除（余额-Token 直接绑定，见 REQ-085）；余额展示位置由 Banner 移入右上角菜单顶部（REQ-087）；本条（Banner 展示 + 积分÷汇率折算）作为中间态历史保留 |
| REQ-063 | 余额详情与消费明细可读化：①点击右上角余额入口后的详情（含流水）中仍以积分形式记载的内容改回「余额」单位，全站口径统一（沿用 REQ-051 折算：余额 ¥ = 积分 ÷ 汇率）；②消费明细写明消耗**项目中文标签**（断前尘 / 预测 / 起卦深度解读 / 塔罗解读 / 雷诺曼解读 / 星座本命解读 / 追问 / 充值 / 注册赠送等，按扣费 ref 映射，不再显示裸 `consume`）；③每条明细同时显示**消耗的档案名**；④历史流水按同规则映射展示（标签 + 档案名） | P1 | 🟠 | 2026-09-07 用户确认（选项 1 + 档案名）；延伸自 REQ-051（详情/流水口径，REQ-051 仅覆盖 Banner 展示）；关联 REQ-010/012 |
| REQ-085 | 「积分」概念全站废除 → 余额与 Token 绑定（计费体系改造，2026-09-07 拍板）：①全站不再使用「积分」（前端 / 后台 / prompt 提示语 / 文案 / 埋点语义），统一「余额 ¥」；存量积分账户按 1 元 = 10 积分折算为余额口径（内部历史换算策略开发定，展示层折算）；②余额与 Token **直接绑定**：1 元 = 10,000 tokens（由 1 积分=1000 tokens × 1 元=10 积分 推导），经 system_configs 可调；LLM 消耗按 tokens→元 从余额扣减（原 interpret / 逐法扣费 / 会话扣费链改余额扣减，扣费 ref 不变）；注册赠送迁移为**余额 22 元（=22 万 tokens）**（system_configs 可调）；③后台「赠积分」改「**余额充值 / 余额调整**」（增 / 减均可，留审计）；REQ-050 Agent「赠积分」动作同步改为余额充值（高风险确认不变）；④影响面迁移清单：REQ-010/011/012/035（充值档位改 ¥：0.1 元测试档 = 1,000 tokens 等）/051/063/072/075/076（按 token 扣 → 展示余额口径）及前端 pay-hint「消耗 N 积分」类提示全部改余额口径；文案配合 REQ-086 角标 | P1 | 🟡 | 2026-09-07 确认：1 元=10,000 tokens、注册赠送 22 元、后台改余额充值/调整、Agent 动作同步；被影响行开发时同步更新备注；关联 REQ-051/063/072/076/012/010 |

## C. 横向扩展（Phase A–D）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-020 | 三大入口 + 左侧 nav-rail + 模块主题背景 | P1 | 🟢 | 国学/西式/MBTI 皮肤 |
| REQ-021 | 生肖流年（确定性零 LLM，免费引流） | P1 | 🟢 | Node `/zodiac` |
| REQ-022 | 临时起卦：六爻/梅花/小六壬/观音灵签 + 断卦付费 | P1 | 🟢 | Node `/divination`，大六壬/金口诀/奇门置灰 |
| REQ-023 | 塔罗：抽牌 + 牌阵 + 综合解读付费 | P1 | 🟢 | Node `/tarot`，卡面美术已就位 |
| REQ-024 | 雷诺曼：抽牌 + 关键词组合 + 深度解读付费 | P1 | 🟢 | 复用 `/divination`（lenormand） |
| REQ-025 | 星座：本命盘 + 深度解读付费 | P1 | 🟢 | Node `/astrology` + 法达查表，盘型 tab 仅本命 |
| REQ-026 | MBTI：60 题标准版 + 判型 + 16 型文案 | P1 | 🟢 | 纯代码零 LLM，前端页面已完成；A4 文案多源优化已接入（c7dfd84） |
| REQ-027 | 导航交互重构：三大模块落地宣传页 + 左侧按钮仅落地页 + Banner 右上角菜单 | P1 | 🟢 | 落地页→HUB 两级；未登录「登录/注册」、已登录汉堡菜单。（2026-09-07 二轮复验通过：BUG-001 小屏遮挡已修复、导航交互整体验收通过 → 🟢） |
| REQ-028 | 统一档案体系：星座/国学/MBTI 共用同一 case 档案；case 加 mbti_type；星座改名；菜单加「档案管理」 | P1 | 🟢 | 星座复用 case 生辰（去独立表单）、MBTI 关联 case、汉堡菜单加档案管理。（2026-09-07 二轮复验通过：占星盘合并展示（09adfa2）、档案查看直接开已存盘（13f52e5）等联动问题已修复 → 🟢） |

## E. 体验与交互优化（2026-09-07 用户走查批次）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-037 | 全站 UI 文案体系优化美化：贴合功能实际、具吸引力，调性高贵/奢华/内涵；全程「趋势参考、不作建议」合规话术（REQ-006 红线内建） | P2 | 🟠 | B1 文案已接入（落地页/品牌/合规/Hub，18bef25）；buttons/tips/error 常量已定义，全站按钮文案替换待续 |
| REQ-038 | 国学预测与西式占卜两个 Hub：方法改竖向单列排列（原横向两列），选项内容展示更充分、页面不拥挤 | P2 | 🟢 | 关联 REQ-020/027；Hub 单列已落地（12e14b7）。（2026-09-07 二轮复验通过 → 🟢） |
| REQ-039 | 档案绑定与展示统一：①星座星盘按档案幂等——同一档案只生成一次并绑定，再入直接复用不重复生成；②档案详情新增「占星盘」子类展示已生成星盘；③MBTI 测试结果持久化绑定 case，再入档案不复测、档案内可见结果 | P1 | 🟢 | 关联 REQ-025/026/028；星座幂等 db03950 + build_archive 51c3daf + 档案展示 b9eddb5；「选已有档案」档案项 UI 明显化另见 BUG-006（UI 类）。（2026-09-07 二轮复验通过：退回 4 问题已修复（cc8d02e/439115d/MBTI 直达）→ 🟢） |
| REQ-040 | 档案详情页改版：顶部标题显示当前档案的用户命名（非固定「我的命理档案」）；页尾按钮重设计：删除「退出登录」，保留「回到档案列表/回到首页」，新增「国学预测/西式占卜/MBTI」三直达按钮——携带当前档案进入对应模块主功能（非落地页），该档案对应模块无数据时页内引导生成、不自动生成 | P2 | 🟢 | 关联 REQ-027/028；档案名标题+3直达+删退出登录已落地（12e14b7）。（2026-09-07 二轮复验通过 → 🟢） |
| REQ-041 | 生肖流年模块图标重新设计（统一整体产品风格） | P3 | 🟢 | 生肖 medallion 图标已接入（b61f4e4 + sprite c16a3c8）。（2026-09-07 二轮复验通过 → 🟢）；关联 REQ-021 |
| REQ-042 | 塔罗模块交互改版 v2（2026-09-07 用户修订）：①流程=「占问问题文本框」+「选择牌阵」，**删除「预测方向」步骤**；②牌阵选择改**单列按钮列表**（非双列），每项标题=牌阵名 + 说明文字（该牌阵适用何种占卜，静态文案）；③抽卡结果 UI 重做——**上方**只平铺本次抽到的全部卡（卡图/标题/标签），卡面不嵌含义；**下方**以段落文字自第一张起逐张输出：标题 + 标签 + 正位/逆位 + 对应含义（固定可复用文本、不依赖 LLM），集中便于查看；④抽卡页顶部承接所填占问问题文本；⑤落地页顶部不做扇形美术（预测方向已删，直入占问问题/选牌阵），公版署名移至页脚小字；⑥底部「重新抽卡/重选牌阵/返回」全宽并列大按钮；⑦80 张牌图本地压缩 WebP + 分级缩略图提速（保留相对路径、懒加载维持、不引外部 CDN） | P1 | 🟢 | 关联 REQ-023；v2 已落地（a8bef1f：删预测方向、牌阵单列+说明、抽卡图/文拆分上下布局）；雷诺曼 REQ-043 同步 v2。（2026-09-07 二轮复验通过 → 🟢；⑦ WebP 提速为性能优化，另行跟踪不阻塞验收） |
| REQ-043 | 雷诺曼模块改版（对齐塔罗 v2，2026-09-07 修订）：①**删除「预测方向」步骤**；②三个牌阵（单张/3 张/9 宫格）改**单列排版**，每个标题下加说明文字（适用场景）；③抽卡后卡位不放解释——解释全部归拢到**下方段落**（自首张起：标题 + 标签 + 正/逆位 + 含义，固定可复用文本）；④底部按钮对齐塔罗（全宽并列） | P2 | 🟢 | 关联 REQ-024；按 v2 返工落地（a8bef1f：删方向、牌阵单列+说明、解释下移归拢）。（2026-09-07 二轮复验通过 → 🟢） |
| REQ-044 | 临时起卦交互改版 v2（2026-09-07 用户修订）：①列表页：7 种方法（六爻/梅花/小六壬/观音灵签 + 置灰大六壬/金口诀/奇门）改**单列罗列**，每项显示：方法名（如「六爻」）在上 + 该法**简易介绍**在下（**不放「摇卦/报数」标签**）；②流程 (a) 列表页不放占问问题，直接点选具体方案 → (b) **弹窗页**：弹出对话框填写「占问问题」并带「开始起卦」按钮（**问题可选**——可留空直接起卦，填写则带入断卦上下文；梅花易数/小六壬弹窗内另含时间选项，默认当天，对齐 REQ-055⑤。2026-09-07 修订：原 v2「必填」改回**可选**）→ (c) 结果页展示起卦内容 + 「深度断卦（付费）」；③底部「重新起卦/返回」全宽并列大按钮 | P1 | 🔵 | 关联 REQ-022；v2 差异返工点：原 4b5d554 已将问题框放页面——v2 改为列表无问题框、问题移入方案弹窗；取消标签需求（用户确认）；六爻完整卦象见 BUG-004、结果页重构见 REQ-055。（2026-09-07 备注：v1 口径问题框+全宽按钮 4b5d554 已真机复验通过；v2 弹窗/单列列表已实现并置 🟠；2026-09-07 v2.1 修订：占问问题由必填改回**可选**（可空直接起卦）→ 退回 🔵 返工后重验；观音灵签交互/独立结果见 REQ-079） |
| REQ-045 | 星座页新增「太阳/月亮/上升」三格与布局调整：①星盘页新增三格，每格展示该档案对应星座整体趋势描述（感情/事业等）；②复用文案按男女各一套正文 = 12 太阳×2 + 12 月亮×2 + 12 上升×2 共 72 篇，**先以占位文本实现结构与布局，正文后续补全**；③星盘正中 ASC/MC 字样去除；④「本命深度解读」按钮上移至三格下方、行星落座/落宫等详细信息上方 | P1 | 🟢 | 关联 REQ-025；三格 + 72 篇正文 + 去 ASC/MC + 解读按钮上移已完成（6787563）。（2026-09-07 二轮复验通过 → 🟢） |
| REQ-046 | 星座/MBTI「新建档案」入口与建档按钮路径区分：①MBTI 人格测试与西式占卜·星座页面补「新建档案」入口（进入建档流程）；②国学/西式/MBTI 从模块页进入「新建档案」→ 建档完成后直接进入该模块主流程并显示对应主按钮（开始排盘/生成星盘/开始答题），不显示其他模块按钮；③从右上角「档案管理」进入的档案页：提供「选择档案进行修改/查看」与「回首页」，不显示模块主流程按钮 | P1 | 🟢 | 关联 REQ-026/028/039/040；①②新建档案入口+returnTo 回跳（4b5d554）+ ③CasesPage 精简（1f1a8cd，按钮 4→3：查看档案/重命名/删除档案 + 回首页）。（2026-09-07 二轮复验通过 → 🟢；③ 列表展示口径后续按 REQ-065 CRM 行式列表执行，以 REQ-065 为准；建档按钮/排盘解耦按 REQ-067） |
| REQ-047 | MBTI 结果分享：①MBTI 结果页提供「分享」：生成绑定该档案的专属链接，他人打开**免登录**填写（本场景豁免 ADR-0007 强制注册：填写人仅答 MBTI，不建 case、不触其他模块）；②每次填写结果作为一条历史记录存入该档案（各次可回看），并将该档案当前 MBTI 结果**更新为最新填写**（2026-09-07 修订：原「不覆盖」口径废止）；分享入口仅在 MBTI「选档案后」操作区 | P2 | 🟢 | 关联 REQ-026/039；后端 37c339e + SPA fallback 9d68524 + 前端 6618d31；分享入口提前 + BUG-009 加固（d19f07c/659237f）。（2026-09-07 二轮复验通过：入口时机正确、链接免登录直达正常 → 🟢）；MBTI 入口/结果/写入口径最终整合见 REQ-056 |
| REQ-052 | 生肖流年改为档案驱动：生肖页移除手动年份输入，改为档案选择（下拉列当前用户档案，未登录引导登录）；选中档案后自动将档案出生年份换算为对应生肖并出结果 | P2 | 🟢 | 用户确认「仅档案选择、去手动」；取舍提示：REQ-021 原为免登录引流模块，登录化后引流链路变化（已由用户拍板）；已落地（02a6c7c 档案驱动 + 0049ae3 年份切换改为结果标题左右箭头、去独立年份卡）。（2026-09-07 二轮复验通过 → 🟢）；关联 REQ-021/028/046 |
| REQ-053 | 档案详情字段展示改版：①「出生年/月/日/时辰」4 字段合并为「出生年月日时辰」，其下补充「中式干支传统写法」与「农历生日及出生信息」两字段；②「经度」「纬度」合并为单一字段展示；③新增「生肖」字段；④占星盘内容合并（2026-09-07 比对结论已出，分层落地）：L1 前端抽取共享「盘面渲染单元」（pointRow / 行星落座四轴 / summary chip-逆行-格局，wheel 已共享 AstroWheel），AstrologyPage 与档案 AstroNatalPanel 共用；L2 档案页内 astroCard（natal）与完整盘面「占星盘」tab（charts.western）双份内容收敛——同档案只呈现一份完整占星展示（推荐以 natal 为准或 western 经 astroNorm 归一化后走 natal 套件）；L3 数据层唯一事实源（astrology 生成回写 charts.western）暂缓、待后端评审。保留星座页三格/解读/流年等专属内容 | P1 | 🟢 | 关联 REQ-039/040/045；④评估完成（2026-09-07 代码比对）：可合并、分层实施。①②③字段改版 + L1/L2 占星合并已落地（09adfa2/13f52e5）。（2026-09-07 二轮复验通过 → 🟢；L3 数据层唯一事实源为暂缓工程项，另行跟踪不阻塞验收）；风险备忘：natal/charts.western 双快照不一致、无经纬度降级等详见 REQ-039 链路 |
| REQ-054 | 档案内历史记录逐条删除：占星盘本命盘 / MBTI 等并入档案后的每条历史记录旁提供「删除」按钮，删除前二次确认 | P2 | 🟢 | 关联 REQ-047/039；逐条 + 二次确认（2026-09-07 用户确认）；已落地（09ed98b 删除端点 + b9d3999 前端二次确认）。（2026-09-07 二轮复验通过 → 🟢；测试组补 2 自动化用例：DELETE 隔离/删除后消失/不回写 mbti_type 全绿） |
| REQ-055 | 起卦结果页重构 + 免费静态解读（六爻/梅花易数/小六壬）：①六爻结果改整齐列表布局——爻位逐行、爻符号/六亲/干支/世应/阴阳/六神分列对齐，动爻明显标识、世应标记突出（原排版挤作一团）；②六爻排盘组件下方新增**免费静态解读区**：本卦大意/变卦大意/动爻简要提示/世应基础含义；保留「深度断卦（付费）」为高阶入口，页尾免责声明保留；③梅花易数补齐类六爻的卦象展示 UI；④梅花易数/小六壬结果下方补静态现代汉语免费解释；⑤梅花/小六壬在占问问题上方新增**时间选项**（日期默认今天 + 时辰默认当前，必填） | P1 | 🟢 | 关联 REQ-044/022、BUG-004（视觉部分归本条，数据正确性已随 BUG-004 闭环）；已落地（b56a687：分列布局 + 免费静态解读 + 梅花/小六壬时间选项）。（2026-09-07 二轮复验通过 → 🟢；64 卦大意等重内容先占位、正文后补，补全并入文案轮次跟踪） |
| REQ-056 | MBTI 人格测试入口与结果页交互定稿 v2：①档案选择列表仅展示「档案名称 + 已测 MBTI 类型」（未测显示「未测试」）；②勾选档案后下方操作区按钮：开始测试 / 我已知道类型（直接输入）/ 新建档案 / 管理档案；③该档案已测过 → 勾选后**直接显示其 MBTI 结果**（不重新测试），结果页提供「重新测试」按钮；④「分享测试链接」按钮（**仅在勾选档案后的操作区**）：生成该档案专属链接，他人免登录填写后，档案当前 MBTI 结果**更新为最新填写内容**，同时保留各次填写历史可回看 | P1 | 🟢 | 作为 MBTI 入口/结果/分享的权威口径，整合并取代 REQ-039③、REQ-046①、REQ-047 的 MBTI 相关子项（原行描述保留为历史）；已落地（f2fe9ca：档案列表显类型+四操作+已测直达+分享仅选档案后）。（2026-09-07 二轮复验通过 → 🟢；分享依赖 BUG-009 已修复闭环）；关联 REQ-026/028 |
| REQ-057 | 星座模块新增「恋爱关系」板块：独立入口按钮，点击进入后展示「当前档案太阳星座」与其余 11 星座的匹配度；每对 = 固定匹配度分值（星级或百分比）+ 说明文字（静态可复用文案 12×11 对，与三格/文案轮次同文案体系与合规口径） | P2 | 🟠 | 与 REQ-030（合婚/合盘，P3 已归档）区分：本条为太阳星座配对轻内容，非双人生辰合盘；文案同 REQ-045/037 策略（结构先行、正文后补）；关联 REQ-025/045 |
| REQ-058 | 国学「九法合一」排盘等待页增强：等待界面提供「回到首页」入口（若当前流程已含则仅补缺）+ 提醒文案「排盘等待时间偏长，可先去体验其他功能」（等待开始时展示） | P2 | 🟠 | 2026-09-07 用户确认：仅「回到首页」+ 提示语即可（不做「前往其他板块」）；现状部分等待流程已有回到首页（frontend `back_home`），开发核对九法合一等待页补齐提示语 |
| REQ-060 | 全局页脚贴底（Sticky Footer）：免责声明 + 用户协议 + 隐私政策所在页脚，在页面内容不足一屏时恒贴视口底部；内容超出一屏时页脚随文档流落在页尾（不悬浮、不遮挡内容）；覆盖全站页面（落地页/Hub/各功能页/档案页等共用全局页脚） | P2 | 🟠 | 2026-09-07 用户确认：Sticky 贴底语义；建议实现：内容区 min-height 撑满（100vh − 页脚高）+ 页脚留在文档流，无需 JS 监听 |
| REQ-061 | 占卜动效与翻牌交互：①观音灵签：点击「开始」弹出**可交互签筒与签**——点击签筒或摇晃手机（devicemotion）掉落一支签，掉落的签转化为签文文字内容；②六爻与梅花易数：点击「起卦」后先播放一段**起卦动画**，再给出卦象内容；③小六壬：点击「开始起卦」先播放一小段动画再开始起卦；④塔罗/雷诺曼：选择牌阵点抽牌后先播放**抽牌动画**，随后按牌阵数量出现**背朝上的卡牌**（如 3 牌阵 3 张）；⑤翻牌：用户点击牌背翻转为正面并展示对应解析（塔罗含正/逆位）；⑥**一键全翻**：增加「一键全翻」按钮，可将全部卡牌一次性翻转展示 | P2 | 🟠 | 前置交互层：与 REQ-044（起卦 v2）、REQ-042/043（塔罗/雷诺曼 v2）流程衔接，动效/翻牌结束后仍按各模块 v2 结果布局展示；摇晃手机需处理权限与不可用降级（点击签筒为兜底）；关联 REQ-022/023/024/044。**开发范围（2026-09-07 确认）= ①前端交互逻辑 ②交互方案 ③代码实现**；美术素材（签筒/签/动画资源等）由用户另找人生产后接入——开发先用占位/程序化动效与现有卡面实现，预留素材替换点（卡面替换走 REQ-059⑤）。**开发前置门禁（2026-09-07）**：塔罗 / 六爻 / 雷诺曼等动画与翻牌交互进入编码前，开发必须先向需求方提交：①**详细交互方案**（含动效拆分、触发、实现路径、预期效果示意）；②**美术素材表**（对象清单 / 规格尺寸 / 数量 / 风格 / 动效帧拆分，供美术外包排期）；经需求方确认方案与预期效果后才可开发；素材接入沿用已预留替换点。**门禁已通过（2026-09-07）**：交互方案 + 美术素材表已提交并获需求方确认（docs/占卜动效翻牌交互方案.md），进入开发；开发先用占位/程序化动效，素材由美术 Agent 生产后接入（卡面替换走 REQ-059⑤） |
| REQ-062 | 落地页宣传大色块透明度：国学预测 / 西式占卜 / MBTI 三宣传大色块（nav-rail 三按钮右侧的白色宣传色块）改为**白底 70% 半透明**（rgba(255,255,255,0.7)），色块内文字 / 内容保持 100% 不透明 | P2 | 🟠 | 2026-09-07 用户确认：白底 70% 文字不淡；落地页三模块宣传块共用样式类，改一处即可（关联 REQ-027 落地页） |
| REQ-064 | 首页按键调整与「档案管理」入口迁移（2026-09-07 v2 修订，取代旧「nav-rail 第 4 键」方案）：①首页左侧四按键**缩减回 3 个**（国学预测 / 西式占卜 / MBTI），撤销原 nav-rail「档案管理」第 4 键；②「档案管理」入口改放**三大模块落地宣传界面**各自主按钮下方——国学：在「进入国学预测」下方；西式：在「进入西式占卜」下方；MBTI：在「开启人格测试」下方；③样式统一：**透明小按钮**，字号小于上方主按钮；④**仅登录显示**（未登录不显示）；点击进入档案管理列表 | P2 | 🟡 | 2026-09-07 用户确认：仅模块页小按钮（不要左缘浮钮）、仅登录显示；取代旧方案（曾以 nav-rail 第 4 键实现 🟠，按此回改迁移）；关联 REQ-027/046/065/070 |
| REQ-065 | 档案管理列表 CRM 化：①列表页将完整档案窗口缩小为**单行条目**——每行显示：档案名 + 生日（如有手机号/邮箱则一并显示）；②档案模型新增两个**非必填**字段「手机号」「电子邮箱」，建档表单可选填、档案「修改」入口可编辑（含格式校验）；③每条目**最右侧**排「查看 / 修改 / 重命名 / 删除」四按钮，同屏可见更多档案 | P1 | 🟠 | **取代 REQ-046③ 已验收「卡片 + 3 键（查看档案/重命名/删除档案）」口径**（2026-09-07 用户最新指示为准：CRM 行式 + 4 键含修改）；需后端 cases 表加列（ensure_schema 迁移）+ 建档/修改/archive 链路带出新字段；关联 REQ-046/053/001 |
| REQ-066 | 「功能设置」页（右上角菜单入口，即早期所称「用户设置」；菜单按钮名统一为「功能设置」）：以开关为主，本版本支持 4 个开关并做系统功能适配——①开关「动画与抽卡模拟」：默认**开**；关闭后各占卜界面跳过动效直接出结果（观音灵签签筒、六爻/梅花/小六壬起卦动画、塔罗/雷诺曼抽牌动画与抽卡模拟，适配 REQ-061）；②开关「占卜界面默认模式」：三选项 (a) 默认手动快速填写结果（适配 REQ-069）(b) 默认项目起卦/抽卡方式（**默认值**）(c) 两方式都可用（两入口并行、不设固定默认）——按所选适配各占卜界面默认入口；③开关「Banner 模块下拉导航」：默认**关**；控制 REQ-071 banner 正中下拉是否展示；④开关「分享表单太初 UI」：控制分享模块（MBTI 分享表 / 新建档案「分享帮填」表）是否带太初 UI——「开」：表单内显示太初 logo、登录注册与免责提示；「关」：仅表单，填完仅可保存（MBTI 可查看结果，页面关闭后链接重开失效），收到链接者无法经该界面到达任何太初功能页（保持访客隔离）；⑤开关「背景图显示」：默认**开**——开启时显示运维后台（REQ-059）配置的全部背景图（含太初先生 Agent 会话页背景槽）；关闭时运营背景图一律不显示，界面回到纯色配色默认样式；⑥开关「牌面图片显示」（塔罗 / 雷诺曼）：默认**开**——显示牌面图片；关闭后两功能改为**纯文字列表模式**：仅显示抽到的卡名 + 正/逆位 + 对应含义段落（下方解释），隐藏全部牌面图片（扇形 / 牌背 / 翻牌动画默认跳过）；⑦开关「太初先生 Agent」：默认**开**——开时 banner 显示 Agent 图标入口；关时隐藏入口（既有会话与数据保留，重开可见） | P2 | 🟠 | 2026-09-07 开关清单定稿（默认：动画开 / 导航关 / 默认模式 (b) 自动 / 背景图开 / 牌面图开 / Agent 开）；适配 REQ-061/069/071/047/056/070/059/076 与分享隔离；关联 REQ-027（菜单）/ REQ-001 |
| REQ-067 | 档案与「九法合一」解耦 + 建档页按钮重设：①概念解耦：档案为独立数据实体，「九法合一」仅是档案的一种使用方式，二者不再绑定；②**统一建档组件**（各入口共用同一表单与按钮组）：新增**档案名输入（选填，非必填）**；底部按钮 (a)「开始排盘」保留——点击仅调用**确定性非 LLM 排盘**产出盘面（不进断前尘/预测等 LLM 的九法合一通道），盘面生成后可入档案查看，后续深度解读到具体模块（九法合一等）另行启动；(b) 新增「**新建下一个档案**」按钮（保存当前并继续建下一个）；③模块直达逻辑（REQ-046② 建档后进入模块主流程）由各模块自行衔接，不再默认绑定排盘/九法流程 | P1 | 🟠 | 2026-09-07 确认：排盘=仅产出盘面、解读另走模块；统一建档组件适用于各入口；细化/取代 REQ-046② 建档默认行为；关联 REQ-046/028/005/002/004 |
| REQ-068 | 「九法合一」模块入口改「选择档案」UI（对齐星座/MBTI 模式，参考星座界面视觉）：①进入后先「选择档案」（复用选档案组件样式：档案项明显可选 + 选中态，可新建档案入口）；②选中档案后下方显示两个按钮：**「查看 / 修改结果」** + **「跑九法合一开始预测」**（UI 参考星座页按钮层级）；③「查看/修改结果」：档案已跑过九法 → 进入结果查看（含校准/追问/重跑修改）；未跑过 → **置灰/隐藏并引导先跑预测**；④「跑九法合一开始预测」：新跑完整九法链路（断前尘/预测，沿用现有异步编排、逐法扣费、幂等与断点续跑机制） | P1 | 🟠 | 2026-09-07 确认：未跑置灰引导、跑过可查看修改；衔接 REQ-067（档案=实体、九法=使用方式）与 REQ-056/046（选档案交互）；关联 REQ-005/004 |
| REQ-069 | 各占卜模块新增「快速手动输入实际结果」模式（场景：用户在真实世界完成卜卦/抽牌后，仅录入结果走解读，跳过随机生成）——覆盖**六爻 / 梅花易数 / 观音灵签 / 小六壬 / 塔罗 / 雷诺曼**（排除九法合一与 MBTI）：①六爻与梅花易数：共用**逐爻点选录入器**——逐爻点选阴阳（可标动爻），6 爻录完自动成卦 → 走既有免费静态解读与「深度解读（付费 LLM）」；②观音灵签：从签号列表**快速选择实际摇中的灵签**，下方即时展示签文并可继续深度解读；③小六壬：提供「输入月/日/时三数自动落宫」或「直接点选实际得到的掌诀（结果宫）」两种便捷录入；④塔罗/雷诺曼：按所选牌阵数量从牌库面板**逐张选牌并定正/逆位**，选完组成结果（复用牌义与解析展示，含一键全翻后的翻牌展示逻辑）；⑤通用：手动录入入口置于各模块流程中（与自动起卦/抽牌并行切换），解读与计费沿用既有链路（免费静态 + 深度 LLM 付费按 interpret 扣费） | P1 | 🟠 | 2026-09-07 确认：范围=六法全含、六爻/梅花逐爻点选成卦（共用录入器）、塔罗/雷诺曼按牌阵数量选牌+正逆位；关联 REQ-022/023/024/044/042/043/055/061。**已知遗留（2026-09-08）**：小六壬「任意三数/掌诀」手动录入——前端即时预览已做，但后端落库仍用实际时刻、未持久化用户所录三数/掌诀；彻底闭环需后端 `generateXiaoliuren` 扩展 manual 字段（三数/落宫），待排期 |
| REQ-070 | 「分享帮填」按钮：新建档案界面 / 档案管理「新建档案」按钮旁新增「分享帮填」：①点击生成**一次性分享链接**（绑定发起者）；②他人打开链接在**未登录状态**完成整个档案建立（**姓名必填**；其余字段同统一建档组件：出生年月日时辰 / 农历等展示字段按建档标准；手机号/邮箱可选填）；提交成功后新建档案**归属发起者名下**，链接随即失效（一次性）；③权限限制：帮填访客仅能提交该档案，无法使用排盘 / 九法合一等任何其他功能（触及时提示需登录或注册）；**访客会话与发起者会话彻底隔离**，不可见发起者既有档案与数据（防 BUG-011 式越权，复用 REQ-047/056 分享通道的隔离实现）；④不需要专门管理页——提交的档案直接进入发起者档案列表（按档案名区分） | P2 | 🔵 | 2026-09-07 确认：归分享者、链接一次性、无需管理页；（2026-09-07 验收退回 🔵：新建档案界面未见「分享帮填」入口——BUG-012，修复后重新 🟠）；关联 REQ-067/065/001/047/056 |
| REQ-071 | Banner 正中「模块下拉导航」：整个页面顶部 banner（「太初」与「余额」之间正中）加一个下拉按钮：①内容：显示**全部支持模块**（按三大模块分组小标题：国学预测 / 西式占卜 / MBTI，组内列功能标题——九法合一 / 生肖流年 / 临时起卦；星座 / 塔罗 / 雷诺曼；MBTI 人格测试），**仅标题、无解释文案**；②交互：点击某项**直达该功能页**（等价用户从所属 Hub 点击该功能后的页面，如九法合一→选档案入口、星座→选档案/生成页、塔罗→塔罗落地页）；③约束：居中位置须与右上角余额/汉堡菜单不重叠，移动端小屏交互适配 | P2 | 🟠 | 2026-09-07 确认：分组 + 功能级列表、直达功能页；关联 REQ-027（Banner 菜单）/068/056/042/043/044/021~026 |
| REQ-072 | 三大模块 LLM「配对解析」：国学预测 / 西式占卜 / MBTI 各加入配对解析功能——①交互：(a) 界面显示「配对解析」按钮；(b) 点击后从档案列表选**任意两个档案**；(c) 指定判断内容：**预设关系类型（恋爱 / 朋友 / 家人 / 同事等）单选 + 可补充自由关注点文本**；(d) LLM 据所选档案与该模块数据做配对解析；②数据调用：(a) 国学→两档案的**排盘信息**；(b) 西式→两档案的**星座本命星盘**；(c) MBTI→两档案的**人格信息**；③缺数据档案：**提示先生成**（国学先排盘 / 西式先生成本命星盘 / MBTI 先测或选型），不自动生成；④Prompt：三份 prompt 纳入后台**可修改**（backend/prompts/pair/*，纳入 REQ-048 文件类热改 + 版本回滚） | P1 | 🟠 | **Prompt 要点（2026-09-07 构思，供开发细化完整）**：公共=命理咨询顾问角色、趋势参考口吻不作绝对断言、合规免责、结构清晰分节；国学=输入双方排盘信息（干支/五行/用神/命局特点），侧重五行生克·用神互补·流年趋势，输出契合点/冲突点/相处建议；西式=输入双方本命星盘（日月升、行星落座落宫、相位），侧重元素模式互补·日月相位·四轴互动；MBTI=输入双方类型与倾向维度（E-I/S-N/T-F/J-P 及评语），侧重认知与沟通风格契合/摩擦点。计费：LLM 调用按 interpret 即时扣费（1 积分=1000 tokens，ceil）；关联 REQ-048/057/030（部分覆盖 REQ-030 合婚场景，其专项仍归档） |
| REQ-073 | 六爻静态文案补齐（成稿见 `docs/六爻文案补齐与skill素材对照.md`）：①S01 **64 卦「大意」白话库**（本卦/变卦共用，静卦走 S06 文案）；②S02 六亲释义 5 条；③S03 六神释义 6 条；④S04 世应基础含义卡；⑤S05 常用占断术语短释（旬空/月破/日破/暗动/回头生克/化空化破化墓/入墓/六合六冲/伏神/用忌原仇约 10 条）；⑥S06 动爻/静卦/互卦通用话术与免费区免责；⑦S07 合规尾注。落地：替换 `LiuyaoFreeReading` 占位文本（`PH_WENAN`/`PH_HEX_SUFFIX`/`C3_GENERIC_RULES`），新增词条卡与术语释义展示；爻辞原文（386 条）作为引用附录后续跟踪（S08） | P2 | 🟠 | 成稿含改写规则与合规标注（skill MIT 素材去模型尾巴、趋势参考话术、不作建议）；补充 REQ-055 免费解读区占位后补跟踪项；素材源 reference/liuyao-skills-main（许可合规见文档 §1）。**呈现指引（2026-09-07）：开发按文档 §7 执行**——§6 接入点必须逐项落地、不得残留「文案待补充」占位、无需新后端；词条/术语呈现形态等自由项由开发 Agent 自行设计并在本条备注写明「呈现选型 + 接入位置」；不确定先提问、不得搁置或擅改成稿 |
| REQ-074 | 深度断卦 prompt 增强（引入 **skills/liuyao 分层断卦体系**，明确不混 liuyao-divination 体用派）：分层步骤 = 定用神 → 标月令旺衰与日辰权力 → 空墓散脱/真假空亡 → 演本卦动爻 → 实际变爻 → 日辰裁决 → 取象应期；输入增强：尽量带入 yaosDetail 富字段（空破/暗动/回头生克/化空/伏神/卦身等）与占问问题/档案摘要（配合接口补 question/case 上下文）；输出仍趋势化、≤400 字、末行免责保留；规则素材从 skill references 01–04/12 择要提炼为 prompt 内规则段（不整篇搬录） | P1 | 🟠 | 目标文件 `backend/prompts/interpret/divination.md`，更新后走 REQ-048 后台热改 + 版本回滚 + 回归验证；素材与增强要点见 `docs/六爻文案补齐与skill素材对照.md` §5；关联 REQ-048/069（question 上下文） |
| REQ-075 | 六爻结果「焦点详解」动态 LLM（新按钮/需求点）：结果页对 动爻 / 世应 / 空亡·月破·日破 / 暗动 / 回头生克 / 化空化墓 等富字段提供**逐项点击详解**——LLM 结合该盘 yaosDetail 与 64 卦爻辞原文（S08）做针对性解读（非静态能覆盖，故用动态 LLM）；小额定积分即时扣费（机制同 interpret、缓存复用），无余额引导充值说明 | P2 | 🟠 | 静态词条（S05）已给术语「是什么」，本条给「结合此盘意味着什么」；关联 REQ-073/055/010；前后端与 prompts（可并入 divination interpret 家族或独立 prompt）开发实现时定 |
| REQ-076 | 「太初先生」Agent 会话（banner「余额」与右上角菜单之间的图标按钮）：①界面与交互：(a) 图标进入**固定 session 对话页**；(b) 会话顶部下拉选择**默认档案**（仅本人档案，可随时切换）；(c) 未选档案时可闲聊，咨询档案相关内容先提示选档案。②功能与机制：(a) 可就所选档案内容详细咨询 + 日常闲聊开导；(b) 计费=每来回（用户一问 + 先生一回）按 LLM 实际 token 扣余额（ceil 积分、1 积分=1000 tokens，同 interpret、开启不预扣）；(c) System Prompt **用户不可更改**、**后台可配置**（纳入 REQ-048 热改 + 版本回滚）——角色「太初先生」玄学大师：看开世事、融禅修与道家、略通佛学、善开导；进入会话先按角色设定给开场白；核心=结合所选档案给指引；指引原则=鼓励、积极、引导用户看开。③上下文与记忆：现阶段会话内保守上下文窗口（闲聊标准）；**跨会话长期记忆**已细化并单列 REQ-077（先审方案后开发）。④开关：用户设置「太初先生 Agent」默认开，控制入口显隐 | P2 | 🟠 | 2026-09-07 确认：计费=按实际 token 扣；记忆=见 REQ-077（方案待审批）；与 REQ-050（运维 Agent）区分——本条为 C 端对话大师；提示词文件建议 backend/prompts/agent/master.md（入 REQ-048）；档案数据仅限本人档案（防越权）；关联 REQ-066/048/001/077。**开场白配置（2026-09-07）**：开场白话术细节与对应 Prompt 由开发实现时直接配置好（默认含角色自述开场 + 引导语数句可轮换，贴合太初先生人设）；开场白作为 system prompt 的可配置段（**用户不可改**），与主 prompt 一并纳入后台（REQ-048）供需求方手动调整 |
| REQ-077 | AI 伙伴长期记忆模块（服务「太初先生」跨会话记忆：用户偏好与过往关键事实）：**硬性约束**——服务器 2C1G；**禁止新增任何后台进程 / 独立服务 / 向量数据库 / 自托管向量服务**；完全舍弃向量化与 Embedding；存储为**单 SQLite 文件嵌入现有业务进程**，零外部依赖；**多用户隔离**（各用户记忆独立）；不引入重 Agent 框架。**技术选型**：事实存储 + BM25 全文检索（可基于 SQLite FTS5）；**事实抽取交给外部 LLM API 且异步执行，不得阻塞主对话接口**；召回记忆拼入提示词给模型。**参考项目**：`reference/agent-memory-main/`（README.zh-CN.md、docs/zh-CN/architecture.md、src/ 存储/检索/抽取设计）——**仅借鉴其 Python 侧存储与 BM25/抽取设计，其 go-server / daemon / MCP 独立服务形态一律不采纳**。**开发门禁**：编码前开发须先提交《记忆方案书》供需求方审批——含：表结构（事实表/全文索引）、异步抽取任务模型与失败重试、与现有 FastAPI 进程的资源估算（**服务器压力 / 并发读写 / SQLite 锁与写放大**）、多用户隔离键设计、召回注入 prompt 策略、数据增长与清理策略；**审批通过后才开发** | P1 | 🟠 | 2026-09-07 记忆约束与门禁由需求方给定；关联 REQ-076（太初先生会话）/REQ-066（可作独立开关或随 Agent）；方案书产出后挂本行备注并经审批置 🔵。**方案书已审批通过（2026-09-07）**：docs/记忆方案书.md 经需求方确认，进入开发 |
| REQ-080 | 「单页 + 真实子路径」路由落地（全站 URL 映射上生产）：①全站仍为**单页单 index.html**（不拆多 HTML），前端路由改为基于 **location.pathname + history API**（点导航 `pushState` 切换视图、无整页刷新），替换现有纯内存/哈希式切换；②产出并落地**「模块 → 子路径 URL」映射**（含：落地页 `/`、三模块 Hub、九法合一 `/jiufa`、生肖流年、临时起卦（六爻等各法）、星座、塔罗、雷诺曼、MBTI、档案管理 `/cases`、档案详情、太初先生会话、登录/注册等——映射表由开发先产出并附本条备注）；③服务端：SPA fallback 覆盖所有**非 API / 非静态 / 非 admin** 路径统一回 `index.html`（现机制已具备，审计边界与 404 兜底）；静态资源保持**根绝对路径** `/vendor/*`（沿用 BUG-009 修复约定，禁止相对路径回归）；④兼容与联动：既有分享深链 `/mbti/share/{token}` 与未来的分享帮填 / Agent 等深链统一走该路由体系且**不得破坏**；旧行为保持（访问根路径进落地页）；⑤验收目标：模块直达、刷新停留、前进后退、收藏与复制分享均生效，切换过程无整页刷新 | P1 | 🟡 | 2026-09-07 决策（需求方拍板：不拆多 HTML，采用单页 + 真实子路径）；前置产出「模块→URL」映射表并经确认后再铺开实现；关联 REQ-071（banner 下拉直达）/ 分享深链（REQ-047/070）/ BUG-009 教训 |
| REQ-078 | 数据层与埋点扩充（2026-09-07 审查结论，本轮横向功能底层支撑，各功能落地时按需建，不预建）：**A 新增字段**：`cases` 加 `phone`(String 可空)、`email`(String 可空)（REQ-065 手机号/邮箱，ensure_schema 幂等 ALTER）。**B 新增表**（均走 Base/OpsBase.metadata.create_all 幂等建表）：①`asset_slots`（REQ-059 素材热更：key 唯一 + url + 类型 module/method/mbti_type/card/agent）；②`case_share_links`（REQ-070 分享帮填：token 唯一 + case_id + 一次性失效）；③`pair_readings`（REQ-072 配对解析：两 case_id + relation_type + question + result_json + 扣费 ref）；④`agent_messages`（REQ-076 太初先生会话：user_id + case_id 可空 + role + content + 计费 tokens，或复用 conversations 加 type 区分，开发实现时定）；⑤`agent_memories` + FTS5 索引（REQ-077 长期记忆：user_id + fact + 时间，方案书审批后建）；⑥`user_settings`（REQ-066 功能设置开关：user_id 唯一 + 各开关布尔/枚举，或 system_configs 按 user 扩展，实现时定）。**C 新增埋点**（events 表 + EVENT_NAME_ZH 中文映射）：`zodiac_match`（REQ-057 恋爱关系）、`pair_analysis`（REQ-072 配对解析）、`agent_message`（REQ-076 Agent 会话）、`manual_input`（REQ-069 手动输入）、`case_share_fill`（REQ-070 分享帮填）、`asset_upload`（REQ-059 素材上传，admin）。**D 不重构现有表**：现 28 张表结构稳定，仅增量加字段/加表，不迁移既有数据 | P1 | 🟠 | 关联 REQ-059/065/066/069/070/072/076/077；各表/埋点随对应功能开发时落地并回填本条备注。**已落地（2026-09-08）**：A cases.phone/email ✓；B 6 表+FTS5 随功能建齐（asset_slots / case_share_links / pair_readings / agent_messages / agent_memories+FTS5 / user_settings）✓；C 6 埋点全部发射 + EVENT_NAME_ZH 中文名齐（zodiac_match / manual_input / asset_upload 本轮补齐，pair_analysis / agent_message / case_share_fill 随功能已埋）✓ |
| REQ-079 | 塔罗 / 雷诺曼「占问问题」由必填改为可选：因已新增手动输入占卜内容（REQ-069），塔罗牌、雷诺曼的问题文本框改为**可选填**（可留空直接进入选牌阵 / 抽牌），留空不阻断流程 | P2 | 🟠 | 2026-09-07 用户补充；关联 REQ-042/043/069；前端去除必填校验与必填标记，留空时结果页问题区优雅降级（不显示或显示「未填写」） |
| REQ-081 | 观音灵签交互与结果界面修正（2026-09-07 用户反馈）：①**摇签热区**：摇签/抽签阶段**整个画面可点击触发**摇签（当前仅签筒可点——扩大热区至全屏画面任意处，点击即有反应）；②**独立结果界面**：恢复观音灵签单独的**结果展示界面**——摇签抽签后跳转独立页展示「第 N 签」签号与签文（含后续免费/付费解读），**不再把卦象结果直接内联在「临时起卦」页面最下方** | P2 | 🟡 | 关联 REQ-044（临时起卦 v2.1）/ REQ-022 / REQ-061（签筒动效：美术素材仍走其开发门禁；本交互热区与结果页不依赖美术可先行）；编号 078~080 已被并发条目占用，本条顺延为 081 |
| REQ-082 | 各模块「配对解析」与关系入口布局/样式调整（2026-09-07 用户反馈）：①国学预测「配对解析」（REQ-072）：按钮样式改为与「九法合一 / 生肖流年 / 临时起卦」等该模块功能入口**同款样式**，位置放该页面**最下方**；②西式占卜「配对解析」保持现状不变；③西式占卜「恋爱关系」（REQ-057）：(i) 位置移到「星盘·太阳/月亮/上升整体趋势」界面下方、「本命深度解读」上方；(ii) 显示规则——**未选择档案时不显示**该功能；(iii) UI 改**普通按钮**，文字「恋爱关系」；④MBTI「配对解析」按钮放「档案操作」与「选择档案」**之间** | P2 | 🟡 | 关联 REQ-072/057/056/045/042（入口样式对齐）/069；三按钮同排扩展见 REQ-083 |
| REQ-083 | 星座「恋爱关系」扩展为同排三关系（位置与显示规则同 REQ-082③）：同一排三个**普通按钮**——「看恋爱关系 / 看朋友关系 / 看上下级关系」；恋爱保持既有匹配（当前档案太阳星座 × 其余 11 星座：固定匹配度分值 + 静态说明，12×11）；**新增 朋友关系、上下级关系**采用同一机制（分值 + 静态说明 12×11，正文同恋爱/文案轮次策略后补——2026-09-07 需求方确认：同恋爱机制、正文后补）；未选档案三个按钮均不显示；文案两套（朋友/上下级）按恋爱口径跟踪补全 | P2 | 🟡 | 关联 REQ-057/082/045/037（文案轮次）；正文本补全为跟踪项，不阻塞三按钮入口布局先上 |
| REQ-084 | 三大模块主页 Q 版循环小动画（「Flash」仅为代称，实现形态由开发决定：CSS / Canvas / Lottie / 帧动画等）：①国学预测 / 西式占卜 / MBTI 三个模块落地宣传界面各加一段**循环播放**小型动画——国学：Q 版人物**打坐**，循环有小鸟 / 蝴蝶飞到身上；西式：Q 版人物**仰望星空**，周围划过神秘学元素（连线星座、卡牌等）；MBTI：Q 版人物**躺摇椅上摇晃**；②交互：点击对应动画图标**进入「太初先生」会话界面**（REQ-076）；③素材与实现约定：人物形象由需求方提供参考给美术；**素材接口规格（尺寸 / 格式 / 命名 / 帧规约等）由开发先行提供**；具体实现方案由开发细化；**开发门禁同 REQ-061**（先交动画实现方案 + 素材表经确认后开发）；④开关：跟随功能设置「动画与抽卡模拟」开关①（关闭则不播放 / 隐藏）；⑤装饰动画不得遮挡操作与主按钮，移动端低耗降级 | P2 | 🟡 | 关联 REQ-076 / REQ-066（开关①）/ REQ-061（门禁与素材规范）；三模块动画按美术排期分批接入，预留素材替换点 |
| REQ-087 | 余额展示迁入右上角菜单（2026-09-07）：①banner 不再直接显示余额，改为**菜单内部最顶部**展示当前余额 ¥（仅登录，点开菜单即见）；②余额项**可点击进入余额消耗明细**（对齐 REQ-063 明细口径：项目中文标签 + 消耗档案名 + 余额单位）；③取代 REQ-051 的 Banner 展示方案与 REQ-071 等 banner 布局约束调整 | P2 | 🟡 | 关联 REQ-085/063/051（取代其 Banner 展示口径）；菜单含余额顶栏、右上角组件结构调整 |
| REQ-088 | 「九法合一」下方操作说明调整（2026-09-07）：①说明框名称由「九法合一操作」改为「**开始九法合一**」；②在说明框现有内容**最下方新增一行高亮红字提示**，文案（按需求方原句）：「表示九法合一，开始预测会消耗大量余额，因为会同时跑9个方法。」（若措辞需再校对归 REQ-037 文案轮次；「余额」口径对齐 REQ-085） | P3 | 🟡 | 关联 REQ-068（九法合一入口/开始预测）/ REQ-085（余额口径）/ REQ-086（消耗角标语义一致）；纯前端文案与样式小改 |
| REQ-089 | 档案详情展示与「修改信息」v2（2026-09-07）：**1.档案详情界面**：①点进详情后，在**第一个色块下方新增「修改信息」按钮**；②修改范围现阶段=**手机号 / 邮箱 / 档案名**（不做出生信息等修改）；③建档信息展示区**补充显示手机号与邮箱**（有则显示，无则留空位或省略）。**2.占星盘板块**：①本命盘信息**仅显示盘面（星盘图）**，不显示行星落座落宫详情、四轴及文字说明；②板块**不保留历史**——只取/展示最新一条（排盘幂等已确保单条，如存在多余记录以最新为准并可清理）。**3.MBTI 人格板块**：①档案内**仅显示最新一次判型结果 + 思维倾向（四维倾向）**，不显示优势盲点 / 职场关系 / 成长建议等长文案；②**历史判型记录保留**（可回看，删除走 REQ-054 逐条删除） | P2 | 🟡 | 取代/细化 REQ-039 中「档案内 MBTI 显示评语」细化口径（2026-09-07 最新指示为准）与档案占星盘展示详略；关联 REQ-065（手机号/邮箱与修改）/ 053 / 040 / 054；「思维倾向」指四维倾向条；备注注明替代口径，防与旧验收冲突 |
| REQ-086 | 全站「消耗余额」角标（2026-09-07）：所有会消耗余额的按钮（深度断卦 / 各模块深度解读 / 追问 / 配对解析 / 太初先生发送 / 焦点详解等）统一增加小角标，文案 **「¥ 消耗」**；补充 hover / 详情提示「按实际用量从余额扣除」；余额为 0 或不足时角标转警示色、点击引导（充值入口仍隐藏时提示余额不足）；角标为全局统一组件，样式与各按钮现有风格兼容 | P2 | 🟡 | 2026-09-07 用户选定角标文案「¥ 消耗」；关联 REQ-085/063/072/075/076；文案轮次 REQ-037 只校通用语，不覆盖本组件语义 |

## F. 后台与运维

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-048 | 后台 Prompt 统一管理台（生产热修改）：①覆盖 `backend/prompts/` 全部文件类 prompt——method-prompts（9 法）/ shared（validation·revise 等）/ interpret（tarot·divination·lenormand·astrology 等），后台统一列表 + 查看 + 编辑保存（含 admin 页面）；②热生效：保存后新请求立即生效（沿用按次读文件机制，需验证各读取点无启动缓存），不重启不改代码；进行中 job 默认继续用保存前 prompt；③版本与回滚：每次保存记版本快照（内容 + 时间 + 操作管理员），支持回滚任意历史版本；④保存前校验：非空、模板占位符完整性、长度上限，防改坏线上；PUT 沿袭审计 | P1 | 🟢 | 基于现有 GET/PUT `/admin/prompts`（现仅 method-prompts）扩展（backend/app/admin/router.py）；代码内嵌提示词（合成/合规等）本轮不纳入；2026-09-07 确认：范围=全部文件类、要版本历史+回滚；已实现（后端 88c3669 + admin 前端 ba8e1ad，15 文件类 prompt + 版本快照 + 回滚 + 热生效核实无启动缓存）；测试组 8 自动化用例（test_admin_prompts.py）全绿 + 2026-09-07 真机 UI 查看复验通过 → 🟢 |
| REQ-049 | 登录/注册错误提示细化：①登录失败需区分展示「该账户不存在」或「密码不正确」（当前根因：前端 api() 对任意 401 统一提示「登录已过期，请重新登录」，吞掉后端真实 message「用户名或密码错误」——修复：登录分支不套用通用 401 拦截，改用后端 message）；②注册时必填项未填（用户名/密码/确认密码/验证码）需逐字段提示（当前仅空 body 走 FastAPI 422 detail，前端无友好提示；已在注册前端补校验文案） | P1 | 🟢 | 2026-09-07 真机走查反馈登记；api() skip401 + 登录/注册逐字段提示已落地（1f1a8cd）。（2026-09-07 二轮复验通过：登录失败显示后端真实错误、注册逐字段提示 → 🟢）；关联 REQ-001 |
| REQ-050 | 后台运维接入 OpenClaw Agent（远程 Agent 管理运维后台）：①形态：关键运维动作封装为 Agent 可调用工具/MCP，Agent 经既有 OpenClaw 接入点（bb3a.taichu.xyz 通道）直接调后台 API，用户不在电脑旁时以自然语言完成运维；②范围（2026-09-07 确认）：查看类（报表/用户/积分查询）+ 赠积分 + 重置用户密码/重置 case + 后台错误报告与服务健康查看（prompt 热修改本轮不纳入，后续如需接 REQ-048 能力）；③鉴权与安全：Agent 专用 API token（独立于 admin 账号密码）；写操作沿袭 admin_audit_logs 审计；高风险动作（赠积分/重置密码/重置 case）默认需用户确认后才执行；④既有 bb3a 配对 token 保护与回程通道保持不变 | P1 | 🟢 | 关联 REQ-048（prompt 工具化后续可选）；已实现（3062bf2）：Agent 专用 token + /admin/agent/* 7 端点 + 审计/高风险标记，复用 OpenClaw bb3a 通道。测试组补 8 自动化用例（test_admin_agent.py）全绿（鉴权隔离/查看/高风险写+审计）→ 验收通过；⚠️ 线上启用需部署侧配置 `TAICHU_AGENT_TOKEN`（未配置则通道默认禁用 401） |
| REQ-059 | 前台素材热更（后台运维板块，前台 + 后台双侧）：运维后台新增「素材管理」板块（背景图 + 卡面），上传即热更改前台展示：①模块级 3 槽背景——国学预测 / 西式占卜 / MBTI 落地页背景图（落地页与对应 Hub 页复用同一张）；②Hub 方法级 N 槽背景——国学 Hub 与西式 Hub 中每个方法选项独立背景图（按选项 key，取现有 Hub 选项注册表）；③生效与回退：替换式——上传即替换，未配置 / 删除自动回退现有 CSS 艺术背景，可预览、可删除恢复默认；前台经公开配置接口按 module/key 取 URL（热生效无需部署）；④**MBTI 16 型结果背景**（2026-09-07 补充）：额外 16 槽按 MBTI 类型（INTJ…ESFP）各 1 张，仅在 MBTI 测试结果页 / MBTI 人格详情界面展示，后端按 `mbti_type` 判定返回对应背景 URL，未上传回退默认；⑤**卡牌素材管理**（2026-09-07 补充）：塔罗 78 张 + 雷诺曼 36 张全部卡面可在后台统一**查看与更换**（按卡 key：tarot 牌名 / lenormand 01–36），上传替换后前台抽牌/翻牌/结果即时生效；⑥**太初先生会话页背景槽**（2026-09-07 补充）：额外 1 槽（key=agent，banner「太初先生」会话页背景），上传/删除/回退逻辑同③；受功能设置「背景图显示」开关（REQ-066⑤）统一控制——关闭时本背景同样不显示、回纯色默认 | P2 | 🟠 | 前台现状为 CSS 渐变艺术背景（.bg-guoxue / .bg-xishi / .bg-mbti）；admin 扩展对齐 REQ-048；上传格式/体积/裁切约束开发按区域与卡面比例定；2026-09-07 确认：替换式 + 默认回退、3 模块槽 + N 方法槽 + MBTI 16 型槽（后端判定）+ 塔罗/雷诺曼卡面可换 + 太初先生会话背景槽（随开关⑤） |

## D. 待办 / 未来（Phase 2 后，明确不做或缓做）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-030 | 合婚 / 合盘 | P3 | ⚪ | Phase2 后评估 |
| REQ-031 | 星座三限/月返/日互 | P3 | ⚪ | 盘型 UI 置灰「敬请期待」 |
| REQ-032 | MBTI × 命盘跨读 LLM | P3 | ⚪ | 预留付费点，未实现 |
| REQ-033 | SQLite → PostgreSQL 切换 | P3 | 🟡 | 多实例水平扩展前必须切换 |
| REQ-034 | 金数据商户认证 + 支付全链路验证 | P1 | 🟡 | 用户自行验证 |
| REQ-035 | 购买积分与充值流程：右上角菜单「购买积分」入口 + 支付界面页 + 0.1 元兑 10 积分测试档——**暂不开放**，商户认证通过并真实验证后放开 | P2 | 🟡 | 2026-09-07 用户确认付费逻辑暂时隐藏：前端入口全部保持隐藏（原 4 处 + 菜单「购买积分」）、支付界面不对外；仅后端 charge-code / 轮询保留、代码就绪 |
| REQ-036 | 后台 Phase2 专用端点（各模块 readings 查看） | P3 | 🟡 | 已留端口约定，未实现 |

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-07 | 建立需求表，登记 A/B/C/D 四模块共 24 项需求（含已完成/进行中/待办/归档） |
| 2026-09-07 | 状态机升级为 🟡→🔵→🟠 待验收→🟢 已验收（🟢 仅测试组可设）；使用说明新增「查重合并」规则 |
| 2026-09-07 | REQ-027 置 🟠 待验收（前端提交 9538d33/87588a8），触发测试组验收；REQ-028 前端已提交（6f73acd）后端未提交，保持 🟡 |
| 2026-09-07 | 使用说明新增「可执行性保证」：新登记需求须含可验收口径，含糊/缺口径先向需求方确认再登记 |
| 2026-09-07 | 新增 E「体验与交互优化」REQ-037~044（用户走查批次，均 🟡）；REQ-027 退回 🔵（BUG-001 待修）；REQ-035 并入「购买积分」入口 / 支付页 / 0.1 元测试档细化 |
| 2026-09-07 | 新增 REQ-045（星座日月升三格 + 布局，男女各一套 72 篇先占位）、REQ-046（新建档案入口与按钮路径）、REQ-047（MBTI 免登录分享、结果存历史可回看），均 🟡 |
| 2026-09-07 | REQ-035 收紧：付费（购买积分/充值）逻辑暂不开放，前端入口全部隐藏、仅备代码（用户确认） |
| 2026-09-07 | 新增 F「后台与运维」REQ-048（后台 Prompt 统一管理台：全部文件类 prompt + 热修改 + 版本回滚，🟡） |
| 2026-09-07 | 首批真机复验（测试组）：REQ-028/039/047 验收不通过退回 🔵（附细化口径：档案星盘合并展示、MBTI 档案内容、分享入口时机）；新增 REQ-049（登录/注册错误提示细化，用户走查登记）；REQ-048 补 8 自动化用例待 admin UI 真机 |
| 2026-09-07 | 新增 REQ-050（OpenClaw Agent 远程运维：工具/MCP 直调后台 API，范围=查看/赠积分/重置/错误健康，专用 token + 高风险需确认，🟡） |
| 2026-09-07 | REQ-046 ③ CasesPage 口径定稿：档案管理页（已有档案）卡片按钮 4→3：查看档案 / 重命名 / 删除档案 |
| 2026-09-07 | 新增 REQ-051（积分余额显示：档案移除→Banner 菜单左、积分折 ¥ 动态汇率仅登录）、REQ-052（生肖档案下拉、去手动入口）、REQ-053（档案详情字段合并 + 占星盘合并评估）、REQ-054（历史记录逐条删除 + 二次确认），均 🟡 |
| 2026-09-07 | REQ-042 ④ 改口退回 🔵（塔罗落地页去扇形，预测方向置于「塔罗牌」副标题下、无美术渲染；雷诺曼 REQ-043 同步）；新增 REQ-055（起卦结果页重构 + 免费静态解读 + 梅花/小六壬时间选项，🟡） |
| 2026-09-07 | REQ-053 ④ 占星盘合并评估完成（代码比对）：可合并分层 L1 前端共享渲染单元 / L2 档案内双份占星收敛 / L3 数据层统一暂缓；结论与风险已写入 REQ-053 备注 |
| 2026-09-07 | 塔罗/雷诺曼改版修订（并入 REQ-042/043，均 🔵 返工）：删「预测方向」步骤；牌阵改单列按钮+适用说明；抽卡 UI 拆分——上方仅卡图/标题/标签，含义段落集中下方 |
| 2026-09-07 | 新增 REQ-056（MBTI 入口与结果页交互定稿 v2：档案列表显类型/未测试、勾选后四操作、已测直达结果+重新测试、分享仅选档案后且更新当前结果+留历史）；REQ-047 写入与入口口径同步修订 |
| 2026-09-07 | 新增 REQ-057（星座「恋爱关系」太阳星座匹配板块，分值+静态文字）与 REQ-058（九法合一等待页：回到首页入口 + 等待提示语），均 🟡 |
| 2026-09-07 | REQ-052 生肖流年口径修正（0049ae3）：年份切换由独立年份卡改为结果标题「X · Y 年运程」左右箭头；REQ-052 置 🟠 待复验 |
| 2026-09-07 | 新增 REQ-059（后台「背景图管理」板块：模块级 3 槽 + Hub 方法级 N 槽，上传热更改前台背景，替换式+默认回退，🟡） |
| 2026-09-07 | 新增 REQ-060（全局页脚 Sticky 贴底：免责声明/用户协议/隐私政策不足一屏时贴视口底，🟡） |
| 2026-09-07 | REQ-044 改版 v2（🔵 返工）：起卦列表单列（方法名+简易介绍、无标签），问题框移入方案弹窗（必填+开始起卦），梅花/小六壬时间选项入弹窗 |
| 2026-09-07 | 第二轮真机复验（用户确认全通过）：REQ-027/028/038/039/040/041/042/043/045/046/047/048/049/050/051/052/053/054/055/056 → 🟢（其中 051/053/054/055/056 等 dev 未及回写 🟠 的由测试组按复验闭环）；REQ-044 保持 🔵（v2 弹窗/单列列表待开发实现，v1 口径已验） |
| 2026-09-07 | REQ-059 扩充：④MBTI 16 型结果背景（16 槽按 mbti_type 判定、仅结果/详情页展示）；⑤塔罗 78 + 雷诺曼 36 卡面后台查看与更换 |
| 2026-09-07 | 新增 REQ-061（占卜动效与翻牌交互：观音灵签签筒 / 六爻·梅花·小六壬起卦动画 / 塔罗·雷诺曼抽牌动画 + 牌背 + 翻牌 + 一键全翻，🟡） |
| 2026-09-07 | REQ-061 范围收敛：开发只做前端交互逻辑 + 交互方案 + 代码；美术素材外部生产后接入（占位/程序化动效先行，素材替换点预留，卡面接 REQ-059⑤） |
| 2026-09-07 | 新增 REQ-062（落地页宣传大色块白底 70% 半透明、文字不淡，🟡）与 REQ-063（余额详情/流水：积分记载统一为余额 + 消费明细注明项目中文标签与消耗档案名，历史同规则映射，🟡） |
| 2026-09-07 | 新增 REQ-064（首页第 4 按钮「档案管理」仅登录显示）、REQ-065（档案管理列表 CRM 行式化 + 手机号/邮箱字段 + 行尾四键，取代 REQ-046③ 口径）、REQ-066（用户设置页，开关清单待用户提供），均 🟡 |
| 2026-09-07 | 新增 REQ-067（档案与九法合一解耦 + 统一建档组件：档案名选填、开始排盘=仅非 LLM 盘面、新增「新建下一个档案」，🟡） |
| 2026-09-07 | 新增 REQ-068（九法合一入口改「选择档案」UI：选中后显示 查看/修改结果 + 跑九法合一开始预测，🟡） |
| 2026-09-07 | 新增 REQ-069（六占卜方式「快速手动输入实际结果」模式：逐爻点选成卦 / 观音灵签选签 / 小六壬三数或掌诀 / 塔罗雷诺曼按牌阵选牌定正逆，🟡） |
| 2026-09-07 | 新增 REQ-070（「分享帮填」：一次性链接、他人免登录代建档案归分享者、姓名必填、访客仅可提交不可用其他功能，🟡） |
| 2026-09-07 | 新增 REQ-071（Banner 正中模块下拉导航：三大模块分组列功能标题、点击直达功能页，🟡） |
| 2026-09-07 | REQ-066 开关清单定稿（更名「功能设置」）：动画/抽卡模拟开、占卜默认模式 (b)、Banner 导航关、分享表单太初 UI 开关——含系统适配口径 |
| 2026-09-07 | 新增 REQ-072（三大模块 LLM 配对解析：双档案×关系类型/自由补充×模块数据，Prompt 要点已注、纳入 REQ-048 后台热改，🟡） |
| 2026-09-07 | 六爻文案补齐立项：成稿文档 `docs/六爻文案补齐与skill素材对照.md`；新增 REQ-073（静态补齐 64 卦大意/六亲/六神/世应/术语/通用话术）、REQ-074（深度断卦 prompt 引入 liuyao 分层体系）、REQ-075（六爻焦点详解动态 LLM 按钮），均 🟡 |
| 2026-09-07 | 六爻文案呈现指引（文档 §7 必须项/自由项/兜底约定）写入 REQ-073；REQ-066 新增 开关⑤ 背景图显示（默认开、关=纯色默认样式）、开关⑥ 牌面图片显示（默认开、关=纯文字列表模式） |
| 2026-09-07 | 新增 REQ-076（「太初先生」Agent 会话：banner 图标进入、选默认档案、按 token 计费、系统 prompt 后台可配置、记忆方案待补充）；REQ-066 新增开关⑦ Agent 默认开 |
| 2026-09-07 | REQ-076 开场白配置定：开发默认配好太初先生开场白（含可轮换引导语）、随主 prompt 入后台可手动调整；REQ-061 增加开发前置门禁（动画/交互先交方案 + 美术素材表，经确认后编码） |
| 2026-09-07 | REQ-059 增加 ⑥太初先生会话页背景槽（随 REQ-066⑤ 背景开关统一控制）；REQ-066⑤ 说明覆盖至含 Agent 会话背景 |
| 2026-09-07 | 新增 REQ-077（AI 伙伴长期记忆模块：单 SQLite + BM25 + 异步 LLM 事实抽取，禁独立进程/向量；参考 reference/agent-memory-main 仅借鉴 Python 侧设计；先审《记忆方案书》后开发）；REQ-076 记忆指针改至 REQ-077 |
| 2026-09-07 | 新增 REQ-078（「单页 + 真实子路径」路由落地上生产：history 路由 + 全站模块→URL 映射 + SPA fallback 审计；先产出映射表经确认后实施，🟡） |
| 2026-09-07 | REQ-044 v2.1 修订（问题必填改**可选**，可空直接起卦，退回 🔵）；REQ-070 验收退回 🔵（新建档案界面未见分享帮填入口，BUG-012）；登记 BUG-012；新增 REQ-081（观音灵签整屏摇签热区 + 恢复独立结果界面，🟡） |
| 2026-09-07 | 登记 BUG-013（功能设置全部开关不可点击，需前后端按 REQ-066 开关逐一生效）；新增 REQ-082（配对解析/关系入口布局样式调整：国学最下同款样式、西式恋爱挪位+未选档案隐藏+普通按钮、MBTI 配对解析居中）与 REQ-083（星座同排三关系：恋爱/朋友/上下级，两新关系同恋爱机制正文后补），均 🟡 |
| 2026-09-07 | REQ-064 v2：首页 nav-rail 回 3 键（撤销第 4 键），档案管理入口迁移至三模块落地页主按钮下透明小按钮（仅登录）；新增 REQ-084（三模块 Q 版循环动画：打坐 / 观星 / 摇椅，点击进太初先生；随开关①；门禁先方案），🟡 |
| 2026-09-07 | 计费体系切换：新增 REQ-085（积分概念全站废除、余额-Token 绑定 1 元=1 万 tokens、注册赠送 22 元、后台赠积分改余额充值/调整）、REQ-086（全站消耗按钮角标「¥ 消耗」）、REQ-087（余额移入右上角菜单顶部 + 点击进明细，取代 REQ-051 Banner 展示），均 🟡 |
| 2026-09-07 | 新增 REQ-088（九法合一说明调整：改名「开始九法合一」+ 底部红字余额消耗提示），🟡 |
| 2026-09-07 | 新增 REQ-089（档案详情 v2：「修改信息」按钮（手机号/邮箱/档案名）+ 建档信息补显两字段；占星盘板块只显盘面图、不保留历史；MBTI 板块只显最新结果+思维倾向、历史保留），🟡 |
