# 太初 · 需求表（Requirement Tracker）

> 本表是项目需求的**单一事实源**：新需求在此登记、分配 ID、追踪状态；开发按表推进，完成后更新状态。
> 每次会话涉及的新需求 / 状态变更，收尾前同步回本表。

## 使用说明

1. **登记**：新需求在对应模块新增一行，分配 `REQ-<三位序号>`（全局递增）。
2. **查重合并**：登记前先与全表比对——与已有条目重复 / 高度相似者**不新增行**，并入已有条目（保留原 ID，合并补充描述，备注注明「并入于 <日期>：<新增点>」）；部分重叠者拆并入最贴近条目或在备注交叉引用，避免同一需求多行、状态分叉。
3. **状态**：🟡 待办 · 🔵 进行中 · 🟠 待验收（开发完成等测试）· 🟢 已验收 · ⚪ 已归档（明确不做）。
4. **优先级**：P0 阻塞 / P1 高 / P2 中 / P3 低。
5. **粒度**：一条需求对应一个可验收的用户可感知功能或明确的工程改造，不混多件事。
6. 状态流转：🟡 → 🔵 → 🟠 → 🟢（或 ⚪）。🟢 只能由测试组验收通过后设置；开发完成置 🟠 并在备注写「待验收」。
7. **可执行性保证**：本表发出的需求必须做到「开发可直接照做」——含明确动作主体与范围 + 可验收口径（期望行为 / 通过标准）；描述含糊（如「优化体验」「尽快」）、缺验收口径或多义时，**先向需求方确认补齐后再登记**，不得凭推测落表。

---

## A. 国学主线（Phase 1–7，已上线）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-001 | 用户系统：手机号注册/登录、JWT、图形验证码、设备指纹、IP 限流、失败锁定 | P0 | 🟢 | 注册强制（ADR-0007） |
| REQ-002 | 排盘引擎：9 法确定性排盘（八字×4/紫微/西占/七政/奇门/五运六气） | P0 | 🟢 | 自研为主，Node 常驻服务 + subprocess 降级 |
| REQ-003 | LLM 方法模块：9 法分析 + 校验（仅断前尘） | P0 | 🟢 | DeepSeek，method-result v2 契约 |
| REQ-004 | 异步编排：断前尘/预测 jobId + 前端轮询，并行 3 并发 + 全局信号量 | P0 | 🟢 | 逐法扣费 + 断点续跑 |
| REQ-005 | 前端：建档→等待→校准→预测→追问→档案 全流程 | P0 | 🟢 | 免构建预编译 JS |
| REQ-006 | 合规护栏：双层过滤 + 免责声明 + 全 prompt 合规边界 | P0 | 🟢 | 8 标题固定产出 |

## B. 积分与支付（P1/P2）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-010 | 积分系统：账户/流水/扣费（1 积分=1000 tokens）/充值码/注册赠送 220 | P0 | 🟢 | 逐法扣费 + interpret 即时扣费 |
| REQ-011 | 金数据支付：表单 + API 轮询 + 幂等入账 | P1 | 🟢 | 商户认证待用户自行验证 |
| REQ-012 | 后台：埋点报表、用户管理、积分查看、赠积分 | P1 | 🟢 | `EVENT_NAME_ZH` 中文映射 |
| REQ-051 | 积分余额显示改版：①档案相关界面不再显示积分数字，改为全局显示在 Banner 右上角菜单左侧（仅登录）；②显示形式由「积分」改为「余额 ¥」= 积分÷汇率（1 元=10 积分，动态读 system_configs），**保留积分底层计费逻辑不变**，仅改前端展示 | P2 | 🟡 | 2026-09-07 用户确认口径（余额 ¥、动态汇率、仅登录）；关联 REQ-010/012；首轮走查已提出但当时漏登记，本次补齐 |

## C. 横向扩展（Phase A–D）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-020 | 三大入口 + 左侧 nav-rail + 模块主题背景 | P1 | 🟢 | 国学/西式/MBTI 皮肤 |
| REQ-021 | 生肖流年（确定性零 LLM，免费引流） | P1 | 🟢 | Node `/zodiac` |
| REQ-022 | 临时起卦：六爻/梅花/小六壬/观音灵签 + 断卦付费 | P1 | 🟢 | Node `/divination`，大六壬/金口诀/奇门置灰 |
| REQ-023 | 塔罗：抽牌 + 牌阵 + 综合解读付费 | P1 | 🟢 | Node `/tarot`，卡面美术已就位 |
| REQ-024 | 雷诺曼：抽牌 + 关键词组合 + 深度解读付费 | P1 | 🟢 | 复用 `/divination`（lenormand） |
| REQ-025 | 星座：本命盘 + 深度解读付费 | P1 | 🟢 | Node `/astrology` + 法达查表，盘型 tab 仅本命 |
| REQ-026 | MBTI：60 题标准版 + 判型 + 16 型文案 | P1 | 🟢 | 纯代码零 LLM，前端页面已完成；A4 文案多源优化已接入（c7dfd84） |
| REQ-027 | 导航交互重构：三大模块落地宣传页 + 左侧按钮仅落地页 + Banner 右上角菜单 | P1 | 🔵 | 落地页→HUB 两级；未登录「登录/注册」、已登录汉堡菜单；退回开发（BUG-001 小屏布局遮挡，修复后重新 🟠；前端已提交 9538d33/87588a8） |
| REQ-028 | 统一档案体系：星座/国学/MBTI 共用同一 case 档案；case 加 mbti_type；星座改名；菜单加「档案管理」 | P1 | 🔵 | 星座复用 case 生辰（去独立表单）、MBTI 关联 case、汉堡菜单加档案管理；前端 6f73acd + 后端 2abd7f9 均已提交。（2026-09-07 真机复验退回 🔵：档案内占星盘仅以一条记录存在、未与完整盘面「占星盘」子菜单合并展示；档案内点查看星盘会跳回星座界面要求重新生成而非直接展示已存盘——详见 REQ-039 细化，两需求联动修复） |

## E. 体验与交互优化（2026-09-07 用户走查批次）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-037 | 全站 UI 文案体系优化美化：贴合功能实际、具吸引力，调性高贵/奢华/内涵；全程「趋势参考、不作建议」合规话术（REQ-006 红线内建） | P2 | 🔵 | B1 文案已接入（落地页/品牌/合规/Hub，18bef25）；buttons/tips/error 常量已定义，全站按钮文案替换待续 |
| REQ-038 | 国学预测与西式占卜两个 Hub：方法改竖向单列排列（原横向两列），选项内容展示更充分、页面不拥挤 | P2 | 🟠 | 关联 REQ-020/027；Hub 单列已落地（12e14b7） |
| REQ-039 | 档案绑定与展示统一：①星座星盘按档案幂等——同一档案只生成一次并绑定，再入直接复用不重复生成；②档案详情新增「占星盘」子类展示已生成星盘；③MBTI 测试结果持久化绑定 case，再入档案不复测、档案内可见结果 | P1 | 🔵 | 关联 REQ-025/026/028；星座幂等 db03950 + build_archive 51c3daf + 档案展示 b9eddb5；「选已有档案」档案项 UI 明显化另见 BUG-006（UI 类）。（2026-09-07 真机复验退回 🔵，细化如下：①档案内「占星盘」只是记录条目，未与完整盘面「占星盘」子菜单合并成完整星盘展示 ②档案内点查看星盘跳回星座页要求重新生成——生成过一次后应直接从档案展示 ③档案内 MBTI 仅显示类型，缺四维倾向表单与 16 型评语（答题与手动选型均有此问题）④已选型档案再入 MBTI 主菜单应直达结果页并在底部提供「重新测试」按钮，而非直接进入重测） |
| REQ-040 | 档案详情页改版：顶部标题显示当前档案的用户命名（非固定「我的命理档案」）；页尾按钮重设计：删除「退出登录」，保留「回到档案列表/回到首页」，新增「国学预测/西式占卜/MBTI」三直达按钮——携带当前档案进入对应模块主功能（非落地页），该档案对应模块无数据时页内引导生成、不自动生成 | P2 | 🟠 | 关联 REQ-027/028；档案名标题+3直达+删退出登录已落地（12e14b7） |
| REQ-041 | 生肖流年模块图标重新设计（统一整体产品风格） | P3 | 🟠 | 生肖 medallion 图标已接入（b61f4e4 + sprite c16a3c8）；关联 REQ-021 |
| REQ-042 | 塔罗模块交互改版 v2（2026-09-07 用户修订）：①流程=「占问问题文本框」+「选择牌阵」，**删除「预测方向」步骤**；②牌阵选择改**单列按钮列表**（非双列），每项标题=牌阵名 + 说明文字（该牌阵适用何种占卜，静态文案）；③抽卡结果 UI 重做——**上方**只平铺本次抽到的全部卡（卡图/标题/标签），卡面不嵌含义；**下方**以段落文字自第一张起逐张输出：标题 + 标签 + 正位/逆位 + 对应含义（固定可复用文本、不依赖 LLM），集中便于查看；④抽卡页顶部承接所填占问问题文本；⑤落地页顶部不做扇形美术（预测方向已删，直入占问问题/选牌阵），公版署名移至页脚小字；⑥底部「重新抽卡/重选牌阵/返回」全宽并列大按钮；⑦80 张牌图本地压缩 WebP + 分级缩略图提速（保留相对路径、懒加载维持、不引外部 CDN） | P1 | 🔵 | 关联 REQ-023；65c4404 已落地（牌义文本/全宽按钮/署名）与 v2 差异返工：删预测方向、牌阵单列+说明、抽卡图/文拆分上下布局；雷诺曼 REQ-043 同步 v2；⑦图片 WebP 提速延后 |
| REQ-043 | 雷诺曼模块改版（对齐塔罗 v2，2026-09-07 修订）：①**删除「预测方向」步骤**；②三个牌阵（单张/3 张/9 宫格）改**单列排版**，每个标题下加说明文字（适用场景）；③抽卡后卡位不放解释——解释全部归拢到**下方段落**（自首张起：标题 + 标签 + 正/逆位 + 含义，固定可复用文本）；④底部按钮对齐塔罗（全宽并列） | P2 | 🔵 | 关联 REQ-024；65c4404 已落地部分按 v2 返工（删方向、牌阵单列+说明、解释下移归拢） |
| REQ-044 | 临时起卦交互改版：先选卦法（六爻/梅花/小六壬/观音灵签）→ 输入问题文本框 → 点击开始起卦 → 展示卦象；底部「重新起卦/返回」改全宽并列大按钮 | P1 | 🟠 | 关联 REQ-022；问题框+全宽按钮已落地（4b5d554）；六爻完整卦象见 BUG-004 |
| REQ-045 | 星座页新增「太阳/月亮/上升」三格与布局调整：①星盘页新增三格，每格展示该档案对应星座整体趋势描述（感情/事业等）；②复用文案按男女各一套正文 = 12 太阳×2 + 12 月亮×2 + 12 上升×2 共 72 篇，**先以占位文本实现结构与布局，正文后续补全**；③星盘正中 ASC/MC 字样去除；④「本命深度解读」按钮上移至三格下方、行星落座/落宫等详细信息上方 | P1 | 🟠 | 关联 REQ-025；三格 + 72 篇正文 + 去 ASC/MC + 解读按钮上移已完成（6787563） |
| REQ-046 | 星座/MBTI「新建档案」入口与建档按钮路径区分：①MBTI 人格测试与西式占卜·星座页面补「新建档案」入口（进入建档流程）；②国学/西式/MBTI 从模块页进入「新建档案」→ 建档完成后直接进入该模块主流程并显示对应主按钮（开始排盘/生成星盘/开始答题），不显示其他模块按钮；③从右上角「档案管理」进入的档案页：提供「选择档案进行修改/查看」与「回首页」，不显示模块主流程按钮 | P1 | 🔵 | 关联 REQ-026/028/039/040；①②新建档案入口+returnTo 回跳已落地（4b5d554）；③CasesPage 精简待续——2026-09-07 口径定稿（用户）：档案管理界面**已有档案**时，每档案卡片下方按钮由 4 个改 3 个：**查看档案 / 重命名 / 删除档案**（查看档案=进入档案详情，解读/命盘等子类在详情内查看，对齐 REQ-039/040） |
| REQ-047 | MBTI 结果分享：①MBTI 结果页提供「分享」：生成绑定该档案的专属链接，他人打开**免登录**填写（本场景豁免 ADR-0007 强制注册：填写人仅答 MBTI，不建 case、不触其他模块）；②每次填写结果作为一条历史记录存入该档案，**不覆盖**既有结果，档案内可回看各次 MBTI 记录 | P2 | 🔵 | 关联 REQ-026/039；后端 37c339e + SPA fallback 9d68524 + 前端 6618d31 均已完成；免登录 + 存历史可回看 2026-09-07 需求方确认。（2026-09-07 真机复验退回 🔵：①分享按钮入口时机不符——应在 MBTI「选档案」后即可分享（供朋友代填，档案不必是本人作答），而非答完题/手动选型后才出链接 ②分享链接新窗口打开空白——已登记 BUG-009 待复现，本地 API/路由层复现正常） |
| REQ-052 | 生肖流年改为档案驱动：生肖页移除手动年份输入，改为档案选择（下拉列当前用户档案，未登录引导登录）；选中档案后自动将档案出生年份换算为对应生肖并出结果 | P2 | 🟡 | 用户确认「仅档案选择、去手动」；取舍提示：REQ-021 原为免登录引流模块，登录化后引流链路变化（已由用户拍板）；关联 REQ-021/028/046 |
| REQ-053 | 档案详情字段展示改版：①「出生年/月/日/时辰」4 字段合并为「出生年月日时辰」，其下补充「中式干支传统写法」与「农历生日及出生信息」两字段；②「经度」「纬度」合并为单一字段展示；③新增「生肖」字段；④占星盘内容合并（2026-09-07 比对结论已出，分层落地）：L1 前端抽取共享「盘面渲染单元」（pointRow / 行星落座四轴 / summary chip-逆行-格局，wheel 已共享 AstroWheel），AstrologyPage 与档案 AstroNatalPanel 共用；L2 档案页内 astroCard（natal）与完整盘面「占星盘」tab（charts.western）双份内容收敛——同档案只呈现一份完整占星展示（推荐以 natal 为准或 western 经 astroNorm 归一化后走 natal 套件）；L3 数据层唯一事实源（astrology 生成回写 charts.western）暂缓、待后端评审。保留星座页三格/解读/流年等专属内容 | P1 | 🟡 | 关联 REQ-039/040/045；④评估完成（2026-09-07 代码比对）：可合并、分层实施——L1 净删重复 ~70 行、改动 60–100 行；L2 另 150–300 行；L3 跨日需后端。风险：natal/charts.western 双快照不同时点可能不一致、无经纬度建档 western=null（降级）、patterns 文案口径需真实样本核对（natal 原样 join vs western translatePattern）、勿破坏 REQ-039 幂等复用链路；分析锚点（frontend/public/index.html，以函数名定位）：AstroWheel 7243、AstrologyPage 7356、AstroNatalPanel 4559、WesternPlate 4285、WesternSummary 4272、ChartView 4483、astroCard 4767 |
| REQ-054 | 档案内历史记录逐条删除：占星盘本命盘 / MBTI 等并入档案后的每条历史记录旁提供「删除」按钮，删除前二次确认 | P2 | 🟡 | 关联 REQ-047/039；逐条 + 二次确认（2026-09-07 用户确认） |
| REQ-055 | 起卦结果页重构 + 免费静态解读（六爻/梅花易数/小六壬）：①六爻结果改整齐列表布局——爻位逐行、爻符号/六亲/干支/世应/阴阳/六神分列对齐，动爻明显标识、世应标记突出（原排版挤作一团）；②六爻排盘组件下方新增**免费静态解读区**：本卦大意/变卦大意/动爻简要提示/世应基础含义；保留「深度断卦（付费）」为高阶入口，页尾免责声明保留；③梅花易数补齐类六爻的卦象展示 UI；④梅花易数/小六壬结果下方补静态现代汉语免费解释；⑤梅花/小六壬在占问问题上方新增**时间选项**（日期默认今天 + 时辰默认当前，必填） | P1 | 🟡 | 关联 REQ-044/022、BUG-004（视觉部分归本条，数据正确性仍归 BUG-004）；免费卦理文案：结构 + 通用规则文案先实现，64 卦大意等重内容先占位、正文后补；时间粒度=日期+时辰默认当前（均 2026-09-07 用户确认） |

## F. 后台与运维

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-048 | 后台 Prompt 统一管理台（生产热修改）：①覆盖 `backend/prompts/` 全部文件类 prompt——method-prompts（9 法）/ shared（validation·revise 等）/ interpret（tarot·divination·lenormand·astrology 等），后台统一列表 + 查看 + 编辑保存（含 admin 页面）；②热生效：保存后新请求立即生效（沿用按次读文件机制，需验证各读取点无启动缓存），不重启不改代码；进行中 job 默认继续用保存前 prompt；③版本与回滚：每次保存记版本快照（内容 + 时间 + 操作管理员），支持回滚任意历史版本；④保存前校验：非空、模板占位符完整性、长度上限，防改坏线上；PUT 沿袭审计 | P1 | 🟠 | 基于现有 GET/PUT `/admin/prompts`（现仅 method-prompts）扩展（backend/app/admin/router.py）；代码内嵌提示词（合成/合规等）本轮不纳入；2026-09-07 确认：范围=全部文件类、要版本历史+回滚；已实现（后端 88c3669 + admin 前端 ba8e1ad，15 文件类 prompt + 版本快照 + 回滚 + 热生效核实无启动缓存）；测试组已补 8 个自动化用例（test_admin_prompts.py）全绿，真机 admin UI 走查待用户执行 |
| REQ-049 | 登录/注册错误提示细化：①登录失败需区分展示「该账户不存在」或「密码不正确」（当前根因：前端 api() 对任意 401 统一提示「登录已过期，请重新登录」，吞掉后端真实 message「用户名或密码错误」——修复：登录分支不套用通用 401 拦截，改用后端 message）；②注册时必填项未填（用户名/密码/确认密码/验证码）需逐字段提示（当前仅空 body 走 FastAPI 422 detail，前端无友好提示；已在注册前端补校验文案） | P1 | 🟡 | 2026-09-07 真机走查反馈登记（用户明确要求细化）；本地复现：POST /api/auth/login 不存在账号 → 后端 401 {code:2001, message:用户名或密码错误}，前端错误显示为「登录信息已过期」；POST /api/auth/register 空 body → 400 {code:1001, detail: body.xxx Field required}；关联 REQ-001；BUG-002 复验通过后衍生 |
| REQ-050 | 后台运维接入 OpenClaw Agent（远程 Agent 管理运维后台）：①形态：关键运维动作封装为 Agent 可调用工具/MCP，Agent 经既有 OpenClaw 接入点（bb3a.taichu.xyz 通道）直接调后台 API，用户不在电脑旁时以自然语言完成运维；②范围（2026-09-07 确认）：查看类（报表/用户/积分查询）+ 赠积分 + 重置用户密码/重置 case + 后台错误报告与服务健康查看（prompt 热修改本轮不纳入，后续如需接 REQ-048 能力）；③鉴权与安全：Agent 专用 API token（独立于 admin 账号密码）；写操作沿袭 admin_audit_logs 审计；高风险动作（赠积分/重置密码/重置 case）默认需用户确认后才执行；④既有 bb3a 配对 token 保护与回程通道保持不变 | P1 | 🟡 | 关联 REQ-048（prompt 工具化后续可选）；复用既有 OpenClaw 通道（ops/deploy/README.md）与现有 /admin/* 端点 |

## D. 待办 / 未来（Phase 2 后，明确不做或缓做）

| ID | 需求 | 优先级 | 状态 | 备注 |
|---|---|---|---|---|
| REQ-030 | 合婚 / 合盘 | P3 | ⚪ | Phase2 后评估 |
| REQ-031 | 星座三限/月返/日互 | P3 | ⚪ | 盘型 UI 置灰「敬请期待」 |
| REQ-032 | MBTI × 命盘跨读 LLM | P3 | ⚪ | 预留付费点，未实现 |
| REQ-033 | SQLite → PostgreSQL 切换 | P3 | 🟡 | 多实例水平扩展前必须切换 |
| REQ-034 | 金数据商户认证 + 支付全链路验证 | P1 | 🟡 | 用户自行验证 |
| REQ-035 | 购买积分与充值流程：右上角菜单「购买积分」入口 + 支付界面页 + 0.1 元兑 10 积分测试档——**暂不开放**，商户认证通过并真实验证后放开 | P2 | 🟡 | 2026-09-07 用户确认付费逻辑暂时隐藏：前端入口全部保持隐藏（原 4 处 + 菜单「购买积分」）、支付界面不对外；仅后端 charge-code / 轮询保留、代码就绪 |
| REQ-036 | 后台 Phase2 专用端点（各模块 readings 查看） | P3 | 🟡 | 已留端口约定，未实现 |

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-07 | 建立需求表，登记 A/B/C/D 四模块共 24 项需求（含已完成/进行中/待办/归档） |
| 2026-09-07 | 状态机升级为 🟡→🔵→🟠 待验收→🟢 已验收（🟢 仅测试组可设）；使用说明新增「查重合并」规则 |
| 2026-09-07 | REQ-027 置 🟠 待验收（前端提交 9538d33/87588a8），触发测试组验收；REQ-028 前端已提交（6f73acd）后端未提交，保持 🟡 |
| 2026-09-07 | 使用说明新增「可执行性保证」：新登记需求须含可验收口径，含糊/缺口径先向需求方确认再登记 |
| 2026-09-07 | 新增 E「体验与交互优化」REQ-037~044（用户走查批次，均 🟡）；REQ-027 退回 🔵（BUG-001 待修）；REQ-035 并入「购买积分」入口 / 支付页 / 0.1 元测试档细化 |
| 2026-09-07 | 新增 REQ-045（星座日月升三格 + 布局，男女各一套 72 篇先占位）、REQ-046（新建档案入口与按钮路径）、REQ-047（MBTI 免登录分享、结果存历史可回看），均 🟡 |
| 2026-09-07 | REQ-035 收紧：付费（购买积分/充值）逻辑暂不开放，前端入口全部隐藏、仅备代码（用户确认） |
| 2026-09-07 | 新增 F「后台与运维」REQ-048（后台 Prompt 统一管理台：全部文件类 prompt + 热修改 + 版本回滚，🟡） |
| 2026-09-07 | 首批真机复验（测试组）：REQ-028/039/047 验收不通过退回 🔵（附细化口径：档案星盘合并展示、MBTI 档案内容、分享入口时机）；新增 REQ-049（登录/注册错误提示细化，用户走查登记）；REQ-048 补 8 自动化用例待 admin UI 真机 |
| 2026-09-07 | 新增 REQ-050（OpenClaw Agent 远程运维：工具/MCP 直调后台 API，范围=查看/赠积分/重置/错误健康，专用 token + 高风险需确认，🟡） |
| 2026-09-07 | REQ-046 ③ CasesPage 口径定稿：档案管理页（已有档案）卡片按钮 4→3：查看档案 / 重命名 / 删除档案 |
| 2026-09-07 | 新增 REQ-051（积分余额显示：档案移除→Banner 菜单左、积分折 ¥ 动态汇率仅登录）、REQ-052（生肖档案下拉、去手动入口）、REQ-053（档案详情字段合并 + 占星盘合并评估）、REQ-054（历史记录逐条删除 + 二次确认），均 🟡 |
| 2026-09-07 | REQ-042 ④ 改口退回 🔵（塔罗落地页去扇形，预测方向置于「塔罗牌」副标题下、无美术渲染；雷诺曼 REQ-043 同步）；新增 REQ-055（起卦结果页重构 + 免费静态解读 + 梅花/小六壬时间选项，🟡） |
| 2026-09-07 | REQ-053 ④ 占星盘合并评估完成（代码比对）：可合并分层 L1 前端共享渲染单元 / L2 档案内双份占星收敛 / L3 数据层统一暂缓；结论与风险已写入 REQ-053 备注 |
| 2026-09-07 | 塔罗/雷诺曼改版修订（并入 REQ-042/043，均 🔵 返工）：删「预测方向」步骤；牌阵改单列按钮+适用说明；抽卡 UI 拆分——上方仅卡图/标题/标签，含义段落集中下方 |
