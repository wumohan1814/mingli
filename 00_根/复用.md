# 太初 · 复用

> 读者是 Agent。实现新功能前扫一遍；收尾时若新增了可复用件，补进来。

## 一、设计值（启动时必填，之后不许硬编码）—— 这一栏不许留空
> 任何样式用这里的值，禁止魔法数字。
> 来源说明：颜色 / 圆角 / 阴影 / 动效均为 `frontend/public/index.html` 的 `:root` CSS 变量**实际值**（已逐项核对，与 `docs/standards/04-前端设计令牌.md` 一致，仅 `--font-sans` 实测多一项 `"Hiragino Sans GB"`）；间距 / 字号在 index.html 中**尚无 token**（standards/04 §8 登记为缺失），取 standards/04 的约定值，标注「04」。

| 用途 | 名称 | 值 |
|---|---|---|
| 主色（墨蓝） | `--ink-500` | `#44558F` |
| 主色最深（底色 / Banner） | `--ink-900` | `#1A2138` |
| 强调色（皮肤 accent，国学默认 = 玉青） | `--skin-accent` | `#3E7C6B` |
| 强调色淡色（背景 / hover） | `--skin-accent-soft` | `rgba(62,124,107,.14)` |
| 品牌副色（金） | `--gold-500` | `#B8893E` |
| 背景（页面底 · 宣纸暖灰） | `--paper` | `#F7F4EC` |
| 卡片底（表面白） | `--surface` | `#FFFFFF` |
| 正文 / 标题文字 | `--text-1` | `#1F1F26` |
| 次要文字 | `--text-2` | `#5C5C68` |
| 占位 / 禁用文字 | `--text-3` | `#8A8A94` |
| 边框 | `--border` | `#E5E0D4` |
| 间距（4px 基准，未 token 化，建议用后补成 `--space-*`） | xs / sm / md / lg / xl / 2xl | 4 / 8 / 12 / 16 / 24 / 32 px（04） |
| 字号 | 12 / 13 / 14 / 16 / 18 / 24px | 辅助标签 → 正文 → 页面标题 → Hero（04；正文默认 14px） |
| 圆角 | `--radius-sm` / `--radius-md` / `--radius-lg` / `--radius-pill` | 8 / 12 / 16 / 999 px |
| 阴影（卡片默认） | `--shadow-card` | `0 2px 12px rgba(26,33,56,.10)` |
| 动效 | `--dur` / `--ease` | `.22s` / `cubic-bezier(.4,0,.2,1)` |
| 字体 | `--font-serif` / `--font-sans` / `--font-latin-serif` | 衬线（标题古籍感）/ 无衬线（正文 UI）/ Georgia 西文（index.html :root） |

> 皮肤值以 `[data-skin="guoxue|astrology|tarot|xishi|xingzuo|mbti"]` 覆盖 `--skin-accent` / `--skin-banner` 等；新模块皮肤先查 index.html `:root` 段皮肤表，不自创色值。

## 二、本项目已有件（优先复用，别重造）
| 件 | 什么时候用 | 位置 |
|---|---|---|
| 排盘服务（Python / Node 双运行时） | 任何命盘 / 占卜的确定性计算：八字等走 lunar-python；紫微 / 西占 / 七政 / 奇门 / 五运六气走 Node 常驻服务（连接失败自动降级 subprocess，字节级保真） | `backend/app/paipan/engine.py` + `backend/paipan-node/server.mjs`（:9317，`TAICHU_PAIPAN_NODE_URL`） |
| `method-result v2` 契约 | 所有方法模块（`app/methods/`，9 法）输出的统一结构；跨模块唯一协商点，禁止私改字段 | `backend/app/methods/base.py`（`parse_method_result()`）；规范 `docs/standards/03` |
| `chart.json` 契约（档案全量结构） | 档案唯一事实源：`charts.chart_json` 全量落库、REST 投影裁剪（ADR-0006）；重排 `version+1` | `backend/app/models/`（charts 表）+ `docs/standards/03-接口与数据字典.md` |
| 积分扣费服务 | 逐法扣费 + revise / query 即时扣费；排盘 / 校准不扣；余额不足 `5002` | `backend/app/credits/`（Code-Wiki §4.9） |
| admin 后台 | 后台管理（Banner / prompts / 用户 / 数据等） | `backend/app/admin/router.py` + `frontend/public/admin.html` |
| 前端预编译脚本 | **改 JSX 后必跑**；守护 `<script type="text/babel">` 实时转译红线 | `node frontend/scripts/precompile.js` |
| 异步任务编排 | 长时编排：jobId + 代码轮询，编排器独立 session | `backend/app/jobs/orchestrator.py` |
| LLM 客户端 | 解读 / 校验 / 修正对话（DeepSeek，OpenAI 兼容）；key 只走 `backend/.env`，严禁硬编码 | `backend/app/llm/client.py` |
| 鉴权（多用户隔离） | JWT（HS256）取 `userId` 注入所有查询；任何 SELECT/UPDATE/DELETE 必带 `user_id` | `backend/app/auth/router.py` |
| 校验 / 合规护栏 | LLM 输出校验、合规拦截 | `backend/app/validation/validator.py` + `backend/app/compliance/guardrails.py` |
| 记忆服务 | AI 伙伴记忆抽取 / 召回 | `backend/app/memory/service.py` |
| 前端通用组件 | 弹窗一律用 ModalBase（11 个业务弹窗已迁移）；Toast 单例 `toast(msg,2600)`；Skeleton 骨架屏；`--art-*` 美术令牌 | 现于 `frontend/public/index.html`（ModalBase 在 2848 行起、~91 行；皮肤/组件/页面 CSS 共 3 块 1922 行）→ 节110 阶段1/2 拆至 `css/*.css` + `js/components.js` |
| 埋点 | 业务事件上报；写库失败静默不阻断业务 | `backend/app/events/service.py`（`record_event`） |
| 前端权威入口 | 只有 `frontend/public/index.html`（+ `admin.html`）；`frontend/src/` 已归档（节110）不参与运行 | `frontend/public/index.html` |

## 三、从库里借来的件（跨项目）
| 资产 | 版本 | 用在哪 | 做了什么适配 |
|---|---|---|---|
| （空） | — | — | — |

> （空）—— 本项目是破竹首个实例，尚未建跨项目库。收尾新增跨项目件时补进本表。

## 四、统一做法（不许自创）
| 场景 | 统一做法 |
|---|---|
| 加载中 | Skeleton 骨架屏（text / circle / rect / page，shimmer 扫光 1.5s）；短提示走全局单例 `toast(msg, 2600)`（standards/05 §3.3、§3.5） |
| 失败 | **失败禁止静默失败**（太初守则：重试设硬上限、连续同类错误快速中止、失败原因落错误字段供前端展示）；有状态弹窗参照 ShareFillModal 三态（成功 / 加载 / 失败）范式（standards/05 §3.2） |
| 需要确认 | 一律走 ModalBase 通用弹窗基座（ESC / 遮罩点击关闭、背景滚动锁定；宽弹窗传 `width`，左对齐传 `align="left"`），禁止再手写 `modal-mask`（standards/05 §3.1） |
| 空态 | 列表 / 档案空态统一用 Empty 空状态组件（技术债 P2，部分页面已有）；不新造空态形态（standards/05 §6） |
| 样式 | 一切视觉属性用 CSS 变量：皮肤色必走 `--skin-*`，动效必走 `--dur`/`--ease`，**禁止硬编码色值 / 尺寸**（standards/04 §1.2） |
| 组件编码 | Props 解构 + 默认值，禁 `props.xxx` 散点读取；按钮带 `aria-label`、图片带 `alt`；新组件先查本表并登记（standards/05 §1.2、§5） |

## 五、红线
- 不许硬编码颜色 / 间距（用设计值，见 §一）
- 静态资源必须根绝对路径 `/vendor/*`（BUG-009 教训：相对路径在深层分享路由 404、被当 text/html 执行；`--art-*`、`/data/*` 同理，一律根绝对路径）
- 单文件超过 2 万行必须拆（`frontend/public/index.html` 已 22046 行到上限的教训 → 节110 解耦中，规范见 §六）

## 六、前端拆分规范（节110 解耦 · 阶段0 定下，后续各阶段遵守）
> 目标：`frontend/public/index.html` 22046 行 → 「入口壳 + `css/*.css` + `js/*.js`」，免构建不变。

1. **命名**（kebab-case，同 standards/01）：
   - CSS：`css/tokens.css`（`:root` 令牌 + 皮肤）、`css/components.css`（reset + 壳 + 通用组件 + 盘面）、`css/pages.css`（页面/业务样式）。更细的 pages-<域> 拆分如需再拆留待后续。
   - JS：`js/components.js`（ModalBase/Icon/DisclaimerFooter/SkinSwitcher/toast/轨道/精灵 等通用件+共享工具，**必须早于 views 加载**）；`js/views-<域>.js`（视图，阶段3）。全局状态+api+文案别名+埋点+App 路由+挂载 留在 `index.html`（阶段4 骨架）。
2. **加载顺序**：见 `导航.md` §7.1（vendor → data → globals → components → views-* → app）。
3. **precompile 适配（关键）**：拆出的 `.js` 只放**已预编译的普通 JS**（从 index.html 原样搬出），**禁止再写 JSX**——precompile 门禁只扫描 index/admin 两个 HTML 内联块；且 `@babel/preset-react` 未在任何 package.json 声明（node_modules 只有 `@babel/core`），新 .js 内的 JSX 无法被门禁编译。JSX 只允许留在 index.html 骨架。**红线不变**：禁止浏览器端 `<script type="text/babel">` 实时转译。
4. **资源路径**：拆出的 .css/.js 内 `/vendor/*` `/art/*` `/data/*` 一律根绝对路径（BUG-009）。
5. **分阶段**：0 建体系（本阶段）→ 1 拆 CSS（`css/*.css`）→ 2 拆通用件（`js/components.js`）→ 3 拆视图（`js/views-<域>.js`，按域逐节）→ 4 收敛（入口壳 <2000 行；单 .js/.css <5000 行）。每阶段 git 留底 + 跑 precompile 门禁 + 用户亲验。
6. **单一事实源**：权威入口仍是 `frontend/public/index.html`；`frontend/src/` + Vite 脚手架已归档至 `docs/_archive/前端-vite-ts-参考实现/`（见 §二「前端权威入口」）。
