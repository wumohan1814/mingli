# migrations/ — 迁移脚本

两库各自独立的版本线：

| 子目录 | 库 |
|---|---|
| `analytics/` | 用户分析库 |
| `feedback/` | 跨用户质疑库 |

## 纪律

- 每个迁移必须可重复执行判定（幂等）并提供**回滚脚本**。
- 破坏性变更须在 `docs/standards/05-版本迭代记录.md` 标注"迁移要求"。
- 迁移工具选型见技术栈清单 T1（候选 SQLAlchemy 2 + Alembic）。
