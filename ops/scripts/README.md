# scripts/ — 运维脚本

## 必备

- **备份**：双库定期备份 + 异地留存（无主体阶段一封归零的沉没成本极高）。
- **回滚**：镜像回滚 + 数据库回滚脚本。
- **数据订正**：忘记密码的**人工处理**脚本——MVP 无找回通道（R7），由运维进库重置
  `password_hash`，**全程不得暴露明文密码**。
- **成本巡检**：定期跑单 case 首跑成本，对照 `TAICHU_COST_GATE_MODE` 阈值。

## 纪律

- 涉及生产数据的脚本必须支持 `--dry-run`。
- 所有脚本的操作留审计日志。
