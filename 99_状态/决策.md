# 决策记录 · 为什么速查表

> 本文件只追记「为什么」；ADR 全文在 `_archive/adr/`（历史档案，**不再作为决策流程入口**；2026-09-11 从 `docs/adr/` 归档）。
> 新决策直接追加本表；推翻旧决定须新增一行并标注「取代 ADR-xxxx」。只追加、不改写历史条目。

| 编号 | 决定（一句话） | 为什么（一句话） | 影响谁 |
|---|---|---|---|
| ADR-0001 | 排盘层自研 + 复用边界：底层用开源库（lunar-python / iztro / mingyu-core），胶水、切片、打分、提示词全部自研，mingli-reference 仅作参考 | 原样复用 mingli-reference Skill 在执行中会丢步骤（校验子会话被省略）、且与多用户隔离、异步、计量等产品化需求不匹配；历法正确性由成熟开源库兜底，契约归命理自主 | 后端排盘 / 切片 / 打分 / 提示词；前端 chart.json、method-result v2 契约 |
| ADR-0002 | 校验子会话仅断前尘阶段执行，抽为独立 validation 模块，用便宜模型单独计量 | 「命题质量」因子（占契合度 30%）只在断前尘后的校准阶段被 score.py 读取；两阶段都校验成本约 2 倍，预测阶段校验价值低 | 后端 validation 模块与 score.py；LLM 成本口径（首跑 ~23 次调用，¥0.3–0.5/case 作废 → 建议 ¥0.5–1.0） |
| ADR-0003 | bazi-hunyin-caiyun（婚姻 / 财运 / 事业）两阶段纳入，成为常驻第 9 法 | 第一阶段主钩子是事业运势，该专题直接覆盖；常驻后免去运行时判断补切 | 方法清单 8→9 法；断前尘串行时长与成本 +12%；前端事业主问入口 |
| ADR-0004 | 阶段 4 路由决策独立为 routing 模块，路由表配置化、零 LLM，决策落库留存 | 否则「这次跑哪几法」无归属；交 LLM 决定则每次预测多一次调用且结果不确定，违反确定性原则 | 后端 routing 模块；新增方法必须同步登记路由表；前端主问 / 主场景选择 |
| ADR-0005 | 断前尘 / predict 改异步：POST 返回 jobId + 前端代码轮询（零 LLM），新增 jobs 模块与表 | 断前尘 9 法串行可达数分钟，原 REST 全同步与风险描述自相矛盾；代码轮询零 token，MVP 最简 | 后端 jobs 表 + 2 个异步端点；前端轮询 / 退避 / 超时 UI 状态机；其余端点保持同步 |
| ADR-0006 | 归档以数据库为唯一事实源，不引入 Obsidian / archive.py，Markdown / PDF 导出留 Phase2 | 命理档案是 Phase2 横向扩展的数据底座与护城河；双写有一致性负担，导出产物是派生而非事实源 | 后端 archive 端点（结构化 JSON 含推理链路）；前端渲染可读性；Phase2 导出功能 |
| ADR-0007 | MVP 维持强制注册（R7 现状）；获客投放、日活可测时复评 | MVP 首要目标是量出单位经济，转化优化收益无法评估；匿名 case + 合并引入数据一致性复杂度，强制注册让数据从首次调用绑定 userId | 鉴权与多用户隔离验证；后续转免注册需匿名 case 表与合并逻辑（数据迁移） |
| ADR-0008 | JWT 采用 HS256，密钥 MINGLI_JWT_SECRET 走环境变量（随机 ≥32 字节、各环境不同） | MVP 单进程单实例无验签分发问题；HS256 最简、验签快；短访问令牌 TTL 已限制泄露窗口 | 后端鉴权；拆多进程 / 多实例或引入第三方验签方（Phase2）时升级 RS256 |
| ADR-0009 | 横向扩展复用内置 mingyu-core（生肖 / 起卦 / 塔罗 / 星座 / MBTI），确定性计算零 LLM 免费、interpret 可选付费 | 引擎与中文文案已内置且依赖已存在，零新增 npm 依赖；确定性免费成为 Freemium 引流钩子 | 前端 2 个 HUB + 6 模块页面；新增 4 表（user_id 隔离）；MBTI 60 题题库与 16 型文案（⚠️ **此部分已被下方「节122 立项」取代**：题库换成 IPIP 大五人格、16 型别名删除）；塔罗卡面先占位 |
| FE-001 | 前端免构建单文件架构：React CDN UMD + 预编译 JSX，代码集中在 frontend/public/index.html，后端静态托管 | MVP 部署环境不确定（可能无构建链）、单人前端、迭代快、代码量可控；部署最简、便于迁移 | 前端开发 / 验收与 BUG-009 类排查；超 30k 行或团队 ≥3 人时评估迁 Vite 工程化（Phase2；2026-09-10 已补 Zustand UMD 状态管理） |
| 破竹-重构（D1–D5） | 治理层按「破竹」框架重构：AGENTS.md 一行指向 / 00_根·40_节·99_状态·50_痕 / 台账冻结 / 取消三角色、用户为唯一验收人；运行层零改动 | 收敛治理成本、单会话一节、凭 = 真实产物；破竹即从命理经验提炼的下一代框架（用户拍板 D1–D5 全默认） | 全项目工作方式；旧台账（需求表/Bug表）冻结为历史；历史待验收 80 项走节100 全貌走查批量清算 |
| 台账冻结方式（**已被下一条「台账归档方式」取代**） | 需求表/Bug表「原地冻结」（顶部横幅 + 不再更新），不物理移入 _archive | 保持 tools/gen_ledger_snapshot.py 与全貌状态引用的可用、避免 185KB git 移动churn、可逆 | 实时状态以 00_根/入口.md + 40_节/ 为准；本表历史查询 |
| 台账归档方式（**取代上一条「台账冻结方式」**） | 需求表 / Bug管理表 / 台账状态一览 / 台账快照-*.csv **物理归档**至 `_archive/台账冻结/`；生成器 `tools/gen_ledger_snapshot.py` → `tools/_archive/`；引用同步（入口/全貌/导航/README/docs README） | 台账已冻结为纯历史（内容已在 `40_节/` 与 git 历史里）、留在 docs/ 顶层反而抬高检索成本；破竹 §7「归档 > 删除」且可逆；用户 2026-09-11 拍板"用不到了" | docs/ 顶层只剩现行参考文档；实时状态以 `00_根/入口.md` + `40_节/` 为准；历史查询走 `_archive/台账冻结/` |
| 组C 本地向量化**否决** | 记忆层不引入 embedding/向量库：本地模型（torch + MiniLM）在 894MB 机器上必崩，远端 embedding API 也只在"数字证明语义是瓶颈"后才允许复议 | 生产机实测 **available 仅 226MB**、swap 已用 0；本地向量化估计 +400~650MB（必崩）。且项目未上线、记忆表 0 行，无任何数字证明语义召回是瓶颈 | 后端依赖清单（不新增 `sentence-transformers`/`torch`/`numpy`/`sqlite-vec`）；参考项目 agent-memory 的向量部分不搬 |
| 节118 记忆优化走**方案 A（三步走）** | 记忆层按 ①离线基准（造尺子）→ ②召回可用性（查询表达 + 短问兜底 + tags 进场）→ ③打分公式（RRF + trust/access_count 进场 + importance 校准）分步做，**每步用基准复测对比，无收益即停损** | 根因是"检索表达结构性失效 + 已入库信号未读取"，不是"算力不足"；先造基准才符合 v0.15「性能的凭是数字」，避免"我觉得会更好" | 后端 `app/memory/`；离线基准脚本（新增，离线运行，不占线上资源） |
| 参考项目目录**清仓**（`reference-not in project/` 删除） | 用户决定删除该目录；**删前**把全部可借鉴项 + "决定不做"的原因**全量登记进节文件**，此后一律以节文件为准 | 外部参考目录不做长期依赖（约 300MB / 870 个 md，留着抬高检索成本）；但**删掉就没法再考古**，所以"能做的"与"不做及原因"都必须先落盘，否则将来会重复调研 | 后续所有"参考项目借鉴"类工作以 `40_节/待办/节119-参考项目借鉴后续迭代总账.md` 为唯一入口；节116/117/118 的"来源路径"降级为历史标注 |
| **原件不保留**（清仓的收尾口径） | `reference-not in project/` 整个删除；**原件一律不保留**；将来需要时按 `00_根/来源.md` §三「重新获取地图」重新获取；调研结论以节119 为准 | 用户 2026-09-11 拍板："**原件不需要保留，等到以后有需要的时候再重新获取**"；且破竹 v0.31 §4 排他清单已把「上游参考项目 / vendored 第三方完整源码」列为**一律不读不搜** | 后续借鉴工作走 `节119` + `00_根/来源.md`；不再新建"参考项目精华"目录 |
| **第三方内容登记机制启用**（破竹 v0.30/v0.31） | 新增根文件 `00_根/来源.md`：登记所有外来内容的 来源/许可/我改了什么/是否去痕/上线要通报 + **大白话结论**；**只参考思路也记一行**；并含「原件不保留 · 重新获取地图」 | 破竹把「第三方内容合规」做成独立机制（`03` §来源模板 + `阶段③_收尾/交付前检查.md`），因为**上线后不可逆**；节119 清仓正好需要一张合规台账 | 所有"参考项目借鉴"类工作；上线前合规检查的**唯一依据**；收尾第 3 项「更新根」的一部分 |
| **节120 出生时间正确性链立项**（优先级高于 116/117/118） | 真太阳时补**均时差** + **1986–1991 夏令时** + **输入不确定性声明出口**，分三步各自可上线 | 用户 2026-09-11 指示"这个也单独立一个节吧"；三处参考项目**独立指向**且命理代码缺口**已核实** → 属**正确性**问题而非优化；受影响人群（1986–91 出生者现 35–40 岁）是消费主力 | 排盘层 `paipan/engine.py` + `api/cases.py`；**已入库历史盘面处置另案**（不重算） |
| **输出形态选「甲 · 不打分」** | 命理对外输出只给「**吉/凶/平 + 置信度 + 分歧标注**」，**不输出吉凶总分 / 百分比 / 数字评分 / 匹配置信度** | 用户 2026-09-11 拍板选甲。理由：排盘是**确定性**的（能算准），但解读是**概率性**的——给分数会把"算不准"包装成"算得准"；且节116 的方向是"**分歧标注而非抹平**"，与"压成一个分数"**互斥** | `method-result v2` 保持**无分数字段**；`synthesizer.py` 的输出形态；`scorer.py` 的 0–1 契合度**继续只作内部校准、不给用户看**。**乙/丙两套备选方案与完整实现路径已登记在 `40_节/待办/节121-输出形态三方案.md`**——将来若要转方向，照那份直接做 |
| **tags 升级为独立召回通道**（节118 新增项） | 把 `tags_json` 从「候选集加分项」升级为**独立召回通道**（`tags_json LIKE` 或 entity_index 式），仍**不引入向量、不改 FTS DDL / 触发器 / 表结构** | 消融归因实测：本语料下 tags 是**唯一有真实信号**的通道（理论上限 37.1%），当前只吃到 `7/26 ≈ 27%`，因为它只在候选集上**加分排序**、未成为**召回通道** | `backend/app/memory/service.py`；⚠️ **生产收益待实测**（离线语料把主题词硬编码进 tags，会**高估** R3 在生产（LLM 抽取）的收益） |
| **节122 立项：MBTI 题库整体替换为 IPIP 大五人格 + 对外文案商标合规**（**与 ADR-0009 互链**：取代其「MBTI 60 题题库与 16 型文案」部分，**ADR-0009 其余部分仍有效**） | ① 放弃现有 60 题题库，**整库替换为 IPIP-NEO-120 普通话译本**（公有领域，官网直供，五维中文信度 α ≥ .799）② 用已发表的大五↔MBTI 相关系数**映射回四字母**（带"边界"标注）③ **对外文案去除 MBTI 商标**（主文案零提及，仅在结果页脚注放"非关联声明 + 权利人官方致谢句"）④ **16 型别名（建筑师/逻辑学家…）一并删除**，五栏文案一并处理 | 用户 2026-09-11 决定："MBTI 的题库现在溯源不到，考虑到版权和商业化的风险…换成 IPIP 大五人格"。**依据（已核实原文）**：IPIP 官网写明"公有领域…可复制、编辑、翻译…任意目的，免申请免付费"，许可页写明"**商业使用已获自动授权**"；而 Myers & Briggs 官方政策写明"The MBTI instrument **may not be reproduced without written permission**"且"**Use of MBTI trademarks must only refer to genuine MBTI products and services**"——**用户原设想的"（类似 MBTI 框架）"正好命中这条禁令**，必须换写法 | `backend/app/mbti/{data/questions.json,data/types.json,scoring.py}` + `backend/app/api/mbti.py`（**契约变更 = 单向门**）+ `api/pair.py` + 前端 5 个 JS（**答题 UI 要从"二选一"改成"5 点量表"**）+ `tests/unit/test_mbti.py`（**大面积重写**）。✅ **零 DDL、零新增依赖、零后台任务、零服务器负担** |
| **节122 副发现：`types.json` 从未纳入来源核查**（新增覆盖纪律） | 「换题库」这类动作**必须把同一模块的全部静态内容一并纳入范围**，不能只盯用户点名的那一个文件 | `00_根/来源.md` §五 只核查过 `questions.json`（题库），**`types.json`（16 型别名 + 五栏文案）从未核查**；而实测其 16 个 alias 为 **16Personalities（NERIS Analytics）16 型角色名的直译**（建筑师/逻辑学家/指挥官/辩论家/提倡者/调停者/主人公/竞选者/物流师/守卫者/总经理/执政官/鉴赏家/探险家/企业家/表演者，**逐一对上**）。⚠️ 未核实其商标/版权登记状态 | 避免"换了水源还在喝旧井的水"；后续同类动作（换任何数据集）按此执行 |
| **节122 第二轮：档案人格数据分两笔账**（用户 2026-09-11 补充需求） | ① **平台只产出大五五维**（公有领域量表）→ **OCEAN 要记进档案** ② **四字母降级为"对照参考"** —— 平台算出的只是**建议**，**用户点了"保存"或手输之后才写入档案**，且档案里那一行的语义是「**用户认定/自填**」而不是「平台测出来的」 ③ 16 型别名**暂不删**（用户："这个需求还会改变"） | **合规上更强而非更弱**：平台**没有输出"MBTI 测评结果"** → 直接绕开 Myers & Briggs 官方政策那条「**商标只能指代正品**」（商标已在中国注册，**注册号 4621499**）。⚠️ **已更正（见「第四轮」行）**：Agent 曾误判 **Arneson (2016) 不存在**（学术库检索不到），实为 **CPP 白皮书、不进学术索引** → **结论撤回**；用户 2026-09-11 裁决：结果页**只在脚注提一次 MBTI 并附官方致谢句**，映射改用 **Arneson (2016) 的 r 值** | 后端新增"保存类型"写入路径 + 档案页两条账分开展示（**优先零 DDL：复用 `mbti_results` 的 JSON 列**）；前端结果页加"采纳"动作。落地节 = **节125**（新增，未立项） |
| **节122 第四轮：映射权重（设计值）定案** | 四字母映射的**权重 = Arneson (2016) 的四个 r 值**（**用户 2026-09-11 抄录提供**）：**E–I ↔ 外向性 −0.74~−0.79｜S–N ↔ 开放性 0.65~0.72｜T–F ↔ 宜人性 0.44｜J–P ↔ 尽责性 0.49**。换算：`z(倾向) = r × z(大五维度分)`（单预测变量标准化回归 β = r，非自创）；**N 不参与**（与 "four of the five" 自洽）；区间取中值 | 🚨 **符号不许照抄** —— 抄来的负号可能源于原作者把 **I 端**当正向计分，**照抄会把 E/I 整体翻转**；因此**只取 `|r|` 当强度，方向一律用权利人 MBTI Facts 页确认的四条关系定**。⭐ 另一关键收益：**r² 把"边界标注"从理念变成可计算规则**（E–I .55~.62 / S–N .42~.52 → 相对可信、少标边界；**T–F .19 / J–P .24 → 八成变异大五解释不了 → 本就该更常标「边界」**），与 **节121「甲 · 不打分」** 完全同向 | 节123 的计分与映射实现；**落地必做"ENFJ / ISTP 双验"**（造假答卷验符号）。⚠️ **数值属"用户提供 · Agent 未亲验原文"**（表是图片、PDF 工具不支持）→ 上线前若需合规举证宜补原文核对（**非阻塞**） |
| **节128 立项：命理自研内核（全部自研，不再依赖 `mingyu`）** | **① 代码自研**：逐步替换 `mingyu-core` 提供的 **~17 个能力**（实测 16 项 import，`server.mjs` L124–142）② **文案重建**：先取全文案 → 交**文案 Agent 分类**（公有领域 / 用户自有 / 必须重写）③ **同时核实外部**干净来源（古籍 / 公有领域） | 用户 2026-09-11 裁决："**选择全部自研，完全不用 `mingyu` 的任何东西**"（理由：术数是开放性传统文化内容、无版权保护、且"**没有任何地方规定必须用 mingyu 的内核**"）。⚠️ **立项理由须写"自主可控 + 不受上游许可证变动影响"**（⭐ 本次 AGPL 事件即活例），🚫 **不写"规避版权"** —— `mingyu-core` 是 **MIT、用它本就合法**，自研它**不是合规整改**；**用错理由会导致优先级误判** | 替换 `backend/paipan-node/server.mjs` 的 16 项 import；**逐能力替换、逐能力下线**（**每项须对拍验证**：新实现 vs 旧实现同输入比输出）；建议从**八字 / 紫微**开始（不依赖 mingyu + 认知度最高）；⭐ **全部完成后「禁止升级 `mingyu-core`」的约束自然解除**。⚠️ **前置未决**：用户"文案是我配的"这一陈述**Agent 无法核实**（实测 `Brhiza/mingyu` 贡献者只有 `Brhiza`(1046)/`Jerryxumaomao`(1)/`863401402`(1)，**不含 `wumohan1814`**；⚠️ 但**内容可由他人代提交**，故不等于否定）→ **需用户举证**（原稿 / 约定 / 或说明是 AI 生成）；🔒 **且一律从用户自己的原稿取文案，不从 AGPL 仓库复制**（同一内容、来源不同，法律地位与举证成本不同） |
| **节122 第三轮：映射方案被迫降级**（独立核查取到的原文事实） | 四字母映射**从"线性加权换算"降级为"对照参考"**：**`N` 无对应维度**（McCrae & Costa 摘要口径 "four of the five"）；**四维并不独立**（J–P 与 S–N 相关、C 同时与 T–F 相关）；**不存在"大五→四字母"的换算规范，也不存在其准确率的同行评议评估**；4 篇原始文献的 **r 值全部拿不到（付费墙）** | 三条独立原文事实 → **"一个字母由一个维度独立决定"的前提不成立**；在此前提下若仍输出干净的四字母，**违反节121「甲 · 不打分」的核心立场**（不把不确定性伪装成确定性） | `40_节/待办/节122-…` §4.3 已改写：**N 不参与映射**、允许输出「**介于 X 与 Y 之间**」、未补取到 r 值前**只能用方向不能用权重** |

| 节116 裁决层走**方案 B（一次性全做）** | 裁决层重建 + 证据分层 + 正文自检 + 模糊否定闭环**四步一次性落地**（不走 A 的分步独立提交） | 用户 2026-09-11 拍板"倾向于 B、一口气全做"；四步已落 `synthesizer.py`（`_adjudicate` 加权裁决 + 分歧检测 + 同源去重 + 置信度聚合）/ `methods/base.py`（L1–L5 证据层 + 正文自检）/ `scorer.py`+`cases.py`+`orchestrator.py`（方向级模糊否定） | 解读输出形态（与节121「甲·不打分」一致：吉凶平 + 置信度 + 分歧标注）；解读提示词；校准链 |
| 节117 预判标记层走**方案 B（九法全做）** | 标记层覆盖**九法**；已落地**八字4法**（`paipan/marker.py`：月令十神 / 根气 / 寒燥 / 七杀 / 墓库），**紫微 / 西占 / 七政 / 奇门 / 五运六气 5 法待做** | 用户 2026-09-11 拍板"倾向于 B、一口气全做"；⚠️ 5 个非八字法**无参考、须逐法新设计口径**，且与**节128 自研内核**（将替换排盘层）重叠 → 建议其余 5 法挂**节128 之后**，避免白写 | `paipan/marker.py` + `jobs/orchestrator.py`（切片注入 `_markers`）；新增方法须登记自己的标记表 |

| 架构收敛（本轮评估） | 清理 9 个占位目录（synthesizer/guardrails/gateway/dispatcher/archive/services/shared/repositories/feedback）+ 死代码（events/dispatcher.py、case_repo、collector、get_current_user、filter_forbidden_domains、_decide_trend、_apply_disclaimer）+ 死配置 `validation_model`；`jwt_secret` 去弱默认改**必填**（落实 ADR-0008，本地/根 .env 已注入强随机 64 hex）；修正「standards/→docs/standards/、复用·库 4 项、data 三库、导航 §7 行数」等索引漂移 | 破竹协议架构评估：占位目录+死代码违反铁律2·8（单一事实源·删机制）；弱密钥是多用户隔离红线风险；索引漂移会让下个 session 被旧文档带偏。⚠️ **隔离字面加固（case_id-only 读补 user_id）暂缓**：审计判定无泄漏、改动有回归风险，留待有测试保护的独立节 | 后端结构；config.py；导航/复用/入口/data README |
| **节133 ③「同一八字不重复花钱」= 不做**（2026-09-14 用户拍板） | 维持现状：不做跨档案的八字哈希缓存（不新增缓存表、不改计费口径） | 用户 2026-09-14 拍板选 A（不做）。理由：**全项目共享缓存直接撞红线②（A 不能看 B）**——同一八字不同用户命中同一份缓存，等于让 B 白拿 A 花钱买的、且带 A 档案语境的解读；只限同用户内做则内测期收益很小，却要付出一次 DDL + 提示词改版缓存失效的设计成本。⚠️ 同时**证伪原节记录**「八字哈希缓存机制已存在（见 engine.py）」——实际只有 per-case 结果复用（`MethodResult.cached`），不是跨档案缓存 | 不新增表 / 不改缓存 key；`backend/app/paipan/engine.py` 与计费口径保持原样；节133 就此结案 |
| **节139：西占退出「九法→八法合一」**（2026-09-12 拍板 / 2026-09-14 落地） | `xizhan` 从 `METHOD_KEYS` / `DEFAULT_METHODS` 摘出，综合流水线 9 法→**8 法**；`chart.json.western` **继续生成**（档案「占星盘」tab + 西式占卜独立链路保留）；历史测试数据直接清理；「性格天赋」辅法由西占改为 **bazi-pattern** | 西占属「西式占卜」大模块、不属国学预测；摘出后**综合报告不再含西占 LLM 扣费**。⚠️ **契约影响**：`METHOD_KEYS` 长度即**进度分母与断前尘法数**——原代码把 `totalMethods` 与建 job 的 `total` **硬编码为 9**，本节一并改为 `len(METHOD_KEYS)`（消除同步点） | 后端 methods/slicer/routing/jobs/api；前端文案与断前尘轨道节点；`xizhan` 目录与 prompt **保留但不注册** |
| **节146：付款充值链路整条拆除**（2026-09-13 拍板 / 2026-09-14 落地，**含一次不可逆 DDL**） | 删金数据客户端 / 轮询 / 充值码三文件 + `POST /api/credits/charge-code` + lifespan 轮询任务 + 9 项 `jinshuju_*` 配置 + `RechargeCode` 模型 + 前端整套充值/到账轮询；**物理 `DROP TABLE recharge_codes`**（迁移 `data/migrations/analytics/0001_drop_recharge_codes.sql`，含回滚脚本）。**保留**：余额账户与逐法扣费、余额↔积分换算、后台/Agent 人工调分、`service.recharge()` 通用入账、注册赠送 220、历史 `recharge`/`serial:{}` 标签映射、`total_recharged` | 用户 2026-09-13 原话「删除之前做过但隐藏掉的付款充值逻辑」「以后也不会需要恢复」+ 明确要求保留余额与后台调分。**推翻**节102（REQ-034）/节103（REQ-035）的前提 → 两节已加作废注记并移入 `_archive/废弃节-节146/`；`docs/架构设计.md` §10 整节移入 `_archive/` | 后端 credits/config/models/admin；前端 index/ui-copy/views-home；`docker-compose.yml` 的 JINSHUJU env；`data/migrations/`。⚠️ **线上库 DROP 与线上 `.env` 清理尚未执行**（随下次部署） |
| **节141：公开注册关闭（内测白名单制）**（2026-09-13 拍板 / 2026-09-14 落地） | 前端 `AuthPage` 只留登录 + 常驻内测提示（原文：「本项目只开放给内部人员，请联系开发者获得测试资格。」）；后端 `POST /api/auth/register` 加开关 `MINGLI_ALLOW_PUBLIC_REGISTER`（**默认 false → 403 + code 2003**），**不物理删除端点**；新账号只能由后台「新增 C 端用户」创建 | 用户 2026-09-13 拍板「常驻登录页提示、不带联系方式」。**取舍理由**：用配置开关而非删端点——测试环境置 true 即可继续造数（零测试返工），且将来若恢复只需改配置（判据 4 开关）。⚠️ **前端隐藏 ≠ 关闭**：必须由后端拒绝，否则可被直接调接口绕过 | 前端 views-onboarding / views-mbti / ui-copy；后端 auth + config；`.env.example`、`standards/03`、`standards/06` 三处 env 文档同步 |

| **节145：品牌改「命理」+ 角色改「王先生」+ 首页 Q 版动画整块删除**（2026-09-13 拍板 / 2026-09-14 落地） | ① 全项目中文品牌字样「太初」→「命理」（**含 `40_节/完成`、`99_状态` 等历史文档**）② 角色「太初先生」→「王先生」，自称按同一人设改写（greeting-3「贫道太初…」→「我是王先生…」）③ 首页落地页 Q 版循环动画**整块删除**（渲染块 / CSS 整段 / 序列帧函数 / 3 个 alt 文案键 / 11 个素材 / 2 个生成脚本），**素材不保留备份** | 用户 2026-09-13 原话「从全项目中删除"太初"这两个字」「连历史内容也一并修改」「按照你的推荐来整块删就好，以后也不会需要重回复（恢复）」「王先生的人设和太初先生一致，只是名字更改」。**冻结边界（本节口径）**：设计工具产物（`太初三主题设计方向.design/` **目录名保留**、`.design_library/`）、归档（`_archive/`、`tools/_archive/`）、vendored 第三方（`backend/paipan-node/vendor/`）、节139 三个历史基线快照、**节145/147 自身的改名对照文档**、节文件「用户原话」逐字栏 | 前端 ui-copy/views-*/index/admin/legal/manifest/sw；后端 agent/labels/admin/prompts/memory；`00_根/` 五件 + docs/；历史节文件。⚠️ **文档里对冻结目录的路径引用必须写旧名**——机械替换会误改，本节已回退 9 处（先例：改任何名字后必须单独核对冻结项的磁盘真实名） |
| **节145 副发现：开场白存在两份重复文案，且 ML_COPY 那份零引用** | `views-agent.js` 的 `AGENT_GREETINGS` 是**运行时唯一权威**；`ui-copy.js` 的 `agent.greeting-1..4` 定义后**全项目零引用**。本节只做**内容同步**（两处都改成王先生口吻），**不做代码层合并** | 铁律 2（单一事实源）：重复记录 = 缺陷；但合并要改「脚本顶层常量解引用时机」这条已知脆弱链（见 `99_状态/已知问题.md`「免构建前端语法错误可长期潜伏」条），**风险 > 收益 → 留待 i18n 机制（节135 阶段 F）真正需要时再合并** | `00_根/复用.md` §四已写明「开场白唯一权威 = `AGENT_GREETINGS`」；将来删任一份前先查引用方 |

| **节147：项目代号 taichu→mingli 全改**（2026-09-13 拍板 / 2026-09-14 落地） | ① JS 标识 `TC_*`→`ML_*` 及全部 `tc*`/`*Tc*` 帮助函数 ② CSS 令牌 `--taichu-*`→`--mingli-*` ③ 浏览器本地存储 7 键 + DOM id + SW 缓存前缀 ④ env 前缀 `TAICHU_`→`MINGLI_` ⑤ 三库文件名 `taichu_*.db`→`mingli_*.db` ⑥ DB 列 `share_taichu_ui`→`share_mingli_ui`（附迁移+回滚 SQL）⑦ docker 网络/卷/镜像名、宿主 `/opt/taichu`→`/opt/mingli` ⑧ `chart.json` 的 `meta.source` 取值 ⑨ 参考项目 `mingli`→`mingli-reference` | 用户 2026-09-13 原话「从此以后，这一整个项目的名字也会改成"mingli"，而不是叫"taichu"」+「第一条我认为需要改。第二条设计工具自己产生的以及归档的内容不需要改」+ 第三轮「旧目录撞名选 A：把旧参考项目改名 mingli-reference，本项目占用 mingli」。**冻结**：设计工具产物（目录名保留）、归档、vendor、节139 三个历史基线快照、节文件「用户原话」栏、节145/147 自身的改名对照文档 | 前端 js/html/css/sw；后端 config/conftest/models/api/tests；`docker-compose.yml`/`Caddyfile`/`Dockerfile`/`start.*`/`ops/*`；`00_根/` + `docs/`。⚠️ **本地存储键改名 = 现存用户一次强制登出 + 皮肤偏好重置**（内测期不做兼容迁移；已在 `sw.js` 保留旧图片缓存前缀的一次性回收） |
| **节147 · 域名与 SSH 私钥文件名「有意不改」**（2026-09-14 开工时当面确认；依据节142） | `<我的域名>` / `bb3a.<我的域名>`（`Caddyfile` 与文档）与服务器私钥文件名 `~/.ssh/<你的私钥文件>` **保留原名**，登记为节147 的具名例外；两者都给了「若以后要改」的安全步骤（`ops/deploy/README.md` 的「例外两项」表） | **实测 `https://<你的域名>/api/health` 返回 `{"status":"ok"}`，DNS 与 Let's Encrypt 都在跑** → 改域名要新注册 + 改 DNS + 重签证书，**有线上中断窗口**；而节142 §八 已拍板「**不保留独立的"官方托管运营服务"**……开发者本人部署的开源服务不算"官方"，仅限本人自行使用」→ 该域名只是**部署者自填的实例配置**，不是产品标识，改名收益为零。SSH 私钥同属外部真实文件，改错直接失去登录通道 | 后续任何「全项目 grep 不到旧名」的检查都要把这两项排除；第三方自部署时在自己的 `Caddyfile` 里换成自己的域名 |
| **节147 · GitHub 仓库名由用户改名**（2026-09-14 用户选 B） | `deploy.sh` / `docs` 里的仓库地址已改为 `git@github.com:wumohan1814/mingli.git`；**用户在 GitHub 上同步改名**（旧名 301 重定向兜底） | 改远程仓库名只能在 GitHub 侧做；而**部署脚本用的已是新地址 → 改名必须早于服务器下次 `git pull`**，否则线上更新会失败 | `ops/deploy/deploy.sh` / `ops/deploy/README.md` 的 clone 地址；⚠️ **在用户完成 GitHub 改名之前，服务器不应拉新代码** |
| **节147 遗留待裁决：CSS `--tc-*` 令牌族（273 个）** | 本期**未动** `--tc-*`（`--tc-ink-a04` / `--tc-white-a70` / `--tc-cinnabar-*` …）。它名字里也有 `tc`，但节147 拍板的映射表只点名 `--taichu-*`（原文「仅 tokens.css 一个文件：87 定义 + 11 自引用」） | 两个令牌族**互相独立**（`--taichu-ink-*` 与 `--tc-ink-*` 并存且用途不同；`standards/04` 实测 421 个令牌 = taichu 75 + tc 273 + art 9 + 旧族 64）→ 合并属**设计令牌契约变更**（节134 刚定 v1.2），回归面（六套皮肤 + 规范文档 + 设计稿）远大于本次机械改名 | **待用户裁决**：认定 `--tc-` 也是 TaiChu 就要另立一节，按节134 的令牌变更流程走；不认定则本行即为「它是通用色值短前缀」的定论 |
| **`--tc-*` CSS 令牌族不动**（2026-09-14 核查后定，**取代上一条「待裁决」**） | 用户先说「CSS 的令牌也一起改了吧」，Agent 核查后发现：`--tc-*`（273 个）**不是项目代号**，是 `tokens.css` 里由节136-B codemod 生成的**主题色语义族**（`--tc-white` 反白 / `--tc-amethyst` 紫晶 / `--tc-suit-swords` 塔罗宝剑 / `--tc-foil-*` 金箔 / `--tc-wood-*` 木色 / `--tc-stage-*` 舞台 / `--tc-dqc-*` 大衍棋），逐条注释全是**颜色角色**，没有一处是项目名 → **本期不动**，并把「`--taichu-*` 已全改完、`太初` 二字在代码层已清零」这个事实回报用户 | 用户给这条指令的**理由是**「确保整个项目中没有代码层面的"太初"两个字」—— 该目标在 `--taichu-*`→`--mingli-*` 完成后**已经达成**；把 273 个颜色令牌改叫 `--ml-*` 会让名字失去含义（`--ml-white` 读作"MingLi white"），且属**设计令牌契约变更**（节134 刚定 v1.2，涉及六套皮肤与设计稿） | 若用户仍要改：一条脚本即可（`--tc-`→`--ml-`，同步 `standards/04`/`05` 与 tokens.css 自引用）；**建议先看本行核查结论再定** |
| **节147 更正：域名改为「可配置项」，真实域名/内网地址/私钥名一律不进仓库**（2026-09-14 用户第四条指令，**取代上上条「域名与 SSH 私钥文件名『有意不改』」**） | ① `Caddyfile` 主站地址改为 `{$MINGLI_SITE_DOMAIN}`（env 注入；缺配置则 Caddy 启动失败）② 原 `bb3a` OpenClaw 接入点从 `Caddyfile` 抽成**可选附加站点**（`ops/deploy/caddy-extra.example` → 本地 `caddy-extra.caddy`，`.gitignore` 排除）③ 仓库内所有真实域名 / 服务器公网 IP / 隧道 IP / Tailscale 主机名 / SSH 私钥文件名**全换占位符** ④ `.env.example` 只放占位值 ⑤ `.gitignore` 新增 `ops/deploy/caddy-extra.caddy` 与 `docs/未公开/` | 用户原话：「我认为这个域名应该是一个可配置项，只不过现在我把它配置成了 <我的域名>（原话带真实域名，按隐私要求以占位替代）。**如果我开源的话，我的域名是不是也会跟着这个项目暴露**」—— 这是**开源前的隐私问题**，优先级高于「grep 干净」；那行「保留不改」当时只算了「改域名有中断窗口」，**漏掉了"一开源就暴露"这一面**。新方案两边都满足：**线上照用（值在 `.env`）、仓库里不出现** | `Caddyfile`、`docker-compose.yml`（透传 env + 挂载 extra）、`.env.example`、`.env`、`.gitignore`、`ops/deploy/README.md`、`docs/登录指南.md`、`docs/Code-Wiki.md`、`docs/架构设计.md`、`docs/archify/*.json`、`backend/app/admin/router.py` 注释、`00_根/导航.md`。⚠️ **部署新增一步**：`.env` 必须补 `MINGLI_SITE_DOMAIN`，否则 caddy 起不来 |
| **节147 · 品牌口径更正：`太初` → `命理`（不是「命理太初」）**（2026-09-14 用户第一条指令） | 节145 按 09-13 需求原话做成「命理太初」（381 处）；用户 09-14 更正「我要求改名是从"太初"改到"命理"，而不是改成"命理太初"」→ 收紧为**「命理」**，再改 379 处 / 98 文件，并再改 4 个文件名（`命理用户协议.md` / `命理隐私政策.md` / `节128-命理自研内核.md` / `命理-纯单机…评估.md`） | 需求原话写的就是「改成"命理太初"」，Agent 照做没有错；**错在"最终字符串"没在开工前用一句话向用户确认**（该教训已写进 `99_状态/已知问题.md`「改名必答三问」第 ③ 问）。最终口径：**PWA 安装名 / 协议标题 / 后台标题 / 免责声明 / `app.name` 一律是「命理」** | 前端 `ui-copy.js`/`index.html`/`admin.html`/`legal/*`/`manifest` + 后端可见串 + `docs/` + `00_根/` + `AGENTS.md` + 历史节文件。⚠️ **`太初` 二字现已全项目清零**（冻结项除外），旧名只留在节145/147 的改名对照文档与「用户原话」逐字栏 |
| **未公开文档区建立**（2026-09-14 用户第四条指令） | 新建 `docs/未公开/`（`.gitignore` 排除），把 `docs/命理-纯单机占卜工具上架可行性评估.md` 移入 → **经营/策略/上架可行性类文档保留在本地磁盘但永不入库** | 用户原话：「docs 中这一类型的东西都可以不上传 github」。这类文档含商业判断，不适合随源码开源 | `docs/未公开/` 是这类文档的**唯一归口**；`docs/` 其余部分按「可公开」对待 |
| **合规文书运营主体抽成配置，服务端渲染注入**（2026-09-14 用户拍板，**取代上一条「待拍板」**） | ① 仓库里 `frontend/public/legal/*.html` 与 `docs/legal/*.md` 的真实姓名/手机号/邮箱**全换成 `{{OPERATOR_NAME}}` / `{{OPERATOR_CONTACT}}` / `{{OPERATOR_EMAIL}}` 占位符** ② 真实值走 `.env` 的 `MINGLI_OPERATOR_NAME` / `_CONTACT` / `_EMAIL` ③ 后端在**服务端渲染**时注入（`app/legal.py` + `main.py::SPAStaticFiles`）④ **三项任一为空 → `/legal/*.html` 返回 500**（fail closed），启动打 warning ⑤ 新增回归 `backend/tests/unit/test_legal_docs.py`（4 例） | 用户原话：「我同意把主体信息抽成 MINGLI_OPERATOR_NAME / _CONTACT / _EMAIL 这类配置，线上渲染时注入，仓库里两份文书用占位符」。**为什么服务端渲染而不是构建时替换**：这两份 HTML 由后端 SPA 静态兜底直出（Caddy 的 `@static` 白名单不含 `/legal/*`）→ 服务端注入让「**仓库里是占位符**」与「**线上是真实主体**」同时成立，部署侧零额外步骤，也不会被 `git reset --hard` 冲掉。**为什么 fail closed**：宁可 500，也不发布一份没有运营主体的协议（那才是真的合规事故）。⚠️ 实测踩坑：`StaticFiles` 在 Windows 上把路径归一成 `legal\用户协议.html`（反斜杠），前缀判断必须先归一分隔符 —— 靠新增的 4 例回归抓出来 | `backend/app/legal.py`（新）、`main.py`（imports + lifespan 体检 + `SPAStaticFiles` 注入）、`config.py`（3 字段）、`frontend/public/legal/*.html`、`docs/legal/*.md`、`.env.example`、`.env`、`backend/.env`、`tests/conftest.py`、`docs/standards/03` §7 + `06`、`ops/deploy/README.md`。⚠️ **部署新增一步**：线上 `.env` 必须补 `MINGLI_OPERATOR_*` 三项，否则协议页 500 |

| ⚠️ **遗留待你确认：`LICENSE` 的版权人仍是真名**（2026-09-14，**未擅自改**） | `LICENSE:3` = `Copyright (c) 2026 <真名>`。它与上面两条同属「一开源就公开个人信息」，但性质不同：开源许可的版权行**本来就要求填权利人**，属正常署名 | 上一轮只批了「两份文书」，LICENSE 不在授权范围内，故**没动**。另：节142 §八 已拍板「发布时换成 Prosperity-3.0.0 全文、版权行届时填权利人」 | 建议方向（待你一句话）：① 保持真名（开源署名常态，可接受）② 换成组织名/笔名 ③ 等发布换 Prosperity 时一并定。**在这之前不动 LICENSE** |

| **🔴 待你拍板：真实域名与运营者联系方式仍留在 git 历史里**（2026-09-14 第二轮复核发现，**未擅自处理**） | 工作区层**已按节147 硬规矩清干净**（以 `.env` 里的真实值当探针实测）：真名 / 邮箱 / 手机 / 域名在 `.env` 之外**零命中**；但两处仍有 —— ① **工作区**：`_archive/` 8 个归档文件仍含真实域名（`Session交接文档.md`、`retrospective/2026-09-10-*`、`台账冻结/{Bug管理表,需求表}-*`、`开发日志.md`、`架构设计-支付系统-金数据.md`、`测试组交接文档.md`、`破竹迁移/AGENTS-太初守则-归档-*.md`）＋**`LICENSE` 含真实姓名**（已单独立项，见下方「LICENSE 的版权人仍是真名」行，同一个人，不重复计）② **git 历史**：真实域名 **27 个 commit**、运营者**真名 3 个 commit**、**邮箱与手机各 2 个 commit** | **为什么只清工作区不够**：`_archive/` 是节147 的**冻结项**（改名不动归档），而「个人基础设施不进仓库」这条硬规矩是**同节后立**的 → **冻结项成了硬规矩的缺口**；更要紧的是**历史层比工作区更硬** —— 光清工作区，`git log -S` 照样翻得出来，**而节142 定的开源时机（节157 之后）一到就公开**。**三个修法**：**甲 · 换域名 + 换联系方式**（随线上迁移一并做）→ 历史里的旧值退化成「一个已废弃的域名」，零历史重写、成本最低，**推荐**；**乙 · `git filter-repo` 重写历史**剔除这些串 → 彻底，但**所有 commit hash 变化**（本地与服务器须重新 clone），属**单向门**，须先做镜像备份；**丙 · 接受它**（内测期不动）→ 但**开源前必须重过一遍本行** | 修甲 → `ops/deploy/README.md` 迁移步骤 + `.env` 三键；修乙 → 全仓库 hash + 服务器部署流程 + 任何已 clone / fork 的副本；修丙 → 无动作，风险记在本行。⚠️ **本条数字曾在第二轮首测时测错一次**（探针被 shell 的 CP936 解码污染 → 真名误记为 0 个 commit），已用正确编码（`[IO.File]::ReadAllLines`）复测并更正；陷阱本身已登记进 `99_状态/已知问题.md` |

| **节148 · 自研内核地基 + 依赖锁定 + 对拍框架**（2026-09-14 落地） | ① 内核语言 JS/TS 定案落地：新建 `backend/paipan-node/paipan-core/`（`type:module`，唯一依赖 `lunar-typescript@1.8.6` 钉死），四段纯函数管线（输入归一化→历法/起局→格局判定→输出装配）+ 单一权威规则源（`rules/` 每能力一个 `Object.freeze` 表）② `mingyu-core` **`^0.2.1`→`0.2.1` 钉死精确版**（`^` 区间上游一旦以 AGPL 发 0.2.2+，`npm install` 会静默拉进 AGPL——来源.md 硬约束落实）③ 对拍框架 `tools/compare/`：旧/新实现同输入字段级 diff，**关键排盘字段 100% 才允许下线旧实现**（母节 DoD 1 落地），golden 快照可离 vendor 回归 ④ 样板能力小六壬走通「新实现→对拍 1144/1144→开关切换」全流程，`server.mjs` 增 `MINGLI_PAIPAN_CORE_XIAOLIUREN`（默认 off 行为零变化）⑤ 对拍黑盒实测历法约定：年干支=立春精确时刻换年、月干支=节气精确时刻换月、日干支=晚子时 23:00 换日、时辰序 13 标签（0 早子/12 晚子）——已写进内核注释与 README §4 | 节128 要求「每能力对拍通过才下线对应 import」需要硬性闸门；vendor 无法自证版本号、npm 无 lockfile 是已知 AGPL 入口风险（来源.md 记过两遍）；历法约定（立春/节气/晚子时换界）正是最容易与 vendor 漂移且最影响排盘正确性的地方，不实测确认等于把口径赌在猜上 | `backend/paipan-node/paipan-core/`（11 文件）+ `server.mjs` 小六壬开关 + `backend/paipan-node/package.json`；后续子节 149–157 照 `paipan-core/README.md` §7 八步接入；`scan-deps.mjs` 输出 18 个待勾销 import 清单 |
| **节149 · 出生时间校正面（N1 均时差 / N2 夏令时 / N3 不确定声明）**（2026-09-14 落地） | ① **单一权威校正面**：`app/paipan/birthtime.py` + `paipan-core` true-solar-time 能力同算法同表（Python↔Node 跨语言 20 组交叉一致），`engine.py` 消费它替换原「仅经度项」内联近似，修正时间喂**所有**下游并对 Node 关内部真太阳时（防二次校正）② **DST 默认开启**（1986–1991 官方表，事实修正）③ EoT 采 **NOAA/Spencer 全式**（锚点最大偏差 0.39 min，旧近似式留 `superseded` 痕）④ **chart.json 契约附加式扩展**：`meta.input_uncertainties` + `calendar.dst_applied`（既有字段语义不变）⑤ 前端 ML_COPY 只放行标签，message 由后端单一源下发 | 节120 N1–N3 是排盘正确性缺口（夏令时窗内时柱可能整体错一位，受影响人群现 35–40 岁）；「真太阳时默认值」仍是单向门——**维持现状默认关**，仅 DST 为事实修正默认开；Spencer 全式因任务书 ≤0.3 分钟精度要求而选定（3 个零点日锚点按 ±2 天内过零口径） | `engine.py` / `birthtime.py` / `tests/unit/test_birthtime.py`（64 例）/ `tests/fixtures/chart.json`（重生成，DST 窗内出生快照时柱前移 1 小时）/ `paipan-core` true-solar-time 4 文件 + crosscheck / `ui-copy.js` + `views-cases.js`（N3 展示）。⚠️ **仅影响新盘，历史盘面不重算**（节120 禁碰） |
| **节157 · 塔罗/雷诺曼自研收口（抽牌内核 + 牌阵唯一来源 + 自撰牌义）**（2026-09-14 落地） | ① **PRNG 完全反推**（黑盒探针）：随机源 = `mulberry32(FNV-1a-32(String(seed)))`（FNV 逐 UTF-16 码元）+ Fisher-Yates 降序洗牌 + 自牌顶连续取牌 + 逐张独立正逆位抽样；样本消耗数与 `meta.random.samples` 与旧实现逐值相等 → 同 seed 全牌阵 100% 复现 ② **牌阵唯一来源**：内核 TAROT_SPREADS（19 阵含产品口径：四牌四季/凯尔特十字/七张马蹄/六芒星）+ LENORMAND_SPREADS；`mingli-tarot-spreads.mjs` monkey-patch 退役（文件保留作对拍基准适配器 `legacy-tarot.mjs` 用）；对拍基准 = vendor+覆盖层（与生产装配一致）③ **牌义正文自撰**（78+36，花色元素×牌阶两维主题从零生成；首轮 6 处与旧实现撞词已全改写，**零逐字重合**复核通过）④ **server.mjs 切换**：/tarot 与 lenormand 移除 4 个 vendor import，缺 seed 由 `ensureSeed`（crypto randomInt）注入——既满足内核确定性要求又保持随机体验 ⑤ ⚠️ **更正节157 原前提**：CONTENT 的 cards-zh.js 实测只有牌名/图，牌义正文实际在后端响应 → 内核自带正文，前端零改动 | 用户核心要求「内核独立于 mingyu、可忽略其 license」：塔罗/雷诺曼是数据+随机机制类能力，最干净的形态就是「自研随机源 + 自研牌阵 + 自撰正文」三件套全部收进内核；PRNG 若不反推则同 seed 无法 100% 复现（历史记录复盘/测试都依赖它）；牌义不重写则正文仍是 vendor 文本 | `server.mjs` / `paipan-core`（tarot/lenormand rules×4 + capabilities×2 + fixtures+golden + tests×2）/ `run-compare.mjs`（legacyModule 支持 + tarot 适配器）/ `tests/unit/test_tarot.py`（raw 契约改指内核）。遗留：大桌传统固定组合表未重建（信息性差异 21 条，大桌前端不消费） |

## 用户偏好（破竹 v0.26 · 三层升级路径之 ① 项目层）

> 载体 = 本文件（已有文件，不新建）；来源 = Agent 各节收尾观察到的用户偏好；生效 = 下一节细化时。
> 升级判据：①→②（库层）= 第 2 个项目也出现；②→③（协议层）= ≥2 项目验证 **且用户明确说"这是我一贯的要求"**。

| 观察到的偏好 | 证据来源 |
|---|---|
| **结论先行 + 大白话**（"改了什么 → 证据 → 需要你决定什么"），不要过程堆砌 | 用户明确表示"不懂代码和结构"，多次要求大白话 |
| **偏好多步骤但可回退的方案** > 一次到位 | 节110 选"方案②渐进切片"而非一把拆 |
| **倾向直接执行、少确认**（给指令即动手） | "先 push，再部署"式连续指令 |
| **重视成本 / 资源**（服务器压力、token、镜像体积） | 主动要求"降低服务器压力"；关注 2核1G 构建锁死 |
| **严守边界**：说"不要执行任何内容"时只出方案 | 拆分方案那次明确要求"只指出方案，不执行" |
| **凡会加重服务器负担的优化一律不做**（内存 / CPU / 后台任务） | 组C 重评估时明确："我觉得按照这个方向走，所有会加重服务器负担的项目都不做"；主动关注 2核1G 构建锁死、镜像体积 |
| **外部参考目录不做长期依赖**：决定删除前，要求先把「可借鉴内容」与「决定不做 + 原因」全部落盘进项目 | "你把这些需要做进命理项目中的内容放到节里面以后，我就把整个 reference 的文件夹删掉" |
| **要数字不要感觉**：先取数再定方向，拒绝"我觉得会更好" | 多次要求"先取数，再定"；组C 要求"可以再评估一下吗"之后接受的是带实测行数/内存数字的结论 |
| **Git 只 commit，push 必须等用户确认**（网络不稳，避免反复失败浪费往返） | 用户 2026-09-11 明确指示："你对本地文件的改动**不用每一次都直接 push 到 GitHub 上面**，因为网络的问题，**你要 push 的时候需要我来确定是否要 push**；正常来说，**只要确定的东西 commit 就好**。**本地文件所有内容都 commit 的状态就是 everything up to date**" |
