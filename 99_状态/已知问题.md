## 已知问题 / 环境陷阱（不许重复踩）

> 下个 session 直接生效；踩到先查本表再上报，不重复上报（历史来源 `_archive/破竹迁移/测试知识库-已废弃-测试组角色.md` §3，已归档）。

| 现象 | 根因 / 规则 | 落地检查项 |
|---|---|---|
| git status 显示 backend/packages/ 大面积删除（D） | vendored 依赖目录，删除是已知噪声 | 只 add 本任务显式 pathspec，绝不 git add -A；勿提交该类改动 |
| git status 显示 `backend/pyproject.toml` / `backend/paipan-node/package.json` 为 D（工作区无实体文件） | **⚠️ 此行是 Agent 的推断，不是台账原有记录**（2026-09-11 补）：文件被 git 追踪、工作区无实体 → 显示为 D。**无论成因如何，结论是「无需恢复」** | **部署不受影响**（已核实）：部署在服务器走 `git fetch + reset --hard`，会从 HEAD 恢复所有被追踪文件 → Dockerfile L19-20 的 `COPY` 正常；本地测试一律用**系统 Python**。若确需本地 `npm install`，先 `git checkout -- backend/paipan-node/package.json`。**⚠️ 报「部署会被打断」这类结论前必须先查本表**——2026-09-11 曾因只看代码与 Dockerfile、未查本表而误报一次，白花用户一轮往返 |
| git 盘点报 .tmp_pytest/ 权限拒绝（permission denied / 拒绝访问） | 无读权限的测试临时目录 | 属测试噪声，忽略并跳过该目录 |
| workspace-write 沙箱跑 pytest 大面积报 sqlite3.OperationalError: unable to open database file（99 个 setup error） | tests/conftest.py 用 tempfile.mkdtemp() 建 0o700 临时目录，沙箱拒绝在其中建 SQLite 文件——沙箱限制，不是项目 bug | pytest 与 run_regression.py 一律在 danger-full-access 下执行 |
| test_paipan.py::test_fixture_snapshot_anchor 失败 | 既有夹具漂移：western.summary.patterns[0] 中 / 英文案差异（非引擎 bug） | 勿当回归报；确认输出正确后重生成快照或标注跳过；dev 改星盘翻译（BUG-007 / b61f4e4）后可能触及该基线 |
| `*_node_endpoint_*` 偶发 RemoteProtocolError / WinError 10054 | node v24 + Windows 特定交互问题；生产镜像 node v22.14.0 无此问题 | 标记跳过或用目标版本复验，不当 Bug 报 |
| mingyu-core 探测报 MISSING | ESM-only 包（exports 只提供 import），CJS require.resolve 误报 | 用 import(pathToFileURL(...)) 探测（已固化进 env_doctor.py） |
| git status 显示 backend/paipan-node/server.mjs、vendor/ 下文件为 M | 沙箱 stat 噪声（git hash-object 与 HEAD 一致，无真实差异） | 用 git diff --stat 复核，勿误提交 |
| 某测试单跑绿、混跑红 | app.config.settings 是模块级单例，首次 import 即定格；模块顶层 os.environ 注入受收集顺序影响（integration 先于 unit） | 依赖 settings 的测试用 autouse fixture + monkeypatch.setattr(settings, ...) 注入，禁用模块顶层 os.environ |
| admin 测试 PUT / rollback 真实改写 backend/prompts/*.md | 写盘动作未隔离会污染真实 prompt，直接影响线上 LLM 输出 | monkeypatch app.admin.router.PROMPT_BASE 到 tmp_path 并预置测试文件 |
| 断言「资源不存在」按 REST 直觉断 HTTP 404 失败 | 错误码 ≠ HTTP 状态：1xxx（1002 不存在）→400、2xxx→401/403、3xxx case→404、4xxx 排盘→422、5xxx LLM / 积分→502 | 按 body code 断言（如 resp.json()["code"] == 1002） |
| backend/.venv 里没有 pytest | 依赖装在系统 Python（Python313） | 测试用 `cd backend && python -m pytest -q`（系统 python）；体检 python backend/scripts/env_doctor.py |
| 镜像 / 环境缺 Node 22 | 容器必须同时含 Python 3.13 + Node 22（排盘引擎双运行时硬约束） | 部署验证镜像双运行时；MINGLI_NODE_BIN 缺省走 PATH |
| MINGLI_JWT_SECRET 落盘 / 泄露 | HS256 对称密钥，泄露即全局可伪造（ADR-0008） | 随机生成 ≥32 字节、各环境不同值、走平台密钥管理不落文件；泄露即全量失效重登 |
| 环境变量改了但不同步 | 唯一来源 ops/env/.env.example，新增变量须同步：.env.example → 06-环境与部署说明 → 03-接口与数据字典 §7 | 新增 / 修改变量时三处同步（MINGLI_VALIDATION_ENABLED / MINGLI_JOB_TIMEOUT_* 等已有约定） |
| zodiac 用例曾断言 noble is None | BUG-003 修复（B2 回填）后 noble 永不为空、顶层新增 zodiacWuxing（keys 16→17） | 已随修复同步 test_zodiac.py，断言回填值「三合：猴、龙；六合：牛」；勿再按旧结构断言 |
| 中文文件经 shell 整文件覆写后内容损坏（README 混入残留源码、个别字符被吞） | pwsh 命令行传递中文/特殊字符（如 here-string）编码不可靠，污染写入内容 | **中文 / UTF-8 文件只用受控编辑工具（edit/write）或显式 UTF-8 编码写入，禁用 shell 整文件覆写**；写后 read 抽查首尾与特殊字符（↔/反引号等） |
| standards 版本记录编号引用滞后（原 AGENTS.md 写 `standards/05-版本迭代记录.md`，实为 `07`；05 = 前端组件库规范） | 文档编号变更未同步更新引用处 | 引用规范文件时先 grep 实际文件名核对再写；已在本项目 00_根/导航.md §4 修正为 07 |
| 截图不是机制（破竹 v0.9） | 验收对象 = 真实环境（用户手动操作）；截图只在**用户明确要求**时一次性做；禁止 Agent 主动截图 / 无头 CLI 自我验收 / 为截图拉起浏览器 | 节100 走查曾主动截 10 张（费请求，新规下仅用户要求才截）；版本对比用 git 切换（用户说"看看上一版"） |
| 编辑工具写 .md 偶发 `ReplaceFileW EIO (Win32 1175)` | 原子替换文件（ReplaceFileW）遇瞬时 I/O 错误，疑似 OneDrive 同步 / 杀软扫描 Documents 目录干扰（节110 拆 index.html 时反复遇到） | 重试 1–2 次即恢复；仍失败换更短的 old_string；避免并发编辑同一文件（节110 曾因并发编辑导航.md 触发） |
| 「我已知道类型，直接输入」**只拉文案、不落库** → 手输类型的用户进「配对解析」会报「未测 MBTI」 | **实测（2026-09-11）**：`frontend/public/js/views-mbti.js` L327-343 `pickType()` 只做 `setResult({id:null, type:t, scores:null, mode:'manual'})` + 一次 `GET /api/mbti/types/{code}`，**全程无写入**；后端**只有判型路径**会写 `case.mbti_type`（`backend/app/api/mbti.py` L192）→ 手输**不产生任何持久数据** | ⚠️ **用户记忆里"手输类型即可跳过做题直接匹配"与实现不符**（`api/pair.py` L203-230 会回 400，`test_pair.py` L444-452 有断言）。**本条不单独修** —— 由 `节122`/`节125` 重做该模块时一并修通（需新增"保存"动作）；**在此之前不要把它当 Bug 单独立项** |
| **误判"某文献不存在"**（2026-09-11 实际发生，Agent 踩坑） | 🔑 **学术数据库检索落空 ≠ 文献不存在** —— **厂商白皮书 / 技术报告 / 学位论文 / 内部手册都不进 Crossref / OpenAlex / Semantic Scholar 等学术索引**。Agent 与独立核查员各自检索六条路径全落空（含中英文多轮），据此误判 **Arneson (2016)** 不存在；用户提供出处（**CPP 白皮书**）后核实**成立** | **下"不存在"这类结论前必须多走一步：去权利人 / 出版方官网找**（本次该文就在**权利人自己的 MBTI Facts 页**上，且权利人自称其为 meta-analysis）。**对用户措辞也要降级**：说"**我在学术库里没检到**"，不要说"**它不存在**" |
| `web_fetch` 取长页面**在固定长度处截断**（同一 URL 两次抓取返回**字节级相同**的截断点） | 工具侧固定长度限制 —— **不是页面不存在、不是要注册/付费** | ⚠️ **"工具拿不到" ≠ "数据拿不到"，先换通道再下结论**。2026-09-11 实测：IPIP 官方中文 120 题页只取到 **33/120**；**改用直连下载**（`Invoke-WebRequest -Uri … -OutFile …`，需 `danger-full-access`）拿到**完整 175,394 字节 / 121 个 `<tr>`（表头 + 120 题）**。→ 遇到"只取到一部分"：**直连下载 / 浏览器另存 / curl 落盘**，不要在"截断"上花多轮 |
| `git mv` 之后提交**漏掉正文改动**（`git status` 显示 `RM` 而非 `R`） | `git mv` 把 **HEAD 的旧内容**登记到新路径；**移动之前**对该文件做的编辑**仍是"未暂存"** → `git diff --cached` 会显示**100% 相似度的纯重命名（0 行改动）**，**看起来完全正常** | ⚠️ 2026-09-11 实测踩到一次（节107/108/109 移入 `40_节/完成/`）：**首次提交正文没进去**，靠 `git status` 的 `RM` 才发现。→ **规则：移动文件后必须重新 `git add` 新路径**，并核对 `git diff --cached --stat` 里**相似度不是 100%**（"重命名 + 改内容"应当 < 100%，本次修正后为 50%） |
| `40_节/待办/` 下出现**两个以同一节号开头的文件**（如 `节122-xxx.md` + `节122-yyy.md`） | 把节的支持材料（工单 / 登记表 / 数据）**用节号命名成独立 .md** —— 这违反破竹「**一节 = 一份节文件**」+ 铁律 2（单一事实源） | ⚠️ 2026-09-11 实测发生（节122：主节 + "题库中文文案需求.md" 两个文件都挂了节号）。→ **规则：一个节号只能有一个 `.md` 节文件**；**支持材料一律下沉进「`节NNN-附件/` 目录」**（目录不是"第二个节"），工单/数据/登记表都放进去；**附件文件本身不要再带节号前缀** |
| 编辑时**拿已有的一行当"插入锚点"，却没把那一行写回 `new_string`** → **那一行被吃掉**（标题、列表项都踩过） | Agent 习惯用"最有辨识度的一行"当 `old_string`；**替换 ≠ 插入** —— 若 `new_string` 里不含原行，原行就消失了。Markdown 不会报错，**肉眼也容易漏**（尤其长文件） | ⚠️ **2026-09-11 同一天内连犯 6 次**（节122 标题 / 翻译工单标题 / 节126 标题两处 / `入口.md` 的 `节118` 行两次）→ **规则：`old_string` 里出现的每一行"结构行"（标题、列表项、表格行），`new_string` 必须原样含回**（插在开头或结尾）→ **落地检查：编辑后用 `grep "^#"` 复查标题层级、并 `grep` 复查被当锚点的那行是否仍在** |
| `40_节/待办/节122-附件/` 里出现**非本会话产出**的文件（`apply_translations.py` / `backup-originals/` / 已填 `text_zh` 的 JSON / 层面术语表 / 需替换题项清单） | **并行会话或用户在按 `题库中文文案需求.md` 工单做翻译** —— 属正常协作，**不是异常**（原会话据此误报过"文件不属于我"的疑问） | ⚠️ 2026-09-11 实测：Agent 用**目录级 pathspec**（`git add -- "40_节/待办"`）提交 → **把这些一起扫进了自己的提交**。→ **规则：即使不用 `git add -A`，目录路径对那个目录也等效于 `-A`；一律用显式文件路径**。→ 已 `reset --soft` 拆开、对方文件原样还原 |
| `test_tarot.py::test_api_tarot_draw_bad_spread_type_400` **恒红**（2026-09-14 节137 回归时发现） | **陈旧用例**：REQ-122 已把 `celtic/horseshoe/hexagram/fourSeasons` 加进 `app/api/tarot.py` 的 `TAROT_SPREAD_TYPES`（L45-49），用例仍拿 `celtic` 当"非法牌阵"断言 400 → 实测返回 200（Node 端 `mingli-tarot-spreads.mjs` 确有 celtic 覆盖层），**不是产品 bug** | **勿当回归报**；修法：把用例非法值换成真正未开放的值（如 `chakra`）并同步注释。**尚未修**（归属：随任一小改节点顺手修或单独立微改，需用户点一句） |
| **多会话在同一工作区协作**（2026-09-11/12 夜间实测） | **两个 DSH 会话同时改同一个项目**：一方在**写代码**（实测 `synthesizer.py` +262 / `scorer.py` +130 / **新文件 `marker.py`** / `memory/service.py` +161 …），另一方在改登记文档 → 会导致：① **同一文件交错编辑** ② 一方把节**搬进 `完成/`**（用文件移动而非 `git mv` → 呈 `D` + `??` 两个条目）③ **"工作区干净"这个断言随时失效** | **落地纪律（六条）**：① **编辑前必须重读**；⚠️ **"file changed since it was read" 不是报错，是"另一个会话刚写过"** —— 2026-09-11 实测正是它拦下 4 处编辑，否则会覆盖对方改动 ② **被拦下就重新读，不要重试同一锚点** ③ **提交只用显式文件路径**（目录级 `add` 会把对方在制品一起提交）④ 🚫 **绝不 `git reset --hard` / `checkout .` / `clean`**（会毁掉对方在制品）⑤ **汇报"工作区干净"前再查一次 `git status`** —— **本会话刚被这条打脸**：上一轮确认干净，几分钟后就冒出 650 行新改动 ⑥ **发现对方在改同一文件或同一主题时：先报告用户，不争抢**（例：一方在 `节119` 写"决定3 仍待裁决"，而事实是**已裁决** → 应**加更正框、不删原文**，并请用户协调） |
| 免构建前端（纯 `<script>` + 全局变量）**语法错误可长期潜伏、整文件静默失效**（节105 阶段3+4 提交 9c960df 引入，节135 阶段G 才发现并修复，2026-09-12） | 批量替换 UI 文案键时埋了 6 处语法错误：5 处对象字面量计算属性漏方括号（`UI_COPY.x.y: v` 应为 `[UI_COPY.x.y]: v`，见 glossary-zh.js×3 / views-zodiac.js×2）+ ui-copy.js 丢失 `onboarding: {` 模块声明行（ML_COPY 括号失衡，整个 ML_COPY 未定义、建档页文案全失效）。另有**架构时序缺陷**：`const UI_COPY = ML_COPY.ui` 原定义在 index.html 主引导内联脚本（L75），晚于 L39–L55 的数据/业务脚本；顶层常量（DIVIN_METHODS、PAIR_REL_ICONS、glossary/hexagrams 计算键等 7 文件）加载时即解引用 → ReferenceError 使整个脚本文件不执行 | 改动数据/业务脚本后**必做三道**：① 全量 `node --check`（js/ + data/ + data/content/，当前 26 文件）——浏览器对经典脚本 SyntaxError 只报一条、整文件不执行，免构建无构建期拦截；② 浏览器用**全新 URL 新标签**验 console（旧标签 console 消息跨导航累积、html 还可能被缓存，会误判修复无效）；③ 验证顶层常量/组件 typeof 真实存在（语法过了≠顶层运行时过）。**铁规则：脚本顶层引用的全局别名必须在更早的 `<script>` 中定义**——UI_COPY 已提前为 ui-copy.js 之后的独立内联脚本（节135） |

| `frontend/scripts/smoke-check.js` 的「关键全局变量存在性」**恒红一项**：`✗ 缺失 toast()`（2026-09-14 节145 回归时发现） | **陈旧门禁**：节110 把 `toast()` 从 index.html 拆到 `components.js`，而 smoke-check 只在 `index.html` 里搜 `/function\s+toast\s*\(/`（L108）→ **不是回归、不是产品 bug**（其余 8 项通过，`node --check` 与 `precompile` 两个真门禁全绿） | **勿当回归报**；修法：把该检查改为「index.html + 拆出的 js/ 合并搜索」或按实际归属文件查。**尚未修**（归属：随任一小改节点顺手修，或单独立微改——出口是 `01` §7 裁决） |
| 本表旧条「`test_tarot_draw_bad_spread_type_400` **尚未修**」**与事实矛盾** | 该用例**已于 2026-09-14 修好**（改用确实未开放的 `notASpread`，`test_tarot.py` 16 passed），旧行未同步 → **同一事实两处说法不一致（铁律 2 缺陷）** | 本行即更正指针：**以 `00_根/入口.md` 交接状态与 git log 为准（已修好）**；上方旧行的「尚未修」作废（按本表「只追加、不改写历史条目」的体例，不删旧行） |

| **改名/换前缀类工程必答两问**（2026-09-14 节145+147 连做两节实测） | ① **「新串是否包含旧串」** —— `太初`→`命理太初` 会让**已经叫「命理太初」的地方二次前缀**成 `命理命理太初`（实测 3 处）；`taichu`→`mingli` 天然无此问题 ② **「新串是否与既有名词撞名」** —— 新代号 `mingli` 与**旧参考项目同名**，机械替换会把「参考项目 mingli」也变成「本项目 mingli」（实测 29 处需消歧） | 机械替换必须**按「先特异、后通用」排序**；第一问为「是」时要在规则表里把新串也列成保护项；第二问为「是」时用**前后瞻守卫 + 文件白名单 + 显式保护真实标识**（本次保护了 `high-confidence-mingli-skill-main`、`wumohan1814/mingli`、`mingli-*.json`）。**替换完必须单独核对冻结项的磁盘真实名**（本次回退 11 处） |
| **枚举待改标识不能只 grep 项目代号**（2026-09-14 节147 实测） | 只 `grep taichu` 会**漏掉** `TC_COPY`（大写缩写前缀）与 `shallowEqTc`（词尾缩写）—— 这两族合计 **417 处**，占本节改动量的三分之一 | 枚举要**扩散式**：先抓前缀（`\bTC`、`taichu_`、`--taichu-`、`Taichu`）→ 再抓「包含该片段的驼峰标识」（`[A-Za-z_]*Tc[A-Za-z0-9_]*` 等）→ 逐一判定，并把判定结果写进节文件（本次 10 个 `tc*` + 8 个 `*Tc*` 逐个列出） |
| **机械替换会污染「已删除」的历史说明**（2026-09-14 节147 实测） | 节146 已删的 `TAICHU_JINSHUJU_*` 有若干条「已整组删除」说明；直接做 `TAICHU_`→`MINGLI_` 会造出**从未存在过**的变量名 `MINGLI_JINSHUJU_*` | 对「已废弃标识」的名字，**改为去前缀的中文措辞**（本次改为「`jinshuju_*` 配置」），不要机械换前缀 |

## 框架反馈（破竹 v0.25 · 触发式：只在框架"实际绊到"时记）

> 四条触发：① 规则显得多余 ② 规则无法执行 ③ 花了不该花的代价 ④ 框架可以更简单。
> 处理出口：`01` §7.4 复盘表（转协议改进 / 是噪音 / 已解决）。首次实例期间只记不改。

| # | 触发 | 现象 | 建议 |
|---|---|---|---|
| F1 | ③ 花了不该花的代价 | 协议在**首次实例执行期间被反复修改**（v0.4 → v0.27，13+ 次），实例需多次重新对齐（重读 + 改文档 + 重排）——`00` §8 的「首次实例执行期间不改协议」与「确需当场改走正式流程」二者并存，实际发生的是框架频繁改、实例反复跟 | 把首次实例期间的协议变更**批量攒到实例收尾一次性对齐**，或明确启用 `00` §8 已有的"项目实例锁定版本"选项 |
| F2 | ② 无法执行（需额外代价） | 破竹框架位于项目工作区之外（`../Ratoon-破竹/`）：读协议无碍，但「库登记 / 归档」需**跨工作区写入** → 受限沙箱下需提权才能完成 | 框架可考虑允许 `库/` 落在工作区内，或给出明确的跨目录写入约定 |
| F3 | ② 无法执行（曾自相矛盾） | `00_协议.md` 头部版本号曾长期滞后于内容（写作 v0.4、内容已含 v0.5–v0.9），"版本号在文件头"这条规则当时无法自证 | **已解决**（v0.27 定为"版本号 = 99 最高条目一致"）——记为已解决 |
| **F4** | **③ 花了不该花的代价**（**是"事实纪律"缺一条，不是规则多余**） | 铁律 1 要求「**事实归 Agent，能查的一律自己查**」，但**没提示"查不到"本身也会被误当成事实**。2026-09-11 实测：Agent 与独立核查员**各自**检索学术库与搜索引擎（Crossref / OpenAlex / Semantic Scholar 等**六条路径** + 中英文多轮）**全部落空** → 于是把「**我没检到**」写成了结论「**它不存在**」→ **误判 Arneson (2016)**（实为 **CPP 白皮书，不进学术索引**，且就挂在**权利人自己的官网**上）。同源失误：`web_fetch` 的**固定长度截断**也曾被当作"页面内容就这么多"（实际只取到 33/120 题） | 建议在破竹 **Agent 自查条**（`00` §1 末尾）补一条：**「"我没查到" ≠ "它不存在"」—— 下"不存在 / 拿不到"类结论前，必须先换通道或换信源**（去权利人 / 出版方官网 · 直连下载而非受限抓取 · 换匹配方式如按内容而非按编号），**并且对用户把措辞降级为"我在 X 里没检到"** |
| F5 | ② 规则无法自证（自身超限） | `00_协议.md` v0.37 实测 **205 行 > 自定硬上限 200 行**（2026-09-12 节135 握手时按铁律 5 顺手点数发现）；按 §8 首次实例执行期间只记不改 | 实例收尾时统一评估：把「附：目录总览」等挪入按需文档，恢复 ≤200；在那之前以本条留痕为准。**✅ 2026-09-14 节137 复测：197 行，已回到 ≤200**（v0.37 未变，属内容精简而非新版本） |
| **F6** | **② 规则无落点**（表体不在必读索引里） | 本文件开篇写着「**踩到先查本表再上报**」，但 `00_协议.md` §3 读取索引把「关键决定 / 已知问题」列为**"推翻旧决定 / 细化、结构反思时"**才读 —— **不在每轮必读**。**2026-09-14 节137 实测踩到**：Agent 在 workspace-write 下跑 pytest 撞 `sqlite3.OperationalError: unable to open database file`，自行诊断 + 申请提权花掉一次往返，之后才看到表里**第 10 行早已登记同一现象**（"一律在 danger-full-access 下执行"） | 建议把本文件的**表体**纳入每轮开工必读（或允许把"环境陷阱"子集注入项目 `AGENTS.md`）。否则该表只能**事后解释**、无法**事前避坑**——它的价值恰好在于"下次不再踩" |
