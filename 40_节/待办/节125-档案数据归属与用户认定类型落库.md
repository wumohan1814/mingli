# 节 125 · 档案数据归属与"用户认定类型"落库（IPIP 大五 · 第 3 步）

状态：🟡 **待办 · 未开工**（可与节123 并行，只加写入路径、不改计分）｜ 强度：**中-大改**（新增写入路径 + 档案页语义变更）｜ 日期：2026-09-11

> **来源**：用户 2026-09-11 **第二轮补充需求** → `40_节/待办/节122-MBTI题库替换为大五人格与商标合规.md`「用户第二轮补充需求」+ §六「节125」。
> ⚠️ **详细方案与复述不在本节重复**：**开工前先完整读节122**。本节只写**范围 + 前置 + 禁碰**。

## 用户要的是什么（大白话复述 · 定稿）
> **档案里的人格数据分成两笔账**：
> - **平台只负责算大五（5 个维度分）** —— 这是"有据可依"的那一半，**存进档案**
> - **四字母降级为"对照参考"** —— 平台算出的只是**建议**；用户点了"保存"或自己手输之后才进档案，而**档案里那一行的语义是「用户自己填的」**

## 目标（可观察的验收标准）
1. **修通现有堵塞**：手输类型 → 落库 → 去「配对解析」**不再报「未测 MBTI」**
2. 结果页出现**"采纳"动作**："按大五参考换算是 INTP —— 要保存为你的类型吗？"
3. 档案页**两条账分开展示**：① 人格类型（**用户认定**，保留表格形态）② **OCEAN 五维**（平台产出，新增）
4. 旧数据**被标为「旧版平台判型」**、只读保留，**不被静默当成"用户自填"**

## 范围（动哪些文件 · 已实测定位）
| 文件 | 改什么 |
|---|---|
| `backend/app/api/mbti.py` | **新增"保存类型"写入路径**（今天只有判型路径 L192 会写 `case.mbti_type`）；给"人格类型"加**来源语义** |
| `backend/app/api/pair.py` | `_load_mbti_profile()`（L203-230）**改用大五五维**；报错文案「未测 MBTI」随之调整 |
| `backend/app/profile/archive.py` | `_get_mbti()`（L106-119）→ 两条账分开返回 |
| `frontend/public/js/views-mbti.js` | 结果页加"采纳/保存"；手输（`pickType` L327-343）后**真正落库** |
| `frontend/public/js/views-cases.js` | 档案页两条账分开展示 + 来源标注 |
| `prompts/pair/mbti.md` | 配对解析提示词改为**吃大五五维**（用户在 §七 已裁决"改用大五五维，四字母只做展示"） |
| `backend/tests/unit/test_pair.py` | L444-452 的「未测 MBTI」断言随语义调整 |

## 前置条件
1. 🟠 **节123 完成**（需要新的 `scores` 形状与五维数据）
2. ✅ 用户已裁决：档案「人格类型」**只存用户认定过的值**（甲案）
3. ✅ 用户已裁决：配对解析**改用大五五维**
4. ✅ 用户已裁决：手输**限 16 型枚举**（不做自由文本）

## 🚨 必须先修好的现实（**实测发现，与用户记忆不符**）
> 现有「我已知道类型，直接输入」**是堵的**：`views-mbti.js` L327-343 `pickType()` 只 `setResult({id:null,…,mode:'manual'})` + 拉一次文案，**全程不落库**；后端只有判型路径写 `case.mbti_type`。
> → **只手输没做测试的用户，在「配对解析」里会拿到 `未测 MBTI`**（`api/pair.py` L203-230 + `test_pair.py` L444-452 有断言）。
> **用户新需求的 3(a)/3(b) 恰好把这条路修通** —— 本节的核心动作之一就是这个。已记入 `99_状态/已知问题.md`（标注"不单独修，由本节一并修通"）。

## 禁碰清单
- 🔒 **零 DDL 优先**：复用 `mbti_results` 已有的 JSON 列承载来源标记；**确需给 `cases` 加列必须先回头找用户**
- 🔒 **不得静默改写旧数据语义**（现有平台判型值不得被当成"用户自填"）
- 🔒 **不得把四字母当成平台"测出来的结果"输出** —— 档案里那条**只能是用户认定过的值**（这一条同时是产品语义和合规防线）
- 🔒 手输**限 16 型枚举**，`String(8)` 放得下

## 用户结论
待验收
