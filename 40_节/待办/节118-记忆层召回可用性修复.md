# 节 118 · 记忆层「召回可用性」修复（零新增算力）

状态：🟡 待办 ｜ 强度：**中改**（不动表结构、不新增依赖、不新增后台任务；只改查询表达与打分）｜ 日期：2026-09-11

## 用户原话
"我觉得按照这个方向走，所有会加重服务器负担的项目都不做。但是，你可以看一下整个记忆有没有正确的优化方向，把这个优化方向写成一个结文件。"

## 前置前提（用户给定 · 直接决定本节取舍）
1. **项目尚未上线** → 线上 `agent_memories=0` 是"没人用过"，不是 bug；记忆效果**只能靠离线基准检验**，不能靠线上数据。
2. **凡是会加重服务器负担的项目一律不做**（本地向量化、后台线程/定时器、请求路径上的重计算）。

## 我的复述
记忆层的问题**不是"算得不够多"**（缺语义、缺向量），而是**"已经免费拿到的信号没被读取"＋"检索表达方式结构性失效"**。所以正确的优化方向是**零新增算力的召回质量修复**：修查询表达、修打分公式、修写入侧质量、建离线基准。全程不新增依赖、不新增常驻内存、不新增后台任务。

## 期望形态（验收标准 · 用户可观察）
- 有**一个离线基准**（不联网、不依赖向量）给出恢复前后对比：命中率 + 延迟 p50/p95 + 单次召回查询次数
- **短问（≤2 字）与自然问句都能命中相关记忆**，而不是永远退化成"按重要度+新鲜度取"
- 记忆块里出现的条目**与当前问题相关**（人工抽查可判断）
- **线上资源零新增**：不新增依赖、不新增常驻内存、不新增后台任务；单次召回查询次数不增或仅微增（**须给实测数字**）

## 覆盖声明（v0.21）
- 本节点证明：现状 BM25 召回**结构性地近乎不命中**（已本地复测，见 §取数③）；且多条**已入库信号从未被读取**（已逐行核对 `service.py`）
- 本节点未证明：修复后的**实际收益数字**（未上线无线上数据，须先建基准再取数）；也未证明"记忆对最终解读质量的贡献有多大"（属另一问题，本节不涉）

## 取数（Agent 用 · 已复测，2026-09-11）

### ① 服务器余量（生产机实测）
| 项 | 值 |
|---|---|
| 内存 | total 894MB，**available 仅 226MB** ｜ swap 1024MB **已用 0** |
| CPU | 2 核，load average `0.14 / 0.06 / 0.04` |
| 磁盘 | 30G 用 66%，剩 9.8G |
| 我们的容器 | `app-web-1` 156.4MiB ｜ `app-caddy-1` 20.4MiB |
| 其余内存 | 阿里云 agent 群 ~94MB（loongcollector 59 + AliYunDun 19 + Monitor 16）+ dockerd 60 + tailscaled 40 + containerd 28 等 |
| 容器内依赖 | `sqlite_vec / numpy / jieba / torch / onnxruntime` **全部未装**；`httpx` 已装；sqlite 3.46.1 |

→ **226MB 是唯一余量**。本地向量化（`import torch` + 模型，估计 +400~650MB）**否决**（见 §明确不做）。

### ② 线上数据规模（生产库实测）
```
taichu_analytics.db 3.66MB   agent_memories 0 / agent_memories_fts 0 / agent_memory_tasks 0 / agent_messages 0
                             users 3 / divinations 48 / conversations 4
taichu_ops.db 672KB          events 2866
taichu_feedback.db 20KB      feedbacks 0
user_settings.agent_enabled = 1（默认开）
```
→ 未上线、零线上数据；**任何"效果"判断必须走离线基准**。

### ③ 复测：BM25 召回表达方式（本地 SQLite 3.50.4 / FTS5，DDL 与线上一致 `tokenize='trigram'`）
| 查询表达 | 命中 |
|---|---|
| **现状**：整句短语 `"我今年财运怎么样"` | **0** |
| **改法**：3-gram OR `"我今年" OR "今年财" OR "年财运" …` | **1** ✅ |
| 2 字词项 `"财运"`（加引号 / 不加引号） | **0 / 0** |
| 2 字短问 婚姻 / 财运 / 事业 / 什么 | **全 0** |
| 无关问句 `"我想知道我什么时候能结婚"` | 0（正确的 0，无对应记忆） |

→ **结论**：`trigram` 下**短语查询 = 要求记忆原文包含用户原句的连续子串**；而 **<3 字的词项根本无法匹配**（与 `service.py` L368 `len(eff_query) >= 3` 的门槛同源）。因此线上实际生效的通道**几乎恒为 recency** —— 即"按重要度+新鲜度取记忆"，与当前问题无关。**这是"记忆看起来有、其实没用"的结构性根因。**

## 现状坐标（已逐行核实 `backend/app/memory/service.py`，1053 行）

### 已具备 · 不需重做
| 能力 | 位置 |
|---|---|
| 时间衰减 `exp(-age/τ)`，τ=30 天 | L290 |
| 混合分 `rel + 0.35·importance·fresh` | L389 |
| 自适应清理（180 天低价值软删 → 再 30 天物理清除 → WAL checkpoint） | L993 |
| 上限驱逐（`MAX_FACTS_PER_USER=300` 强制） | L692 / L632 |
| 精确去重 + 刷新（`content_hash` 唯一约束） | L673-691 |
| token 预算自裁（1200） | L397-408 |
| 抽取队列：5s 轮询 asyncio 协程 + 3 次指数退避 + pending≥500 背压 | L924-954 / L62 |
| 召回埋点（channel / hits / elapsed_ms） | L423 |
| 「异常静默返回 `""`」的降级语义（记忆是增强不是依赖） | L351 / L376-384 |

### 缺口 · 本节要修
| # | 缺口 | 位置 | 性质 |
|---|---|---|---|
| **R1** | **整句当短语 → 结构性不命中** | L293-295 `_fts_quote` + L309 | 检索表达（**根因**） |
| R2 | <3 字查询无 BM25，直接退化 recency | L368 / L381-384 | 召回兜底口径 |
| R3 | 双通道**二选一**（bm25 XOR recency），不是融合 | L370-393 | 打分结构 |
| R4 | `tags_json` 由 LLM 免费产出、已入库，**检索从不使用** | L126 / L704 / L303-314 | **免费信号被丢弃** |
| R5 | `trust` 列写入并维护，**打分从不读取** | L684-685 / L389 | **免费信号被丢弃** |
| R6 | `access_count` 只用于驱逐，**不进召回打分** | L649 / L389 | **免费信号被丢弃** |
| R7 | `importance` 由 LLM 自由给（0~1），无校准/钳制 | L124 | 打分噪声源 |
| R8 | 无任何基准/评测 | 全库无 | **无数字可依** |

> 注：抽取 prompt 已明确"忽略…数字排盘细节、涉及他人的信息"（L121）——**确定性排盘事实不进记忆层**是既有正确设计，本节维持。

## 优化方向（四个方向 · 全部零新增算力）

### 方向一 · 召回可用性修复（**最优先，成本≈0**）
1. **查询表达改造**（修 R1）：整句短语 → **切分为 ≥3 字词项 + `OR` 连接**；可"短语优先，无命中再 OR 兜底"两段式
   - 零 schema 改动、零新依赖、同一索引；`MATCH` 词项由 1 → ~N，在单用户 ≤300 条索引上可忽略
2. **短问兜底**（修 R2）：<3 字时**不再直接退化 recency**，改用**一次 `LIKE '%词%'` 扫该用户活跃事实**（≤300 行，微秒级），保住相关度
3. **tags 进场**（修 R4）：tags 已由 LLM 免费产出却被丢弃 → 至少作为 **Python 侧关键词补充源**
   - 若要 tags 进 FTS 索引，需改 FTS DDL + 3 个触发器 → **属迁移，另案评审**（见 §禁碰清单）

### 方向二 · 打分公式修复（成本 = 纯算术，无新查询/新内存）
4. **RRF 多路融合**（修 R3）：把"二选一"改为融合 bm25 + recency（+ 将来的其它通道）
   - 借参考项目 `controller/router.py` L51-56（`1/(k+rank)` 累加，k=60，**6 行、零依赖**）
   - 代价：条件成立时**会多一次 recency 候选读取**（≤300 行）→ 属"微增"，**必须由基准给数字后决定去留**
5. **`trust` / `access_count` 进打分**（修 R5/R6）：已被维护却从不读取的信号，纯算术接入
6. **`importance` 校准**（修 R7）：按 `fact_type` 先验 + 钳制区间，降低 LLM 数值漂移对排序的影响

### 方向三 · 写入侧质量（成本 = 0，同一个 LLM 调用内）
7. 抽取 prompt（L117-129）要求产出**可检索关键词**并与 `tags` 对齐（现在只说要 2~5 个"简短关键词"，没定义用途）
8. 近重复事实治理：现在只按 `content_hash` **精确**去重（L158-160），语义重复仍会白占 300 条额度

### 方向四 · 离线基准（零线上负担，**建议第一个做**）
9. 自建离线基准：离线、不联网、不依赖向量，用播种类语料
   - **baseline 必须是"当前实现"**——参考项目自带的 baseline 是语义检索本身，不搬向量则无意义；其 `NEGATIVE` 项 30/30 是平凡项、会虚高总体，**不可当验收线**
   - 指标：命中率 / 延迟 p50·p95·p99 / 单次召回查询次数 / 进程峰值内存

## 借鉴来源（参考项目 · 已调研并**重新定级**）
| 来源 | 结论 |
|---|---|
| `agent-memory-main/controller/router.py` L51-56（RRF） | ✅ **可用**：6 行、零依赖、零内存 → 方向二 #4 |
| `agent-memory-main/controller/conflict.py`（矛盾检测） | ⏸ **本期不做**：无向量时须逐条近邻比较，成本/收益无数字支撑 |
| `agent-memory-main/controller/forgetting.py`（幂律衰减 + 升降层） | ⚪ **已具备**：太初已有指数衰减 + 清理 + 驱逐，不重做 |
| `agent-memory-main` 向量化（`sentence-transformers` + `sqlite-vec`） | ❌ **否决**：见 §明确不做 |
| `agent-memory-main/benchmarks`（locomo_lite） | 🟡 **借形态，不借数据**：改造为"不依赖 embedding"的形态后可用 |

## 明确不做（用户既定约束 + 实测支撑）
| 不做 | 依据 |
|---|---|
| 本地向量化（sentence-transformers / torch） | 实测 available 仅 **226MB**，估计需 +400~650MB → **必崩或进 swap 抖动** |
| 远端 embedding API | 内存无害，但引入**外部依赖 + 网络延迟**，且**无任何数字证明语义召回是瓶颈** → 不值得 |
| 任何后台线程 / 定时器 / 独立进程 | 违反"不加重负担"；现状 worker 是**单事件循环内协程**（L924），已是最省形态 |
| 把 O(N²) 近邻比较塞进清理任务 | `run_memory_cleanup()` 在 L948 是**同步**跑在事件循环上 → 会**阻塞全进程所有请求**（比吃内存更危险） |
| 矛盾检测（本期） | 成本/收益无数字支撑；待基准给出数字后再议 |

## 方案选项
- **A（推荐）**：三步走 —— ①**先建离线基准**（拿到"现状到底多差"的数字）→ ②**方向一**（查询表达 + 短问兜底；收益最大、成本最低）→ ③**方向二**（RRF + 信号进场）。每步都用基准复测对比
  - 代价：三次复测；效果：**每步收益都有数字，任一步无收益可立即停损**
- **B**：只做方向一（最小改动，先止血）
- **C**：四方向一次做完（含写入侧 prompt 与基准）
- 用户选择：{{待定}}

## 禁碰清单
- **不改表结构 / FTS DDL / 触发器**（`database.py` L110-121）——tags 进 FTS 属迁移，须单独立项评审
- 不改 `method-result v2` 与 `chart.json` 契约（单向门）
- **不改 worker 形态**：保持进程内单 asyncio 协程（L924）；**禁止新增线程/定时器/守护进程**
- 不改 `recall_memories` 签名与"异常静默返回 `""`"的降级语义（L351、L376-384）
- **不改用户隔离**：`m.user_id = :uid` 强制过滤（L310）必须保持；关键词/tags 补充**不得跨用户带内容**
- 不改计费链路（抽取 LLM 不扣用户余额，维持 L32-33 现状）

## 冲突检查
- 与既有降级语义「记忆是增强不是依赖」（L351）**一致**：本节所有改动仍在同一条静默降级路径内
- 与 **v0.15 性能纪律**「性能的凭是数字」**一致**：本节不先改代码，先建基准取数
- 与 **v0.17 乐高原则**（轻量/接缝/埋点/开关）**一致**：零新依赖；召回埋点已存在（L423）可直接用于对比
- 与 `_archive/adr/` 内既有 ADR 的逐条比对：**待落地时复核**（本节未逐条核对，按 v0.21 如实声明）
- 规格出处：`_archive/记忆方案书.md`（原 `docs/记忆方案书.md`，本轮治理已归档；`service.py` 全篇以其 §1–§6 为规格）

## 结构反思（破竹 §8）
- **哪里难**：记忆层最贵的能力（语义）在这台机器上买不起，于是"看起来只能靠加算力"。但真正的问题不在算力层，而在**检索表达层**——一个 `_fts_quote()` 把整句包成短语，就让整个 BM25 通道事实上停摆，而 recency 兜底把这件事**掩盖得很好**（永远有记忆注入，只是不相关）。
- **结构上怎么改**：把"召回"拆成三层各自独立可测——**表达（怎么问）→ 融合（怎么排）→ 打分（怎么加权）**。现状三层混在 `recall_memories` 一个函数里（L338-428），所以任何一处失效都无法单独观察。
- **下次会怎样**：有了离线基准，召回质量从"凭感觉"变成"看数字"；将来若要上语义通道，也是在同一套基准下与 BM25 对比后再定——**而不是先装模型再找理由**。

## 控制点结果
- 回归：待落地时补（记忆召回 + 太初先生对话主链路 + 断前尘 + 报告 8 板块；重点是**记忆块注入不破坏既有 system 段结构**）
- 安全 / 数据：**用户隔离不得被削弱**（L310 强制 `user_id` 过滤）；`LIKE` 兜底路径同样必须带 `user_id` 过滤；记忆块不得夹带其他用户内容（沿用 L89-92 边界规则）
- 性能：**必须给数字** —— 单次召回的**查询次数 / 耗时 / 进程峰值内存**前后对比；**不得突破 226MB 余量**；**禁止新增后台任务**

## 本节指标（v0.21）
- 模型 + provider：{{执行方填}}

## 用户结论
待验收
