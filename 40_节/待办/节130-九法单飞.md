# 节 130 · 九法"单飞"（哪一法拆成独立功能）

状态：🟡 **待办 · 未开工**（⏳ 前置：用户裁决"要不要单飞"）｜ 强度：**中-大改**（新增入口 + 可能碰计费）｜ 日期：2026-09-11

> **来源**：`40_节/完成/节126-mingyu最新状态借鉴与九法单飞评估.md` §2.4（用户指示"细化成新的节"）。
> **本节只写"范围 + 前置 + 禁碰"**；**九法清单与候选排序的完整依据在节126 §二**（破竹 铁律 2，不重复记录）。

## 一、候选排序（**Agent 建议，节126 §2.4 已论证**）
| 排序 | 候选 | 为什么 | 成本 | 认知度 |
|---|---|---|---|---|
| 🥇 | **`bazi-hunyin-caiyun` 八字婚姻财运** | ⭐ **代码里本来就标为「专题类」**（ADR-0003）→ **不是新增能力，是换入口**；"看婚姻/看财运"最好懂 | 最低 | 高 |
| 🥈 | **`ziwei` 紫微斗数** | 独立学科；中文用户认知度最高的单法之一 | 低 | 高 |
| ⚠️ | 西洋占星 | 太初**已有** `views-xishi.js` + 星座模块 → **先查重** | 低 | 高 |
| ❌ | 七政四余 / 奇门终身局 / 五运六气 | 术语门槛高 → **不适合独立获客** | — | 低 |

## 二、🔑 待用户裁决（**本节唯一的阻塞项**）
> **单飞是"提升可发现性"，还是把用户从「九法合一」这条付费主链分流走？**

| 立场 | 依据 |
|---|---|
| ✅ 支持 | 太初**已有现成的"单飞模式"**（择日 / 起卦 6 法 / 塔罗 / 雷诺曼 / 星座 / 生肖 / MBTI 都是独立入口）→ **不是新架构，是把已有模式再用一次** |
| ⚠️ 反对 | 九法合一是**付费主链**（断前尘 → 校准 → 预测 → 追问）→ 单飞可能**降低用户进入主链的概率** |

## 三、范围（**Agent 已实测**）
| 项 | 位置 | 说明 |
|---|---|---|
| 九法清单与引擎 | `00_根/导航.md` §methods（`bazi-*`×4 走 `lunar-python`；`ziwei` 走 `iztro`；`qizheng`/`qimen-lifetime`/`wuyun-liuqi` 走 `mingyu-core`） | ⚠️ 若在 **节128 自研**之后做，引擎归属会变 |
| 新增入口 | `frontend/public/js/views-home.js`（导航注册 + hub 卡片） | 参照 `views-guoxue-tools.js` 的 `NinePickPage` |
| 结果渲染 | `frontend/public/js/views-cases.js`（`ArchivePage` + **9 法命盘渲染**） | ⚠️ **先评估复用度**：复用 or 另写视图 |

## 四、🚨 红线：可能碰「什么绝对不能坏」第 ⑤ 条
- 单飞若带来**独立计费口径** → 触碰 **`00_根/入口.md`「逐法扣费与余额口径」**
- → **必须单独立项 + 走破竹 §6 留痕（记决策 + 声明影响范围 + 检查既有引用）**
- ⚠️ **本节的裁决不能顺带改计费**；改计费要**另一个节**

## 五、前置条件
| # | 前置 | 状态 |
|---|---|---|
| ① | **用户裁决"要不要单飞"** | 🔴 待用户 |
| ② | ⚠️ **与节128（自研内核）的顺序**：单飞只换入口、后端不动 → **可与节128 并行**；但若目标法在 `mingyu-core` 里（如 `ziwei` 走 `iztro` 例外），**要确认自研后是否仍成立** | 🟡 待评估 |
| ③ | 若涉独立定价 → **先立项计费节** | 🔴 待用户 |

## 六、硬约束
- 🚫 不做会加重服务器负担的方案（**单飞若只是"同一 analyzer 换入口"≈ 零新增算力**，符合要求）
- 🚫 不改九法合一主链
- 🚫 不动计费（见 §四）

## 七、禁碰清单
- 🚫 **本节点不产码**（立项节）
- 🚫 不改前端页名 / 路由 / 埋点事件名
- 🚫 不为单飞去动 `synthesizer` / 计费 / 主链

## 八、用户结论
⏳ **待裁决**（§二）
