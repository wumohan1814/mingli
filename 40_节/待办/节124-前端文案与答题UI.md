# 节 124 · 前端文案与答题 UI（IPIP 大五 · 第 2 步）

状态：🟢 **已实施（2026-09-14）待真机验收** ｜ 强度：**中-大改**（答题交互形态变更 + 全站文案整改）｜ 日期：2026-09-11

> **来源**：`40_节/待办/节122-MBTI题库替换为大五人格与商标合规.md` §六「节124」—— **节122 方案的第二步实施**。
> ⚠️ **详细方案不在本节重复**：**开工前先完整读节122**（尤其 §3.1–3.4 命名合规 / §5.2 前端影响范围）。本节只写**范围 + 前置 + 禁碰**。
> **本节不做**：后端题库与计分（→ 节123）、档案数据归属与配对解析（→ 节125，本节已连带实施节125 的前端一半）。

## 目标（可观察的验收标准）
1. **答题 UI 从"二选一"改成"5 点量表"**（选项固定：非常不准确 / 不太不准确 / 适中 / 比较准确 / 非常准确）
2. 结果页改为：**五维图 + 四字母放在次级位置 + 边界标注 + 脚注免责**
3. 对外文案**去 MBTI 商标**（主文案零提及）
4. **真实浏览器渲染通过**（用户是前端唯一验收人 → 凭 = 真实页面截图/可点页面）

## 范围（动哪些文件 · 已实测定位）
| 文件 | 改什么 |
|---|---|
| `frontend/public/js/views-mbti.js`（785 行） | **重写**：答题渲染 5 点量表（选项取 `GET /api/mbti/questions` 的 scale）；结果页 = 五维条形图 + 四字母「类型倾向对照」（次级 + ≈ 边界标注）+ 脚注免责；**答题进度 localStorage 存取**（300 题防丢失）；手输（16 型宫格）与分享页同步；「保存为我的类型」动作（节125 共用） |
| `frontend/public/data/ui-copy.js` | 文案整改：`mbti-intro` / `mbti-type-label`(→人格类型取向) / `delete-confirm-mbti` / `pair.meta.mbti.dataTxt`(→双方大五人格维度信息) / `mbti-sub` / `mbti-entry`(→人格测试)；新增结果页/答题/脚注/来源标签一批键 |
| `frontend/public/js/views-cases.js` | 档案「心理测试」板块：类型行 + **来源行**（由大五参考换算并保存 / 用户自填）+ **OCEAN 五维行**（平台产出）（节125 前端一半） |
| `frontend/public/js/views-home.js` | 无代码改动（配对 dataTxt 来自 ui-copy） |

## 前置条件
1. 🟠 **节123 完成**（`GET /api/mbti/questions` 与 `scores` 的形状由它定）
2. ✅ 命名与合规方案已定（节122 §3.2 / §3.3 / §7.1-②）
3. ⏸️ **16 型别名暂不动**（用户明确："原名先不删，因为这个需求还会改变"）

## 命名与合规（**逐条照做，不许自创**）
- 模块名保留已上线的「**心理测试**」；副标题改「**大五人格（OCEAN）测评**」
- 结果页主标题：`你的人格画像` / `大五人格（OCEAN）结果` —— **不是**「MBTI 测评」
- 四字母：**次级位置**，标为「**类型倾向对照**」—— 🛑 **不挂"荣格"名**（J–P 不是荣格的，MBTI Facts 页 Q8 原文）
- 🚫 **禁用词**：「MBTI 测评 / 官方 / 认证 / 权威 / 专业 MBTI」；🚫 **不给百分位**；🚫 **不写"超过 X% 的人"**
- ✅ **脚注文案**（唯一出现 MBTI 的地方，用节122 §3.2 那一版）：非关联声明 + 权利人官方致谢句
- 🚫 **不用「人格匹配」当测评名**（与既有「配对解析」功能撞车，且暗示婚恋匹配）

## 禁碰清单
- **不改前端页名**（`mbti` / `mbti-hub` / 分享 URL `/mbti/share/{token}`）—— 改了会作废已发出去的分享链接
- **不改埋点事件名** `mbti_score`
- **不新造免责机制** —— 挂到 `components.js` 既有全局末行免责上
- **答题进度只用 localStorage**（零后端存储、零服务器负担）

## 用户结论
待真机验收（验收路径：登录 → 心理测试 → 选档案 → 开始测试（300 题 5 点量表，可中途刷新续答）→ 结果页看五维图 + 类型倾向对照 + 保存类型 → 档案详情看两条账）

### 实施记录（2026-09-14）
- `views-mbti.js` 整文件重写：MbtiPage 答题 5 点量表（`choose(value)` 记 `{question_id, value}`）、`startQuiz` 恢复 localStorage 进度、结果页五维条形图（O/C/E/A/N + 偏低/适中/偏高档位文字 +「不是人群排名」注）、四字母次级「类型倾向对照」（`≈` 下划线虚线标边界维度 + r² 分级说明）、脚注（`ocean-footnote` 唯一 MBTI 出现处）、「保存为我的类型」按钮（`POST /api/mbti/save-type`，判型/手输共用）；MbtiSharePage 同步 5 点量表 + 五维结果 + 脚注
- 空 type 边界处理：四维全边界时后端 `type=""` → 结果页显示「你的画像介于多型之间」不抛错
- `ui-copy.js`：主文案 MBTI 商标清零（代码注释与内部标识 `MBTI_FIELDS` 保留，属 节122 §4.5「内部标识一律不改」）；新增结果页/答题/来源标签键
- 门禁：`node --check` 全绿 + `precompile.js` exit=0（散色 0）+ `smoke-check` 9 通过/0 错误
- 服务冒烟：`/api/mbti/questions` 200（25KB，300 题）、`/js/views-mbti.js` 200、`/` 200
