# 节 105 · REQ-128（文案统一管理专项）

状态：🔵 进行中 ｜ 强度：中改偏大 ｜ 日期：2026-09-10 → 2026-09-12

## 用户原话
（引用自需求表 REQ-128）「文案统一管理专项（含存量搬家分阶段；后置：当前功能批次收尾后启动，方案见 docs/文案管理方案.md）」（P3 · 🔵；备注：①阶段 0·试点与立规矩——REQ-127 名词白话卡作为「内容文字 key 化 + 进数据文件」试点；定全站文案 key 规范（界面字 ui.模块.动作、正文类 content.模块.条目）；②阶段 1·框架层 界面字迁入统一文案总表；③阶段 2·档案与主流程；④阶段 3·占卜与内容页；⑤阶段 4·功能页与后台；⑥阶段 5·收口与面板——后台「文案中心」只读面板 + i18n 映射机制铺路。整体验收：全站面向用户文字有统一视图、改一处全局生效。**阶段 0 已完成（99c49b0 + 6f147c5）**：6 套名词白话卡 188 张接入 6 方法结果页，key 规范入 standards/01 §12；**阶段 1 已完成（3041254）**：框架层界面字迁入统一文案总表 TC_COPY；阶段 2~5 后续按序推进）

## 我的复述
将全站面向用户的界面文字统一迁入 TC_COPY 文案总表（`frontend/public/data/ui-copy.js`），做到"改一处全局生效"，并为后续 i18n 翻译铺路。分 6 阶段推进：阶段 0（试点）✅ → 阶段 1（框架层）✅ → 阶段 2（档案与主流程）✅ → 阶段 3（占卜与内容页）✅ → 阶段 4（功能页与后台）✅ → 阶段 5（收口与文案中心面板）。正文类（卦辞/爻辞/牌义/解读模板等）按约定不入 TC_COPY，走各自数据文件。

## 期望形态（验收标准 · 用户可观察）
1. 所有前端界面文字（按钮、标签、提示、错误信息、分区标题等）可在 `ui-copy.js` 一处修改、全局生效
2. key 命名规范：`ui.<模块>.<动作|状态>`，与阶段 1 已有风格一致
3. 为后续多语言翻译打好基础：所有 UI 文案有唯一 key，无散落硬编码
4. 正文类内容（卦辞、牌义、解读模板、名词解释等）不在此范围

## 现状坐标（Agent 用）
- **阶段 0 ✅**（99c49b0 + 6f147c5）：名词白话卡试点 + key 规范立规矩
- **阶段 1 ✅**（3041254）：框架层界面字迁入 TC_COPY（brand/landing/buttons/menu/nav/footer/hub/auth/balance/pwa 等 30+ 模块）
- **阶段 2 ✅**（3aa8253）：档案与主流程
  - 新增 `ui.onboarding` 模块：建档表单全部 UI 文案（~40 key）
  - 扩容 `ui.caseDetail` 模块：档案详情页状态/标签/操作/toast/底部入口等（~40 key）
  - 新增 `ui.chartLabels` 模块：八字/紫微/占星/七政/奇门/五运六气等盘面结构标签（~80 key）
  - `views-onboarding.js` 硬编码中文全部替换为 UI_COPY 引用
  - `views-cases.js` 档案详情区 + 盘面核心结构标签替换为 UI_COPY 引用
  - 不动：行星名映射、格局名映射等数据类内容（走各自数据文件）
- **阶段 3 ✅**（占卜与内容页）：4 个核心页面 UI 文案迁入
  - 新增 `ui.divination` 模块（~100+ key）：7 种占卜方法 name/desc、六爻/大六壬/金口诀/奇门/观音灵签的全部 UI 结构标签、操作提示、状态文字
  - 新增 `ui.xishi` 模块（~40+ key）：塔罗抽卡界面、结果页、牌阵名、正位/逆位、关键词/问题标签、操作按钮
  - 新增 `ui.mbti` 模块（~20+ key）：分类标签（优势/盲点/职场/关系/成长建议）、错误提示、操作反馈、按钮文案
  - 新增 `ui.guoxueTools` 模块（~20+ key）：九法合一 8 种状态标签、入口按钮、空状态说明、费用提示
  - 对应文件：`views-divination.js` / `views-xishi.js` / `views-mbti.js` / `views-guoxue-tools.js` 主要 UI 文案均已替换为 UI_COPY 引用
  - 不动：卦辞/爻辞/签诗/塔罗牌义/MBTI 题目与类型描述等正文数据（走各自数据文件）
- **阶段 4 ✅**（功能页与公用组件）：5 个模块 UI 文案迁入
  - 新增 `ui.home` 模块（~30+ key）：入口标签、模块卡片、问候语、5 项设置开关名称+描述、占卜默认模式、保存状态提示
  - 新增 `ui.agent` 模块（~10 key）：4 条随机开场白、错误提示、输入占位、状态文字
  - 新增 `ui.zodiac` 模块（~30 key）：生肖/星座/配对 tab、12 生肖名、12 星座名、3 种配对类型、标题
  - 新增 `ui.guoxue` 模块（~20 key）：8 种择吉事项标签、今日宜/忌、吉位/吉色/吉数、标题
  - 新增 `ui.components` 模块（~25 key）：8 种星盘类型标签、行星/四轴/逆行/格局等 UI 结构标签、通用按钮
  - 对应文件：`views-home.js` / `views-agent.js` / `views-zodiac.js` / `views-guoxue.js` / `components.js` 主要 UI 文案均已替换为 UI_COPY 引用
- **阶段 5 🔵 进行中**（2026-09-12 本轮：整屏长文案收尾）
  - 迁移 `views-zodiac.js`（14 处）：三关系弹窗（REL_TEXT_FALLBACK / 弹窗标题兜底 / 匹配表分区标签 / 无生肖两处兜底）、结果卡（星君名模板 / 贵人三行标签）、档案选项（未命名档案 / 出生年两处后缀模板 / 档案 id 兜底）、页标题、加载失败兜底 → `ui.zodiac` 新增 19 key
  - 迁移 `views-home.js`（23 处）：余额不足弹窗（需求/余额模板 + 「知道了」按钮）、PairModal 档案兜底（档案 id 模板 / 出生年 / 已测类型 / 列表失败 / 无解读内容 / 数据未生成 / 生成指引兜底）、余额明细页（页题 / 当前余额 / 汇率说明模板 / 空态 / 交易兜底 / 消耗档案 / 加载更多 / 失败标题 + 复用 buttons.retry/back）→ `ui.pair` +7 key、`ui.balance` +10 key、`ui.home` +2 key
  - 迁移 `views-guoxue-tools.js`（16 处）：九法进行中状态（复用既有 status-progress 前后缀 key）、档案名兜底（case-prefix）、出生年后缀模板、NamerModal（档案选择区档/姓/向角标与标签、选填注记、重试档案、空态提示、结果名单模板、列表失败/加载中）→ `ui.guoxueTools` +1 key、`ui.namer` +15 key
  - 修复 `ui-copy.js` 重复键 bug：阶段1 `agent` 块（name/talk/chat-title）与阶段4 `agent` 块同名，JS 后者覆盖前者，致 `TC_COPY.ui.agent.name/talk/chat-title` 运行时 undefined（views-home 导航在用）；已合并入阶段4 块
  - 保留不迁（数据映射 / 后端正文匹配，符合禁碰清单）：星座名↔图标映射、PAIR_REL_ICONS / CONFLICT_CLS / relCls / typeText 映射 key、关系术语值（六合/三合/其他等）、`replace()` 匹配后端正文的 pattern
  - 验证：`node --check` ×4 通过；precompile 门禁通过；UI_COPY/TC_COPY.ui 引用体检 86 处 0 缺失；浏览器冲烟（首页 / 生肖流年页 / 导航「太初先生」）无 console error、无 undefined；设置页 / 余额明细页需登录，留待真机验收
  - **规范层同步（2026-09-12，用户追问「以后新文案/素材是否同一处改」后补，破竹收尾义务）**：四归口（界面词 ui-copy.js / 正文 data/content/ / 视觉令牌 tokens.css / 美术文件 art/）已在 Agent 必读文档固化——`00_根/复用.md` §四两行（「新增面向用户的文字」+「图标 / 美术素材」扩充为物理归口+引用三分法）为唯一权威；`docs/standards/01` §12 补物理归口指针并对接破竹合规自检第 5/6 项；`standards/04` §6/§8 补素材边界与统计漂移修正；`standards/07` 登记本次修订
  - **阶段 5 剩余**：文案中心只读面板 + i18n 机制铺路

## 禁碰清单
- 运行层（backend/ frontend/ data/ ops/）；细化前不改需求表 / Bug 表原文
- 正文类内容（卦辞/爻辞/牌义/解读模板/名词白话卡等）：不入 TC_COPY，走各自数据文件
- 盘面数据值（干支、行星名、格局名等）：属于内容数据，暂不迁入 UI 文案总表

## 冲突检查
- 无

## 用户结论
待验收
