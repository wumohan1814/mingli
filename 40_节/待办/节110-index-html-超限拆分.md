# 节 110 · index.html 超 2 万行拆分（技术债）

状态：🟡 待办 ｜ 强度：大改（涉及前端权威入口，改动面大、须门禁）｜ 日期：2026-09-10

## 用户原话
（审计发现，非用户直接提出）`frontend/public/index.html` 实测 21841 行，超过 `00_根/复用.md` 自设红线「单文件超过 2 万行必须拆」。

## 我的复述
把前端权威入口 `frontend/public/index.html`（21841 行）按功能模块拆分为多文件，解除超限红线，同时保证免构建架构与运行时行为不变。

## 期望形态（验收标准 · 用户可观察）
- index.html 降到 2 万行以下（或拆分为模块化文件由构建/预编译合并）
- 页面行为、路由、样式与原版完全一致（改前/改后截图 + 关键路径冒烟）

## 现状坐标（Agent 用）
- `frontend/public/index.html`（21841 行；ModalBase ≈2830 行、:root ≈37 行；九法/西式/MBTI/档案/设置/后台 各区块）
- 红线出处：`00_根/复用.md` §五
- 约束：免构建 CDN React + 预编译 JSX（`node frontend/scripts/precompile.js` 门禁）；静态资源根绝对路径

## 复用判断
- 库内候选：无
- 结论：新写拆分方案 —— 需先出拆分方案书（按模块切分 / 构建合并 二选一）给用户拍板

## 方案选项
- A：按模块拆为多个 JS/JSX 文件，index.html 只留骨架 + 按需加载（推荐；改动大但根治）
- B：拆分 CSS 变量与组件库为独立文件，index.html 瘦身（渐进，风险低）

## 禁碰清单
- 不改后端契约（method-result v2 / chart.json）；不改前端运行行为；不引入构建链依赖（除非用户拍板）

## 冲突检查
- 与节100 全貌走查、节105 文案管理（key 化可能动 index.html）先后依赖，建议排在全貌走查之后

## 用户结论
待验收
