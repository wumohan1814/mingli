# 节 128 · 太初自研内核（全部自研 · 不再依赖 `mingyu`）

状态：🔵 **执行中 · 已拆分子节**（2026-09-13 用户最终裁决：**即使项目选定 Prosperity 许可、与 MIT 依赖兼容，仍坚持自研**，目标 = 零 mingyu license 依赖、完全自主可控）｜ 强度：**超大工程**（≥17 个能力，拆为节148–157 共 10 个子节逐步执行；编号避开另一 session 已占用的节147 项目代号更名）｜ 日期：2026-09-11

> **2026-09-13 最终裁决（覆盖同日早前「建议撤销」备注）**：用户明确「节 128 必须做，必须保证不受 mingyu license 的开源控制」。Prosperity 许可仅解决"当前使用合规"，不解决"冻结在他人版本/口径跟随上游/护城河"问题 → 自研继续。本节转为**母节/总账**，范围、纪律、文案工单仍以本文为准；执行见 §十 子节拆分。

> **来源**：用户 2026-09-11 指示 —— 见「用户原话」。
> **上游调研**：`40_节/待办/节126-mingyu最新状态借鉴与九法单飞评估.md` §1.5.7（重写/自研的法律分析）。
> ⚠️ **本节只写"范围 + 前置 + 分期 + 禁碰"**；**法律分析不重复**，以节126 §1.5 为唯一事实源（破竹 铁律 2）。

## 用户原话
> 「我指的"自己写"，是指**不使用 `mingyu` 里面的任何内容**，而是**自己从零开始开发这样一个内核**，然后放在太初中去。因为"命语"给太初这个项目提供的所有内容（比如预测方式、占卜方式等），**基本上都是开放性的中国传统文化内容**。这一部分应该是没有版权保护的，也是我们可以自己写的。**我没有看到任何地方规定必须要使用 `mingyu` 的内核。**」
>
> 「我**选择全部自研，完全不用 `mingyu` 的任何东西**。因为我记得当时在使用的时候，mingyu 里面本身自带的一些文字包，**除了古籍上的内容以外，其他都是我配备的**，所以文案方面的工程量本身就没有那么大。不过为了完全规避风险，可以**再把它的包内容和外面核实一下，看能否找到类似的东西**。
> 我的建议是，整个方案上**先从 mingyu 里拿到所有文案，然后交给文案 Agent**，让他判断**哪些是可以直接用、哪些是我们必须要重写的**。」

## 决策结论（用户已定）
1. **全部自研** —— 不再依赖 `mingyu`（不依赖其代码、不依赖其文案）
2. **理由 = 自主可控 + 完全规避风险**（⚠️ **不写"合规整改"** —— 见 §一）
3. **文案分两步**：先**取全文案 → 交文案 Agent 分类**（可直接用 / 必须重写）
4. **同时核实外部**：看能否用**外部干净来源**替代（古籍 / 公有领域）

---

# 一、先把一件事说清（**避免理由用错导致优先级误判**）

| 对象 | 许可 | 含义 |
|---|---|---|
| **`mingyu-core`（现在在用）** | **MIT**（已发布版本授权不可撤销） | ✅ **用它本来就合法** → **自研它不是"合规整改"，是"自主可控"** |
| **根仓库 `Brhiza/mingyu`** | **AGPL-3.0-only** | 🚫 不用（已定）—— **这才是风险来源** |

→ 所以本节的**正确立项理由**是：**① 自主可控 ② 不受上游许可证变动影响 ③ 口径自主 ④ 一次性把合规疑问关掉**（见 §五 收益表）。
→ ⚠️ **不要写成"规避版权"** —— 那会让人以为"不做就有法律风险"，从而**误判优先级**。

---

# 二、范围（**Agent 实测**，来自 `backend/paipan-node/server.mjs` L124–L142）

**共 16 项 import，覆盖约 17 个能力** —— 这就是"全部自研"要替换的全部：

| 来源 | 能力 |
|---|---|
| npm **`mingyu-core@0.2.1`**（2 项） | 星盘（`calculateBirthChartBundle`）、五运六气（`calculateWuyunLiuqi`） |
| 本地 **`vendor/mingyu-core/dist/`**（14 项） | 奇门终身局、生肖流年、六爻、梅花、小六壬、大六壬、金口诀、奇门、灵签、塔罗、雷诺曼、星盘、**择日**、**太乙**、**皇极经世** |

> 🔑 **"从零自研"的现实形态 = MIT 基础库 + 自研上层**：
> - **地基可用合法 MIT 件**：`lunar-python`（已在用）、`iztro`（已在用）→ 历法与基础排盘
> - **要自研的是上层**：格局判定、神煞、切片、**证据层**、文案
> - ⚠️ **"完全不用 mingyu 的任何东西" ≠ "不用任何第三方"** —— 本项目**允许**用其他 MIT 件（这与用户的要求不冲突，且已在做）

---

# 三、✅ 前置：文案归属（**2026-09-11 用户已澄清 → 见 §3.5**）

> 📌 **本节原写"文案归属需举证"，用户随后澄清：文案是**用户让 AI 生成**的 → 见 §3.5 的结论与最简解法。下方原始记录**保留留档**（说明"为什么当时要举证"）。

⚠️ 用户陈述：**「mingyu 里自带的文字包，除古籍内容外，其他都是我配备的」**。

**Agent 核实结果（2026-09-11，**无法证实**）**：
- 实测 `api.github.com/repos/Brhiza/mingyu/contributors` → **只有 3 位贡献者**：`Brhiza`（1046 次）/ `Jerryxumaomao`（1）/ `863401402`（1）
- 🚫 **用户的账号（`wumohan1814`）不在贡献者名单中**
- ⚠️ **但这不等于否定用户的说法** —— **内容可以由他人代提交**（用户把文字包交给作者，作者代为 commit）→ **commit 记录里看不到"内容是谁写的"**
- ⚠️ 另注：`api.github.com/repos/wumohan1814/mingli` 返回 **404**（与"该仓库是私有库"一致，非异常）

**→ 结论：这条事实属「用户陈述 · Agent 无法核实」**，且它**直接决定"文案能否直接复用"** → **必须由用户举证**：
| 举证方式 | 说明 |
|---|---|
| **原始底稿** | 用户自己的文件 / 邮件 / 聊天记录（能证明"先有稿、后有仓库"） |
| **与作者的约定** | 是否存在贡献协议 / CLA / "内容归项目"的口头或书面约定 |
| **或说明是 AI 生成** | 若文案是用户用 AI 生成的 → **版权归属另有一套规则**（可能更自由，但也可能有独创性认定问题）→ 也需说明 |

## 🚨 一条**比"谁写的"更容易被忽略、也更该注意**的法律要点
> **同一份内容，来源不同，法律地位不同：**
> - **从"用户自己的原始底稿"取** → ✅ **无争议**（作者当然可以用自己的作品）
> - **从"AGPL 仓库里复制出来"** → ⚠️ **要解释"这是我的贡献"**，**举证成本高**；且一旦当初的发布方式被认定为对项目的授权，**另行使用需谨慎**
>
> → 🔒 **规定（写进禁碰清单）：本项目一律从"用户自己的原稿 / 外部公有领域来源"取文案，🚫 不从 `Brhiza/mingyu` 仓库复制文本。**
> ⚠️ 这不是因为"你的东西不能用"，而是因为**"取证成本"** —— 何必给自己留一个需要解释的来源。

## 3.5 ✅ 用户澄清（2026-09-11）：「文案其实是我让 AI 生成的」→ **⭐ 最简解法：重新生成一遍**

> 用户原话：「我指的文案是指，当时拿到 mingyu 这个包以后，**里面大部分都是古文，我让 AI 帮我生成了一些大白话的内容**。」

### 3.5.1 归属分析：**两种可能路径下，我们都能用** ✅

| 情形 | 法律后果 | 我们能否用 |
|---|---|---|
| **A. 认定归用户**（用户有"独创性智力投入"：设计 prompt、筛选、调整） | 著作权归使用者 | ✅ **能用** |
| **B. 认定不受版权保护**（纯 AI 生成、无人类创作性投入） | 无著作权 | ✅ **谁都能用，包括我们** |

> 📌 **中国已有司法先例**（2019 北京互联网法院"菲林诉百度"案、2023 北京互联网法院 AI 生成图片案）：**有独创性智力投入的 AI 生成内容可构成作品**。但**《著作权法》尚无明文规定**；美国 USCO 立场则相反（纯 AI 生成不受保护）。
> ⚠️ **以上为一般原则，非法律意见。**
> → **关键结论：无论走 A 还是 B，太初使用它都【没有前置障碍】。**

### 3.5.2 ⭐⭐ Agent 建议的**最简解法：重新让 AI 生成一遍**（**推荐**）

既然文案本来就是 **AI 生成的** → **那就重新生成一遍**，而不是去纠结"能不能复用仓库里那份"：

| 做法 | 干净度 | 成本 | 附带收益 |
|---|---|---|---|
| 🥇 **重新 AI 生成一遍** | ✅ **完全干净** —— 新产物与仓库里那份**没有任何复制关系**，**"有没有抄"这个问题根本不存在** | **极低**（AI 生成很快） | ⭐ **可顺便改进**：按太初的产品语气/免责口径/术语统一重写一遍 |
| 🥈 从用户自己的**原稿/生成记录**取 | ✅ 干净（作者用自己作品） | 低（若原稿还在） | — |
| 🚫 从 `Brhiza/mingyu` 仓库复制出来 | ⚠️ 要解释"这是我的贡献" | **举证成本高** | — |

> 🔑 **这一条把"文案重建"从"大工程量"变成"重跑一遍生成"** —— 也印证了用户的判断（"文案方面的工程量本身就没有那么大"）。

### 3.5.3 ⚠️ 仍需用户确认的一点（**决定范围有多大**）
> **那些 AI 生成的大白话，是"只在太初这边"，还是"也被提交进了 mingyu 仓库"？**
- 若**只在太初** → ✅ **根本不涉及 AGPL**，直接可用（**推荐路径就是"重新生成"或"用自己原稿"**）
- 若**也进了 mingyu 仓库** → 归属仍是用户的（AI 生成、用户发起），但**不要从仓库复制** → 走 §3.5.2 的 🥇 或 🥈

### 3.5.4 ✅ 另两条已坐实的事实（用户 2026-09-11 同意，Agent 已核实）
| 包 | 版本 | 许可证 | 证据 |
|---|---|---|---|
| **`lunar-python`** | 1.4.8 | ✅ **MIT** | PyPI 元数据 `info.license: "MIT"`（作者 6tail） |
| **`iztro`** | 2.6.1 | ✅ **MIT** | npm registry `license: "MIT"`（作者 SylarLong；依赖 6tail 的 `lunar-typescript`/`lunar-lite`） |

→ ✅ **"本项目已在用的 `lunar-python` 与 `iztro` 都是 MIT，属合法状态"成立** → **可作为自研的合法地基**。
> 📌 这将写入 `00_根/来源.md` 的登记（原先"均 MIT"是**间接记载**，本次为**直接核实**）。

## 3.6 ✅ 最终口径（用户 2026-09-11 第三次确认 · **可执行结论**）

> 用户原话：「**只要是 mingyu 包中的文案，我全部重新用 AI 生成一遍。所以需要先把里面用到的文案库导出来，除公网可查的古籍内容以外，其他全部重新生成一遍。**」
> 以及：「**我 AI 生成的是大白话，只是在太初里面并没有涉及到 AGPL。**」

### 3.6.1 两条规则（**写死**）
| 范围 | 处置 |
|---|---|
| **在 `mingyu` 包 / 仓库里的文案** | 🚫 **除「公网可查的古籍原文」以外，全部重新 AI 生成** |
| **只在太初里、由用户 AI 生成的大白话**（**不在 mingyu 里**） | ✅ **干净，可直接保留** —— 用户明确：**不涉及 AGPL** |

### 3.6.2 ⚠️ 一个**必须避免的陷阱**：「导出文案库」≠「把原文搬进项目」
用户说"需要先把里面用到的文案库导出来" —— ⚠️ **这里要小心**：

| 导出什么 | 能不能做 |
|---|---|
| **清单**（路径 / 体量 / 类型 / 主题概括） | ✅ **可以**（路径与字数是**事实**，不是表达） |
| **原文内容搬进太初仓库** | 🚫 **不可以** —— 那正是"把 AGPL 文本带进产品" |

→ ⭐ **正确做法**：**只盘点"要生成什么"（清单 + 主题 + 规格），不导出原文** —— 详见 §3.6.3。

### 3.6.3 ⭐⭐ **设计要点：重新生成要"按主题生成"，不要"照着原文改写"**

| 做法 | 风险 |
|---|---|
| 🚫 **拿原文 → 让 AI 改写** | ⚠️ 仍有「**衍生作品**」风险（改写别人的表达 = 可能构成改编） |
| ✅ **给"主题 + 规格" → 让 AI 从零生成**（如"十神含义的白话解释，每神 150–250 字，面向不懂术语的用户"） | ✅ **完全独立** —— 与原文**没有复制或衍生关系**，且**术数规则本身不受版权保护** |

→ 🔒 **所以工单里写的是"要生成什么"，不是"照着这段改"。**

### 3.6.4 第 0 期产出物（**按此执行**）
- `40_节/待办/节128-附件/mingyu文案盘点.md` —— **清单 + 分类 + 体量 + "要生成什么"规格**（🚫 不含原文）
  - 分类：① 提示词 ② 现代白话文案 ③ **术语表/规则表/数据表**（⚠️ 编排可能有著作权 → 从古籍自己整理）④ **古籍原文**（✅ 可保留）⑤ 界面/本地化文案
- 后续：**重新生成工单**（交文案 Agent，按主题生成，不是改写）

> 🚨 **给盘点任务的硬约束（已写进派发说明）**：**只允许记录路径、体量、类型、"用自己话概括的主题"，以及 ≤2 行的短摘引作为证据** —— **绝不把正文写进本项目文件**。

## 3.7 ✅ 盘点完成 + 交办方式确定（用户 2026-09-11 第四次确认）

### 3.7.1 盘点结果（**已落盘**）
`40_节/待办/节128-附件/mingyu文案盘点.md`（358 行）—— ✅ **不含该仓库任何正文**（只有路径 / 字节数 / 类型判定 / Agent 自述概括）。

| 类别 | 文件数 | 体量 | 需重新生成 |
|---|---|---|---|
| ① 提示词 | 24 | ≈250 KB | 🚫 全部 |
| ② **解读话术 / 证据层** | 22 | **≈788 KB** | 🚫 全部（**最大一块**；**正是节117 想要的那层能力，但必须重做**） |
| ③ 术语表 / 规则表 / 数据表 | 17 | ≈601 KB | ⚠️ 从古籍自己整理（⚠️ **`terms/dictionary.js` 216 KB 是全仓最大单块现代文本，最易被"顺手抄表"**） |
| ④ 古籍 | 17 | ≈275 KB | ✅ 大部分可保留（⚠️ 但有 95 KB 的 `*-rules`/`*-patterns` 疑为**现代整理** → 剔除） |
| ⑤ 界面 / 本地化 | ❓ | **未测** | 🚫 需要（**要自己定位**） |
| ⑥ 文档 | — | ≈161 KB+ | 🚫 只可借思路 |

- **合计约 2.0 MB 文本资产；要生成 46 条**（A 提示词 24 / B 证据层 22 / C 术语数据表 15 / D 文档界面 4）
- ⚠️ **数字 ≠ 工作量**：`src/*.css`(414 KB) 是样式、`caelus`(1.7 MB) 是第三方星历、`chinaBirthPlaceTree.json`(912 KB) 是地名数据 → **三者都不是文案**，已在盘点 §七 单列剔除 → **真正"要生成"约 1.0–1.2 MB**
- ⚠️ **盘点自身的边界**：**全程未读正文** → 所有类型判定**都是按文件名与体量推断**；⑤ 未测；顶栏多个目录未遍历；**体量取自 0.2.1 的 dist，不是仓库 HEAD**

### 3.7.2 ⭐ 用户裁决①：**古籍也全部从公网重新采**（不沿用）
- **理由不是版权**（古籍本就公有），而是 **"来源链干净"** —— **从公网重新采，将来无需解释"这份文本从哪来"**；从 AGPL 仓库复制则要解释
- ✅ **附带好处：直接消掉了"哪个是古籍、哪个是现代整理"的判定难题**（不用判了，全部重新采）

### 3.7.3 ⭐ 用户裁决②：**文案的生成与调研交给另一个 Agent**，本节负责**明确它要做什么**
→ 已产出 **`40_节/待办/节128-附件/文案Agent任务书.md`**（**自包含、可直接转交**），要点：

| 部分 | 内容 |
|---|---|
| **三条最高纪律** | ① 🚫 **不碰 `mingyu` 任何文本**（连"看一下"也不）② 🚫 **按主题从零生成，不照任何描述/参考文本改写** ③ ✅ **古籍从公网重新采并记来源** |
| **两块任务** | **调研**（⭐ 先立**术语总表** → 定公网古籍来源 → 定各批结构规格）→ **生成**（**A → C → B → D**） |
| **内容规范** | 大白话面向普通用户；🚫 **不做数字吉凶评分**（节121 甲）；复用既有免责口径；🚫 **不越界**（医疗/投资/婚恋断言/真人财务数据） |
| **交付格式** | 每条一个 JSON（含 `basis` 记依据、`meta.self_check` 自检）；**古籍单列格式**并标 `is_classic_original: true` |
| **验收 9 条** | 含"**无照抄痕迹**"抽检（抽 5 条，能说清依据且找不到抄自哪里） |
| **边界** | 🚫 不碰代码、不改其他文件、**不提交 git**（Git 由父 Agent 统一做） |

> 🔒 **重申 §3.6.3 的铁则**：**重新生成要"按主题生成"，不要"照着原文改写"** —— 改写仍有**衍生作品**风险；按主题从零生成才**完全独立**。
> ⚠️ **给用户的一句提醒**：`mingyu` 自己那套文案 ≈2 MB → **"全部自研"带来的文案重建量不小**（**用户自己 AI 生成、只在太初的那部分不用重做**，两部分不能混算）。

---

# 四、执行方案（**采纳用户建议的流程**）

## 4.1 责任分工
| 角色 | 职责 |
|---|---|
| **Agent（本会话）** | ① 把 mingyu 的**全部文案盘点出来**（清单 + 位置 + 用途）② **外部核实**：为每一类找**公有领域 / 可替代的干净来源** ③ 出**分类结论**（可直接用 / 必须重写 / 建议换源） |
| **文案 Agent** | 按 §4.2 的判据**逐条裁定**，产出**重写工单** |
| **用户** | ① 提供**文案归属举证**（§三）② 对"可用 / 重写"的裁定**拍板** |

## 4.2 分类判据（**三类，与节126 §1.5.7 同口径**）
| 类别 | 判据 | 处置 |
|---|---|---|
| **① 公有领域** | **古籍原文**、传统术数规则、天文历法事实 | ✅ **可直接用**（⚠️ **但从古籍原文/公有领域来源取；现代点校版另有整理者权利**） |
| **② 用户自有** | 用户提供、**且有举证**的原创文案 | ✅ **可直接用** —— ⚠️ **但必须从用户自己的原稿取**（不从 AGPL 仓库复制） |
| **③ 第三方现代创作** | mingyu 作者写的文案 / 提示词 / 解读话术 / 200 题评测集 | 🚫 **必须重写**（或换外部干净来源） |

## 4.3 分期建议（**Agent 建议，待用户确认**）
⚠️ **17 个能力不可能一口气做完** → 建议**按"不依赖 mingyu + 认知度高"优先**：

| 期 | 内容 | 为什么这个顺序 |
|---|---|---|
| **第 0 期（先做，不动代码）** | **文案盘点 + 外部核实 + 分类裁定** | 用户建议的流程；**成本最低、且决定后续全部工作量的量级** |
| **第 1 期** | **八字**（太初本就**不依赖 mingyu**，走 `lunar-python`）+ **紫微**（走 `iztro`）+ ⭐ **真太阳时/出生时间正确性链**（**已从节120 并入，用户 2026-09-11 裁决**） | **认知度最高、地基已核实为 MIT**（§3.5.4）→ 风险最低、收益最快；且**真太阳时属排盘输入链，与八字同期做一次做对** |
| **第 2 期** | 星盘 / 七政 / 五运六气 | 九法内、依赖 `mingyu-core` |
| **第 3 期** | 三式（奇门 / 大六壬 / 金口诀）+ 太乙 / 皇极经世 | 术语门槛高、实现复杂 |
| **第 4 期** | 横向模块：六爻 / 梅花 / 小六壬 / 灵签 / 塔罗 / 雷诺曼 / 择日 / 生肖流年 | 量大但相对独立 |

> 🔑 **每期完成一项就下线一项 `mingyu-core` 依赖** → **可随时停损**，不必等全部做完才见效。
> ⭐ **附带好处**：**自研完成一项，"禁止升级 `mingyu-core`"的约束就少一条**；全部完成后**该约束自然解除**（不再依赖）。

## 4.4 ⚠️ 与现有节的**顺序冲突**（**需用户裁决**）
| 现有节 | 冲突点 |
|---|---|
| **节120 出生时间正确性链** | ✅ **已并入本节**（用户 2026-09-11 裁决："**真太阳时也并入自研**"）→ 归入 **第 1 期**；**节120 保留为缺口事实源**（N1–N3：均时差 / 1986–1991 夏令时 / 输入不确定性声明），**不重复记录** |
| **节123 / 124 / 125**（MBTI 换源） | ⏳ 等用户交翻译；与自研**无技术冲突**，但**会争同一批注意力** |
| **节116 / 117 / 118** | 节117（确定性预判标记层）与自研内核**天然耦合**（自研时顺手把标记层做出来） |

---

# 五、收益（**理由清单** —— 立项的正确依据）
| # | 收益 |
|---|---|
| 1 | **口径自主** —— 现在 vendor 的包**我们不能改**；自研才能按太初需要定口径 |
| 2 | **不受上游许可证变动影响** —— ⭐ **这次 AGPL 事件就是活例子**（上游今天改了，依赖策略得跟着改） |
| 3 | **体积 / 依赖** —— 可去掉 `vendor/mingyu-core/`（2.4MB）+ `node_modules/mingyu-core`（旧登记 ~23MB） |
| 4 | **`节120` 真太阳时一次做对** |
| 5 | **护城河** —— 核心计算是自己的，不是"别人包里有什么我们才有什么" |
| 6 | **合规疑问一次性关闭** —— 连"MIT 版本会不会被质疑"这类问题都不存在 |

---

# 六、硬约束（**不许违反**）
- 🚫 **不做会加重服务器负担的方案**（2 核 / 894MB，实测可用 226MB；无新增常驻进程 / GPU / 本地大模型）
- 🚫 **排盘 / 校准保持确定性计算零 LLM**（成本红线）
- 🚫 **免构建前端**：不引入打包链
- 🚫 **不从 `Brhiza/mingyu` 仓库复制任何文本或代码**（见 §三 法律要点）

# 七、禁碰清单
- **第 0 期不产码**（只做盘点 / 核实 / 分类）
- 🚫 **不得一次性移除 `mingyu-core`** —— 必须**逐能力替换、逐能力下线**；替换中的能力**不得出现功能空窗**
- 🚫 **不得从 AGPL 仓库取文案**（哪怕是你自己写的也走原稿）
- 🚫 **不得改计费口径**（红线⑤）—— 自研不应顺带改价
- ⚠️ **不得在未验证"新实现与旧实现的排盘结果一致"前就下线旧实现** → **每个能力必须有对拍验证**（新实现 vs 旧实现，同输入比输出）

# 八、冲突检查
- 与 **节126**（mingyu 借鉴 + 九法单飞）：**节126 是上游调研**，本节是其结论的落地；⚠️ **法律分析不重复**（以节126 §1.5 为准）
- 与 **节120**：**建议合并做**（见 §4.4）
- 与 **`00_根/来源.md`**：自研完成后，`mingyu-core` 的登记应从"依赖"改为"历史依赖（已下线）"
- 与 **`00_根/入口.md`「什么绝对不能坏」**：**排盘/校准为确定性计算零 LLM** 这条在自研中**更须守住**

# 九、用户结论
✅ **已定：全部自研**（2026-09-11）
⏳ **待用户**：① 文案归属举证（§三）② 分期顺序确认（§4.3）③ 是否把节120 并入本节（§4.4）

---

# 十、子节拆分（2026-09-13 拍板执行）

> 用户裁决：本节太大，拆成若干子节一步步做。母节管总账与纪律，子节管执行；**每完成一个子节 = 删除对应 import + 下线一块依赖 + 对拍通过**，可随时停损（§4.3 原则不变）。
> 替换对象清单已按 `paipan-node/server.mjs` L124–142 实测核实（18 个 import，17 个能力）。

## 10.1 子节总账

| 子节 | 名称 | 期 | 替换的 mingyu import（server.mjs） | 前置 |
|---|---|---|---|---|
| **节148** | 自研内核基建：管线架构 + 对拍验证框架 + 语言选型（✅ 语言已定 **JS/TS**，2026-09-13） | 0 | 无（搭地基） | — |
| **节149** | 真太阳时与出生时间正确性链（节120 并入） | 1 | 无（mingyu 本就没有） | 148 |
| **节150** | 八字/紫微自研收口 + 证据层（MIT 地基核验） | 1 | 无直接 import（核验 lunar-python/iztro 链路无 mingyu 残留） | 148、149 |
| **节151** | 西洋占星星盘自研（含星历方案） | 2 | `calculateBirthChartBundle`（npm） | 148 |
| **节152** | 七政四余 + 五运六气自研 | 2 | `generateAstrolabe`、`buildAstrolabe*ScopeContext*`、`calculateWuyunLiuqi`（npm） | 151（共享星历） |
| **节153** | 三式自研：奇门时家 / 大六壬 / 金口诀 | 3 | `generateQimen`、`generateLiuren`、`generateJinkoujue` | 148 |
| **节154** | 奇门终身局 + 太乙 + 皇极经世自研 | 3 | `calculateQimenLifetime`（vendor 0.2.2 独有）、`generateTaiyi`、`calculateHuangjiJingshi` | 148、153 |
| **节155** | 六爻 + 梅花易数 + 小六壬自研 | 4 | `generateLiuyao`、`generateMeihua`、`generateXiaoliuren` | 148 |
| **节156** | 观音灵签 + 黄历择日 + 生肖流年自研 | 4 | `drawRandomSign`、`generateAlmanacSelection`、`calculateZodiacYearFortune`、`getYearTaiSui` | 148 |
| **节157** | 塔罗 + 雷诺曼自研 | 4 | `drawTarotSpread`、`tarotSpreads`、`drawLenormandSpread`、`LENORMAND_SPREADS` | 148 |
| 收尾 | vendor/npm 依赖摘除、来源.md 改登记、包体核验 | — | 全部 import 归零后执行 | 151–157 全完 |

## 10.2 依赖关系与并行度

```
节148 基建 ──┬─ 节149 真太阳时 ─ 节150 八字/紫微收口
             ├─ 节151 西洋星盘 ─ 节152 七政/五运（吃星历）
             ├─ 节153 三式 ─ 节154 终身局/太乙/皇极
             ├─ 节155 六爻/梅花/小六壬
             ├─ 节156 灵签/择日/生肖
             └─ 节157 塔罗/雷诺曼
（151 之后各能力节相互独立，可按精力穿插；建议顺序仍按 1→4 期，认知度高、风险低的先行）
```

## 10.3 每个能力子节的统一交付标准（DoD）

1. **对拍通过**：以 vendor/npm 旧实现为黄金参考，同输入批量 fixtures 比对，关键排盘字段一致率 100%（允许无序集合/浮点误差按字段定阈值）；无对拍通过不得下线旧实现（§七 禁碰）
2. **确定性零 LLM**：排盘/校准不调 LLM（§六 红线②）
3. **单一权威规则源**：规则表/术语表只存一份，禁止多处补丁（见经验：规则漂移教训）
4. **纯函数管线**：构局/推导/命名拆为可组合纯函数，不与 IO/LLM/格式化耦合
5. **文案干净**：古籍从公网重新采并记来源；现代白话按主题从零生成（§3.6 纪律），不从 mingyu 仓库复制
6. **下线闭环**：server.mjs 删 import → 功能不出现空窗 → 依赖扫描归零 → 该能力在子节文件中勾销
7. **双线路前瞻**：内核接口形态考虑节142 胖 APK（语言选型节148 定案；若选 JS/TS 可进 WebView，省一个移动运行时）

## 10.4 文案工作流（平行工单，不占子节号）

- §3.7 盘点产物（`节128-附件/mingyu文案盘点.md`）+ 文案 Agent 任务书继续有效
- 文案按能力子节的进度配套交付；每个子节开工时从任务书摘对应工单，不在子节里重复盘点
- ⚠️ 若文案 Agent 已在另一 session 执行，以本母节为准继续，**不叫停**（自研已最终确认）

## 10.5 状态跟踪

- 子节状态以各子节文件头为准；母节本表只登记编号与依赖
- 全部子节完成后：母节移 40_节/完成/，`00_根/来源.md` 的 mingyu-core 改「历史依赖（已下线）」，§五收益逐条验收

