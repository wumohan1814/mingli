# 节140 · T3 实施拆分（2026-09-14 用户拍板后定稿）

> T2 评审已通过（用户回答见节140 主文件 §八）。T3 拆 **4 节**，按序执行，每节独立可验证、可回滚。
> 前置：节139（八法）✅ 已落地；节130 已废弃归档（标签方案吸收）。
> 旧 HUB URL（/guoxue · /xishi · /mbti）= **301 重定向**到 /explore 对应标签（用户拍板）。

---

## T3a · 功能目录静态配置（CONTENT.catalog）—— ✅ 已实施（2026-09-14）

**目标**：一张「方法元数据表」成为全站功能目录唯一事实源（用户已接受该小基建成本）。
**产出**：
- `frontend/public/data/content/catalog.js`：`window.CONTENT.catalog`
  - `methods[]`：**23 条** 每法 `{id, name, blurb(一句白话), culture[], play[], answer[], level(L1-L4), mins, pay, needCase, page, params?, single?}`
  - `intents[]`：6 意图 `{key, title, sub, icon, recs:[{methodId, why}]}`（意图 → 推荐带理由，冷启动人工配置表）
  - `tagMeta`：标签文案（culture/play/answer/level/pay/needCase 各组 chip 显示名）
- `frontend/public/data/content/index.js` 挂载 `catalog`（`window.CONTENT.catalog`）
- `frontend/public/index.html` 加载（data/content/catalog.js 于 content 6 件之后 → 现 7 件）
**验收（已过）**：node --check + precompile（散色 0）绿；node 冒烟 = methods 23 / intents 6 / 无重复 id / 无悬空推荐 / level 全部合法。
**数据口径留痕**：命盘单法（bazi-pattern/ziwei/qizheng/qimen-lifetime/wuyun-liuqi）`single:true` → page 指向 nine-pick（暂经八法合一进入，将来单飞时改 page）；塔罗合并一条（单张/牌阵同页）；时势拆 taiyi/huangji 两条；配对 page:'pair' 由 T3b 导航层特殊处理（弹窗）；起名 page:'guoxue-tools'。

## T3b · 意图首页 + 意图推荐页 —— ✅ 已实施（2026-09-14，含 explore 基础版）

- `LandingPage` 重写为意图首页：6 意图卡（来自 `CONTENT.catalog.intents`）+ 回访区（继续上次=最近 hasReport 档案→archive / 我的命盘=默认档案→nine-pick / 本月流运→zodiac，仅登录显示）+ 「探索全部方式」入口
- 新增 `/intent/:key` 意图推荐页（IntentPage）：推荐卡（方法名+难度/耗时 chip+理由）+「我自己挑方式」逃生舱
- 新增 `/explore` 探索浏览器基础版（ExplorePage）：文化/玩法/难度三组标签筛选 + 卡片墙（L1→L4 排序）——**T3c 主体已提前并入本节点**（目录刚建好即消费展示）
- divination 页支持 `initialMethod` 深链（目录 params.method 落地）；配对 page:'pair' → nine-pick（其页内有两法配对入口）
- 意图卡图标对齐现有 Icon 集（orbit/spark/chat/calendar/nine/bulb）；样式只用既有令牌（散色零新增）
- 门禁：node --check + precompile（散色 0）+ smoke-check（9 过/0 错）绿；服务实测 `/`、`/explore`、`/intent/ask`、`/intent/random`、catalog.js、views-home.js、pages.css 全 200
- 已提交 `1a8eb13`
- **剩余**：旧 HUB URL 301 重定向（T3d）；explore 打磨（标签记忆/搜索/「答什么」筛选组）后续可加

## T3c · /explore 工具浏览器 —— 🟢 基础版已随 T3b 落地

- 标签筛选条（文化/玩法/难度）+ 卡片墙（L1→L4）**已实现**（ExplorePage，消费 `CONTENT.catalog.methods`）
- **旧 HUB URL 301 重定向**（/guoxue → /explore?culture=中华 …）→ 归 T3d 壳层拆除一并做

## T3d · 壳层拆除 + 回归走查（后续节）

- 删除：左导航三键（NavRail 模块键）、三套 landing 模块落地（LandingPage 已改意图页，残余 module/skin 切换逻辑待清理）、3 个 HUB 页、配对三入口收敛到「我和 TA」意图、banner 模块下拉
- 旧 HUB URL 301 重定向（/guoxue · /xishi · /mbti → /explore 对应标签）
- 工具页与后端零改（节140 护栏 1）
- 全站回归走查（首页/explore/工具页/档案/配对/设置），旧 URL 重定向验证
