# 节 136 · 前端 UI 元素集中管理（收口）

> 编号说明：本草稿原误用 135（与已完成的「节135 正文数据统一管理」撞号），2026-09-12 改号 136；135 永不复用。

状态：🟡 **阶段 A–D 全部落地，待用户统一真机验收**（B/C/D 一次性完成，跳过逐阶段真机验收）｜ 强度：中改（纯前端叶子层）｜ 日期：2026-09-12

> 来源：本轮「破竹协议架构评估」——前端 UI 审计判定 **基本集中带少量散落**：集中化骨架（`tokens.css` / `ui-copy.js` 的 TC_COPY / `components.js` 的 ModalBase·toast·Icon / `[data-skin]` 6 皮肤 / `pairs.js`·`term-cards.js` 数据）真实且被主用；散落集中在 `pages.css` 与 10 个 `views-*.js` 的内联色值与文案。
> 本节 = 散落点台账 + 集中方法 + 分阶段执行计划；供后续 session 按阶段逐节执行收口。

## 复述

把散落在前端叶子层（`pages.css` / `views-*.js` / `admin.html`）的硬编码色值、内联中文文案、重复映射、内联图标，按「六层单一来源」逐一回收，最终达到「改一处设计 = 改一个 token / 一个 key」的集中化目标。

## 期望形态（验收标准 · 用户可观察）

- [x] `tokens.css` 补全间距/阴影族（**阶段 A 实际按 standards/04 v1.2 落地为 `--taichu-sp-*` / `--taichu-shadow-*`，见下方裁决①**）
- [x] `tokens.css` 之外无任何 `#hex` / `rgb()` / `rgba()` 硬编码色值 —— 阶段 B 把基线 **535 处全部归零**（现存 0，门禁只许减不许增）
- [~] 无 `toast('中文')` / JSX 内联中文 —— **C1 完成**：全站 50+ 处 toast 迁入 `TC_COPY.ui.toast`（69 key，含 12 处拼接→`fmtTpl`）；整屏长文案与 admin `UI_LABELS`（后台独立体系、不加载 TC_COPY）按裁决保留，见 C 记录
- [~] 无手写 `modal-mask`、无内联 SVG / emoji —— 真彩色 emoji 已清零（☕💡→`Icon`）；星座符号 ♈-♓、五星 ★☆、✓✕ 排版符号保留；6 处「内联 SVG」经核为 sprite `<use>` 引用 / 数据可视化画布（紫微圆盘·大衍棋命盘），不属 UI 图标，保留
- [x] 「花色→颜色」映射单一来源；`SkinSwitcher` 覆盖 6 skin 且已挂功能设置页（不再 `display:none`）
- [x] 每阶段 `node frontend/scripts/precompile.js` 门禁 + git 留底（A/B/C/D 各有 commit）；真机验收本次为 A–D 完成后**统一一次**（用户拍板跳过逐阶段）

## 散落点清单（现状坐标 · 台账，行号来自本轮只读审计）

### P1 色值散落（危害最大：改一处设计要到处改）
- `css/pages.css` 全文件 ≈144 处 `#hex` + ≈138 处 `rgba`：
  - `:215` 五色一行；`:601-603` `qm-tag` 三态；`:627-636` 塔罗花色；`:756-770` 落地渐变；`:941-1193` 摇签/硬币/小六壬金属色；`:1231-1235` 花色（与 views-xishi 重复）
- `admin.html:12-16` 自建调色板（与 C 端 tokens 双轨）；`components.css` 少量散色
- `tokens.css` 缺 `--space-*` 间距族、`--shadow-*` 只有单个 `--shadow-card`

### P2 文案散落（与 REQ-128 同源）
- 44–46 处 `toast('中文')` 内联提示，全未走 `TC_COPY`：`views-guoxue.js:151/161/165/169/511/516/520/838-847`、`views-divination.js:3596-3891`、`views-home.js:479-508`、`views-cases.js:1580-1679`、`views-agent.js:127/198/209`，及 views-zodiac / views-mbti / views-xishi / views-guoxue-tools 零星处
- 整屏内联文案：`views-zodiac.js:641-866`、`views-home.js:588-667`、`views-guoxue-tools.js:149-323`
- `components.js:448-457/475/506/524/534/542/563/568/583-604` 通用件硬编码 + 自建 `ASTRO_SCOPE_CN` / `PLANET_SYM` 小词典
- `admin.html:186-324` 全内联 `UI_LABELS`（后台与 C 端文案双轨）

### P3 一致性散落
- 「花色→颜色」映射重复 3 处：`views-xishi.js:660` / `:2078` vs `css/pages.css:628-636` / `:1231-1235`
- 皮肤主色 JS 复刻（应走 `--skin-*`）：`components.js:713-721`、`views-home.js:919-1008`、`views-zodiac.js:292`
- 内联 SVG：`views-zodiac.js:17-108`；emoji 图标：`views-agent.js:245`（☕）等
- `SkinSwitcher`（`components.js:398-410`）仅覆盖 3 skin 且 `.skin-switch` 在 `components.css:124` 被 `display:none`，与 6 skin 体系脱节

## 集中方法（六层单一来源 · 红线）

| 层 | 唯一来源位置 | 收口禁止 |
|---|---|---|
| 设计令牌 | `css/tokens.css` | 硬编码色值/间距/阴影 |
| 皮肤（6 主题） | `tokens.css` 的 `[data-skin]` | JS/视图复刻主色 |
| 文案 | `data/ui-copy.js`（`TC_COPY.ui`） | `toast('中文')` / 内联中文 / 自建词典 |
| 通用组件 | `js/components.js` | 手写 `modal-mask` / 自建弹窗 |
| 图标美术 | `--art-*` + `Icon` | 内联 SVG / emoji |
| 数据 | `data/pairs.js` / `term-cards.js` | 视图内复刻映射 |

## 分阶段执行（每阶段 = 一节，不混塞）

- **阶段 A · 令牌补全**（零视觉变化，先打底座）✅ 2026-09-12 落地：`tokens.css` 新增 standards/04 v1.2 全族 `--taichu-*`（色阶/皮肤别名/字体/字号/间距/圆角/三层阴影/玻璃/动效/语义别名）；`precompile.js` 增加**基线式**散色门禁（基线 `scripts/hardcode-baseline.json`，现存 535 处只许减不许增，`--update-hardcode-baseline` 随 B 阶段收缩）。
- **阶段 B · 色值回收**（清 P1）：`pages.css` / `components.css` / `admin.html` 硬编码色 → `tokens.css`（复用已有或新增语义 token，如 `--metal-gold-*`、`--tarot-suit-*`）。
- **阶段 C · 文案回收**（清 P2，与 节105 REQ-128 协同）：44–46 处 toast / 整屏内联 / `components.js` 小词典 → `ui-copy.js`；`admin.html` UI_LABELS → 后台文案中心或共享 `ui-copy.js`。
- **阶段 D · 一致性归一**（清 P3）：花色映射三处合一；JS 复刻主色 → `--skin-*`；内联 SVG / emoji → `Icon` 或 `--art-*`；`SkinSwitcher` 补全 6 skin 并解除 `display:none`。

## 冲突检查 / 关联

- 与 **节105 REQ-128（文案统一管理）**：P2 文案回收是其「toast/内联」子集，**实施阶段 C 前先读节105**，避免重复或口径冲突。
- 与 **节134（三主题视觉系统升级）**：节134 已定新 token 骨架（午夜蓝/香槟金/象牙白）与 `[data-theme]` 方向，**阶段 A 补 token 前先读 `docs/standards/04-前端设计令牌.md` / `05-前端组件库规范.md`**，不得与节134 骨架冲突。
- 与 **节110 拆分规范**：拆出的 `.js` 禁止写 JSX、资源走根绝对路径，收口时仍遵守。

## 禁碰清单

- 🚫 `backend/` `data/` `ops/` 不动（纯前端）
- 🚫 不改 `method-result v2` / `chart.json` 契约
- 🚫 不做视觉重设计（那是节134 的活）；本节只「回收散落」、不改设计值

## 硬约束

- 一次会话一节（阶段 A–D 各自成节，不混塞第二个需求）
- 每步 git 留底 + `node frontend/scripts/precompile.js` 门禁 + 用户亲验
- 不新增依赖、不加重服务器负担（铁律 8：优先删/收，而非加机制）

---

## 阶段 A 完成记录（2026-09-12）

### 裁决（细化中发现跨节冲突，用户 2026-09-12 拍板"先做 A"，默认建议生效）

1. **令牌命名/选值按 standards/04 v1.2（节134 定稿），不按本文早期文字的 `--space-*`（4px 基准）**：新增一律 `--taichu-*`；间距 8px 基准 sp-1/2/3/4/6 = 8/16/24/32/48；圆角新值 10/14/20/28；阴影三层柔和值。**旧令牌一律不动、不重指**（把 `--radius-*`/`--shadow-card` 重指新值会产生视觉变化，属节134 运行层）。墨蓝/金色阶 400–900 新旧名同值重复为过渡态，节134 收口为单点。
2. **门禁取基线式而非全量禁止**：现存散色实测 535 处（css 427 / js 54 / 两个 HTML 54），全量禁止会立即不可用；基线只拦"比基线多"，B 阶段回收后用 `--update-hardcode-baseline` 收缩。
3. **阶段顺序（用户 2026-09-12 拍板）**：**节136 A–D 全部完成后，才开节134 运行层**——先把 UI 素材/色值/文案全部集中到一处，节134 才能基于统一状态高效改进。即顺序固定为 A（✅）→ B → C → D → 节134 视觉升级。代价（已告知、用户接受）：B/D 在旧皮肤体系（`data-skin`/旧令牌名）上收口，节134 落地时再做一次新令牌消费迁移与 `data-skin→data-theme` 映射；阶段 A 已让令牌底座与 v1.2 对齐，B 阶段新增语义令牌直接用 `--taichu-*` 命名可减少二次改名。C 的先后不再单独前置，按 B→C→D 原立项顺序推进。

### 改动（3 个文件，纯前端）

- `frontend/public/css/tokens.css`：`:root` 内新增 v1.2 令牌块（无消费者，零视觉变化）；皮肤 5 新名以 `var(--skin-*)` 别名动态跟随现行 6 皮肤。
- `frontend/scripts/precompile.js`：新增硬编码色值基线门禁（扫描 css/*（tokens.css 豁免）/js/*/两个 HTML；命中 `#hex` 与 `rgb()/rgba()`；基线键 POSIX 正斜杠跨平台一致）；JSX 与散色两道守卫均先于 HTML 编译。
- `frontend/scripts/hardcode-baseline.json`：新建基线（12 个文件，共 535 处）。

### 控制点结果

- 回归（脚本门禁）：`node --check` 通过；`precompile.js` 全绿；两个 HTML 编译块数 0、**字节数完全不变**（零视觉变化的脚本侧证据）。
- 反证：探针文件含 `#aabbcc` → 门禁 exit(1) 并报出文件/行号；删除探针后复跑通过。
- 性能 / 资源：不适用（纯 CSS 变量声明 + 构建脚本，运行时零新增逻辑）。
- 安全 / 数据：不适用。
- 真机验收：⏳ 待用户（预期页面与改动前逐像素一致）。

### 本节指标（阶段 A）

- 模型 + provider：Trae 自研模型 ｜ 澄清往返：1（拍板"先做 A"）｜ 耗时：本次会话内

---

## 阶段 B 完成记录（2026-09-12，色值回收）

### 改动（纯前端，commit 25ce5b9；缺陷修复 e208a7d）

- 285 种去重散色值 → 新增 **260 个 `--tc-*` 语义令牌** + 复用 9 个已有令牌（透明色族 `--tc-{族}-a{alpha}`）。
- **481 处散色同值等价替换**：`pages.css` 370 + `components.css` 57 + 10 个 `views-*.js` 54；`index.html` Canvas 2D 改 `getComputedStyle().getPropertyValue()` 运行时读 token（Canvas 不支持 `var()`）；`admin.html` 保持独立调色板（不加载 tokens.css），`:root` 用块级豁免、业务色用行豁免。
- 基线 **535 → 0**，门禁全绿。

### 过程中修复的两个严重缺陷（e208a7d）

1. codemod `DEFS.map(([group, rows]) => …)` 只解构前两元素致生成变量乱码 → 改 `([group, ...rows])`。
2. 新令牌块被写到 `:root{}` 闭合之外致变量不生效 → 移回 `:root` 内。

### 控制点结果

- 门禁：现存 **0 处散色**，只许减不许增。
- 定义/消费对账：256 个被消费的 `--tc-*` **100% 有定义**（0 未定义）；浏览器实测 CSS 变量全部可读。
- `node --check` 全通过。同值替换 = **零视觉变化**（B 阶段硬性边界）。
- 真机验收：⏳ 并入 A–D 统一验收。

---

## 阶段 C 完成记录（2026-09-12，文案回收）

### C1 toast 集中（commit 35f0b37；语法修复并入 e208a7d）

- 全站 **50+ 处** `toast('中文')` 迁入 `TC_COPY.ui.toast`（**69 个 key**，`data/ui-copy.js`）。
- **12 处字符串拼接 toast → `fmtTpl` 模板化**；`fmtTpl` 工具由 `views-xishi.js` 上移至 `components.js`（全局可用）。
- 修复 `ui-copy.js` toast 模块注入时多出的一个闭合 `}`。

### C2 裁决（简化，不单独扩做）

- `components.js` 相位色 `aspectColor` 收口**并入阶段 D1**（与花色同套视觉语言，一起做更内聚）。
- **admin `UI_LABELS` 不迁移**：后台是独立体系（不加载 `tokens.css` / `ui-copy.js`，独立登录态），量大且与 C 端零共享，强行并入会迫使后台加载 C 端资源，违"优先删/收不加机制"。后台若未来统一，另起一节。
- 整屏长文案（生肖/设置/国学工具页）属 REQ-128（节105）正文/整屏文案范畴，不在本节"toast 提示收口"范围，留节105 后续。

---

## 阶段 D 完成记录（2026-09-12，一致性归一，commit 9d25e22）

### D1 花色映射三处合一 + 相位色（**全节唯一计划内视觉变化点**）

- `tokens.css` 新增 **13 个令牌**：3 塔罗主色 `--tc-suit-major/wands/cups`（紫晶 5B4B8A / 金 B8893E / 朱砂 C0392B）+ 5 塔罗浅底 `--tc-suit-{major,wands,cups,swords,pentacles}-bg`（rgba 半透明）+ 5 相位色 `--tc-aspect-{conjunction,square,trine,opposition,sextile}`。
- CSS 三处统一读令牌：塔罗卡边框（`pages.css:629-637`）、浅底芯片（`:713`）、fc-art 卡框（`:1232-1236`）。
- JS 侧：`TAROT_SUIT_COLOR`（views-xishi）与 `aspectColor`（components）及所有 fallback 统一为 `'var(--tc-suit-*)'` / `'var(--tc-aspect-*)'`。
- **视觉变化（仅浅底芯片 5 花色，CSS 标签色与 JS 占位块本就两套值，本次合一）**：cups 蓝→朱砂浅底、pentacles 金→绿浅底、swords 灰蓝→剑蓝浅底、major 深紫→紫晶浅底、wands 赤陶→金浅底。牌框主色为同值收口、无变化。
- 收尾修正：扑克牌四花色（hearts/diamonds/spades/clubs）只有牌框主色场景、无浅底芯片，删除 D1 误加的 4 个无消费 `*-bg` 令牌（零影响）。

### D2 皮肤切换器 + 图标 + emoji

- **SkinSwitcher**：从 3 皮肤（仅国学可用、其余 disabled）补全为 **6 皮肤全启用**（国学 / 西式占卜 / 塔罗 / 占星 / 星座 / 人格测试），并**挂载到「功能设置」页**（此前组件定义了但从未渲染）；`applySkin` / `skin` 由 App 经 props 传入（与其他 view 一致）。`TC_COPY.ui.skin` 补 4 个皮肤名，新增 `home.set-skin` / `set-skin-desc`。
- **真彩色 emoji 清零**：新增 `Icon name="bulb"`；agent 空状态 ☕ → `chat`、生肖"行动建议" 💡 → `bulb`（配套 `.signal-bulb` 对齐）。
- **保留项（经甄别非装饰 emoji）**：十二星座符号 ♈-♓（占星专业符号 `SIGN_SYM`）、五星评分 ★☆、✓ 打勾 / ✕ 关闭（黑白排版符号，配合既有 CSS class，跨平台稳定）。
- **内联 SVG 调查结论**：清单所指 6 处全部是外部 sprite `<use href>` 美术引用（生肖/星座/关系图标，viewBox 100）或数据可视化画布（紫微圆盘按 12 宫角度动态绘制、大衍棋命盘引擎绘制容器），**不是 UI 图标，不应迁入 Icon**；真正的 UI 小图标已全部走 `Icon`（15 个 name）。

### 控制点结果

- 门禁：现存 **0 处散色**；`node --check`（components / views-xishi / views-home / views-agent / views-zodiac / ui-copy）全通过；`index.html` JSX 编译块 0。
- 浏览器冒烟（静态服 :8080）：首页 console **0 error**；13 个新令牌 `getComputedStyle` 全部有值无空；SkinSwitcher 临时挂载实测 6 按钮、文案正确、初始 active=国学、0 disabled，点塔罗→`data-skin=tarot`、点回国学→恢复；Icon bulb/chat/cross/check 的 path 数 3/1/2/1 正常。
- 性能/安全/数据：不适用（纯令牌引用与组件复用，运行时零新增依赖）。
- 真机验收：⏳ 待用户统一验收（重点看塔罗 / 雷诺曼占位块浅底的 D1 视觉变化点）。

### 本节指标（B–D）

- 模型 + provider：Trae 自研模型 ｜ 散色替换 481 处、toast 50+ 处、新令牌 273 个（B 260 + D 13）｜ 提交：25ce5b9 / e208a7d / 35f0b37 / 9d25e22（A=f4ddde2）。
