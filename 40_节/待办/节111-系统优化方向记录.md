# 节 111 · 系统优化方向记录（解耦后执行）

状态：🟡 待办（由节110 解耦过程中审查代码产出；解耦 4 阶段完成后另开节执行）｜ 强度：大改 ｜ 日期：2026-09-10

## 定位
本文件是**系统级优化点清单**（服务器压力 / 性能 / 渲染效果与速度），由节110 执行方在解耦过程中按 v0.15 排查四步审查代码产出。**本次只记录、不执行**；解耦完成后另开一节逐条执行。

## 记录格式（v0.15：性能的凭是数字，无数字不落）
| # | 位置（文件/区块） | 现象（哪里慢/重） | 根因 / 优化方向 | 改前数字 → 预期改后 | 代价 / 风险 | 优先级 |
|---|---|---|---|---|---|---|
| A | `art/{reference,samples,_drafts_r3}` | 美术参考/样本/草稿被当运行时资产静态服务 + 落入 SW 缓存根 | 交付件归档出 public（或收窄 SW 边界） | art 57.68MB（75 文件）→ ~13.4MB（3 目录 44.26MB 出） | 归档即不再服务；先确认无直链、SW 本就 cache-as-you-go | 高 |
| B | `vendor/babel.min.js` | 3.14MB 死文件（预编译后加载行已移除，index/admin 引用 0） | 预编译后清理遗留 → 删除 | vendor 3.38MB → ~0.24MB | ⚠️ 节110 §8 禁碰 vendor/，本次仅记录 | 中 |
| C | `index.html` 全文 + head `/data/pairs.js` | 入口 1149.1KB/22046 行 + pairs.js 253.7KB 同步加载，阻塞首屏 | 单文件超限 → 解耦（节110 本体）；pairs 按需/压缩 | 22046 行 → 阶段4 shell <2000 行、单文件 <5000 行 | 拆分带来加载顺序风险（`导航.md` §7 牵连表管控） | 高 |
| D | Waiting 5399 / Calibration 5564 / Predict 5957 + 充值 2160 | jobId 轮询每 2s、充值每 4s，并发下持续打 `/api/jobs`、`/api/credits/balance` | 前端定时轮询 → SSE / 长轮询 / 指数退避 / 后端主动通知 | ~30 req/min/用户（job）、~150 req/单次充值 → 请求量显著下降 | 后端改动，不在本次解耦范围 | 高 |
| E | `frontend/.npm-cache`（685 文件）+ `frontend/scripts`（259 文件） | node 缓存与探针被 git 追踪，放大 checkout / Docker build context | `.gitignore` 排除缓存与 `_tmp_*`/探针 | 685+259 文件入库 → 0（仅留活件） | 先区分活件（precompile/smoke-check）与死件 | 中 |
| F | `sw.js` 44–48 | SW 按 `/art/` 全根缓存，art 存量 57.68MB 已超 50MB 配额 → LRU 频繁淘汰 | 归档非运行资产后 art 存量达标 | art 57.68MB → <50MB（A 项落地后） | 无（A 项落地前不构成功能缺陷） | 低 |

> 阶段0 只填了前端/静态资源；admin.html 与 backend 相关条目由节110 阶段1–4 审查时追加。

## 排查范围（用户点名）
- **前端**：index.html / admin.html 的资源体积、渲染循环、样式与皮肤、动画、列表渲染、首屏
- **后端**：接口查询、循环、内存、DB 读写、LLM 调用、job 轮询
- **部署 / 镜像**：镜像体积、构建产物、静态资源压缩、CDN、缓存
- **服务器压力**：2 核 1G 主机下的内存 / CPU / 磁盘 / swap（注：本次部署已暴露「构建上下文过大」问题，已修 .dockerignore，见 git 提交 a08d6b0）

## 执行方式（解耦完成后另开节）
- 逐条按 v0.15「改前 X → 改后 Y」取数验收
- 涉及用户不知情的代价（8MB 图 / 全表查询 / 高频轮询）→ 主动向用户提代价选项

## 用户结论
待验收
