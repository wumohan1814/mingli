# 节 111 · 系统优化方向记录（解耦后执行）

状态：🟡 待办（由节110 解耦过程中审查代码产出；解耦 4 阶段完成后另开节执行）｜ 强度：大改 ｜ 日期：2026-09-10

## 定位
本文件是**系统级优化点清单**（服务器压力 / 性能 / 渲染效果与速度），由节110 执行方在解耦过程中按 v0.15 排查四步审查代码产出。**本次只记录、不执行**；解耦完成后另开一节逐条执行。

## 记录格式（v0.15：性能的凭是数字，无数字不落）
| # | 位置（文件/区块） | 现象（哪里慢/重） | 根因 / 优化方向 | 改前数字 → 预期改后 | 代价 / 风险 | 优先级 |
|---|---|---|---|---|---|---|
| （待节110 执行方填写） | | | | | | |

## 排查范围（用户点名）
- **前端**：index.html / admin.html 的资源体积、渲染循环、样式与皮肤、动画、列表渲染、首屏
- **后端**：接口查询、循环、内存、DB 读写、LLM 调用、job 轮询
- **部署 / 镜像**：镜像体积、构建产物、静态资源压缩、CDN、缓存
- **服务器压力**：2 核 1G 主机下的内存 / CPU / 磁盘 / swap（注：本次部署已暴露「构建上下文过大」问题，已修 .dockerignore，见 git 提交 a08d6b0）

## 执行方式（解耦完成后另开节）
- 逐条按 v0.15「改前 X → 改后 Y」取数验收
- 涉及用户不知情的代价（8MB 图 / 全表查询 / 高频轮询）→ 主动向用户提代价选项

## 用户结论
待验收
