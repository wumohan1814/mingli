# 节 114 · 前端资产瘦身（死图归档 + 塔罗降分辨率 + sw.js 注释）

状态：🟢 已执行（2026-09-11 上线验证）｜ 强度：中改（纯静态资产，零业务逻辑改动）

## 用户原话（2026-09-11 拍板）
"第一项（清死图）先做；第二项（塔罗牌面）先新建脚本再做、做完归档脚本；第三项（首屏懒加载）暂缓不做；第四项（sw.js 注释）顺手改。"

## 我的复述
按取数结论落地前端三项：① 把 ~50MB 未被引用的图片原件归档（不进线上/镜像但保留可重压）；② 塔罗卡面 720→480 降分辨率；③ 修 sw.js 过时注释。首屏懒加载按用户决定不做。

## 执行结果（凭是数字）

| # | 内容 | 改前 | 改后 | 证据 |
|---|---|---|---|---|
| 1 | 死图原件归档 | 无（散落 frontend/public） | 140 文件 → `_archive/美术-素材原件/`（git 记 R100 重命名，历史保留） | 塔罗 720px jpg 22MB + 雷诺曼根 png 17MB + art 死 png 11.5MB = **~50.7MB** 不再上线/进镜像 |
| 2 | 塔罗牌面降分辨率 | 80 webp 720×1200 = **15.3MB** | 480×800（q82）= **8.4MB**（-45%） | 脚本 `tools/_archive/process_tarot_downscale.py`（已按要求归档）；线上 `curl -sI` 返回 93KB 卡面 |
| 3 | 首屏懒加载 | — | **不做**（用户拍板暂缓） | — |
| 4 | sw.js 注释 | 写「Caddy 只反代、静态由后端提供」（节113 后过时） | 改为「Caddy 直服静态 + 1h 缓存头」 | 线上 `curl /sw.js` 已返回新注释 |

### 汇总（凭是数字）
- `frontend/public` 总量：**76MB → 19MB**
- Docker 镜像：**990MB → 868MB**（-122MB）
- 死图引用核对：代码已全用 `.webp`（tokens.css / views-home.js / views-xishi.js），被归档的 png/jpg **0 处引用**（grep 复核）

### 部署与回归（2026-09-11 线上验证）
- 死图 `/tarot/.../720px/*.jpg`、`/lenormand/*.png`、`/art/mingli-pet/*.png` 全部 **404**；新 webp 全部 **200** + `cache-control`
- `/api/health` ok、`/` 200、`/admin.html` 200；web 容器无重启循环
- 塔罗卡面画质：**待用户浏览器亲验**（本次模型不支持读图，无法自行目检）

## 控制点结果
- 回归：纯静态资产，未动任何 `.js`（除 sw.js 注释）/ `.css` 引用路径；grep 确认被归档件 0 引用
- 安全 / 数据：无接口 / 数据 / 资金改动
- 性能：见上表；静态已由 Caddy 直服（节113），本节的体积下降直接惠及首访流量与镜像/磁盘

## 结构反思（破竹 §8）
- 哪里难：「每个 webp 旁边都留着 png/jpg 原件」是美术出图流程的系统性遗留——上一节（节111）清过一次 `reference/samples`，这次清的是「散落在正式目录里的原件」。
- 怎么改：本次是「清存量」。**下次会怎样**：建议美术交付规范里明确「原件只进 `_archive/`，`frontend/public/` 只放线上用到的 webp」——否则每轮出图都会再攒一批。
- 归档脚本：按用户要求把处理脚本留档 `tools/_archive/process_tarot_downscale.py`，日后要换分辨率可从归档的 720px 原件 + 该脚本重出。

## 留痕
- 变更（大白话）：把约 50MB 用不上的大图原件挪出线上（但没删、还在仓库归档区）；塔罗牌从 720 压到 480、体积几乎减半；顺手改了一处过时注释
- 凭怎么产生：git commit `ef4367e`；线上 `curl` 头/状态码 + 镜像 `docker images` 前后对比
- 覆盖声明：证明了体积下降、死图下线、页面/接口无回归；**未证明**塔罗画质（待用户目检）

## 本节指标
- 模型 + provider：deepseek-v4-pro / DeepSeek Harness
- 脚本语言说明：用户说「新建 JS 文件」，但本地/容器 Node 均无图像库（sharp 未装），而项目资产脚本历来是 Python + Pillow（`tools/process_assets*.py`，Pillow 在 backend 依赖内）——故按项目惯例用 **Python** 脚本，已在脚本头注释 + 本表说明，避免为一次性脚本引入新依赖（铁律 8）。

## 用户结论
- 第 1/4 项：待验收（已上线）
- 第 2 项：**待用户过目塔罗卡面画质**（若放大看觉得糊，可从归档 720px 原件改 540/600 重出）
- 第 3 项：不做（用户拍板暂缓）