# 节150 · 八字/紫微自研收口与证据层（1 期）

- **状态**：🟢 **已完成**（2026-09-14 落盘；核验+收口为主，见 §完成证据）
- **母节**：节128
- **强度**：中改（核验+收口为主，非从零重写）

## 背景与范围

八字本就不走 mingyu（Python 侧 `lunar-python` MIT 地基 + `app/paipan` 自研：shensha/marker/scorer/firdaria 等）；紫微走 `iztro`（MIT，合法地基，允许保留）。本节不是重写排盘，而是**把 9 法（节139 后为 8 法）中的命理地基彻底收口到"零 mingyu + MIT 地基 + 自研上层"形态**：

1. **依赖扫描核验**：八字 4 法（bazi-pattern / bazi-dayun-liunian / bazi-shensha-nayin / bazi-hunyin-caiyun）与 ziwei 的全链路（排盘→切片→analyzer→prompt）无任何 mingyu 数据/文案残留
2. **上层规则自研核验**：格局判定、神煞、大运流年、十神、证据字段是否全部来自自研规则表；凡发现间接借自 vendor 的规则表，按主题从零重整理（古籍公网采源）
3. **证据层/标记层落地**：节117（确定性预判标记层）在本期顺手做入八字/紫微结论结构（哪些是确定性盘面事实、哪些是 LLM 解读，分层标注）；节117 仍为独立节跟踪进度
4. **真太阳时接入核验**：节149 的修正源被八字/紫微排盘实际使用
5. 文案：白话解读按文案 Agent 工单交付，古籍从公网采

## 交付物

- 依赖扫描报告（import/数据/文案三个维度）证明零 mingyu
- 8 法中 5 个命理法（4 八字+紫微）排盘结果与现行结果对拍一致（换地基前后结果零变化是底线）
- 证据层字段上线，前端展示口径另跟产品节

## 验收

- 八字/紫微在"移除 mingyu 后"的假想环境下可独立运行（本验收以依赖扫描+对拍为证，不实际提前删包）
- 排盘结果与当前线上一致；真太阳时修正后结果符合节148 用例
- 确定性零 LLM（排盘与标记层）

## 禁碰

- lunar-python / iztro 是 MIT 合法地基，不重造历法轮子（母节 §二 明确允许第三方 MIT）
- 不改计费口径

---

# 完成证据（2026-09-14 落盘）

## 1. 交付对照

| 交付物（上文） | 结果 |
|---|---|
| 依赖扫描核验（import/数据/文案 三维） | ✅ **零 mingyu 残留**：① **import 维**——bazi 4 法走 `lunar-python` + `app/paipan` 自研（shensha/marker/scorer/firdaria），ziwei 走 `iztro`（`ziwei.cjs` 纯包装）；server.mjs 的 18 个 mingyu import 全部属 extra/divination/tarot/zodiac/astrology（151–157 的替换对象），与八字/紫微无关 ② **数据维**——`app/paipan/*.py` 全量 grep `mingyu` 仅 2 处注释（engine.py L11-12 描述 extra 方法，非本链）③ **文案维**——`backend/prompts/**` 全量 grep `mingyu|命语` **零命中**（八字/紫微提示词全部自研） |
| 上层规则自研核验 | ✅ 格局判定/神煞/大运流年/十神/证据字段均来自自研规则表（`shensha.py`/`marker.py`/`scorer.py` + 自研 prompt）；未发现间接借自 vendor 的规则表 |
| 证据层/标记层落地 | ✅ L1–L5 证据层级契约在 `methods/base.py`（节116 产物）对八字/紫微 analyzer 生效；`paipan/marker.py` 八字4法确定性标记（月令十神/根气/寒燥/七杀/墓库）已注册并随 `mark_chart` 注入切片；**ziwei 等非八字法标记按节117 口径「后续逐法添加」，挂节128 之后**（不在本节） |
| 真太阳时接入核验 | ✅ 节149 校正面在 `engine.py` 排盘前统一修正：1988-07-15 08:00（夏令时窗内）→ 校正 07:00 → 八字时柱 **壬辰（辰时）**（未校正会错排巳时）；八字/紫微/七政全部吃同一修正源 |
| 文案 | 八字/紫微白话解读属文案 Agent 工单（节128 §10.4），古籍公网采源，本节不重复盘点 |

## 2. 验收证据（亲自复核）

```
- 依赖扫描：grep mingyu|Mingyu|MINGYU 于 backend/app（16 处全为注释/README 或待替换方法）、backend/tests（9 处全为 import 断言夹具）、prompts/**（0 命中）
- 排盘对拍一致：tests/unit/test_paipan.py 9 passed（含 chart.json 快照锚点——节149 后按新引擎重生成，bazi/ziwei 输出与快照深一致）
- 确定性：同输入两次排盘（剥离 generated_at/timeline_20y/current_year）深等 ✓
- 标记层：mark_chart(chart) 返回全部 8 法条目，bazi-pattern 含 bazi 标记集 ✓
- ziwei 链路：soul=文曲、12 宫完整（iztro 子进程正常）✓
- 真太阳时接入：DST 出生时柱 = 壬辰（辰时）✓
```

## 3. 结论

八字（4 法）+ 紫微已是「零 mingyu + MIT 地基（lunar-python/iztro）+ 自研上层」形态，**在假想移除 mingyu 的环境下可独立运行**（不实际提前删包，以扫描+对拍为证）；本节无代码改动（149 的校正面为唯一有意变更，已单独落节）。

## 4. 遗留（不属于本节）

- ziwei 及西占/七政/奇门/五运六气的确定性标记（节117 其余 5 法）：按母节口径挂**节128 全部子节完成后**另立节
- 八字/紫微的白话文案重建：随文案 Agent 工单交付
