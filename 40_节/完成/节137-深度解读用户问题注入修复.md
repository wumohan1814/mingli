# 节137 · 深度解读用户问题注入修复

状态：🟢 通过（后端代码+prompt 已在库，本节点补齐 prompt 声明与回归用例）｜ 强度：中改 ｜ 日期：2026-09-13（开工）→ 2026-09-14（收尾）

## 用户原话
本节由上个 session 立项（无逐字原话留存于本节点），立项依据为「用户在起卦/抽卡前选填的『占问问题』在点『深度解读』时没有被注入 LLM prompt，填了等于白填」。

## 我的复述
临时起卦（含雷诺曼）和塔罗两处，用户填的「占问问题」要真的进到深度解读的 LLM prompt 里；没填就照旧给默认解释。

## 期望形态（验收标准 · 用户可观察）
- 临时起卦/雷诺曼：填了问题 → 深度解读围绕该问题展开；不填 → 仍是默认解释
- 塔罗：深度解读已带 question（复核确认，无需改动）
- 同一 id 二次点「深度解读」仍命中缓存（零 LLM 零扣费）
- 零 DB 变更

## 现状坐标（Agent 用）
- `backend/app/api/divination.py` `interpret_divination()`（L523-528）——`user_payload` 组装处
- `backend/prompts/shared/divination-common.md` —— 10 法共用段（输入声明唯一处）
- `backend/prompts/interpret/lenormand.md` —— 雷诺曼单文件 prompt
- `backend/prompts/interpret/tarot.md` + `backend/app/api/tarot.py:202` —— 塔罗（已注入，只复核）
- `frontend/public/js/views-divination.js:2293/2384/2547/2580` —— 起卦 seed.question 出口
- `frontend/public/js/views-xishi.js:1821/1883` —— 雷诺曼 seedObj.question 出口
- `backend/tests/unit/test_divination.py` —— interpret 回归用例位

## 复用判断
- 库内候选：无（库内无 prompt 注入类资产）
- 结论：**改现有链路** —— 不必新写。`div.seed_json` 已存 question，零 DDL 直接读；prompt 输入声明已在 `divination-common.md` 单点承载，只改这一处即对 10 法生效（§9.1 判据 5 单点）

## 方案选项（中改及以上必填）
- A（采用）：**零 DDL，从 `seed_json` 现读** —— 代价：无迁移、无新列；效果：老行自动兼容（无 question 键即跳过）
- B（未采用）：给 `divinations` 加独立 `question` 列 —— 代价：一次 DDL + 回填；效果：与 tarot_readings 结构对称，但收益仅"查询方便"
- 用户选择：A（本节点既定方案，无新增往返）

## 禁碰清单
- 西式占星（xizhan）链路 —— 无问题输入 UI，不在范围
- 计费口径与 `interpretation_json` 缓存语义（红线⑤）
- `tarot_readings.question` 列与 `/tarot/draw` 契约
- 11 个 `divination-<method>.md` 法门专用文件（它们的「输入说明」只列本门富字段，不含键清单，无需改）

## 冲突检查
- 节116 / 节121（不打分、正文自检）：无冲突
- REQ-044 v2.1「有填才随 seed 透传」：一致（前端本就只在非空时写键，后端相应只在有键时注入）
- `divination-common.md` 原文写「user JSON 固定只有三个键…不要臆造其它字段」→ **与注入 question 相抵**，本节已消解（3 键 → 4 键）

## 不许被推翻的约定（**给未来的自己看**）
- 断卦/解读 user JSON 的键清单一律以 `divination-common.md` 为准（10 法）+ `lenormand.md`（雷诺曼）**两处**，新增键必须同时改这两处
- `seed_json.question` 是占问问题的**唯一存放位置**；不许新增独立列，不许在 result 里复制一份
- question 缺省时 user_payload **不得出现该键**（不是空串、不是 null）

## 控制点结果
- 回归：**通过** —— `pytest tests/unit/test_divination.py tests/unit/test_divination_focus.py tests/unit/test_tarot.py` = 42 passed / 1 failed。唯一失败 `test_tarot_draw_bad_spread_type_400` **为既存陈旧用例**（REQ-122 已把 celtic 列入 `TAROT_SPREAD_TYPES`，用例仍断言 400），与本节点改动无交集（git 未动 tarot 侧任何文件），已登记 99_状态/已知问题.md
- 安全 / 数据：**通过** —— 越权仍由 `_get_owned_divination` 拦截（跨用户 404 用例在套内通过）；question 仅用户自填文本，无凭据
- **性能：不适用** —— payload 增加一个字符串键，无查询/循环/缓存变化；缓存命中路径未改（二次点击零 LLM 用例通过）

## 设计取向（**大改必填**；中改写"已过五条判据"或"无命中"）
- 命中 **判据 5（单点）**：键清单只在 2 个 prompt 文件承载，不散进 11 个法门文件；**判据 1（轻量）**：零 DDL、零新表、零额外查询
- 判据 2/3/4 无命中：无易变接缝、无新埋点、无需开关

## 结构反思（`01` §8.1 触发条件命中时**必写**，否则写"未触发"）
- **触发（判据 1：本节定位代码 ≥3 次搜索）**
- 哪里难：同一个"输入键清单"散在 2 个 prompt 文件里（`divination-common.md` + `lenormand.md`），`lenormand.md` 本可只写"见公共段"却自抄了一份，本次改键要改两处
- 结构上怎么改：把 lenormand 的键清单收敛成一句指针（"输入键同 `shared/divination-common.md`，另见 result 结构"），只留 result 字段说明
- 下次会怎样：再新增键只改 1 处；本次不改（避免在修 bug 的节点里顺手重构 prompt，归因会混）

## 留痕（**文字必写；截图仅用户要求时**）
- 变更（大白话）：① 后端在生成深度解读时，会先从这次起卦的存档里把用户填的问题取出来，一起交给 AI；② 两份给 AI 的说明文档原本写着"输入只有三个字段、别管别的"，等于在主动叫 AI 忽略问题，现已改成四个字段并说清"有就围绕问题答、没有就不许自己编"；③ 补了一条自动化测试，盯住"填了要带上、没填不能硬塞"。
- 后端凭：`pytest -k interpret` → `7 passed, 10 deselected`；新增用例 `test_api_divination_interpret_injects_question` 断言 `payload["question"] == "今年换工作合适吗"` 且 system prompt 含 `"question"` 声明；无 question 时断言 `"question" not in payload`
- **测试环境：Windows（pwsh） / Python 3.13（全局解释器，backend/.venv 无 pytest） / 模型+provider：本节未调真实 LLM（`chat` 全程 monkeypatch，零 token 零扣费）**
- **凭怎么产生的：`cd backend && python -m pytest tests/unit/test_divination.py -k interpret -q`（临时库由 tests/conftest.py 写入系统 temp，不触碰真实 data/*.db）**
- **什么没测：真机 UI 点按（需起前后端 + 真实 LLM 扣费）；塔罗 side 未跑真机（只复核了 prompt 与 tarot.py:202 注入代码）**
- **覆盖声明：证明了**"seed_json.question → LLM user_payload.question"这一跳在真实 FastAPI app（TestClient）+ 真实 DB 读写下成立，且缺省路径不退化；**未证明**真实 LLM 的输出是否真的围绕问题（那需要真机看产物），也未验证前端 UI 到 seed 的那一跳（前端代码为只读复核）
- 截图：无

## 本节指标（定义见 `01` §6；落盘时一并写）
- 请求数：**未能核验**（会话内无法导出账单；本节工具调用约 14 次，Agent 自数、低可信） ｜ 往返次数：1（用户一次批量令，本节内零澄清往返） ｜ 交付前耗时：约 25 分钟
- **模型 + provider：deepseek-v4-flash（DSH harness；provider 未在会话内暴露）**

## 用户结论
待用户确认（批量指令内的一节；真机验收可并入 145/147 后的统一真机走查）
