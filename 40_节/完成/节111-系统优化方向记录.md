# 节 111 · 系统优化方向记录（解耦后执行）

状态：🟢 已执行（2026-09-11，待用户验收）｜ 强度：大改 ｜ 日期：2026-09-10（记录）/ 2026-09-11（执行）

## 定位
系统级优化点清单（服务器压力 / 性能 / 渲染效果与速度），由节110 执行方在解耦过程中按 v0.15 排查四步审查代码产出。原定位「只记录、不执行」，解耦完成后本节逐条执行。

## 我的复述
执行 6 项系统优化（A–F），逐条按「改前 X → 改后 Y」取数验收；涉及用户不知情的代价（删除、轮询改造、加载方式）已提前摆选项。

## 用户拍板（2026-09-11）
- D 轮询：**指数退避**（纯前端，不动后端）
- C pairs.js：**defer 延迟加载**
- B babel.min.js：**删除**
- A / E / F：默认执行（可逆）

## 执行结果（凭是数字）

| # | 位置 | 改前 | 改后 | 结果 |
|---|---|---|---|---|
| A | `art/{reference,samples,_drafts_r3}` | 45.3MB / 20 文件 | 归档至 `docs/_archive/美术-参考样本草稿/`（44.27MB） | art **57.7MB/75 → 13.41MB/55**；运行时引用检查 **0 命中** |
| B | `vendor/babel.min.js` | 3.07MB 死文件（0 引用） | 删除 | vendor **3.31MB → 0.24MB** |
| C | head `pairs.js` | 247.8KB 同步加载阻塞首屏 | `<script defer>` | 首屏不再被阻塞；`TC_PAIRS` 全为函数内懒访问、首页不引用（安全核验通过） |
| D | `views-cases.js` 3 处 job 轮询 | 固定 2s `setInterval` | 指数退避 2s→30s，有进展重置 | 空闲 5 分钟任务：改前 150 次 → 改后约 10 次；余额查询已在此前优化为会话内单次（无 4s 残留） |
| E | `.npm-cache`(685) + `scripts/_tmp*` 探针 | 942 文件被 git 追踪 | `.gitignore` + 取消追踪 | 仓库 **4982 → 4040 文件** |
| F | `sw.js` MAX 50MB，art 超标 | 57.7MB | 13.41MB | A 落地后自动达标，无需改 SW 代码 |

## 控制点结果
- 回归：`node --check views-cases.js` 通过；`node frontend/scripts/precompile.js` 通过（index.html 1381 行、admin.html 2445 行，0 编译块）
- 安全 / 数据：**不适用**（纯静态资产与前端轮询，无用户数据 / 资金 / 凭据改动）
- 性能：见上表「改前→改后」
- 后端：未改动（D 为纯前端退避）

## 结构反思
- 未触发（导航.md §7 牵连表已准确指向 views-cases.js / sw.js / data 文件，定位无走回头路）
- 小注：节111 记录里的行号（Waiting 5399 / Calibration 5564 / Predict 5957 / 充值 2160）是拆分前 index.html 行号，执行时已按拆分后实际文件重新定位。

## 留痕
- 变更（大白话）：美术参考图移出网站、删 3MB 死文件、清 942 个 git 缓存垃圾、247KB 数据文件不堵首屏、轮询改智能退避
- 后端凭：不适用（无后端改动）
- 凭怎么产生：git commit `d544381` + `9848a91`；precompile / node --check 命令输出原文
- 覆盖声明：证明了静态体积、git 追踪数与轮询请求量下降、门禁通过；**未证明**线上真实流量变化（需部署后观察）

## 本节指标
- 模型 + provider：deepseek-v4-pro / DeepSeek Harness
- 子代理：本会话 code_flash 子代理因会话已锁定的子代理模型策略不可用，改由主代理直接执行（settings.yaml 已补 `deepseek-official` 路由，下个会话生效）

## 用户结论
待验收
