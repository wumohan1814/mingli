# 节 160 · 真机走查修复：首页意图卡布局 + 占卜深链直达

状态：🟢 已修复（2026-09-16，用户真机验收反馈；待真机复核）｜ 强度：微改（2 文件，无契约变更）｜ 日期：2026-09-16

## 用户原话（真机验收反馈）
1. **首页改版设计失误**：「你想问什么」菜单全部集中在屏幕右方；应平铺占满整个界面，现在左半边是空的。
2. **页面跳转路径问题**：(a)「问件事」里点「六爻」/「观音灵签」应直接进入对应界面，而不是落到「临时起卦」hub 页（塔罗/雷诺曼是直达的）；(b)「随手抽」里点「观音灵签」同样问题。

## 根因（已定位）
| # | 根因 | 位置 |
|---|---|---|
| 1 | **残留死 CSS 右推首页**：节140 T3d 已删除左侧 nav-rail（index.html L1230 注明），但 `pages.css` 整段 `.nav-rail` 样式 + BUG-001 时代「手机小屏右移避开 nav-rail」的 `@media (max-width:480px){ .landing-hero{margin-left:150px} }` 未清 → 首页英雄区（含意图卡）在 ≤480px 屏被右推 150px，左半空 | `frontend/public/css/pages.css` L138–158 |
| 2 | **占卜深链只选中方法、没开操作面板**：`DivinationPage` 的 `initialMethod` effect 只 `setMethod(...)`，而视图状态机 `inDetail = asking \|\| !!data`——方法列表页在 `!inDetail` 时渲染 → 深链落到「选择起卦方式」hub 页；塔罗/雷诺曼是独立页（TarotPage/LenormandPage）直达，所以体验不一致 | `frontend/public/js/views-divination.js` L2376–2381 |

## 修法（2 文件）
1. `pages.css`：删除整段 `.nav-rail` 死 CSS + 右推首页的 `@media` 避让（−21 行），留一行注释说明「nav-rail 已随 T3d 整块删除，不再给首页留避让」。
2. `views-divination.js`：`initialMethod` effect 由 `setMethod` 改为复用 `openAsk(m)`——合法方法直达其「操作面板」（含六爻/梅花/灵签/小六壬/六壬/金口诀/奇门/圣杯全部 divination 方法，与塔罗/雷诺曼直跳体验一致）；依赖只留 `[initialMethod]`（组件按页重挂载，首渲染闭包即正确，避免重复开面板）。

## 验证
- `node --check views-divination.js`：exit 0
- `node frontend/scripts/precompile.js`：EXIT=0（无散色违规；本次只删 CSS、无新增色值）
- git 改动仅 2 文件（pages.css −21 / views-divination.js +10−3），precompile 未改动 index.html
- 逻辑核对：深链 → `openAsk` → `asking=true` → `inDetail=true` → 方法列表隐藏、操作面板直出；「返回选择」→ `backToList` → 回到列表，initialMethod 不变不重复触发

## 待你复核（真机）
1. 首页「你想问什么」6 卡平铺占满屏宽、左右对称
2. 「问件事」点六爻/观音灵签、「随手抽」点观音灵签 → 直达对应起卦操作面板（非 hub 列表）
