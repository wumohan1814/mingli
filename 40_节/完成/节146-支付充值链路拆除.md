# 节146 · 拆除付款充值链路（金数据 / 小金商务），保留余额积分体系

状态：🟢 通过（本地库已 DROP；真机走查待并 145/147 后统一做）｜ 强度：中改（删 3 个后端文件 + 1 个端点 + 轮询挂载 + 9 项配置 + 前端整套充值代码 + `recharge_codes` 表 DROP；触及资金红线，余额/扣费口径绝不能坏）｜ 日期：2026-09-13（拍板）→ 2026-09-14（实现收尾）

## 用户原话
> (a) 删除之前做过但隐藏掉的付款充值逻辑。
> (b) 删除"金数据"、"小金商务服务"等所有相关内容。
> (c) 保留余额和积分换算的逻辑。
> (d) 保留运维后台可以调整积分的逻辑。

## 我的复述
把"用户花钱充积分"的整条链路（金数据表单、充值码、轮询到账、前端充值/到账轮询代码）从产品里彻底删掉；但积分账户本身、逐法扣费、余额（元）与积分的换算展示、后台人工加减积分全部原样保留。

## 期望形态（验收标准 · 用户可观察）
- 全项目（`_archive` 除外）无金数据/小金商务/充值码/charge-code 的代码与现行文档引用；后端启动日志无 jinshuju_poll 任务
- 直接调 `POST /api/credits/charge-code` 返回 **404/405**；前端任何页面无充值入口、无到账轮询网络请求
- 余额查询、余额明细、余额不足提示、逐法扣费、注册赠送行为**全部不变**（既有测试 + 真机一次扣费占卜）
- 后台「余额充值/调整」与 Agent 调分端点正常，增/减均入账且审计流水正确
- 余额 ¥ 展示与 `recharge_rate` 热更新仍生效
- 历史充值/赠送流水在明细页仍显示中文标签，不出现裸 ref
- 后台删用户级联正常；后端测试全绿

## 现状坐标（Agent 用）
- 后端删除面：`app/credits/jinshuju.py`、`app/credits/poller.py`、`app/credits/codes.py`（整文件）、`app/credits/router.py` 的 charge-code 端点、`app/main.py` lifespan 的 `jinshuju_poll`、`app/config.py` 9 项 `jinshuju_*`、`app/models/analytics.py::RechargeCode` + `models/__init__.py` 导入/`__all__`、`app/admin/router.py` 导入与删用户级联序列
- 前端删除面：`index.html` 的 `RECHARGE_*` 常量 / `getRechargeUrl` / `stopRechargePoll` / `startRechargePoll` / `resumePendingRechargePoll` / `startRecharge` + 启动处调用 + `credit_recharge_start` 埋点；`data/ui-copy.js` 4 条充值文案；`views-home.js` 充值按钮死注释、`enterBalance` + 到账提示 effect
- 保留面（白名单）：`credits/service.py` 全部、`router.py` 的 balance/transactions、`labels.py` 的 recharge/serial 映射、`total_recharged`、`system_configs.recharge_rate`、`fetchCreditBalance()`（余额胶囊与明细页共用）
- 数据层：`recharge_codes` 表 + 迁移 `data/migrations/analytics/0001_drop_recharge_codes.sql`（含回滚）
- 归档：`_archive/架构设计-§10-支付系统-金数据（节146作废归档）.md`、`_archive/废弃节-节146/`（节102/103）

## 复用判断
- 库内候选：无
- 结论：**纯删除 + 既有配置机制沿用**，不新写任何东西（挂载用既有 lifespan，端点用既有错误码信封）

## 方案选项（中改及以上必填）
- **表去留**（单向门）——A 保留空表 / **B 物理 DROP（采用，用户 2026-09-13 拍板）**：代价：一次性不可逆 DDL（已写回滚脚本，可逆）；效果：库里不留死表。理由：充值从未真实开放，存量仅测试假数据（实测 0 行）
- **端点去留** —— A 加开关停用 / **B 物理删除（采用）**：理由：节点已确认「以后也不会需要恢复」，且删除后端到端无残留；代价：若将来要恢复需重写（用户已明确不会）
- **前端隐藏 vs 删除** —— **删除（采用）**：理由：留下 100+ 行死代码 + 4 条死文案 = 未来的误读源（判据 8）
- 用户选择：表=B、端点=B、前端=删（拍板已齐，本节内零澄清往返）

## 禁碰清单（= 保留白名单，回归清单）
- 余额账户 + 逐法扣费：`credits/service.py` 的 `balance()/consume()/check_balance()/transactions()`
- 余额↔积分/tokens 换算：`credit_per_token=1000`、`recharge_rate=10.0`、`get_recharge_rate()`、前端汇率探测与 ¥ 展示
- 后台/Agent 人工调积分：`POST /admin/credits/manual`、`POST /admin/agent/credits/manual`、`manual()/adjust_balance()/manual_adjust()`
- 通用入账函数 `service.recharge()`（type ∈ recharge/manual/free/refund）——**函数名不改**
- 注册赠送 `free_credit_on_register=220`
- 历史流水可读性：`labels.py` 的 `recharge` / `serial:{}` 标签映射（**只删注释里的金数据描述**）
- `total_recharged` 字段、余额查询/流水分页端点、后台配置项 `recharge_rate`
- 入口红线⑤（逐法扣费与余额口径）、`method-result v2` / `chart.json` 契约

## 冲突检查
- **节102 / 节103**：冲突（两节以"商户认证后恢复充值"为前提）→ 已在头部加「节146 作废」注记并移入 `_archive/废弃节-节146/`（归档可逆）✓
- **入口红线⑤**：本节约束为"只拆充值入口，不动计费内核"，§禁碰清单即回归清单 ✓
- **节141（关闭公开注册）**：无冲突，注册赠送逻辑保留（后台开号仍走该口径）✓
- **节133 / 节139**：无交集 ✓
- **节145（品牌更名）**：无逻辑冲突；`legal/*.html` 中的第三方收款表述本节点**未动**（它提的是小鹅通/知识星球，不含金数据；且按节点安排与节145 一起处理）——已登记
- **`apscheduler` 依赖**：本节删除后 `backend/app` 内**已无使用点**，但依赖仍留在 `pyproject.toml` 且 `scripts/env_doctor.py` 仍校验其可导入 → **本节不动**（越界会碰部署依赖链），登记为后续清理项

## 不许被推翻的约定（**给未来的自己看**）
- 本产品**没有任何用户侧付款渠道**（用户拍板「以后也不会需要恢复」）；余额的唯一来源 = 后台开号赠送（type=free）或后台/Agent 人工调分（type=manual）
- `serial:{serial}` 与 `type=recharge` 的**标签映射必须保留**（`labels.py`）——删了会让余额明细页出现裸 ref；同理前端 `views-home.js` 的 `recharge: '充值'` 兜底映射保留
- `service.recharge()` 是**通用入账函数**（不只服务支付），**不许因为它名字像"充值"而删**
- `recharge_rate` 配置键**继续有效**（余额 ¥ 展示与换算），只是不再被金数据轮询读取
- 若将来有人要恢复支付：**这是一次全新的立项**（需重写客户端/轮询/表），不许在旧节里"顺手加回来"

## 控制点结果
- 回归：**通过** —— `python -m pytest tests -q` = **213 passed / 2 failed**（两个失败均为既存红：`test_fixture_snapshot_anchor` 快照漂移、`test_tarot_draw_bad_spread_type_400` 陈旧用例，均与本节无交集）。前端 `node --check` 3 个改动脚本全过；`precompile.js` 门禁 exit=0
- 安全 / 数据：**通过（含一次不可逆 DDL，已留回滚）** —— ① `POST /api/credits/charge-code` → **405 Method Not Allowed**（端点确已不存在）② `GET /api/credits/balance` → **401 令牌无效**（仍存在且鉴权正常）③ `Settings.model_fields` 中 `jinshuju*` 字段数 = **0**（9 项配置确已移除）④ 本地库：`recharge_codes` 表 **DROP 成功**（DROP 前 0 行），`credit_accounts` / `credit_transactions` **保留** ⑤ 越权/隔离路径未改动
- **性能：不适用（正向减负）** —— 移除一个常驻定时任务（未配置 token 时本就不启动，配置了则每 5 分钟一次出网调用）；前端少 1 个轮询定时器（原每 4 秒一次余额查询，最长 10 分钟）；后端启动少一次配置分支判定

## 设计取向（**大改必填**；中改写"已过五条判据"或"无命中"）
- 命中 **判据 8（优先删机制）**：整节是**纯减法**——删 3 个文件、1 个端点、1 个定时任务、9 项配置、1 张表、前端整套轮询、4 条文案；没有新增任何开关/开关表/降级分支
- 命中 **判据 5（单点）**：余额读取 `fetchCreditBalance()` **收敛为一处**（原为充值流程与余额胶囊共用，删除时特意保留而非连带删除）
- 判据 2/3/4 无命中：无易变接缝、无新埋点、无需开关（反向：删掉了一个开关）
- ⚠️ **有意简化已标注**：`apscheduler` 依赖暂留（见"冲突检查"），Code-Wiki 已标注"节146 后已无使用点"

## 结构反思（`01` §8.1 触发条件命中时**必写**，否则写"未触发"）
- **触发（判据 2：同区域被第 2 次及以上修改 —— credits 模块本会话内第二次改动（节141 白名单登记））**
- 哪里难：**"充值"这个词同时指两件事**——"用户付款买积分"（要删）与"往余额里加钱"（`service.recharge()` / `total_recharged` / 后台"余额充值"，**要留**）。全项目 grep `recharge` 会同时命中两者，靠名字无法区分，只能逐个看语义；`views-home.js` 里甚至有一段"充值按钮已隐藏"的死注释，删它时极易误删隔壁的余额胶囊
- 结构上怎么改：把"支付渠道"相关的命名从 `recharge*` 里**分离出去**（如 `payment_*`），让"入账"与"收款"在名字上不再撞车；本轮已通过注释在 `service.recharge()` 与 `labels.py` 就地标注"通用入账，不只服务支付"
- 下次会怎样：再遇到"删充值"这类需求，grep 一次就能分清哪些是要删的收款链路、哪些是要留的入账口径

## 留痕（**文字必写；截图仅用户要求时**）
- 变更（大白话）：① 用户花钱买积分这条路整个拿掉了——金数据的对接程序、定时去拉订单的轮询、充值码、"生成充值码"这个后端接口、9 项金数据配置，还有前端那一整套"点充值→跳表单→每 4 秒查一次到账"的代码，全删；② 存充值码的那张数据库表也删掉了（表里本来就是空的，删之前写了可回滚的脚本）；③ **你的积分系统一点没动**：余额能看、看明细能看、占卜照样按用量扣、后台照样能给人加减积分、注册赠送照旧；④ 明细里历史那些"充值"字样的流水标签也留着，不会变成看不懂的乱码；⑤ 旧的两节"支付相关"文档加了作废说明，挪进归档区（没删原文）。
- 后端凭：`pytest tests -q` → `2 failed, 213 passed in 98.95s`；端点探测 → `POST /api/credits/charge-code -> 405 {"detail":"Method Not Allowed"}`、`GET /api/credits/balance (无效令牌) -> 401 {"code":2001,...}`；`jinshuju settings present: []`；DB 探测 → `recharge_codes in before: True`（0 行）→ `recharge_codes in after: False`，`credit tables kept: ['credit_accounts','credit_transactions']`
- **测试环境：Windows（pwsh） / Python 3.13（全局解释器） / Node v24（排盘引擎） / 模型+provider：本节零 LLM 调用**
- **凭怎么产生的：`cd backend && python -m pytest tests -q`（conftest 指向系统 temp 临时库）；端点探测用 `TestClient(app)` + 专用临时库 `.tmp_pytest/verify146/*.db`；DDL 用 `python -c` 执行 `data/migrations/analytics/0001_drop_recharge_codes.sql`（幂等 `DROP TABLE IF EXISTS`）**
- **什么没测：① 真机走查（全站无充值入口、无到账轮询请求、余额明细历史标签显示、后台调分、一次完整扣费占卜）② 线上库的 `recharge_codes` DROP **未执行**（需随下次部署执行同一脚本，执行前做常规备份）③ 线上 `.env` 里现存的 `TAICHU_JINSHUJU_*` 键未清理（改后即被 pydantic 忽略，`extra="ignore"`，但应清掉）④ 后台删用户的级联删除未新增用例直接覆盖（依赖既有 admin 测试绿灯间接覆盖）**
- **覆盖声明：证明了**——端点确已消失（405）、余额端点仍在且鉴权正常（401）、9 项配置字段确已移除（`model_fields` 计数 0）、本地表确已 DROP 且余额两表完好、全量回归 213 项通过；**未证明**用户可见终态（无真机走查），也未处理线上库与线上 env（见"什么没测"）
- 截图：无

## 本节指标（定义见 `01` §6；落盘时一并写）
- 请求数：**未能核验**（无法导出账单；本节工具调用约 42 次，Agent 自数、低可信） ｜ 往返次数：0（拍板齐备，本节内零澄清往返） ｜ 交付前耗时：约 85 分钟
- **模型 + provider：deepseek-v4-flash（DSH harness；provider 未在会话内暴露）**

## 未同步项（**显式登记，不许当成已完成**）
- `docs/standards/07-版本迭代记录.md` 里历史条目提到「金数据支付接入（REQ-011）」——**历史版本记录，按体例不改**（它记的是当时发生了什么）
- `legal/用户协议.html` / `隐私政策.html` 中的第三方收款表述（小鹅通/知识星球）**未动**——按节点安排随**节145** 一并处理
- `apscheduler` 依赖（`pyproject.toml` + `scripts/env_doctor.py` + `tools/memory_bench/README.md`）**未清理**——已登记为后续小项
- `.workbuddy/memory/2026-09-06.md`（Agent 记忆库）中的金数据调研笔记**未动**（非产品文档，属历史记忆）

## 用户结论
待用户确认
