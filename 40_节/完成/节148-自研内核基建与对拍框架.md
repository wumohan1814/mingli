# 节148 · 自研内核基建（0 期）：语言选型 + 管线架构 + 对拍框架

- **状态**：🟢 **已完成**（2026-09-14 落盘；对拍门禁 100% 通过，样板走通「新实现 → 对拍 → 开关切换」；见 §五 证据）
- **母节**：节128（§十 拆分子节总账、DoD、禁碰纪律）
- **强度**：中改（无业务功能交付，全是地基）
- **交付物落点**：`backend/paipan-node/paipan-core/`（11 文件，内核 + 对拍框架 + 扫描脚本 + 测试 + 规范文档）

## 目标

为 17 个能力的逐个自研替换搭好地基，使后续每个子节只关注算法本身；并锁定一项不可逆的架构决策：**自研内核用什么语言写**。

## 一、头号决策：内核语言（✅ 2026-09-13 用户已拍板：B = JS/TS）

**裁决：自研内核用 JavaScript/TypeScript 编写**，与 paipan-node / iztro 同生态，可在 WebView 同进程运行，为节142 胖 APK 省掉一个移动 Python 运行时。

落地含义（本节执行时遵守）：
- 内核目录建在 Node/前端侧（具体路径开工时定，候选 `paipan-core/`），Python 后端经现有子进程/HTTP 契约调用，**接口 JSON 形态不变 → 前端与编排层零改动**
- 紫微 iztro（JS）同生态直接复用；八字 lunar-python（Python）的上层逻辑在 JS 侧重写或寻找 MIT 的 JS 历法库（节150 收口时核实，历法轮子不重造原则不变）
- 对拍框架用 JS/TS 写；类型与 lint 配置在基建节一并建立
- 与 Python 侧的长期 IPC 保留到内核全部迁移完后再评估是否去除

| 方案 | 优点 | 代价 |
|---|---|---|
| A. Python（进 FastAPI） | — | 未选：胖 APK 需带 Python 运行时 |
| **B. JS/TS ✅ 选定** | 与 iztro 同生态；胖 APK 可进 WebView 同进程；排盘本就跑在 Node | Python 后端继续经子进程/HTTP 调用；对拍工具链新建 |
| C. 混合 | — | 未选：双运行时长期并存 |

## 二、交付物

1. **内核目录骨架**（语言定案后建）：`paipan-core/`（或 `app/paipan_core/`），按能力分子模块；统一输入/输出契约（对齐现有 Node HTTP 接口的 JSON 形态，保证前端零改动）
2. **纯函数管线规范**：输入归一化 → 历法/起局（纯函数）→ 格局判定（纯函数）→ 输出装配；构卦/动爻/互变卦等推导禁止与 IO/LLM/格式化耦合（教训：耦合函数会放大索引小错为全链路错配）
3. **单一权威规则源**：每个能力的规则表/术语表/数据表只允许一份，调用方只读不抄
4. **对拍框架**（核心）：
   - fixtures 批量喂给「旧实现（vendor/npm）」与「新实现」，输出归一化后字段级 diff
   - 浮点字段阈值可配；无序集合排序后比；生成对拍报告（一致率/差异样例）
   - 门禁：关键字段一致率 100% 才允许在对应子节下线旧实现
   - fixtures 由旧实现产出但需人工抽查代表性（生辰跨年代/节气边界/早晚子时/夏令时等）
5. **依赖锁定**：`mingyu-core` 写死精确版本（现状 package.json 是 `^0.2.1` 而奇门终身局用 vendor 0.2.2 构建产物，口径需在本节对齐核实），对拍期间禁止升级
6. **依赖扫描脚本**：一条命令列出 server.mjs / 内核中所有 mingyu 引用，供逐节勾销
7. 零 LLM 校验：内核测试全确定性，不触网

## 三、验收

- 一个示例能力（挑最简的，如小六壬或生肖太岁）走通「新实现 → 对拍 100% → 开关切换」全流程作为样板（该能力的正式替换仍归对应子节，本节只证明管线可用）
- `node --check` / 后端 `node --check server.mjs` 与 Python 语法检查全绿
- 后续子节可直接复用对拍框架，不需要再造

## 四、禁碰（母节纪律在本节的具体项）

- 不在本节替换任何业务能力（样板除外）
- 不新增常驻进程/GPU/本地模型（母节 §六）
- 不动前端、不动计费

---

# 五、完成证据（2026-09-14 落盘 · 与交付物逐一对应）

## 5.1 交付物对照

| 交付物（§二） | 落点 | 状态 |
|---|---|---|
| 1. 内核目录骨架 + 统一输入/输出契约 | `paipan-core/`（package.json `type:module` + 唯一依赖 `lunar-typescript@1.8.6`；`src/pipeline|rules|capabilities/`）；输出形状与 server.mjs `stripInternal` 后契约逐键对齐 | ✅ |
| 2. 纯函数管线规范 | `paipan-core/README.md` §1（四段：输入归一化→历法/起局→格局判定→输出装配；段间只传普通对象；禁 IO/LLM/格式化耦合；时间口径只在一处定义） | ✅ |
| 3. 单一权威规则源 | `src/rules/xiaoliuren.js`（六宫表/掌诀歌/时辰表/换日口径 `Object.freeze`；调用方只读不抄；测试对歌诀逐字断言防漂移） | ✅ |
| 4. 对拍框架 | `tools/compare/{diff.js, run-compare.mjs}`：fixtures 喂旧/新实现 → stripInternal 归一化 → keyFields 字段级 diff（浮点容差可配、无序集合 `sortArraysBy`）→ 报告 + **门禁：关键字段 100% 才 PASSED**；golden 快照存取；`--legacy`/`MINGLI_PAIPAN_LEGACY_DIR` 可覆盖旧实现路径（内核目录零旧包名字面量，scan-deps 是真门禁） | ✅ |
| 5. 依赖锁定 | `backend/paipan-node/package.json`：`mingyu-core` `^0.2.1` → **`0.2.1`**（精确版，注释说明 AGPL 风险）；`paipan-core/package.json` 钉死 `lunar-typescript@1.8.6` | ✅ |
| 6. 依赖扫描脚本 | `tools/scan-deps.mjs`：一条命令列出 server.mjs 全部 mingyu 引用（**18 个 import 待勾销清单**）+ paipan-core 命中必须 0（门禁） | ✅ |
| 7. 零 LLM 校验 | 内核零 LLM/零触网/零 IO；缺输入抛 `PaipanInputError` 绝不回落「当前时间」；`params.now` 仅注入 `meta.calculatedAt` 且不参与哈希；测试不触网不读真实时钟 | ✅ |

## 5.2 样板能力走通全流程（小六壬）

- **新实现**：`src/capabilities/xiaoliuren/index.js` 导出 `generateXiaoliurenCore(params)`，与 vendor 同签名同形状。
- **对拍**：22 条夹具（10 条规格要求 + 8 条节气/闰月/世纪边界补强）× 52 关键字段 = **1144/1144 一致率 100.00%，RESULT: PASSED**（亲自复跑确认）。信息性差异仅 4 个 meta 路径（`engineVersion` 0.2.2→0.1.0-mingli、`inputHash`/`resultId` 自研 FNV-1a、`calculatedAt` 运行时刻）；**verse/sequence/palaceOrder/ganzhi/calculation 零信息性差异**（与旧实现逐字一致）。
- **开关切换**：`server.mjs` 新增 `MINGLI_PAIPAN_CORE_XIAOLIUREN=on` 开关（**默认 off → 行为零变化**）；冒烟实测 off（engine 0.2.2）与 on（engine 0.1.0-mingli）同输入输出**除 meta 外逐字段相等**。正式替换与 import 勾销仍归节155。
- **fixtures 代表性**：跨年代（1900/1960s/1990s/2020s/2100）、早晚子时、立春精确时刻前后 10 分钟边界对、闰二月/闰四月、月末晚子时、跨年晚子时、正月初一但在立春前。
- **对拍发现的历法约定**（黑盒探针，未读 vendor 源码，已写进 README §4 与内核注释）：年干支=立春精确时刻换年（`getYearInGanZhiExact`，非正月初一非整日界）；月干支=节气精确时刻换月；日干支=晚子时 23:00 换日；农历月日=民用日零点换日；时辰序 13 标签（0 早子/12 晚子）；时干支=五鼠遁自实现；闰月=月序取绝对值。
- **有意分歧（已登记 README §4）**：缺 customDate 旧实现静默回落当前时间 → 内核抛错；`{month,day,hour}` 数字模式旧实现接受但忽略 → 内核抛 `numeric_mode_unsupported`；ISO 字符串旧实现拒绝 → 内核接受（放宽）。

## 5.3 门禁证据（亲自复跑）

```
node --check ×8（server.mjs + 内核 7 文件）          → 全绿 exit=0
node paipan-core/tests/xiaoliuren.test.mjs          → 17/17 pass exit=0
node paipan-core/tools/scan-deps.mjs                → server.mjs 待勾销 18；paipan-core 命中 0；PASSED
node paipan-core/tools/compare/run-compare.mjs      → 22/22 PASS；100.00%（1144/1144）；PASSED
node paipan-core/tools/compare/run-compare.mjs --use-golden → 22/22 PASS；PASSED
服务冒烟（off vs on，同输入 1990-05-12T07:30+08:00） → 除 meta 4 字段外逐字段相等
后端 pytest（节148 后回归）                        → 见入口.md 交接（预期 218 passed / 1 已知夹具漂移）
```

## 5.4 收尾留痕

- `99_状态/决策.md`：+「节148 依赖锁定与自研内核地基」行（内核语言 JS/TS 落地、mingyu-core 钉死 0.2.1、对拍框架为后续子节唯一正确性闸门）。
- `00_根/来源.md`：+ `lunar-typescript@1.8.6`（MIT，6tail）直接依赖登记（原仅记作 iztro 传递依赖）。
- `00_根/复用.md`：§二 +「自研内核 paipan-core + 对拍框架」行。
- `00_根/入口.md`：节148 状态行更新。
- **遗留（不属于本节）**：server.mjs 18 个 mingyu import 仍在线（逐节勾销）；真机走查；数字模式语义变更已登记。
