# 节 159 · MBTI 大五人格 120 题版（官方普通话译本）

状态：🟢 通过（2026-09-16，双版本并存：120 快速版 + 300 完整版，默认 120）｜ 强度：中改（后端双题库 + 前端版本选择，零 DDL）｜ 日期：2026-09-16

## 用户原话
> 一次做完 142, 129, 131, 117，**MBTI120题版**，135。

## 我的复述
给大五人格测试加一个 **120 题版**（IPIP-NEO-120 官方普通话译本，约 8 分钟），与现有 300 题完整版（约 20 分钟）并存、用户可自选版本；判型/结果/档案链路两版共用，零 DDL。

## 期望形态（验收标准 · 用户可观察）
1. 心理测试入口可选「快速版 120 题」/「完整版 300 题」（默认 120）
2. 120 版答题 5 点量表、进度可续（localStorage 按档案+版本隔离）、结果页与 300 版同构（五维图 + 类型倾向对照 + 边界 + 免责）
3. 后端契约零破坏：`GET /api/mbti/questions?version=`、`POST /api/mbti/score`（body 可带 version，默认 300 兼容旧客户端）
4. 判型口径两版一致（同一映射/边界/归一公式，仅题库不同）

## 数据来源与裁定（合规，来源.md 已登记）
- 题库 = 官方普通话译本 `ipip-neo-120-zh.json`（120 条 text_zh，公有领域，来源.md 节122 已登记）+ Johnson (2014) 计分键页 `ipip-neo-120.json` 的 dim/facet/keying 映射。
- 映射方法：**text_en 归一化逐字匹配**（117/120 命中）；3 条措辞不同的 O6 题（中文页 28/58/88）按登记裁定映射计分键页 69/70/71（28→+、58→+、88→−），text 保留官方中文题面。裁定依据写入 `questions-120.json` 顶层 `_comment` 字段。
- ⚠️ **待拍板（滞后决策）**：来源.md 节122 曾计划「替换第 28 题」（政治倾向题，中文语境敏感）；本次保留全部 120 题（含 3 条 O6 政治倾向题），是否替换/保留由用户拍板（IPIP 许可允许 edit）。
- 尺寸：N 24 / E 24 / O 24 / A 24 / C 24（30 层面各 4 题）；reverse 55 条。

## 现状坐标（Agent 用）
- 既有题库 `backend/app/mbti/data/questions.json`（IPIP-NEO-300，节123 落地）；计分 `scoring.py`；API `api/mbti.py`；前端 `views-mbti.js`（节124 落地）。

## 方案选项
- A：120 题**替换** 300 题 —— 一步到位，但丢 300 完整版（用户未说删）
- B：**双版本并存**（120 默认 + 300 可选），契约向后兼容 —— 零回归，改动集中在 4 个文件 + 前端选择器
- 用户选择：B（本次实施；「是否最终只留一版」列滞后决策）

## 实施记录（2026-09-16）
- **题库**：新增 `backend/app/mbti/data/questions-120.json`（120 条，id=item_no 1..120，text=官方 text_zh，dim/facet/reverse 按匹配/裁定，scale 复用 300 版 5 档文案，meta+_comment 记录依据）
- **计分**：`scoring.py` 支持 `load_questions(version="300"|"120")` 与 `score(answers, version=...)`，双题库独立进程内缓存；非法 version 抛 ValueError；计分公式/MAPPING/BOUNDARY_THRESHOLD 一字未改
- **API**：`api/mbti.py` 四个端点（GET /questions、POST /score、GET /share/{token}、POST /share/{token}/score）支持 version（query 或 body，默认 "300"，非法 400）
- **前端**：`views-mbti.js` MbtiPage 与 MbtiSharePage 各加版本选择器（chip 复用既有样式，默认 120）；点「开始测试」才按所选版本拉题库；localStorage 进度键含版本（`mingli_mbti_progress_{version}_{caseId}`，旧键孤儿数据不迁移）；判型带 version
- **测试**：新增 `backend/tests/unit/test_mbti_120.py` 12 用例（题库结构/O6 裁定/ENFJ+ISTP 双验/非法版本/维度不全/300 版零回归/四端点契约）；`test_mbti_120.py + test_mbti.py` = **35 passed / 0 failed**
- **门禁**：node --check views-mbti.js 过；前端 precompile 门禁绿（主代理统一跑）；全量 pytest 306 passed / 2 failed（2 失败 = 既存环境项：test_fixture_snapshot_anchor 夹具漂移 + Node 服务未起时的 extra.mjs 兜底降级，均与本节无关，已核）

## 留痕（文字）
- 变更（大白话）：人格测试现在可以选 120 题快速版（官方中文题本、约 8 分钟）或 300 题完整版；老接口默认仍走 300 题，不受影响。
- 凭：pytest 35 passed（含 12 个新用例）；node --check 通过；全量回归 306 passed（2 项既存环境失败已核）。
- 覆盖声明：**证明了** 双题库加载/计分/四端点契约/前端版本选择与进度隔离；**未证明** 真机 UI 全路径（纯静态可验证部分已过，登录→答题→结果→保存链路待真机验收）与 O6 政治题的最终取舍（待拍板）。

## 待拍板（滞后决策）
1. **默认版本**：当前前端默认 120（快速版）；是否改默认 300 / 只留一版？
2. **O6 政治倾向题（28/58/88）**：保留全部 120 题（现状）还是按来源.md 旧计划替换第 28 题等（IPIP 许可允许 edit）？
3. **旧进度键孤儿数据**：接受不迁移（现状）还是补迁移？
