# 节139 · 西占退出八法合一（原「九法合一」）

状态：🟢 通过（代码/文案/数据清理完成，真机走查待并 145/147 后统一做）｜ 强度：中改 ｜ 日期：2026-09-12（拍板）→ 2026-09-14（实现收尾）

## 用户原话
（本节拍板逐字见 `00_根/入口.md` 与下方决策表；2026-09-12 用户四条拍板，2026-09-13 确认「理财/财运全部保留」与本节无关）

## 我的复述
把「西式占星」从国学那条「九法合一」综合解读流水线里摘出去，以后叫「八法合一」；西占自己的独立星盘功能（西式占卜 HUB → 星座）和档案里的「占星盘」只读标签页原样保留。

## 期望形态（验收标准 · 用户可观察）
- 国学预测入口全部呈现「八法合一」，**进度分母 8**、断前尘轨道动画 **8 个节点**
- 综合解读不再跑西占（不再产生西占 LLM 扣费、不进合成报告）
- 「逐法解读」tab 无西占；历史测试数据已清
- 档案详情「占星盘」tab 保留，仍可只读查看
- 西式占卜 HUB 的星座星盘功能完整不受影响
- 问「性格天赋」→ 紫微主算 + **八字格局**辅助
- 账单旧记录「西占」标签仍正常显示
- 其余功能（配对/起名/择吉等）回归不退化

## 现状坐标（Agent 用）
- 后端注册表：`app/methods/__init__.py`（`METHOD_KEYS`）→ 切片 `app/paipan/slicer.py`（`DEFAULT_METHODS` / `build_fragment`）→ 预判标记 `app/paipan/marker.py` → 路由 `app/routing/router.py` → 编排 `app/jobs/orchestrator.py` → 端点 `app/api/cases.py`
- 前端：`data/ui-copy.js`（文案总表，最大面）、`js/views-cases.js`（等待页文案 + 断前尘轨道 + 逐法映射）、`js/views-guoxue-tools.js`、`js/views-home.js`、`index.html`（`DQC_METHODS`）、`admin.html`
- 保留面：`app/methods/xizhan/`（目录）、`prompts/method-prompts/xizhan.md`、`app/credits/labels.py`（历史账单标签）、`api/astrology.py` + `astrology_readings`

## 复用判断
- 库内候选：无
- 结论：**改现有链路的注册表与文案**，不新写任何东西（西占 analyzer 一行不删，仅从注册表摘出）

## 方案选项（中改及以上必填）
- A（采用）：**从注册表摘出**（`METHOD_KEYS` / `DEFAULT_METHODS` 各删一行）→ 下游（编排/切片/聚合/进度）全部自动变 8 法 —— 代价：零；效果：一处改、全线生效
- B（未采用）：在编排里加 `if method != "xizhan"` 过滤 —— 代价：过滤器散落多处、将来必漏；效果：与 A 相同
- 用户选择：A（拍板已齐，无新增往返）

## 禁碰清单
- 档案详情「占星盘」tab（读 `chart.json.western`）与 `chart.json` 排盘契约 —— **不动**
- 西式占卜 HUB 星座页 + `api/astrology.py` + `astrology_readings` —— **不动**
- `app/credits/labels.py` 的 `METHOD_ZH["xizhan"]="西占"` —— **保留**（历史流水/账单标签）
- 前端 `REQ126_METHOD_KEY_ZH` 的 `xizhan` 条目 —— **保留**（degraded 键/历史读数兜底）
- 余额与逐法扣费口径（红线⑤）、`method-result v2` / `chart.json` 契约
- 路由名 `/jiufa`、前端 key 名（`nine-pick` / `method-nine` / `item-nine-pick` / `nine-methods` / `guoxue-nine-*`）—— 标识符，用户不可见，改名零收益且破链接

## 冲突检查
- 节138（财运下线，已废弃）：本节不再依赖它；synthesizer 为含财运的 8 板块原版，**只做上游减一法，不动合成器** ✓
- 节116（综合裁决）/节121（不打分）：无冲突
- 入口红线⑤（逐法扣费与余额口径）：只减一个方法，计费内核未动 ✓
- 节135/节136 四归口（文案归 `ui-copy.js`、UI 元素集中）：本次改动**全部落在 `ui-copy.js` 的既有键值**上，未新增散落硬编码 ✓

## 不许被推翻的约定（**给未来的自己看**）
- 「方法数」只有一个事实源 = `app/methods/__init__.py::METHOD_KEYS`；**进度分母与总数一律 `len(METHOD_KEYS)`**，不许再出现硬编码 9/8（本节已把 `cases.py` 的 `totalMethods: 9` 与 `total=9` 两处硬编码改为 `len(METHOD_KEYS)`）
- 切片缺省集 = `slicer.DEFAULT_METHODS`（7 片）+ `SPECIAL_METHODS`（`bazi-hunyin-caiyun`）= 断前尘 8 法
- `xizhan` 目录与 prompt **保留但不注册**；将来若要复用，必须连同注册表/路由一起评估（不许只改一边）
- 前端 `DQC_METHODS` 必须与真实八法同序同名（断前尘动画节点数 = 法数）

## 控制点结果
- 回归：**通过** —— `python -m pytest tests -q` = **211 passed / 2 failed**。两处失败**均为既存红**且与本节无交集：① `test_paipan.py::test_fixture_snapshot_anchor`（已知问题表第 11 行：`western.summary.patterns[0]` 中/英文案漂移）② `test_tarot.py::test_api_tarot_draw_bad_spread_type_400`（陈旧用例，本节新登记）。
  前端：`node --check` 6 个改动脚本全过；`frontend/scripts/precompile.js` 门禁 exit=0（index/admin 行数与体积未变）
- 安全 / 数据：**发现项已处理** —— 按拍板清理历史测试数据：`method_results` 中 `method_key='xizhan'` **1 行 → 0 行**（总行 14 → 13），清理后库内 method_key 集合恰为 8 法。**线上库未执行**（见"什么没测"）。多用户隔离未受影响（聚合仍带 `user_id`）
- **性能：不适用（正向减负）** —— 断前尘少 1 法：每 job 少 1 次 analyze + 1 次 validate + 1 次切片，编排并发上限与内存占用不变；无新增查询/循环/缓存

## 设计取向（**大改必填**；中改写"已过五条判据"或"无命中"）
- 命中 **判据 5（单点）**：删注册表一行即全线生效，且把两处硬编码 9 改成 `len(METHOD_KEYS)`，消灭了"改注册表还要记得改分母"的同步点
- 命中 **判据 8（优先删机制）**：没有新增任何过滤器/开关/名单，纯减法
- 判据 2/3/4 无命中：无易变接缝、无新埋点、无需开关

## 结构反思（`01` §8.1 触发条件命中时**必写**，否则写"未触发"）
- **触发（判据 1：本节定位代码 ≥3 次搜索 + 判据 2：同区域被第 2 次及以上修改）**
- 哪里难：**「方法数」这一个数字散在 5 处以上**（`METHOD_KEYS` 列表、`cases.py` 的 `totalMethods: 9`、`cases.py` 建 job 的 `total=9`、前端 `ui-copy` 的 `/9）`、`views-cases.js` 的 `total || 9`、`index.html` 的 `DQC_METHODS` 长度），改一处必须全项目搜；本次盘点清单里**前两处硬编码根本不在原清单上**，是靠跑测试才炸出来的
- 结构上怎么改：后端已收敛为 `len(METHOD_KEYS)`（本次完成）；前端余下 2 处（`/9）` 文案、`total || 9`）仍是字面量，**应改由接口返回的 `totalMethods` 驱动**
- 下次会怎样：再增删一法时只改注册表 + 一处文案，不必再全项目搜数字

## 留痕（**文字必写；截图仅用户要求时**）
- 变更（大白话）：① 国学那条「九法合一」从今天起只跑八家，名字统一改成「八法合一」（含进度条分母、等待页动画的 8 个圈）；② 西占不再参与综合解读，但档案里那张「占星盘」和西式占卜自己的星座功能一点没动，旧账单里的「西占」字样也照旧显示；③ 问「性格天赋」原本是紫微配西占，现在改成紫微配八字格局；④ 顺手把库里 1 行西占测试数据删了；⑤ 等待页那圈原本写的是 9 个「装饰性」方法名（里面还有六爻、大六壬这些根本不在流水线里的），现已换成真实八法名。
- 后端凭：`pytest tests -q` → `2 failed, 211 passed in 84.44s`；`method_results` xizhan 行 `1 → 0`（总 14 → 13），清理后 method_key 集合 = `['bazi-dayun-liunian','bazi-hunyin-caiyun','bazi-pattern','bazi-shensha-nayin','qimen-lifetime','qizheng','wuyun-liuqi','ziwei']`
- **测试环境：Windows（pwsh） / Python 3.13（全局解释器） / Node（排盘引擎） / 模型+provider：测试用 FakeLLM/monkeypatch，零真实 LLM 调用、零扣费**
- **凭怎么产生的：`cd backend && python -m pytest tests -q`（conftest 将三库指向系统 temp 临时库，绝不触碰真实 data/*.db）；前端 `node --check js/*.js data/ui-copy.js`；`node frontend/scripts/precompile.js`**
- **什么没测：① 真机 UI 走查（八法文案、断前尘动画 8 节点、逐法解读 tab、占星盘 tab、性格天赋路由）② 西式占卜 HUB 星座链路真机 ③ 线上库的 xizhan 历史行清理（本节只清了本地库，线上需随下次部署执行一次等价 SQL）④ 真实 LLM 下「性格天赋」是否真的按紫微主+八字辅出报告**
- **覆盖声明：证明了**注册表/切片/路由/编排/聚合/进度分母/前端文案与门禁在真实 DB + TestClient 下自洽，且断前尘法数与后端常量一致（211 项回归通过）；**未证明**用户可见终态（文案观感、动画节点、真机点击链路），也未证明合成报告在减法后仍完整（合成器逻辑未改，但未做真机端到端）
- 截图：无

## 本节指标（定义见 `01` §6；落盘时一并写）
- 请求数：**未能核验**（无法导出账单；本节工具调用约 45 次，Agent 自数、低可信） ｜ 往返次数：0（拍板齐备，本节内零澄清往返） ｜ 交付前耗时：约 95 分钟
- **模型 + provider：deepseek-v4-flash（DSH harness；provider 未在会话内暴露）**

## 未同步项（**显式登记，不许当成已完成**）
- `docs/`（Code-Wiki / 架构设计 / standards/03·05 / archify 三个 json）中仍有约 40 处「9 法」计数与「西占」列名——**本节点声明范围外**，且节147 要整体重写文档与图。**默认归入节147 一并处理**（若要提前处理，另开微改）
- `frontend/scripts/_baseline-index.html`、`_pristine-index.html`、`_check1.js` 三个**历史基线快照**内的「九法」不动（它们是门禁对照基线，改了会破坏对照意义）

## 用户结论
待用户确认
